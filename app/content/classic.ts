import type { DesignPrompt, RawStudyTopic } from "./types";

export const classicTopics: RawStudyTopic[] = [
  {
    id: "classic-feed-fanout",
    week: 4,
    day: 1,
    tier: 1,
    title: "Feed Fan-out Strategies",
    eyebrow: "Pay at write or at read",
    estimatedMinutes: 75,
    summary:
      "Choose where a feed pays its join cost: when an author publishes, when a reader opens the feed, or through a hybrid that treats high-fan-out authors differently.",
    whyItMatters:
      "One post from an account with 20 million followers is either 20 million inbox writes now or a 20-way merge on every read later. That placement decision, and nothing else in the design, sets write amplification, feed-open latency, freshness, and which accounts turn into hotspots. Follower counts are heavy-tailed enough that no single policy survives the whole graph, which is why real feeds run both and route authors between them by measured cost.",
    objectives: [
      "Derive fan-out-on-write, fan-out-on-read, and hybrid designs from follower-count and freshness requirements.",
      "Define an idempotent materialization pipeline with an explicit source of truth.",
      "Explain how celebrity accounts change partitioning, queueing, and cache behavior.",
    ],
    concepts: [
      "fan-out on write",
      "fan-out on read",
      "hybrid fan-out",
      "celebrity problem",
      "materialized inbox",
      "eventual consistency",
      "write amplification",
      "feed freshness",
    ],
    deepDive: [
      {
        title: "Write-time materialization",
        summary: "A post commit emits one durable event; workers expand it into follower inbox entries outside the author transaction.",
        points: [
          "Partition the event log by author so posts from one author stay ordered, then hand follower expansion to workers in bounded batches of a few thousand recipients each.",
          "Store compact `(recipientId, postId, authorId, createdAt)` entries — roughly 32 bytes apiece, so a 500-row inbox costs about 16 KB per user — and enforce uniqueness on `(recipientId, postId)` so replay is harmless.",
          "Advance a per-post fan-out checkpoint only after each batch is durable; lag is observable and retry does not duplicate visible entries.",
          "Materialize only into followers seen in the last 30 days and backfill a dormant follower on the open that brings them back, which removes the amplification spent on accounts that will never read the row.",
        ],
      },
      {
        title: "Read-time merge",
        summary: "The read path fetches recent posts from followed authors and performs a bounded k-way merge rather than precomputing every inbox.",
        points: [
          "Fetch a limited head per author or author shard, merge by the product ordering key, and stop once the requested page plus slack is filled.",
          "Keep author timelines independently cacheable; do not issue one storage request per follow for users with very large graphs.",
          "Use a candidate service or grouped author shards to cap fan-in, then hydrate post bodies after candidate selection.",
        ],
      },
      {
        title: "Hybrid celebrity handling",
        summary: "Materialize ordinary authors while merging high-fan-out authors at read time to bound publish amplification.",
        points: [
          "Choose the threshold from observed fan-out cost and queue-delay SLOs, not from a hard-coded follower count.",
          "Record the policy version on feed generation so a threshold change can be backfilled without duplicate candidates.",
          "Protect celebrity timelines with replicated caches and request coalescing because the write hotspot becomes a read hotspot.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "When to perform the social-graph join",
        preferA: "Fan-out on write when reads dominate, follower counts are bounded, and opening the feed must be consistently fast.",
        preferB: "Fan-out on read when posting volume is high, readership is sparse, or follower counts are extremely skewed.",
        watch: "A global policy ignores the long tail; hybrid policies add operational and correctness complexity.",
      },
      {
        decision: "What the inbox stores",
        preferA: "Store post IDs and ranking features when post edits, deletes, and policy changes must appear quickly.",
        preferB: "Store denormalized render data when hydration latency dominates and edits are rare.",
        watch: "Denormalization multiplies invalidation work and can leak deleted or access-revoked content.",
      },
      {
        decision: "Freshness versus smoothing",
        preferA: "Reserve capacity for immediate fan-out when seconds-level freshness is a user-facing SLI.",
        preferB: "Queue and batch expansion when throughput and cost matter more than immediate appearance.",
        watch: "Queue age, not just queue depth, reveals whether old posts are missing their freshness objective.",
      },
    ],
    failureModes: [
      {
        mode: "Celebrity fan-out overload",
        symptom: "Publish latency is normal but fan-out lag and worker saturation spike for a small set of authors.",
        mitigation: "Route those authors to read-time merge, batch follower scans, rate-limit backfills, and reserve queue capacity by priority.",
      },
      {
        mode: "Duplicate or missing inbox entries",
        symptom: "Users see repeated posts or holes after worker retries and partial batch failures.",
        mitigation: "Use deterministic entry keys, durable batch checkpoints, idempotent upserts, and a reconciliation scan from post events to inbox rows.",
      },
      {
        mode: "Deleted content remains visible",
        symptom: "Cached candidates hydrate to missing data or continue rendering after moderation or privacy changes.",
        mitigation: "Authorize during hydration, propagate tombstones on a high-priority path, and keep candidate-cache TTLs bounded.",
      },
    ],
    interviewQuestions: [
      "What graph and traffic measurements determine your fan-out policy?",
      "How does a retried fan-out event avoid producing duplicate feed entries?",
      "How would the design change if one author has hundreds of millions of followers?",
    ],
    decisionChecklist: [
      "Name the canonical post store and social-graph source of truth.",
      "Estimate publish amplification and feed-read fan-in at peak.",
      "Define ordering and freshness guarantees explicitly.",
      "Bound queue work and isolate celebrity traffic.",
      "Cover deletes, blocks, retries, backfills, and regional failure.",
    ],
    exercise:
      "Given a feed where 99.9% of authors have fewer than 5,000 followers but the top 100 exceed 20 million, define the fan-out policy, event and inbox keys, retry boundary, and a migration plan when an author crosses the threshold.",
    prerequisites: ["caching-queues", "replication-partitioning"],
    relatedDesigns: ["classic-newsfeed"],
    quiz: [
      {
        prompt: "Which mechanism most directly makes a retried fan-out batch safe?",
        options: [
          "A unique index on the (recipientId, postId) pair",
          "A longer TTL on the cached candidate page list",
          "Ordering every author's posts in one partition",
          "Randomly assigning each batch to a free worker",
        ],
        answerIndex: 0,
        explanation: "A deterministic entry key turns replay into an idempotent upsert. The tempting wrong answer is per-author partition ordering: it keeps posts in sequence but does nothing to stop a retried batch from inserting the same inbox row twice.",
      },
      {
        prompt: "Why do production feeds route celebrity authors to read-time merge?",
        options: [
          "It gives the feed one global ordering across authors",
          "It makes every follower's feed strongly consistent",
          "It bounds publish amplification with a read-time merge",
          "It removes the need to cache celebrity timelines at all",
        ],
        answerIndex: 2,
        explanation: "Merging the high-fan-out tail at read time keeps one publish from creating an unbounded materialization burst. The misconception in the last option is that pulling removes caching work; the write hotspot simply becomes a read hotspot, so those timelines need replicated caches and request coalescing.",
      },
      {
        prompt: "An author with 20 million followers posts once. Under pure fan-out on write, what does that single publish cost?",
        options: [
          "One inbox write, expanded lazily at read time",
          "Twenty million inbox writes, one per follower",
          "One write per follower shard, so a few hundred",
          "Roughly 5,000 writes, the median follower count",
        ],
        answerIndex: 1,
        explanation: "Write-time materialization is linear in follower count, so one post becomes 20 million inbox rows. The shard-count option encodes the common error that sharding shrinks the work; sharding only spreads the same 20 million writes across more workers.",
      },
      {
        prompt: "Publish latency looks normal, but fan-out lag and worker saturation spike for a handful of authors. What is the diagnosis?",
        options: [
          "The read-time merge fetches too many author heads",
          "The post store is undersized for peak publish volume",
          "Candidate-cache TTLs are too long for those authors",
          "Follower expansion for high-fan-out authors saturates",
        ],
        answerIndex: 3,
        explanation: "Normal publish latency shows the author transaction committed fine, so the backlog is in the asynchronous expansion of a few enormous follower sets. Blaming the post store is the classic misread: fan-out runs outside that transaction, so its saturation never shows up as slow writes.",
      },
    ],
    recallCards: [
      { id: "feed-fanout-choice", prompt: "Compare fan-out on write and fan-out on read, and describe the hybrid that production systems actually use.", answer: "Fan-out on write pushes each post into every follower's inbox at publish time: reads are a cheap sequential scan, but a celebrity with millions of followers turns one write into millions, and the write cost is paid even for inactive followers. Fan-out on read merges the author timelines at query time: writes are trivial but reads are expensive and hard to cache. Production systems split by author: push for ordinary accounts, pull for high-follower accounts, then merge the two at read time, so the expensive path is bounded to a small set of accounts." },
      { id: "feed-inactive", prompt: "Explain why fan-out on write must account for follower activity and how the inbox is bounded.", answer: "Materializing into every follower's inbox spends write amplification on accounts that may never read, so systems fan out only to recently-active followers and backfill on demand when a dormant user returns. The inbox itself is bounded to a few hundred recent entries because nobody scrolls further, keeping memory predictable and letting the store hold IDs plus a ranking score rather than full post bodies, which are hydrated from the source of truth at read time." },
    ],
  },
  {
    id: "classic-feed-ranking-cache-pagination",
    week: 4,
    day: 2,
    tier: 1,
    title: "Feed Ranking, Caching, and Pagination",
    eyebrow: "Stable pages over shifting data",
    estimatedMinutes: 80,
    summary:
      "Build a stable, low-latency feed from candidate IDs, mutable ranking signals, layered caches, and opaque cursors that survive concurrent inserts.",
    whyItMatters:
      "Page one and page two are two separate queries against data that moved in between. A post inserted at the top shifts every offset by one, so `OFFSET 30` re-serves an item the reader already scrolled past, and a rescore between the two requests can drop another one entirely. Everything here — what the cursor encodes, why the policy version is part of the cache key, why a deletion tombstone jumps the invalidation queue — exists so the second query answers the same question the first one did.",
    objectives: [
      "Separate candidate generation, ranking, hydration, and policy filtering into measurable stages.",
      "Design cache keys and invalidation paths that preserve deletes and access changes.",
      "Define cursor semantics for chronological and ranked feeds without offset-scan drift.",
    ],
    concepts: [
      "candidate generation",
      "ranking features",
      "cache-aside",
      "request coalescing",
      "hot-key splitting",
      "keyset pagination",
      "opaque cursor",
      "stable tie-breaker",
    ],
    deepDive: [
      {
        title: "Candidate-to-render pipeline",
        summary: "Keep feed IDs and ranking metadata separate from mutable post bodies and viewer-specific policy checks.",
        points: [
          "Generate three to five times a page of candidates — around 150 for a 30-item page — so block, privacy, and deletion filtering can remove a fifth of them without a second round trip to storage.",
          "Hydrate only the IDs that survived ranking. Hydrating all 150 candidates to render 30 multiplies post-store reads fivefold for results nobody will see.",
          "Split the 300 ms p99 into named stage budgets — 120 ms to fetch candidates, 60 ms to rank, 90 ms to hydrate, leaving 30 ms for render and transit — because a single end-to-end target tells you the page was slow and never which stage regressed.",
          "Version ranking features and the model or config snapshot that produced a page, so a relevance complaint can be traced to a rollout and rolled back rather than argued about.",
        ],
      },
      {
        title: "Layered cache design",
        summary: "Cache immutable post fragments broadly and viewer-specific candidate pages narrowly.",
        points: [
          "Use cache-aside with single-flight fills and jittered TTLs to prevent a popular feed from stampeding storage.",
          "Key ranked pages by viewer, cursor boundary, and ranking-policy version; cap the number of cached pages per viewer.",
          "Push delete and authorization tombstones through a faster invalidation path than ordinary content edits.",
        ],
      },
      {
        title: "Stable pagination under mutation",
        summary: "A cursor captures the last ordering tuple and any snapshot semantics required by the product.",
        points: [
          "For chronological order, query strictly after `(createdAt, postId)` so equal timestamps cannot duplicate or omit records.",
          "For ranked order, include a rank-session or candidate-set version because scores may change between page requests.",
          "Encode and authenticate cursor state as an opaque token; reject incompatible policy versions rather than silently changing semantics.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Chronological versus ranked pagination",
        preferA: "Use a simple `(time, id)` keyset when deterministic freshness and explainability dominate.",
        preferB: "Use a rank-session cursor when relevance matters enough to hold a candidate snapshot or policy version.",
        watch: "Re-ranking every page independently produces duplicates and gaps as scores move.",
      },
      {
        decision: "Cache fully rendered pages versus candidate IDs",
        preferA: "Rendered pages minimize reads for mostly immutable public content.",
        preferB: "Candidate IDs keep edits, viewer policy, and deletes correct with targeted hydration.",
        watch: "Viewer-specific rendered pages have poor reuse and a large invalidation surface.",
      },
      {
        decision: "Push invalidation versus bounded staleness",
        preferA: "Push tombstones for safety, privacy, and moderation changes.",
        preferB: "Use TTL expiry for ordinary rank and engagement changes where brief staleness is acceptable.",
        watch: "Treating every mutation as urgent can overload the invalidation channel.",
      },
    ],
    failureModes: [
      {
        mode: "Pagination drift",
        symptom: "Users see the same item twice or miss items when new posts arrive between requests.",
        mitigation: "Use a unique ordering tuple, strict keyset predicates, and an explicit rank-session or snapshot contract.",
      },
      {
        mode: "Cache stampede",
        symptom: "A popular key expires and storage QPS, tail latency, and timeout rates rise together.",
        mitigation: "Apply request coalescing, early refresh, jittered TTLs, stale-if-error, and per-key load shedding.",
      },
      {
        mode: "Policy-stale cache",
        symptom: "Blocked, private, or deleted posts remain visible despite correct source-of-truth state.",
        mitigation: "Recheck access during hydration, use versioned policy keys, and distribute high-priority tombstones.",
      },
    ],
    interviewQuestions: [
      "What exactly is encoded in the next-page cursor?",
      "Which content is safe to cache globally, and which must be viewer-specific?",
      "How do you keep a mutable ranked feed from duplicating items across pages?",
    ],
    decisionChecklist: [
      "State the feed ordering and freshness contract.",
      "Use a unique, indexed cursor tuple rather than offset pagination.",
      "Separate candidates, ranking, policy checks, and hydration.",
      "Version ranking and cache keys for rollback.",
      "Design urgent invalidation for deletion and access revocation.",
    ],
    exercise:
      "Specify `GET /feed` for a ranked feed: define the cursor payload, supporting index, cache hierarchy, deletion path, and behavior when the ranking policy changes between page one and page two.",
    prerequisites: ["classic-feed-fanout", "caching-queues"],
    relatedDesigns: ["classic-newsfeed"],
    quiz: [
      {
        prompt: "What makes a chronological cursor safe when many posts share a timestamp?",
        options: [
          "A larger page size so collisions fall inside it",
          "A shorter TTL on the cached candidate page list",
          "An offset that skips the rows already returned",
          "A unique tie-breaker such as post ID in the key",
        ],
        answerIndex: 3,
        explanation: "The query boundary needs a total order, and `(createdAt, postId)` stays strict even when timestamps collide. The offset option encodes the belief that OFFSET is equivalent to a keyset predicate; offsets shift whenever rows are inserted, so they both duplicate and skip.",
      },
      {
        prompt: "Which cache entry has the broadest safe reuse across viewers?",
        options: [
          "A viewer's fully ranked and hydrated first page",
          "An immutable public post fragment, keyed by ID",
          "A viewer's block-list and privacy filter result",
          "A per-device presence record with a short lease",
        ],
        answerIndex: 1,
        explanation: "An immutable fragment is neither viewer-specific nor frequently invalidated, so one copy serves everyone. The ranked first page is the tempting answer because it saves the most work per hit, but it is keyed by viewer and policy version, giving it near-zero reuse and a large invalidation surface.",
      },
      {
        prompt: "A feed page has a 300 ms p99 budget: candidate fetch 120 ms, rank 60 ms, hydrate 90 ms. What is left?",
        options: [
          "30 ms for render and network overhead",
          "270 ms, because the stages run in parallel",
          "0 ms; the stages already sum to the budget",
          "150 ms, measured from the slowest stage",
        ],
        answerIndex: 0,
        explanation: "120 + 60 + 90 = 270 ms, leaving 30 ms of the 300 ms budget for render and transit. The parallel option encodes the misconception that stage budgets overlap; candidates must exist before ranking and ranking before hydration, so these budgets add rather than max.",
      },
      {
        prompt: "A blocked author's posts keep appearing for one viewer even though the block is committed in the source of truth. What fixes it?",
        options: [
          "Raise the TTL so pages are refreshed less often",
          "Add request coalescing to the candidate fetch",
          "Recheck access at hydration and push a tombstone",
          "Cache rendered pages instead of candidate IDs",
        ],
        answerIndex: 2,
        explanation: "Cached candidate lists are a hint about ordering, never the authority on visibility, so authorization has to be re-evaluated during hydration and urgent changes pushed as tombstones. Coalescing is the tempting pick because it is the other famous cache remedy, but it addresses stampedes and leaves stale policy exactly as stale.",
      },
    ],
    recallCards: [
      { id: "feed-cursor", prompt: "Explain why offset pagination breaks on a ranked feed and what a cursor must encode.", answer: "Offset assumes a stable ordering; on a live feed items are inserted and rescored between page requests, so a fixed offset silently skips or repeats items. A cursor must encode a stable position in the specific ordering being used - typically the last item's sort key plus its ID as a tiebreaker - and, for a ranked feed, the ranking policy version and any seed used, so page two continues the same ordering that produced page one rather than a newly rescored one." },
      { id: "feed-invalidate", prompt: "Describe how deletion and moderation must propagate through a cached feed.", answer: "Cached feeds hold IDs, so a deleted, blocked, or moderated item can persist in inboxes and caches long after the source of truth changed. The safe pattern is to hydrate IDs against the authoritative store at read time and filter there, so the source of truth always wins, plus push explicit tombstones for urgent safety and privacy changes rather than waiting for TTL expiry. Treat the cached list as a hint about ordering, never as the authority on visibility." },
    ],
  },
  {
    id: "classic-realtime-connections",
    week: 4,
    day: 3,
    tier: 1,
    title: "Real-Time Connection Architecture",
    eyebrow: "Connections are state, not calls",
    estimatedMinutes: 75,
    summary:
      "Operate persistent client connections through stateless-enough gateways, leased routing state, bounded buffers, heartbeats, and controlled reconnect behavior.",
    whyItMatters:
      "Two million idle sockets are two million file descriptors, TLS sessions, and outbound buffers that exist whether or not anyone is typing, so a fleet sized from message rate will be an order of magnitude short. The connection tier runs out of the resources nobody put in the estimate — per-socket memory, heartbeat writes, and the reconnect burst that arrives the instant one zone disappears.",
    objectives: [
      "Choose long polling or WebSockets from traffic shape and delivery requirements.",
      "Route events to a user's active connections without making one gateway a permanent source of truth.",
      "Size heartbeats, buffers, reconnects, and load shedding around explicit resource limits.",
    ],
    concepts: [
      "WebSockets",
      "long polling",
      "connection gateway",
      "session directory",
      "heartbeats and leases",
      "connection draining",
      "slow-consumer backpressure",
      "reconnect jitter",
    ],
    deepDive: [
      {
        title: "Gateway ownership and routing",
        summary: "Gateways own sockets; a soft-state directory maps users and devices to gateway instances.",
        points: [
          "On connect, authenticate once, allocate a connection ID, and renew a short lease keyed by `(userId, deviceId, connectionId)`.",
          "Publish outbound events through user-keyed partitions; routers resolve current gateways and tolerate stale directory entries.",
          "Make reconnection resume from a durable message cursor so losing a gateway loses sockets, not accepted messages.",
        ],
      },
      {
        title: "Capacity and lifecycle",
        summary: "Connection capacity is constrained by memory, descriptors, kernel buffers, TLS cost, and heartbeat traffic.",
        points: [
          "Budget 30 to 60 KB per idle socket once TLS state, kernel read and write buffers, and application bookkeeping are counted, so 200,000 connections on one gateway is 6 to 12 GB of resident memory before a single message is queued.",
          "Estimate concurrent connections separately from messages per second, and leave enough headroom that losing one zone of three still fits in the survivors rather than in the autoscaler's reaction time.",
          "Pick the heartbeat interval as a capacity decision, not a liveness one: two million sockets on a 30-second interval is roughly 67,000 inbound frames per second of pure overhead, and doubling the interval halves that bill while doubling the ghost-session window.",
          "Drain an instance by refusing new sockets, telling connected clients to reconnect with jitter, and waiting a bounded grace period before killing what remains.",
        ],
      },
      {
        title: "Handshake cost on the recovery path",
        summary: "The expensive event is a connect, not a connection, and every displaced client performs one at the same moment.",
        points: [
          "Price the connect path separately: certificate exchange, token verification, and the session-directory write are all per-connect costs, and a zone loss presents the whole fleet's worth of them within a few seconds.",
          "Cap accepted handshakes per second per gateway and answer the overflow with a retry-after hint, because rejecting a client early costs one round trip while accepting and then dropping it costs the full negotiation on both sides.",
        ],
      },
      {
        title: "Backpressure at the edge",
        summary: "Every connection needs a bounded outbound queue and an explicit overflow policy.",
        points: [
          "Track queued bytes and oldest-event age per socket; message count alone misses a few very large frames.",
          "Coalesce replaceable signals such as presence, but never silently drop durable chat messages.",
          "Disconnect persistently slow consumers with a resume cursor so durable history is fetched after reconnect.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "WebSockets versus long polling",
        preferA: "Use WebSockets for frequent bidirectional events and low delivery latency.",
        preferB: "Use long polling for sparse events, simpler infrastructure, or constrained intermediaries.",
        watch: "WebSockets reduce handshake overhead but create stateful capacity and deployment concerns.",
      },
      {
        decision: "Sticky routing versus external session directory",
        preferA: "Use bounded stickiness to reduce directory lookups within a healthy edge pool.",
        preferB: "Use a leased directory when any producer must locate any active device connection.",
        watch: "Treat directory entries as hints; stale records must not cause message loss.",
      },
      {
        decision: "Buffer versus disconnect a slow client",
        preferA: "Buffer briefly when bursts are common and memory is bounded.",
        preferB: "Disconnect and require cursor-based catch-up when a client remains behind.",
        watch: "Unbounded per-socket buffers turn one slow client class into fleet-wide memory exhaustion.",
      },
    ],
    failureModes: [
      {
        mode: "Reconnect storm",
        symptom: "A gateway or zone loss causes synchronized handshakes, authentication load, and repeated connection failures.",
        mitigation: "Return retry hints, require exponential backoff with jitter, rate-limit handshakes, and reserve recovery capacity.",
      },
      {
        mode: "Stale connection routing",
        symptom: "Publishers repeatedly route to a dead gateway and online users appear unreachable.",
        mitigation: "Use expiring leases, negative acknowledgments that evict stale routes, and client cursor catch-up.",
      },
      {
        mode: "Slow-consumer memory exhaustion",
        symptom: "Gateway memory and event-loop latency climb while a subset of sockets accumulates queued bytes.",
        mitigation: "Enforce byte limits, coalesce ephemeral events, shed or disconnect slow clients, and expose per-connection lag metrics.",
      },
    ],
    interviewQuestions: [
      "How many concurrent sockets can one gateway hold, and what resource is limiting?",
      "How does an outbound event locate every active device for a user?",
      "What happens to accepted messages when a gateway dies?",
    ],
    decisionChecklist: [
      "Estimate concurrent connections, heartbeat QPS, and outbound bytes.",
      "Keep durable messages outside gateway memory.",
      "Use leases for routing state and cursors for recovery.",
      "Bound per-connection buffers and define overflow behavior.",
      "Plan jittered reconnects, connection draining, and zone loss.",
    ],
    exercise:
      "Design a gateway tier for two million concurrent chat connections. Estimate heartbeat load, specify the session-directory lease, define slow-client limits, and walk through a full-zone failure.",
    prerequisites: ["networking", "caching-queues"],
    relatedDesigns: ["classic-chat"],
    quiz: [
      {
        prompt: "Where must an accepted chat message live before the sender gets a durable acknowledgment?",
        options: [
          "In the gateway's per-socket outbound buffer",
          "In durable message storage or a durable log",
          "In the session directory entry for the user",
          "In a sticky-routing cookie on the balancer",
        ],
        answerIndex: 1,
        explanation: "Gateways own transient sockets, so acceptance has to cross a durable boundary before the client is told the message is safe. Picking the session directory reflects the misconception that it is durable state; it is soft, leased routing state that is deliberately allowed to go stale.",
      },
      {
        prompt: "What is the safest policy for a socket that stays persistently behind?",
        options: [
          "Grow its outbound buffer until it catches up",
          "Silently drop the oldest durable chat frames",
          "Block the event loop until the socket drains",
          "Disconnect it and resume from a durable cursor",
        ],
        answerIndex: 3,
        explanation: "A bounded queue protects the whole gateway while cursor-based catch-up preserves durable delivery. Growing the buffer is the merciful-looking answer and is precisely how one class of slow client turns into fleet-wide memory exhaustion.",
      },
      {
        prompt: "Two million sockets each send a heartbeat every 30 seconds. What steady rate must the tier absorb?",
        options: [
          "About 2 million heartbeats per second",
          "About 33,000 heartbeats per second",
          "About 67,000 heartbeats per second",
          "About 60,000 heartbeats per second",
        ],
        answerIndex: 2,
        explanation: "2,000,000 divided by a 30-second period is roughly 67,000 heartbeats per second, which is why heartbeat interval is a capacity decision. The 2-million answer drops the interval entirely and reads concurrency as a per-second rate, the most common error in sizing a connection tier.",
      },
      {
        prompt: "After a zone loss, handshake and authentication errors spike far above steady-state load. What is happening?",
        options: [
          "A reconnect storm; add backoff with jitter",
          "Stale directory routes; evict them by lease",
          "Slow consumers; enforce per-socket byte caps",
          "Long-poll overhead; move to WebSockets now",
        ],
        answerIndex: 0,
        explanation: "Every displaced client reconnects at once, so recovery traffic hits authentication and handshake paths together and must be spread with jittered backoff, retry hints, and reserved capacity. Stale routes are the tempting diagnosis since the directory really does hold dead entries after a zone loss, but that failure shows up as unreachable users, not a handshake spike.",
      },
    ],
    recallCards: [
      { id: "rt-routing", prompt: "Explain how a message reaches a specific user's live connection across a gateway fleet.", answer: "Each gateway owns the sockets it accepted, so the system needs a session registry mapping user or device to the gateway currently holding the connection, written on connect and removed on disconnect with a TTL for crash cleanup. A sender looks up the registry and forwards to that gateway directly, rather than broadcasting to every gateway, which would make delivery cost scale with fleet size. The registry is hot and must tolerate staleness, so gateways reject or forward messages for sessions they no longer own." },
      { id: "rt-reconnect", prompt: "Describe the reconnect-storm failure and its mitigations.", answer: "When a gateway or zone drops, every affected client reconnects at once, and recovery traffic can exceed steady-state load by orders of magnitude, hitting authentication, session registry, and history backfill simultaneously - often toppling the replacement capacity. Mitigate with randomized exponential backoff so clients spread out, server-supplied retry hints, admission control that sheds reconnects rather than failing them slowly, resume tokens so a reconnect replays from a cursor instead of refetching history, and capacity headroom sized for the recovery spike rather than steady state." },
    ],
  },
  {
    id: "classic-message-ordering-delivery-sync",
    week: 4,
    day: 4,
    tier: 1,
    title: "Message Ordering, Delivery, and Multi-Device Sync",
    eyebrow: "Order within one conversation",
    estimatedMinutes: 85,
    summary:
      "Promise only per-conversation order, make at-least-once delivery harmless, and give every device a durable cursor for offline catch-up and receipt state.",
    whyItMatters:
      "Exactly-once delivery is not something a messaging system can buy; what it can build is a message that is harmless to deliver twice, and the client message ID, the per-conversation sequence, and the monotonic read watermark all exist to make the second copy indistinguishable from a no-op.",
    objectives: [
      "Define per-conversation sequence allocation and gap handling without depending on client clocks.",
      "Model accepted, delivered, and read acknowledgments as distinct durable transitions.",
      "Synchronize multiple devices through idempotent message IDs and monotonic cursors.",
    ],
    concepts: [
      "per-conversation sequence",
      "client-generated idempotency key",
      "at-least-once delivery",
      "offline inbox",
      "delivery receipt",
      "read watermark",
      "device cursor",
      "causal ordering",
    ],
    deepDive: [
      {
        title: "Sequence allocation and storage",
        summary: "A conversation's write owner assigns a monotonic sequence after deduplicating the sender's client message ID.",
        points: [
          "Route a conversation to one logical partition or leader and atomically persist `(conversationId, sequence, messageId, bodyRef)`.",
          "Return the existing sequence when the same `(conversationId, senderId, clientMessageId)` is retried.",
          "Use the sequence for conversation order and a server timestamp for display; never infer order from device clocks.",
        ],
      },
      {
        title: "Delivery and receipt state",
        summary: "Durable acceptance is not the same event as delivery to a device or reading by a user.",
        points: [
          "A durable acknowledgment means the message can be replayed; gateway push is an opportunistic low-latency path.",
          "Represent delivered state per device only if product requirements need it; aggregate read state as a monotonic per-user watermark when possible.",
          "Cost the two representations before arguing about the UX: a 500-member group exchanging 1,000 messages a day generates 500,000 per-message receipt rows daily against 500 watermark rows, a thousandfold difference that grows with membership rather than with traffic.",
          "Publish receipt changes as idempotent events and reject watermark regressions caused by delayed devices.",
        ],
      },
      {
        title: "Offline and multi-device catch-up",
        summary: "Each device asks for changes after its last durable cursor and can safely receive overlaps.",
        points: [
          "Page by conversation sequence or a user-inbox cursor; retain enough history to cover the offline window.",
          "Send a high-water mark with each page so clients know whether they are caught up despite concurrent arrivals.",
          "Deduplicate by message ID locally and reconcile gaps before advancing the committed device cursor.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Ordering scope",
        preferA: "Use per-conversation ordering for intuitive chat semantics and independent partition scaling.",
        preferB: "Use causal metadata only when cross-conversation causality is a real product requirement.",
        watch: "Global total order creates a coordination bottleneck without meaningful user value.",
      },
      {
        decision: "Receipt granularity",
        preferA: "Track a monotonic read watermark for compact one-to-one or ordered group conversations.",
        preferB: "Track per-message receipts when compliance or detailed group UX explicitly requires them.",
        watch: "Per-message, per-member receipts grow multiplicatively for large groups.",
      },
      {
        decision: "Push versus pull recovery",
        preferA: "Push over active connections for low latency.",
        preferB: "Pull from a durable cursor for offline recovery and after routing uncertainty.",
        watch: "Push-only systems lose state whenever a client or gateway is unavailable.",
      },
    ],
    failureModes: [
      {
        mode: "Duplicate send after unknown commit",
        symptom: "A sender times out, retries, and recipients see the same logical message twice.",
        mitigation: "Require a stable client message ID and deduplicate it atomically with sequence assignment.",
      },
      {
        mode: "Sequence gap",
        symptom: "A device observes sequence 43 before 42 and renders a misleading order.",
        mitigation: "Buffer briefly, request the missing range, and advance the durable cursor only through contiguous sequences.",
      },
      {
        mode: "Receipt regression",
        symptom: "An old device sync makes a conversation appear unread after a newer device marked it read.",
        mitigation: "Store read state as a monotonic maximum sequence and make updates conditional on increasing the watermark.",
      },
    ],
    interviewQuestions: [
      "At what exact boundary do you acknowledge a send to the client?",
      "How do retries interact with sequence allocation?",
      "How does a device recover messages after being offline for a week?",
    ],
    decisionChecklist: [
      "Name the required ordering scope and partition key.",
      "Use client message IDs for idempotent send retries.",
      "Separate durable acceptance, delivery, and read semantics.",
      "Define cursor retention, gap recovery, and high-water marks.",
      "Keep read watermarks monotonic across devices.",
    ],
    exercise:
      "Specify `sendMessage` and `syncMessages` for a multi-device chat client, including idempotency keys, sequence assignment, acknowledgment semantics, gap recovery, and read-watermark updates.",
    prerequisites: ["classic-realtime-connections", "consistency-idempotency"],
    relatedDesigns: ["classic-chat"],
    quiz: [
      {
        prompt: "Which identifier must stay stable when a client retries a timed-out send?",
        options: [
          "The gateway instance ID handling the socket",
          "The server wall-clock timestamp of the send",
          "The client-generated message ID for the send",
          "The TCP connection ID the retry arrives on",
        ],
        answerIndex: 2,
        explanation: "Deduplication happens atomically with sequence assignment on `(conversationId, senderId, clientMessageId)`, so the retry returns the sequence already committed. Choosing the timestamp encodes the belief that time identifies a message; a retry carries a new time and device clocks disagree anyway.",
      },
      {
        prompt: "What is the compact representation of read state in an ordered conversation?",
        options: [
          "A monotonic highest-read sequence per user",
          "A per-message, per-member receipt matrix",
          "An unbounded list of unread timestamps",
          "The gateway's local clock at read time",
        ],
        answerIndex: 0,
        explanation: "A watermark says everything through one sequence is read, so updates are idempotent and stale regressions are rejected by taking the maximum. The receipt matrix is tempting because it looks more precise, but its cost grows multiplicatively with group size and it is only justified by compliance or detailed group UX.",
      },
      {
        prompt: "A 500-member group exchanges 1,000 messages a day. What does per-message receipt state cost versus watermarks?",
        options: [
          "500 rows versus 1,000; watermarks cost more",
          "1,500 rows versus 500; the gap stays small",
          "1,000 rows either way; both scale alike",
          "500,000 rows versus 500 watermark rows",
        ],
        answerIndex: 3,
        explanation: "Per-message, per-member receipts are 500 x 1,000 = 500,000 rows a day, while watermarks need one row per member. The 'both scale alike' option encodes the misconception that receipt cost tracks message volume alone and forgets the per-member multiplier that makes large groups explode.",
      },
      {
        prompt: "A device receives sequence 43 before 42 in a conversation. What should it do?",
        options: [
          "Reassign sequences from the device clock",
          "Buffer, refetch the range, then advance",
          "Advance the cursor and drop the gap",
          "Acknowledge 43 and re-request everything",
        ],
        answerIndex: 1,
        explanation: "The durable cursor may only advance through contiguous sequences, so the client buffers briefly, requests the missing range, and then commits. Advancing past the hole is the tempting shortcut and loses message 42 permanently, because the cursor never looks backwards again.",
      },
    ],
    recallCards: [
      { id: "msg-ordering", prompt: "State what ordering guarantee is realistic in a distributed chat system and how it is achieved.", answer: "Total global ordering is neither achievable nor needed; the realistic guarantee is per-conversation ordering, achieved by making the conversation the partition key so all its messages flow through one ordered partition with a monotonic per-conversation sequence number assigned by the owner. Clients order by that sequence rather than by timestamp, since device clocks disagree, and gaps in the sequence are what let a client detect missing messages and request backfill." },
      { id: "msg-delivery", prompt: "Explain how at-least-once delivery is made acceptable to the user.", answer: "The transport retries until acknowledged, so duplicates are inevitable; the client and server both deduplicate on a stable client-generated message ID, which also lets an optimistic local echo be reconciled with the server copy rather than shown twice. Read state and delivery receipts are tracked as monotonic high-water marks per device rather than per message, so acknowledgments are idempotent, cheap, and cannot regress when they arrive out of order." },
    ],
  },
  {
    id: "classic-presence-group-chat",
    week: 5,
    day: 1,
    tier: 1,
    title: "Presence and Group Chat",
    eyebrow: "Soft truth, expiring by design",
    estimatedMinutes: 75,
    summary:
      "Treat presence as privacy-sensitive, expiring soft state while keeping group membership, messages, and authorization durable and versioned.",
    whyItMatters:
      "Presence is the cheapest data in the system and the easiest to let quietly become load-bearing: three million devices renewing a ten-second lease is 300,000 writes a second of information that nobody would miss if the whole tier vanished, which is exactly why it gets its own store, its own failure domain, and no authority over anything durable. The moment a send path consults presence before deciding to persist a message, an expired lease starts losing mail.",
    objectives: [
      "Aggregate device leases into bounded-staleness user presence.",
      "Scale group delivery without creating one durable message copy per online socket.",
      "Enforce membership versions, privacy, and retention on send, sync, and fan-out paths.",
    ],
    concepts: [
      "presence lease",
      "last-seen state",
      "subscription fan-out",
      "membership version",
      "group message log",
      "large-group fan-out",
      "authorization at read",
      "privacy retention",
    ],
    deepDive: [
      {
        title: "Lease-based presence",
        summary: "Each active device renews an expiring lease; user presence is the aggregate of valid device leases.",
        points: [
          "Write heartbeats to a partition keyed by user, coalesce frequent renewals, and expire leases without a synchronous disconnect requirement.",
          "Size the renewal rate before choosing the interval: three million devices on a 10-second lease is roughly 300,000 writes a second, so a store that would be comfortable at 30,000 forces either a longer lease or coarser aggregation, not more replicas.",
          "Publish only state transitions such as offline-to-online, not every heartbeat, to bound subscriber fan-out.",
          "Persist last-seen separately with a privacy policy because presence storage is intentionally ephemeral and may be lost.",
        ],
      },
      {
        title: "Group message distribution",
        summary: "Persist one ordered group message, then route notifications or pointers to members and active devices.",
        points: [
          "Key the durable log by group and sequence; derive per-user inbox pointers only where unread or notification queries require them.",
          "One message to a million-member group is a single log append followed by a million delivery decisions. Batching recipients 10,000 at a time turns that into 100 checkpointed units of work that can be paced, retried, and deprioritized behind small conversations.",
          "For large groups, let online gateways fetch the canonical message body once rather than shipping it through every fan-out batch.",
          "Apply per-group and per-sender quotas so one busy group cannot consume all delivery capacity.",
        ],
      },
      {
        title: "Membership and authorization",
        summary: "Membership is durable, versioned state; delivery is never proof that a later read remains authorized.",
        points: [
          "Record the membership version observed at send and check current membership on history reads and attachment fetches.",
          "Propagate removals and access revocations on a priority path while retaining an audit trail appropriate to policy.",
          "Avoid embedding sensitive membership or presence details in broadly cached payloads.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Presence freshness",
        preferA: "Use shorter leases for rapid offline detection in small, high-value sessions.",
        preferB: "Use longer leases and transition coalescing to reduce heartbeat writes at large scale.",
        watch: "The lease must exceed normal mobile jitter or users will flap between online and offline.",
      },
      {
        decision: "Group fan-out representation",
        preferA: "Materialize per-user pointers when unread counts and fast inbox queries dominate.",
        preferB: "Keep one group log and read by membership when groups are very large or mostly inactive.",
        watch: "Per-user copies amplify writes; pure read-time joins amplify inbox reads.",
      },
      {
        decision: "Authorization timing",
        preferA: "Authorize again on history and attachment reads for revocation correctness.",
        preferB: "Trust a short-lived signed capability for bounded low-latency media access.",
        watch: "Capability lifetime defines the maximum revocation delay and must match the privacy requirement.",
      },
    ],
    failureModes: [
      {
        mode: "Ghost or flapping presence",
        symptom: "Disconnected users remain online, or mobile clients alternate state during transient packet loss.",
        mitigation: "Use leases with grace windows, coalesce transitions, and expose bounded-staleness semantics to clients.",
      },
      {
        mode: "Large-group fan-out lag",
        symptom: "Messages are durable but notification and unread updates fall progressively behind in large groups.",
        mitigation: "Batch recipients, isolate large-group queues, prioritize active users, and measure oldest-undelivered age.",
      },
      {
        mode: "Removed member retains access",
        symptom: "A former member can fetch history or attachments from a stale cache or capability.",
        mitigation: "Version membership, reauthorize sensitive reads, shorten capability TTLs, and prioritize revocation invalidation.",
      },
    ],
    interviewQuestions: [
      "What staleness does online presence promise during a network partition?",
      "How is one group message delivered to a million-member group?",
      "Where is membership checked after a user is removed?",
    ],
    decisionChecklist: [
      "Separate ephemeral device leases from durable last-seen policy.",
      "Publish presence transitions rather than raw heartbeat traffic.",
      "Persist one canonical group message and define fan-out representation.",
      "Version membership and reauthorize sensitive reads.",
      "Isolate large groups and make privacy retention explicit.",
    ],
    exercise:
      "Design presence and delivery for a group with one million members, including lease timing, active-subscriber fan-out, unread state, member removal, and behavior during a regional partition.",
    prerequisites: ["classic-realtime-connections", "classic-message-ordering-delivery-sync"],
    relatedDesigns: ["classic-chat"],
    quiz: [
      {
        prompt: "Why does presence use expiring leases instead of durable connect and disconnect events?",
        options: [
          "Disconnect events are guaranteed to arrive",
          "Leases give presence strong consistency",
          "Leases remove heartbeat traffic entirely",
          "Clients vanish without sending a disconnect",
        ],
        answerIndex: 3,
        explanation: "Expiry bounds the stale-online window without requiring perfect failure detection, which no network provides. Believing the disconnect always arrives is exactly the mental model that produces ghost presence, because crashes and partitions suppress that event.",
      },
      {
        prompt: "What must be durable when a message is sent to a very large group?",
        options: [
          "One canonical group-log entry, keyed by sequence",
          "One full durable body copy per online member socket",
          "One copy in each gateway's outbound buffer",
          "One ephemeral presence event per recipient",
        ],
        answerIndex: 0,
        explanation: "A single ordered log entry preserves order and recovery, and pointers or notifications are derived from it only where unread queries need them. Copying the body per online socket is tempting for delivery speed, but it multiplies durable writes by device count and scatters the authority on ordering.",
      },
      {
        prompt: "Three million devices renew a presence lease every 10 seconds. What write rate does the presence partition absorb?",
        options: [
          "About 3 million writes per second",
          "About 30,000 writes per second",
          "About 300,000 writes per second",
          "About 10 writes per second per device",
        ],
        answerIndex: 2,
        explanation: "3,000,000 renewals spread over a 10-second period is roughly 300,000 writes per second, which is why renewals are coalesced and only transitions are published to subscribers. The 3-million answer ignores the interval and treats every device as writing every second.",
      },
      {
        prompt: "A removed member still fetches group attachments minutes after removal. What explains it?",
        options: [
          "The group log lost its ordering sequence",
          "A signed capability outlives the revocation",
          "Presence leases expired too slowly for them",
          "Fan-out batches were too large to process",
        ],
        answerIndex: 1,
        explanation: "Capability lifetime is the maximum revocation delay, so sensitive reads must be reauthorized against the current membership version and TTLs kept short. Blaming presence leases confuses ephemeral status with authorization; presence never grants access to anything.",
      },
    ],
    recallCards: [
      { id: "presence-soft", prompt: "Explain why presence is treated as expiring soft state and what that buys.", answer: "Presence is high-churn, low-value data whose staleness is tolerable: it is written as a short-TTL heartbeat so a crashed client or gateway self-heals by simply not renewing, with no cleanup path to get wrong. That means presence can live in an in-memory replicated store separate from durable message history, and losing the entire presence tier degrades a status indicator instead of losing messages - which is why it must never share a failure domain or a consistency requirement with the durable path." },
      { id: "presence-fanout", prompt: "Describe why presence fan-out is dangerous at scale and how it is bounded.", answer: "Naive presence broadcasts every status change to every contact, so cost scales with the product of users and contacts and spikes exactly during reconnect storms when everything changes at once. Bound it by subscribing only to presence for conversations currently on screen, batching and debouncing updates over a window rather than emitting each transition, and coarsening state to a few buckets so minor changes produce no event at all." },
    ],
  },
  {
    id: "classic-idempotent-workflows-outbox-sagas",
    week: 3,
    day: 3,
    tier: 1,
    title: "Idempotent Workflows, Outbox, and Sagas",
    eyebrow: "One commit, then publish",
    estimatedMinutes: 85,
    summary:
      "Turn a retried intent into one durable state transition, publish its event without a dual write, and coordinate multi-service work through an explicit saga state machine.",
    whyItMatters:
      "A timeout tells the caller nothing about whether the order was placed, so retrying is a coin flip between a duplicate charge and a lost sale unless the idempotency key, the state transition, and the stored response all commit in one transaction — split them across two systems and you have rebuilt the dual write that the outbox was introduced to remove.",
    objectives: [
      "Define idempotency-key scope, request fingerprinting, retention, and atomic result storage.",
      "Use transactional outbox and consumer inbox patterns to bridge database state and at-least-once messaging.",
      "Model saga progress, compensations, and irrecoverable manual-review states explicitly.",
    ],
    concepts: [
      "idempotency key",
      "request fingerprint",
      "unknown commit outcome",
      "transactional outbox",
      "consumer inbox",
      "change-data capture",
      "orchestrated saga",
      "compensating action",
    ],
    deepDive: [
      {
        title: "Atomic idempotency claim",
        summary: "The idempotency record and business mutation commit in one local transaction.",
        points: [
          "Scope the key to caller and operation, store a request hash, and reject reuse with a different payload.",
          "Insert a pending record under a uniqueness constraint, apply the state transition, then store the canonical response before commit.",
          "On retry, return the committed response; if work is still pending, return its status rather than running a second effect.",
          "Set retention from the longest window anything can replay through, not the shortest: if the client SDK retries for 24 hours but the broker replays for 7 days, a key expired on day two lets a redelivery on day five repeat the effect under an identifier nobody recognizes.",
        ],
      },
      {
        title: "Outbox and inbox delivery",
        summary: "A local transaction writes domain state and an event row; a separate relay publishes rows until acknowledged.",
        points: [
          "Relay by polling ordered keys or reading change data, and mark or checkpoint only after broker acknowledgment.",
          "Expect publication duplicates after relay crashes; consumers atomically record event IDs with their local effects.",
          "Alarm on the age of the oldest unpublished row rather than on row count. A relay that stopped 90 seconds ago and a relay draining a 50,000-row backlog at full speed look identical by depth and nothing alike by age.",
          "Retain outbox and inbox data long enough to cover maximum replay, then compact with audited watermarks.",
        ],
      },
      {
        title: "Saga state and compensation",
        summary: "A saga records each step and drives forward actions or compensations without a distributed transaction.",
        points: [
          "Persist the saga state, next action, attempts, and deadlines before dispatching each idempotent command.",
          "Compensation is a new business action, not time travel; it can fail and must itself be retried and observed.",
          "Define terminal states for completed, compensated, and manual intervention so stuck work is queryable.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Synchronous orchestration versus event choreography",
        preferA: "Use an orchestrator when the workflow has explicit sequencing, deadlines, and compensations.",
        preferB: "Use choreography for simple reactions with low coupling and no central workflow state.",
        watch: "Unbounded choreography hides the end-to-end state; a central orchestrator can become a throughput and ownership bottleneck.",
      },
      {
        decision: "Outbox polling versus change-data capture",
        preferA: "Use polling for operational simplicity and moderate volume.",
        preferB: "Use change-data capture when lower latency and high throughput justify log-integration complexity.",
        watch: "Both still publish at least once and require consumer idempotency.",
      },
      {
        decision: "Idempotency retention",
        preferA: "Retain keys through the longest documented client retry and replay window.",
        preferB: "Archive compact result hashes when indefinite retention is too costly.",
        watch: "Expiring a key too early permits a delayed retry to repeat the business effect.",
      },
    ],
    failureModes: [
      {
        mode: "Check-then-act race",
        symptom: "Concurrent retries both observe no record and apply the same effect.",
        mitigation: "Claim the key with a uniqueness constraint and commit the claim, mutation, and response atomically.",
      },
      {
        mode: "Outbox lag or duplication",
        symptom: "Domain state is committed but downstream work is delayed, or one event is observed repeatedly.",
        mitigation: "Alert on oldest unpublished age, partition relays, replay safely, and require atomic consumer inbox deduplication.",
      },
      {
        mode: "Saga stuck during compensation",
        symptom: "The primary step failed and compensation retries exceed their deadline without a terminal state.",
        mitigation: "Persist retry policy and deadlines, escalate to manual review, and expose each uncompensated obligation as an invariant breach.",
      },
    ],
    interviewQuestions: [
      "What transaction contains the idempotency record and business effect?",
      "What happens if the outbox relay publishes and crashes before checkpointing?",
      "Which saga steps are compensable, and what happens if compensation fails?",
    ],
    decisionChecklist: [
      "Scope and fingerprint every idempotency key.",
      "Persist the canonical response with the business mutation.",
      "Assume broker and relay duplicates.",
      "Make every saga command and compensation idempotent.",
      "Monitor pending age and define manual-review terminal states.",
    ],
    exercise:
      "Design an order workflow that reserves inventory, authorizes payment, and schedules fulfillment. Draw every local transaction, outbox event, idempotency key, compensation, timeout, and manual-review state.",
    prerequisites: ["consistency-idempotency", "caching-queues"],
    relatedDesigns: ["classic-payment-ledger", "classic-notifications"],
    quiz: [
      {
        prompt: "Why can a transactional outbox still publish the same event more than once?",
        options: [
          "The database cannot enforce row uniqueness",
          "The relay can crash after ack, before checkpoint",
          "Consumers always reorder broker partitions",
          "The outbox row gets written outside a transaction",
        ],
        answerIndex: 1,
        explanation: "The broker acknowledgment and the relay checkpoint are two systems with no shared transaction, so replay is expected and consumers deduplicate in their inbox. The last option states the very thing the outbox eliminates: the row commits with the domain change, which removes the dual write but not the duplicate.",
      },
      {
        prompt: "What should happen when an idempotency key is reused with a different request body?",
        options: [
          "Execute the newer request under that key",
          "Delete the original record and rerun it",
          "Reject it as an idempotency key conflict",
          "Return the stored response and ignore it",
        ],
        answerIndex: 2,
        explanation: "The key is bound to a request fingerprint, so a changed payload is a caller bug and must surface as a conflict. Quietly returning the stored response is the tempting behavior because it looks idempotent, but it reports success for an intent that was never executed.",
      },
      {
        prompt: "Clients retry for up to 24 hours and the broker can replay for 7 days. How long must idempotency keys be retained?",
        options: [
          "24 hours, the client retry window",
          "8 days, the sum of both windows",
          "1 hour, once the response is stored",
          "7 days, the longest replay window",
        ],
        answerIndex: 3,
        explanation: "Retention must cover the longest documented retry or replay window, so the 7-day broker replay sets the floor. Choosing 24 hours covers only the client, and expiring the key early lets a day-old redelivery repeat the business effect under a key nobody remembers.",
      },
      {
        prompt: "Two concurrent retries of one request each applied the charge. Which detail was missing?",
        options: [
          "The claim was not a uniqueness-constrained insert",
          "The broker delivered the events out of order",
          "The consumer inbox retained events too long",
          "The saga had no manual-review terminal state defined",
        ],
        answerIndex: 0,
        explanation: "This is the check-then-act race: both requests read no record and proceeded, which only an atomic claim committed with the mutation prevents. Blaming broker ordering is the usual misdiagnosis, since the two requests were genuinely concurrent and no ordering guarantee would have separated them.",
      },
    ],
    recallCards: [
      { id: "outbox-dual-write", prompt: "Explain the dual-write problem and how the transactional outbox solves it.", answer: "Writing domain state to a database and publishing an event to a broker are two separate systems with no shared transaction, so a crash between them either loses the event or publishes one for a mutation that rolled back. The outbox writes the event as a row in the same transaction as the domain change, making them atomic, and a separate relay reads committed outbox rows and publishes them. The relay can crash after publishing but before marking the row sent, so publication is at-least-once and every consumer must remain idempotent." },
      { id: "outbox-workflow-state", prompt: "State why long-running workflows must persist state and what must be recorded.", answer: "A workflow spanning minutes to days cannot live in process memory, because any deploy or crash would lose it mid-flight with side effects already committed. Persist the workflow instance with its current step, the inputs and outputs of completed steps, an idempotency key per external effect, retry counts, and a deadline, so recovery resumes at the next unexecuted step rather than restarting and duplicating effects." },
    ],
  },
  {
    id: "classic-payment-state-ledger",
    week: 5,
    day: 2,
    tier: 1,
    title: "Payment State Machines and Immutable Ledgers",
    eyebrow: "Money is an append-only fact",
    estimatedMinutes: 90,
    summary:
      "Separate an externally asynchronous payment state machine from an immutable, transactionally balanced double-entry ledger that records financial truth.",
    whyItMatters:
      "A capture that times out has three possible truths — it happened, it did not, or it will in forty seconds — and the API has to answer the caller before knowing which. That is why the state machine carries a pending state that is not a failure, why every money-moving command is bound to an idempotency scope, and why the journal is append-only. A mutable balance can tell you what it holds now; it can never tell you which of the three actually occurred.",
    objectives: [
      "Model legal authorization, capture, settlement, refund, failure, and unknown transitions.",
      "Post balanced, immutable debit and credit entries in one database transaction.",
      "Use optimistic concurrency, idempotency, outbox, and reconciliation without claiming exactly-once delivery.",
    ],
    concepts: [
      "payment state machine",
      "authorization and capture",
      "settlement",
      "refund",
      "double-entry accounting",
      "immutable journal",
      "optimistic concurrency",
      "audit trail",
    ],
    deepDive: [
      {
        title: "Payment transition control",
        summary: "Every command validates the current state and version before creating the next durable transition.",
        points: [
          "Represent provider-pending and unknown outcomes explicitly; never infer failure merely because a call timed out.",
          "Use a version compare-and-swap so concurrent capture or refund commands cannot both advance stale state.",
          "Persist provider request IDs and webhook IDs for idempotent query and callback handling.",
        ],
      },
      {
        title: "Balanced ledger posting",
        summary: "A journal transaction contains two or more entries where total debits equal total credits independently per currency.",
        points: [
          "Insert the journal header, all entries, and the payment transition atomically under a unique business-event ID.",
          "Give every real-world component its own line. A 100.00 capture carrying a 3.00 processor fee posts three: debit merchant receivable 97.00, debit fee expense 3.00, credit customer payable 100.00, so 97.00 + 3.00 = 100.00 balances and the fee stays separately auditable instead of disappearing into a net figure.",
          "Derive account balances from journal entries or maintained aggregates; correct mistakes with reversing entries, never mutation.",
          "Validate the balance invariant inside the same transaction that writes the entries, so an unbalanced posting is impossible to commit rather than something a nightly job discovers.",
        ],
      },
      {
        title: "Amounts, currencies, and rounding",
        summary: "Money is an integer count of minor units, and every division has to name where the remainder lands.",
        points: [
          "Store 12.34 USD as the integer 1234 alongside an explicit currency code. Binary floating point cannot represent 0.10 exactly, and a three-way split of 10.00 leaves 3.33 each plus a cent that must be posted somewhere named rather than rounded away.",
          "Refuse arithmetic across currencies: a 100.00 USD debit against an 85.00 EUR credit is not a balanced transaction. Conversion is its own modeled event recording the rate, its source, and the time it was quoted.",
          "Fix the rounding rule per currency and per operation. JPY has no minor unit at all, so a 2.9 percent fee on 1,050 JPY comes to 30.45 and must resolve to a whole number of yen by a stated rule rather than by whatever the language's default happens to do.",
        ],
      },
      {
        title: "Provider boundary and audit",
        summary: "External authorization and settlement are asynchronous facts reconciled against internal intent and ledger state.",
        points: [
          "Send provider commands through an idempotent adapter and ingest signed callbacks through a deduplicating inbox.",
          "Use a transactional outbox to publish payment and ledger changes after the local commit.",
          "Retain immutable command, response, actor, and correlation metadata sufficient to reconstruct each transition.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Single relational transaction versus distributed ledger updates",
        preferA: "Keep entries for one financial event in one relational transaction for enforceable balance invariants.",
        preferB: "Partition separate accounts only after proving one posting can remain atomic within a chosen ownership boundary.",
        watch: "Splitting a single posting across independent stores turns balance into an eventually repaired property.",
      },
      {
        decision: "Synchronous provider response versus pending state",
        preferA: "Return a final result only when the provider outcome is unambiguous within the request deadline.",
        preferB: "Return pending and expose status when timeouts or asynchronous settlement make the outcome unknown.",
        watch: "Mapping timeout to failure can trigger a duplicate authorization on retry.",
      },
      {
        decision: "Computed versus materialized balances",
        preferA: "Compute from journal entries for low-volume audit paths and verification.",
        preferB: "Maintain transactional aggregates for high-volume balance reads.",
        watch: "Aggregates are projections and require continuous comparison with the immutable journal.",
      },
    ],
    failureModes: [
      {
        mode: "Duplicate financial effect",
        symptom: "Two captures or refunds exist for one client intent after a retry or duplicate webhook.",
        mitigation: "Enforce scoped idempotency and provider IDs with unique constraints in the same transaction as the state and ledger update.",
      },
      {
        mode: "Unbalanced or mutable journal",
        symptom: "A journal transaction’s total debits differ from total credits, or historical entries change after posting.",
        mitigation: "Validate balance at commit, make entries append-only, and correct through linked reversing transactions.",
      },
      {
        mode: "Concurrent illegal transition",
        symptom: "A refund and capture both succeed from an outdated payment version.",
        mitigation: "Use an explicit transition table and optimistic version check, then retry by re-reading current state.",
      },
      {
        mode: "Callback arrives before the local record exists",
        symptom: "A provider webhook for an authorization lands while the originating request is still in flight, and the handler finds no payment row to advance.",
        mitigation: "Key the callback inbox by provider request ID and persist the unmatched event, letting whichever side finishes second perform the join, instead of discarding a callback whose row has not committed yet.",
      },
    ],
    interviewQuestions: [
      "What does the API return when the provider times out after receiving the request?",
      "Which database invariant proves a journal transaction is balanced?",
      "How is an incorrect posting corrected without editing history?",
    ],
    decisionChecklist: [
      "List legal states and transitions, including pending and unknown.",
      "Define one idempotency scope for every money-moving command.",
      "Balance entries atomically by currency.",
      "Keep the journal immutable and corrections linked.",
      "Cover webhooks, outbox publication, reconciliation, and audit evidence.",
    ],
    exercise:
      "Model a partial capture followed by a partial refund: provide the payment transitions, journal transactions and entries, unique keys, provider timeout behavior, and correction path for a wrongly classified account.",
    prerequisites: ["classic-idempotent-workflows-outbox-sagas", "storage-indexing"],
    relatedDesigns: ["classic-payment-ledger"],
    quiz: [
      {
        prompt: "The provider times out after the authorization was sent. What is the correct response?",
        options: [
          "Mark it failed and authorize again at once",
          "Delete the payment row and let the client retry",
          "Hold pending and resolve by provider ID",
          "Post both a success and a failure entry",
        ],
        answerIndex: 2,
        explanation: "The provider may already have committed the authorization, so uncertainty is preserved as an explicit state and resolved by idempotent query or reconciliation. Mapping timeout to failure is the named misconception in this module and is exactly what produces a duplicate authorization on the retry.",
      },
      {
        prompt: "How should an error that has already been posted to the journal be corrected?",
        options: [
          "Update the original entry in place",
          "Delete the whole journal transaction",
          "Change only the materialized balance",
          "Append a linked reversing transaction",
        ],
        answerIndex: 3,
        explanation: "Append-only correction keeps every balance derivable from the journal and leaves the audit trail intact. Editing the materialized balance is tempting because it fixes the number a user sees, but that aggregate is a projection and would immediately disagree with the entries it is supposed to summarize.",
      },
      {
        prompt: "A 100.00 capture carries a 3.00 processor fee. Which posting balances and records the fee explicitly?",
        options: [
          "Debit 100.00; credit 97.00, fee omitted",
          "Debit 97.00 and 3.00; credit 100.00",
          "Debit 100.00; credit 100.00 and 3.00",
          "Debit 97.00; credit 97.00, fee netted",
        ],
        answerIndex: 1,
        explanation: "Debits of 97.00 plus 3.00 equal credits of 100.00, and the fee gets its own line so it stays auditable. The third option adds the fee to the credit side with no matching debit and leaves the transaction 3.00 out of balance, the single most common posting error.",
      },
      {
        prompt: "A capture and a refund both succeeded from the same payment version. What was missing?",
        options: [
          "A version compare-and-swap on the transition",
          "A signed webhook from the payment provider",
          "A longer retention window for provider IDs",
          "A materialized balance on the cash account",
        ],
        answerIndex: 0,
        explanation: "Each command must validate the current state and version and swap it atomically, so the loser re-reads instead of advancing stale state. Reaching for webhook signatures is the usual misdiagnosis: it confirms an external event is authentic and says nothing about two internal commands racing.",
      },
    ],
    recallCards: [
      { id: "ledger-double-entry", prompt: "Explain double-entry recording and why the ledger is append-only.", answer: "Every movement of value is recorded as balanced debit and credit entries against accounts, so the sum across a transaction is zero and the system can prove that value was neither created nor destroyed. The ledger is append-only because a financial record must remain auditable: a correction is a new reversing entry referencing the original, never a mutation or delete, which preserves the full history needed for dispute resolution and regulatory audit. Balances are then a derived fold over entries, often materialized as a snapshot plus subsequent entries for performance." },
      { id: "ledger-external", prompt: "Describe how an external payment processor's ambiguous outcome is handled.", answer: "A timeout from a processor does not reveal whether the charge happened, so the system must never blindly retry: send a caller-generated idempotency key so a retry returns the original outcome rather than charging twice, and record the attempt in a pending state before calling out. Resolution comes from querying the processor for the key's status and from reconciling against the processor's settlement file, with any discrepancy escalated rather than auto-resolved, since silently guessing about money is the failure mode that matters." },
    ],
  },
  {
    id: "classic-retries-reconciliation",
    week: 3,
    day: 4,
    tier: 1,
    title: "Retries and Reconciliation",
    eyebrow: "Ambiguity is the normal case",
    estimatedMinutes: 75,
    summary:
      "Retry only bounded transient work, quarantine poison messages, and reconcile independent records to discover silent loss, duplication, or disagreement.",
    whyItMatters:
      "A retry is a bet that the failure was transient and that the dependency has room for one more request. During a partial outage both halves of that bet are usually wrong at the same moment. Reconciliation is the opposite instrument: it repairs nothing while the incident is running, and it is the only thing that will tell you a week later that the settlement file and the ledger disagree by eleven transactions nobody ever saw fail.",
    objectives: [
      "Classify transient, permanent, throttled, and ambiguous failures before retrying.",
      "Apply deadlines, exponential backoff, jitter, budgets, dead-letter handling, and safe redrive.",
      "Design repeatable reconciliation with watermarks, matching rules, and append-only corrections.",
    ],
    concepts: [
      "retry budget",
      "exponential backoff",
      "jitter",
      "deadline propagation",
      "poison message",
      "dead-letter queue",
      "reconciliation watermark",
      "compensating correction",
    ],
    deepDive: [
      {
        title: "Retry decision matrix",
        summary: "The caller chooses retry behavior from error class, idempotency, remaining deadline, and dependency health.",
        points: [
          "Retry transient transport and throttling errors with capped exponential backoff and full jitter; honor server retry hints.",
          "Do not automatically retry validation or authorization failures, and resolve ambiguous commits through idempotent status lookup.",
          "Count the waits, not just the calls. Against a 400 ms p99 dependency with 200 ms then 400 ms backoff, three attempts consume 400 + 200 + 400 + 400 + 400 = 1,800 ms, so a fourth attempt would return after a 2-second deadline has already expired.",
          "Give exactly one layer in a call chain the right to retry. Three layers each retrying twice turns one client request into as many as 27 dependency calls, which is how a dependency at 90 percent capacity is handed an order of magnitude more load.",
          "Limit attempts and total elapsed time; one request's retry budget must fit inside the user-visible deadline.",
        ],
      },
      {
        title: "Poison work and redrive",
        summary: "Repeatedly failing items leave the hot queue with their full diagnostic and replay context.",
        points: [
          "Move an item to a dead-letter queue after a policy limit and record error class, attempts, payload version, and correlation ID.",
          "Redrive through the normal idempotent consumer after fixing the cause; do not bypass validation or rate limits.",
          "Quarantine by tenant or key when one malformed producer would otherwise monopolize a shared partition.",
        ],
      },
      {
        title: "Independent reconciliation",
        summary: "A deterministic job compares internal records with the external or canonical source over a closed time window.",
        points: [
          "Use stable business identifiers and watermarks, and delay the window until late-arriving records are expected to settle.",
          "Classify missing, duplicate, amount/state mismatch, and unmatched records rather than collapsing them into one error count.",
          "Write reconciliation results and corrective actions immutably so reruns are repeatable and audited.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Aggressive versus conservative retries",
        preferA: "Retry quickly when an idempotent dependency has spare capacity and failures are brief.",
        preferB: "Back off or fail fast when saturation, throttling, or little deadline remains.",
        watch: "Retries consume the same constrained resource that is already failing.",
      },
      {
        decision: "Automatic redrive versus manual review",
        preferA: "Automatically redrive known transient failures after the dependency recovers.",
        preferB: "Require review for malformed data, money movement, or uncertain side effects.",
        watch: "Bulk redrive can recreate the original incident unless paced and isolated.",
      },
      {
        decision: "Online versus batch reconciliation",
        preferA: "Use online checks for narrow, high-value invariants requiring rapid intervention.",
        preferB: "Use batch windows for high-volume independent comparison and lower coupling.",
        watch: "Online coupling can reduce availability; batch detection increases divergence time.",
      },
    ],
    failureModes: [
      {
        mode: "Retry storm",
        symptom: "Dependency errors trigger more request volume, rising queue age, and collapsing recovery capacity.",
        mitigation: "Use jitter, retry budgets, circuit breaking, admission control, and a single retry layer per call chain.",
      },
      {
        mode: "Poison-message partition stall",
        symptom: "One item fails repeatedly while later items in the ordered partition stop progressing.",
        mitigation: "Bound attempts, quarantine with ordering-aware policy, preserve the failed offset, and alert for controlled redrive.",
      },
      {
        mode: "Silent source divergence",
        symptom: "Request success metrics look healthy while provider and internal totals differ over time.",
        mitigation: "Run independent reconciliation by watermark, alarm on classified deltas, and apply idempotent append-only corrections.",
      },
    ],
    interviewQuestions: [
      "Which errors are retryable, and which layer owns the retry?",
      "How do you prevent redriving a dead-letter queue from repeating financial effects?",
      "What independent datasets and keys does reconciliation compare?",
    ],
    decisionChecklist: [
      "Classify errors before selecting a retry policy.",
      "Cap attempts, elapsed time, and total retry load.",
      "Preserve diagnostic context in dead-letter records.",
      "Make redrive use the normal idempotent path.",
      "Define reconciliation watermarks, mismatch classes, and correction ownership.",
    ],
    exercise:
      "Create a retry and reconciliation policy for payment capture: cover timeouts, throttling, validation failure, duplicate webhook, poison events, delayed settlement files, redrive, and manual review.",
    prerequisites: ["classic-idempotent-workflows-outbox-sagas", "caching-queues"],
    relatedDesigns: ["classic-payment-ledger", "classic-notifications"],
    quiz: [
      {
        prompt: "What most effectively prevents synchronized retry bursts against a struggling dependency?",
        options: [
          "Fixed-interval retries with zero delay",
          "Unlimited attempts until it succeeds",
          "Capped exponential backoff with jitter",
          "A retry layer at every hop in the chain",
        ],
        answerIndex: 2,
        explanation: "Backoff lowers the offered load while jitter breaks the lockstep that turns one blip into a storm. Retrying at every hop is the tempting answer because each layer looks locally reasonable, but attempts multiply along the chain, which is why one retry layer per call chain is the rule.",
      },
      {
        prompt: "Why is reconciliation still required when every handler is already idempotent?",
        options: [
          "It finds records two systems disagree on",
          "Idempotency guarantees every event arrives",
          "It makes external providers transactional",
          "It replaces dashboards and alerting entirely",
        ],
        answerIndex: 0,
        explanation: "Idempotency stops an effect from being applied twice; it cannot prove a required effect happened at all, or that a counterparty recorded the same result. The second option is the precise confusion this module corrects, between at-most-once effects and at-least-once delivery.",
      },
      {
        prompt: "A call has a 2-second deadline, the dependency's p99 is 400 ms, and backoff is 200 ms then 400 ms. How many attempts fit?",
        options: [
          "Five attempts, since the waits are free",
          "Three attempts, about 1.8 s in total",
          "Two attempts, if backoff is excluded",
          "Unlimited; the deadline is advisory",
        ],
        answerIndex: 1,
        explanation: "400 + 200 + 400 + 400 + 400 comes to 1,800 ms, so three attempts fit and a fourth would blow the deadline. The 'waits are free' option is the common error of counting only call time, and it is how a retry budget silently overruns the user-visible deadline.",
      },
      {
        prompt: "Success metrics look healthy, yet internal and provider totals drift further apart each week. What detects this?",
        options: [
          "Longer client-side retry budgets",
          "A circuit breaker on the provider call",
          "Dead-letter queue depth alarms",
          "Windowed reconciliation by watermark",
        ],
        answerIndex: 3,
        explanation: "Only an independent comparison over a closed window, with missing, duplicate and mismatch classified separately, can see an effect that never happened. Dead-letter depth is the tempting proxy, but a diverged record usually never failed loudly enough to be dead-lettered in the first place.",
      },
    ],
    recallCards: [
      { id: "recon-why", prompt: "Explain why reconciliation is required even when every step is idempotent.", answer: "Idempotency prevents duplicate effects but cannot detect an effect that never happened or one applied by a system you do not control. Messages are dropped, third parties disagree, bugs skip steps, and manual interventions occur - so an independent process must periodically compare the system's own state against the counterparty's authoritative record and against invariants, then surface differences. Reconciliation is the safety net that converts silent divergence into a visible, actionable discrepancy." },
      { id: "recon-poison", prompt: "Describe how a poison message is handled without stalling a partition.", answer: "A record that deterministically fails will retry forever and, in an ordered partition, block every message behind it. Bound retries per record, then move the failure to a dead-letter destination with its full context and error so the partition advances. That trades strict ordering for liveness, so it is only safe when later records do not depend on the failed one; where order is required, the partition must instead pause and page a human. Dead-lettered records need an owned replay path, not just a queue nobody reads." },
    ],
  },
  {
    id: "classic-notification-orchestration",
    week: 5,
    day: 3,
    tier: 1,
    title: "Notification Orchestration",
    eyebrow: "Earn the right to interrupt",
    estimatedMinutes: 80,
    summary:
      "Turn one durable notification intent into policy-compliant, prioritized, rate-limited delivery attempts across email, SMS, push, and in-app channels.",
    whyItMatters:
      "A ten-million-recipient digest and a two-factor code are the same API call with a different priority field, and the provider carrying both is capped at 5,000 sends per second. The digest owns that pipe for thirty-three minutes unless something structural stops it, so lanes, weighted fairness, and per-tenant token buckets are not tuning knobs — they are what keeps a login code from queueing behind a marketing campaign.",
    objectives: [
      "Separate notification intent, recipient planning, channel delivery, and provider callbacks.",
      "Apply preferences, quiet hours, deduplication, priority, and quotas at explicit stages.",
      "Handle provider throttling, failover, retries, dead-letter work, and delivery-state monotonicity.",
    ],
    concepts: [
      "notification intent",
      "channel fan-out",
      "priority queue",
      "scheduled delivery",
      "user preferences",
      "provider adapter",
      "deduplication window",
      "dead-letter queue",
    ],
    deepDive: [
      {
        title: "Intent and recipient planning",
        summary: "The API durably records intent before asynchronous expansion into recipient-channel deliveries.",
        points: [
          "Reject the request synchronously if the template version does not exist, the audience reference resolves to nothing, or the idempotency key has already been used with a different payload. Everything past this point runs asynchronously and has no way to report a validation error back to the caller.",
          "Expand large audiences in checkpointed batches and store a deterministic delivery key per recipient, channel, and intent.",
          "Evaluate consent, preferences, quiet hours, and channel eligibility near send time so scheduled work uses current policy.",
        ],
      },
      {
        title: "Priority, fairness, and rate control",
        summary: "Channel queues isolate providers while fair scheduling protects transactional traffic and tenants.",
        points: [
          "Separate urgent transactional, normal, and bulk lanes, then apply weighted fairness rather than strict priority starvation.",
          "Size each lane against the provider's real ceiling: 10,000,000 digest recipients through a 5,000-per-second cap is 2,000 seconds of occupied pipe, so the bulk lane needs a stated backlog objective in tens of minutes rather than an autoscaling rule that converts the wait into throttle responses.",
          "Enforce token buckets at every scope that can be abused independently — the provider account, the tenant, the campaign, and the individual recipient — because a per-provider limit alone still lets one tenant consume the whole allowance.",
          "Autoscale workers from oldest-ready age and provider headroom, not raw queue depth alone.",
        ],
      },
      {
        title: "Provider outcome handling",
        summary: "Adapters normalize provider-specific accepted, delivered, bounced, throttled, and permanent-failure outcomes.",
        points: [
          "Persist provider request IDs and deduplicate signed callbacks before advancing delivery state.",
          "Retry transient failures with jitter; send poison or exhausted work to a diagnosable dead-letter queue.",
          "Fail over only when duplicate semantics and sender identity remain acceptable; provider acceptance may make the outcome ambiguous.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Preference evaluation timing",
        preferA: "Evaluate near send for scheduled work and rapid opt-out correctness.",
        preferB: "Precompute eligibility during planning to reduce hot-path reads for short-lived transactional sends.",
        watch: "A stale preference snapshot can violate consent or quiet-hour policy.",
      },
      {
        decision: "Strict priority versus fair queues",
        preferA: "Reserve capacity for urgent transactional notifications.",
        preferB: "Use weighted fairness so bulk work progresses without starving smaller tenants.",
        watch: "Strict priority can starve bulk indefinitely; equal treatment can delay password or security messages.",
      },
      {
        decision: "Provider failover",
        preferA: "Fail over before an unambiguous provider acceptance when availability is more important than affinity.",
        preferB: "Hold and reconcile after an ambiguous acceptance when duplicate user contact is unacceptable.",
        watch: "Two providers generally cannot share an atomic idempotency boundary.",
      },
      {
        decision: "What deduplication is keyed on",
        preferA: "Key on the delivery tuple of intent, recipient, and channel when one event should legitimately reach a person on more than one channel.",
        preferB: "Key on a logical event identity spanning channels when the person should hear about it once regardless of how they are reachable.",
        watch: "The narrow key sends a push and an email for the same event; the wide key suppresses a genuinely second notification that merely shares an event type.",
      },
    ],
    failureModes: [
      {
        mode: "Duplicate user contact",
        symptom: "A recipient receives the same logical notification twice after timeout, callback replay, or provider failover.",
        mitigation: "Use deterministic delivery keys, provider request IDs, callback inbox dedupe, and cautious handling of ambiguous outcomes.",
      },
      {
        mode: "Bulk campaign starvation",
        symptom: "Oldest bulk queue age grows without bound while transactional traffic remains healthy.",
        mitigation: "Use weighted fair scheduling, per-class capacity floors, tenant quotas, and explicit backlog SLOs.",
      },
      {
        mode: "Provider outage amplification",
        symptom: "Retries and failover saturate alternate providers while queue age and throttle responses climb.",
        mitigation: "Circuit-break the failing adapter, honor rate limits, apply jitter and budgets, and degrade low-priority traffic first.",
      },
      {
        mode: "Preference evaluated at planning, applied at send",
        symptom: "Someone who unsubscribed hours ago still receives a scheduled digest that was expanded into deliveries before the opt-out landed.",
        mitigation: "Re-read consent, quiet hours, and channel eligibility immediately before dispatch, and treat the planning-time snapshot as a cost filter rather than as the authority on permission.",
      },
    ],
    interviewQuestions: [
      "At which point is a notification considered durably accepted?",
      "How do preferences and quiet hours interact with scheduled messages?",
      "When is provider failover safe after a timeout?",
    ],
    decisionChecklist: [
      "Persist intent before fan-out and use deterministic delivery keys.",
      "Separate priorities, channels, tenants, and providers for isolation.",
      "Apply preferences and consent at a freshness-appropriate stage.",
      "Rate-limit at provider, tenant, campaign, and recipient scopes.",
      "Define callback dedupe, ambiguous outcomes, DLQ, and redrive.",
    ],
    exercise:
      "Design a notification flow for a security alert and a ten-million-recipient digest sharing the same providers. Specify intent and delivery records, scheduling, preference timing, fairness, retry, failover, and callback handling.",
    prerequisites: ["classic-idempotent-workflows-outbox-sagas", "classic-retries-reconciliation"],
    relatedDesigns: ["classic-notifications"],
    quiz: [
      {
        prompt: "Which metric shows whether scheduled notifications are meeting their delivery objective?",
        options: [
          "Total configured queue capacity",
          "Oldest ready-item age by priority",
          "Queue depth summed over lanes",
          "Worker CPU utilization average",
        ],
        answerIndex: 1,
        explanation: "Oldest ready age states directly how late actionable work is, which is what the objective is written against. Queue depth is the tempting proxy, but it moves with batch size and throughput: a deep queue draining fast is fine, and a shallow stuck one is not.",
      },
      {
        prompt: "Why can immediate failover after a provider timeout create duplicate user contact?",
        options: [
          "The first provider may have accepted it",
          "Preferences cannot be cached anywhere at all",
          "Queues here give exactly-once delivery",
          "SMS carries no provider request ID",
        ],
        answerIndex: 0,
        explanation: "A timeout is an ambiguous outcome, and two providers cannot share one atomic idempotency boundary, so the second send may duplicate a first that quietly succeeded. The exactly-once option is the belief this whole module rejects; brokers and providers deliver at least once.",
      },
      {
        prompt: "A ten-million-recipient digest runs through a provider capped at 5,000 sends per second. How long is the bulk lane busy?",
        options: [
          "About 2 minutes with more workers",
          "About 33 hours because of retries",
          "Under a minute; the cap is per lane",
          "About 33 minutes at the provider cap",
        ],
        answerIndex: 3,
        explanation: "10,000,000 divided by 5,000 per second is 2,000 seconds, roughly 33 minutes, and that floor is set by the provider token bucket. 'More workers' is the standard misconception that autoscaling fixes a rate-limited path; extra workers only convert the wait into throttle responses.",
      },
      {
        prompt: "Transactional delivery is healthy, but the oldest bulk item is now days old. What is the fix?",
        options: [
          "Raise the transactional lane priority",
          "Circuit-break the failing adapter now",
          "Give bulk a weighted capacity floor",
          "Shorten the callback dedupe window",
        ],
        answerIndex: 2,
        explanation: "Strict priority starves bulk indefinitely, so fairness weights, per-class capacity floors and an explicit backlog SLO are what let it progress. Raising transactional priority is the reflex and deepens the starvation, and circuit-breaking targets a provider outage that the healthy transactional lane already rules out.",
      },
    ],
    recallCards: [
      { id: "notif-dedupe", prompt: "Describe how a notification system avoids spamming a user across channels and devices.", answer: "Deduplicate on a logical notification identity rather than per delivery attempt, so one event produces one user-visible notification even when it is retried or arrives on several devices. Apply per-user rate limits and a quiet-period window, collapse related events into a digest instead of emitting each, and record delivery per channel so a push that succeeded suppresses the email fallback. Preference and consent checks must be evaluated at send time, not at enqueue time, since they may have changed while the message sat in the queue." },
      { id: "notif-priority", prompt: "Explain why notification traffic must be prioritized and isolated by class.", answer: "A marketing blast and a two-factor code share infrastructure but not urgency: if they share a queue, a campaign of millions can delay a security-critical message past its usefulness. Separate classes into their own queues with independent rate limits and worker pools so bulk traffic cannot starve transactional traffic, and shed or defer low-priority work first under overload while protecting the latency-critical class." },
    ],
  },
  {
    id: "classic-multipart-content-addressed-storage",
    week: 5,
    day: 4,
    tier: 1,
    title: "Multipart and Content-Addressed File Storage",
    eyebrow: "Let content name itself",
    estimatedMinutes: 85,
    summary:
      "Move large byte streams directly to object storage, verify resumable parts, and atomically commit a manifest of durable content-addressed chunks.",
    whyItMatters:
      "A 50 GB upload over a hotel network will not finish in one request, and a design that concedes this early looks nothing like one that does not: bytes go straight to object storage under narrowly scoped signed URLs, the application tier handles only part bookkeeping, and a single manifest commit is the instant the file exists. Get that boundary wrong and you are either proxying terabytes through servers sized for JSON, or letting a reader open a file that is still half uploaded.",
    objectives: [
      "Design resumable multipart upload with checksums, expiry, and an atomic commit point.",
      "Explain fixed versus content-defined chunks and safe content-addressed deduplication.",
      "Keep manifests, reference tracking, scanning, CDN download, and delayed garbage collection consistent.",
    ],
    concepts: [
      "multipart upload",
      "signed upload URL",
      "chunk manifest",
      "content hash",
      "deduplication",
      "object storage",
      "reference tracking",
      "CDN-backed download",
    ],
    deepDive: [
      {
        title: "Resumable upload session",
        summary: "A metadata service creates an expiring session while clients upload parts directly to object storage.",
        points: [
          "Record expected part ranges, maximum size, uploader authorization, and session expiry before issuing narrowly scoped signed URLs.",
          "Choose the part size as a retry-cost decision: at 8 MB, a 50 GB file is roughly 6,400 parts, so a dropped connection costs 8 MB of re-transfer instead of 50 GB. Halving the part size halves that loss and doubles manifest rows, hash work, request count, and eventual collection work.",
          "Verify each part's length and checksum, allowing safe overwrite of the same part number during retry.",
          "Commit only after all required parts are durable and verified; the commit transaction creates the immutable file-version manifest.",
        ],
      },
      {
        title: "Content addressing and dedupe",
        summary: "A chunk hash names verified bytes, while the manifest preserves order and file identity.",
        points: [
          "Use fixed chunks for simpler range mapping or content-defined chunks for better dedupe after insertions.",
          "Hash on trusted infrastructure or verify client claims before referencing an existing object.",
          "Scope dedupe to an authorization boundary so hash existence cannot reveal another tenant's data.",
        ],
      },
      {
        title: "Lifecycle and delivery",
        summary: "Committed manifests are durable metadata; unreferenced chunks are reclaimed only after a safety window.",
        points: [
          "Track references transactionally or derive them through mark-and-sweep; never delete based on a stale decrement alone.",
          "Quarantine new content until required malware or policy scans complete, without mutating the immutable bytes.",
          "Serve authorized downloads through short-lived URLs and a CDN while keeping metadata and access checks at the control plane.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Chunk size",
        preferA: "Use larger chunks for fewer objects, smaller manifests, and efficient sequential transfer.",
        preferB: "Use smaller or content-defined chunks for resumability, delta reuse, and deduplication.",
        watch: "Very small chunks amplify metadata, requests, hashes, and garbage-collection work.",
      },
      {
        decision: "Reference count versus mark-and-sweep",
        preferA: "Use transactional reference counts when manifest updates and chunk ownership share a reliable boundary.",
        preferB: "Use periodic mark-and-sweep when references are distributed or count repair must be authoritative.",
        watch: "Immediate deletion on count zero is unsafe under races, delayed events, and restore windows.",
      },
      {
        decision: "Cross-tenant deduplication",
        preferA: "Deduplicate within a tenant or encryption domain for safer isolation.",
        preferB: "Use global dedupe only with a threat model that addresses hash-probing and key ownership.",
        watch: "Content hashes can become an oracle that reveals whether another user stores known bytes.",
      },
    ],
    failureModes: [
      {
        mode: "Orphaned upload parts",
        symptom: "Object-storage bytes grow faster than committed file versions and expired sessions accumulate.",
        mitigation: "Expire sessions, tag temporary parts, sweep only past a safety watermark, and monitor orphan bytes by age.",
      },
      {
        mode: "Manifest references missing or corrupt bytes",
        symptom: "A committed file fails checksum verification or returns missing chunks during download.",
        mitigation: "Verify durability and hashes before atomic manifest commit, replicate objects, and continuously scrub stored content.",
      },
      {
        mode: "Unsafe garbage collection",
        symptom: "Live file versions lose chunks shortly after concurrent overwrite, restore, or delayed reference processing.",
        mitigation: "Use epochs and grace periods, mark from canonical manifests, recheck before deletion, and retain deletion audit records.",
      },
    ],
    interviewQuestions: [
      "What exact operation makes an uploaded file visible?",
      "How do you verify a client-provided content hash without trusting the client?",
      "How can garbage collection prove a chunk is no longer referenced?",
    ],
    decisionChecklist: [
      "Keep bulk bytes off application servers through scoped direct upload.",
      "Verify part size and checksums before commit.",
      "Commit one immutable manifest as the visibility boundary.",
      "Scope dedupe and downloads to authorization policy.",
      "Use delayed, auditable garbage collection with a canonical mark source.",
    ],
    exercise:
      "Design upload and download for a 50 GB file over an unreliable connection. Define session, part, chunk, and manifest records; retry behavior; verification; dedupe scope; commit; CDN authorization; and orphan cleanup.",
    prerequisites: ["storage-indexing", "consistency-idempotency"],
    relatedDesigns: ["classic-file-sync"],
    quiz: [
      {
        prompt: "At what point does a new file version become visible to readers?",
        options: [
          "As soon as the first part arrives",
          "When the signed upload URL is issued",
          "At the atomic manifest commit point",
          "After garbage collection has swept",
        ],
        answerIndex: 2,
        explanation: "The manifest commit is the one metadata boundary that proves every chunk is durable and verified, so the version is reconstructable. Treating the first durable part as visibility confuses object-storage durability with completeness: parts can be safely stored while the version is still a fragment.",
      },
      {
        prompt: "What is the security risk of unrestricted global hash deduplication?",
        options: [
          "A caller can probe for known content",
          "It removes checksums from all chunks",
          "It prevents resumable multipart upload",
          "It forces chunk sizes to grow larger",
        ],
        answerIndex: 0,
        explanation: "Observable dedupe behavior becomes a content-existence oracle: uploading a known file and seeing an instant commit reveals another tenant already stores it. The checksum option confuses hashing used for naming with hashing used for verification; dedupe scope changes neither.",
      },
      {
        prompt: "A 50 GB file is chunked at 8 MB. Roughly how many entries does one version's manifest carry?",
        options: [
          "About 640 manifest entries",
          "About 64,000 manifest entries",
          "About 50 entries, one per gigabyte",
          "About 6,400 manifest entries",
        ],
        answerIndex: 3,
        explanation: "50 GB divided by 8 MB is roughly 6,400 chunks, which is why metadata cost scales inversely with chunk size: shrink the chunk tenfold and manifest rows, hashes, requests and collection work all grow tenfold. The one-per-gigabyte option assumes chunks follow a display unit rather than the configured part size.",
      },
      {
        prompt: "A live file version starts returning missing chunks shortly after a concurrent overwrite. What went wrong?",
        options: [
          "Part checksums were never verified",
          "Deletion fired on a stale zero count",
          "The signed download URL had expired",
          "Content-defined chunking was in use",
        ],
        answerIndex: 1,
        explanation: "A delayed decrement can drive a count to zero while a new manifest still references the chunk, so collection needs grace epochs, a mark derived from canonical manifests, and a recheck immediately before deletion. Blaming checksums is tempting because the download fails verification, but the bytes were fine until they were reclaimed.",
      },
    ],
    recallCards: [
      { id: "cas-content-address", prompt: "Explain what content addressing gives you and its main cost.", answer: "Naming a blob by the cryptographic hash of its bytes makes storage inherently deduplicated - identical content maps to one object regardless of who uploaded it - and makes integrity verifiable, since a reader can recompute the hash. It also makes objects immutable, which simplifies caching enormously because a content address can be cached forever. The costs are that mutation requires writing a new object and updating a pointer, deletion requires reference counting across all referrers, and cross-tenant deduplication can leak the existence of content unless scoped." },
      { id: "cas-multipart", prompt: "Describe why large uploads use multipart and what the client-direct pattern avoids.", answer: "Multipart splits a large object into independently uploaded, retryable parts, so a network failure at 90 percent retries one part rather than the whole transfer, and parts can upload in parallel to saturate available bandwidth. Issuing presigned URLs so the client uploads directly to object storage keeps the application tier out of the data path entirely, which avoids proxying terabytes through servers sized for requests, and leaves the service to handle only authorization, part bookkeeping, and the final commit that makes the object visible." },
    ],
  },
  {
    id: "classic-file-sync-version-conflicts",
    week: 6,
    day: 1,
    tier: 1,
    title: "File Sync, Versioning, and Conflict Resolution",
    eyebrow: "Divergence is expected, not exceptional",
    estimatedMinutes: 85,
    summary:
      "Synchronize metadata changes through an append-only cursor, require base-version compare-and-swap for writes, and preserve concurrent edits as explicit versions rather than silent overwrites.",
    whyItMatters:
      "Two laptops, one file, ninety days apart: the protocol has to decide what a rename means when the other side deleted the parent folder, whether a returning device may resurrect content everyone else discarded, and which of two edits gets to be the file. None of those are byte-transfer questions, which is why the namespace, the change log, and the chunk store stay three separate problems that meet only at a version commit.",
    objectives: [
      "Design an append-only change log with stable cursors and snapshot recovery.",
      "Use version preconditions to detect concurrent edits, moves, and deletes.",
      "Choose explicit conflict, merge, or last-writer policy from file semantics and user expectations.",
    ],
    concepts: [
      "change journal",
      "sync cursor",
      "base version",
      "optimistic concurrency",
      "conflict copy",
      "tombstone",
      "delta synchronization",
      "version history",
    ],
    deepDive: [
      {
        title: "Namespace and change journal",
        summary: "A strongly consistent metadata transaction updates the namespace and appends one ordered change record.",
        points: [
          "Key each node by stable ID rather than path so rename and move do not change file identity.",
          "Assign a monotonic cursor within an account or namespace and return changes plus a high-water mark.",
          "When a device cursor is older than retention, require a fresh snapshot and resume from the snapshot watermark.",
        ],
      },
      {
        title: "Concurrent updates and conflicts",
        summary: "A client commits against the version it read; a mismatch triggers product-specific conflict handling.",
        points: [
          "Use compare-and-swap on node version for content, name, parent, and tombstone transitions.",
          "Auto-merge only formats with a well-defined merge function; preserve opaque binary edits as sibling or conflict-copy versions.",
          "Make the conflict result deterministic and sync it to every device so clients do not create an endless conflict loop.",
        ],
      },
      {
        title: "Delete, restore, and delta transfer",
        summary: "Deletion creates a retained tombstone, while chunk manifests make unchanged byte ranges reusable.",
        points: [
          "Retain tombstones longer than the supported offline window so an old device cannot resurrect deleted content silently.",
          "Let retention set the resnapshot boundary explicitly: if devices may be offline for 90 days but tombstones compact at 30, every device returning after day 30 must be forced through a fresh snapshot, because its cursor now points into history that no longer records the deletions.",
          "Restore creates a new version linked to prior history and must resolve any current namespace name conflict.",
          "Compare chunk manifests and transfer only missing hashes; metadata commit still controls which full version is current.",
        ],
      },
      {
        title: "Shared folders and revoked access",
        summary: "A subtree two accounts can both mount makes permission a version rather than a boolean.",
        points: [
          "Carry an ACL version on every node and check it when bytes are fetched, not only when the folder is listed, so a device holding a cached chunk manifest cannot keep downloading content after removal.",
          "Express unsharing as a change-log record in the removed member's own namespace. Their client then learns to unmount the folder, rather than keeping a divergent copy it will eventually try to push back.",
          "Decide once whether a removed member keeps their local files. Whatever the product answers, it has to be the same on every one of their devices and driven by a change record, not by whichever client happens to sync first.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Conflict handling",
        preferA: "Create explicit conflict versions for opaque or high-value files where no safe merge exists.",
        preferB: "Apply deterministic auto-merge for formats with tested semantic merge behavior.",
        watch: "Last-writer-wins is simple but silently discards concurrent work and depends on a trustworthy ordering rule.",
      },
      {
        decision: "Global versus per-namespace cursor",
        preferA: "Use per-namespace cursors for scalable independent ordering and smaller sync scans.",
        preferB: "Use a global account cursor when clients need one simple total change stream.",
        watch: "Global sequencing can become a hot coordination point; multiple cursors complicate atomic cross-namespace moves.",
      },
      {
        decision: "Tombstone retention",
        preferA: "Retain through the maximum offline and restore window for deletion correctness.",
        preferB: "Compact earlier only when old devices are forced to resnapshot before uploading changes.",
        watch: "Short retention permits stale clients to resurrect deleted nodes.",
      },
    ],
    failureModes: [
      {
        mode: "Silent lost update",
        symptom: "One device's edit disappears after another device uploads from an older version.",
        mitigation: "Require base-version compare-and-swap and preserve a conflict version instead of overwriting.",
      },
      {
        mode: "Cursor gap or premature advance",
        symptom: "A client claims to be synchronized but permanently misses a change after a partial page failure.",
        mitigation: "Advance only after applying a contiguous page, return high-water marks, and make page replay idempotent.",
      },
      {
        mode: "Stale-device resurrection",
        symptom: "A long-offline device recreates a file that was deleted on every current device.",
        mitigation: "Retain tombstones through the offline contract and force clients beyond retention to take a fresh snapshot.",
      },
    ],
    interviewQuestions: [
      "What does a sync cursor order, and when may a client advance it?",
      "How are simultaneous edits to the same binary file represented?",
      "How does an offline device learn that a file was deleted months ago?",
    ],
    decisionChecklist: [
      "Use stable node IDs independent of paths.",
      "Append namespace mutation and change record atomically.",
      "Require base versions and define deterministic conflict results.",
      "Retain tombstones through the supported offline window.",
      "Provide snapshot recovery when cursors expire.",
    ],
    exercise:
      "Walk through two offline devices editing, renaming, and deleting the same file. Specify metadata versions, change-log records, cursor replay, conflict output, tombstone retention, and eventual convergence.",
    prerequisites: ["classic-multipart-content-addressed-storage", "replication-partitioning"],
    relatedDesigns: ["classic-file-sync"],
    quiz: [
      {
        prompt: "What stops an offline client from silently overwriting a newer file version?",
        options: [
          "A longer CDN TTL on downloads",
          "A base-version compare-and-swap",
          "A smaller upload chunk size",
          "A last-writer-wins timestamp",
        ],
        answerIndex: 1,
        explanation: "Committing against the version that was read turns concurrent modification into an explicit conflict the product can resolve. Last-writer-wins is the tempting simple rule and is precisely what discards the other device's work, while resting on clocks that disagree between devices.",
      },
      {
        prompt: "What should happen when a device's sync cursor predates the retained change history?",
        options: [
          "Assume nothing changed meanwhile",
          "Replay an arbitrary recent page",
          "Fetch a snapshot, resume from it",
          "Upload every local file as new",
        ],
        answerIndex: 2,
        explanation: "Once the incremental history behind the cursor is compacted, only a consistent snapshot plus its watermark establishes a correct baseline. Re-uploading everything is the tempting 'safe' move and it resurrects deleted nodes while duplicating the namespace.",
      },
      {
        prompt: "Devices may be offline for up to 90 days but tombstones compact after 30. Which devices must be forced to resnapshot?",
        options: [
          "Any device offline more than 30 days",
          "Any device offline more than 90 days",
          "Only devices offline under 30 days",
          "No device; the cursor always resumes",
        ],
        answerIndex: 0,
        explanation: "Retention sets the boundary: at day 31 the cursor points past the surviving history, so incremental resume can no longer see the deletions. Assuming the cursor always resumes is the misconception behind stale-device resurrection, where a returning laptop re-uploads a file that every current device deleted.",
      },
      {
        prompt: "A client reports it is fully synced yet permanently misses one change after a page failed midway. What happened?",
        options: [
          "Tombstone retention was set too short",
          "Nodes were keyed by path, not by ID",
          "Auto-merge ran on an opaque binary file",
          "The cursor advanced past a partial page",
        ],
        answerIndex: 3,
        explanation: "A cursor may only move after a contiguous page has been fully applied, so a partial apply must leave the high-water mark where it was and replay idempotently. Tombstone retention is the tempting neighbor failure, but it produces resurrected files rather than one change silently skipped forever.",
      },
    ],
    recallCards: [
      { id: "sync-detect", prompt: "Explain how a sync client distinguishes a genuine conflict from a stale write.", answer: "Each file version carries causal metadata - a version vector or per-device counter - rather than only a timestamp. On merge the client compares vectors: if one dominates the other the write is simply stale and the newer version wins, but if neither dominates the edits were concurrent and it is a true conflict. Timestamps alone cannot make this distinction, because clock skew makes a newer wall-clock value no evidence of causal ordering, which is how naive systems silently discard edits." },
      { id: "sync-resolve", prompt: "State what a sync product does with a detected conflict and why.", answer: "For authored content it preserves both sides, typically as a conflicted copy attributed to a device, because a document represents user intent that the system cannot correctly merge and silently discarding either version destroys work. Automatic merge is reserved for structures with a well-defined commutative merge, such as a set union or a counter. The rule is that resolution must never lose information without telling the user." },
    ],
  },
  {
    id: "classic-geo-indexing-hot-regions-privacy",
    week: 6,
    day: 2,
    tier: 1,
    title: "Geo Indexing, Hot Regions, and Privacy",
    eyebrow: "Turn space into a key",
    estimatedMinutes: 85,
    summary:
      "Use an approximate spatial index to generate nearby candidates, filter by exact distance and eligibility, and bound freshness, hotspot, and location-privacy risk.",
    whyItMatters:
      "Two drivers 100 meters apart can land in different cells, and a query that trusts cell membership simply does not see one of them. Space refuses to partition cleanly. The index is only a candidate generator, exact spherical distance is the filter, and one downtown block will carry a thousand times the update rate of the county around it. Location is also the most re-identifying field most systems store, which puts precision, expiry, and deletion in the query path rather than in a retention policy nobody reads.",
    objectives: [
      "Compare geohash, quadtree, and database spatial indexes for radius and update workloads.",
      "Query neighboring cells and apply exact geometry to remove approximate-index false positives.",
      "Handle hot regions, location freshness, privacy, deletion, and retention as first-class constraints.",
    ],
    concepts: [
      "geohash",
      "quadtree",
      "spatial index",
      "neighbor-cell expansion",
      "exact distance filter",
      "location freshness",
      "adaptive partitioning",
      "privacy retention",
    ],
    deepDive: [
      {
        title: "Candidate generation by cells",
        summary: "Map each entity to one or more index cells, then cover a query radius with cells before exact filtering.",
        points: [
          "Choose cell precision so typical queries scan a bounded candidate set; include adjacent cells because radii cross cell boundaries.",
          "Size the candidate budget against the cover, not the result limit: with roughly 1 km cells and a 1 km radius the cover is 9 cells — the query's own plus its 8 neighbors — and the exact distance test then discards a large share of what they return, since a square cover is always a superset of a circle.",
          "Fetch candidate IDs from covered cells, hydrate latest coordinates and eligibility, then compute exact spherical distance.",
          "Deduplicate overlapping-cell results. For best-effort live pagination, use `(distance, entityId)` and accept drift; when duplicates or omissions are unacceptable, pin a location snapshot or search-session candidate set and track seen entity IDs.",
        ],
      },
      {
        title: "Moving-object updates",
        summary: "Versioned location updates replace an entity's prior cell membership without allowing late events to regress state.",
        points: [
          "Authenticate the producer and store event time, receive time, version, and expiry with the latest location.",
          "Apply only increasing versions, remove old cell postings idempotently, and tolerate a brief dual-posting window through hydration checks.",
          "Coalesce noisy updates according to distance or time thresholds that match the product freshness SLI.",
        ],
      },
      {
        title: "Hot regions and privacy",
        summary: "Dense cells need independent splits, while precise location requires minimization and access control.",
        points: [
          "Split dense cells recursively or add deterministic subshards; query all child shards while isolating their write load.",
          "Keep stable entity metadata separate from volatile location so each store can use an appropriate retention and replication policy.",
          "Check the caller's authorization and declared purpose before the query runs, round the returned coordinate down to the precision that purpose actually needs, and drop anything past its expiry. A result that is merely accurate is not the same as a result you are permitted to return.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Spatial cell precision",
        preferA: "Use finer cells to reduce false-positive candidates in dense regions.",
        preferB: "Use coarser cells to reduce index fan-out and update churn in sparse regions.",
        watch: "One global precision performs poorly across both downtown and rural density.",
      },
      {
        decision: "Update freshness versus cost",
        preferA: "Ingest frequent updates for safety-critical or rapidly moving entities.",
        preferB: "Coalesce by movement and time when minute-level freshness is sufficient.",
        watch: "Aggressive coalescing reduces write cost but increases stale and incorrectly ordered results.",
      },
      {
        decision: "Index ownership",
        preferA: "Use a managed spatial index when supported query semantics and operational simplicity dominate.",
        preferB: "Use explicit geohash or quadtree partitions when scale and hotspot control require custom placement.",
        watch: "Generic indexes can hide shard skew; custom indexes expand correctness and operational surface.",
      },
    ],
    failureModes: [
      {
        mode: "Boundary miss",
        symptom: "Nearby entities disappear when the query point lies near a spatial-cell edge.",
        mitigation: "Cover all intersecting neighbor cells, deduplicate candidates, and validate with exact distance tests at boundaries.",
      },
      {
        mode: "Dense-cell hotspot",
        symptom: "A small urban cell has disproportionate write QPS, query fan-in, and tail latency.",
        mitigation: "Use adaptive splits or subshards, cap radius and candidates, cache stable metadata, and isolate hot-cell capacity.",
      },
      {
        mode: "Stale or unauthorized location exposure",
        symptom: "Queries return expired, unavailable, deleted, or access-restricted entities.",
        mitigation: "Hydrate authoritative freshness and eligibility, apply TTL and policy filters, and prioritize deletion propagation.",
      },
    ],
    interviewQuestions: [
      "How do you avoid missing results across a geohash boundary?",
      "What changes when one downtown block receives a thousand times the normal update rate?",
      "Where are freshness, authorization, and retention enforced?",
    ],
    decisionChecklist: [
      "Define radius, result limit, freshness, and eligibility requirements.",
      "Use approximate cells only for candidate generation.",
      "Version updates and reject late state regression.",
      "Plan adaptive hot-cell isolation and candidate caps.",
      "Minimize, expire, authorize, and delete location data explicitly.",
    ],
    exercise:
      "Design nearby-driver lookup for a dense city and a sparse region. Choose index precision, update coalescing, exact filtering, cursor, hotspot split, stale-location rule, and privacy retention.",
    prerequisites: ["replication-partitioning", "storage-indexing"],
    relatedDesigns: ["classic-nearby-service"],
    quiz: [
      {
        prompt: "Why is an exact distance filter still required after a geohash lookup?",
        options: [
          "Geohashes encrypt the coordinates",
          "Distance itself cannot be indexed",
          "It gives results a global ordering",
          "Cells include points outside radius",
        ],
        answerIndex: 3,
        explanation: "A covering set of cells is a rectangle-shaped superset of a circular radius, so exact spherical distance is what actually enforces the query. 'Distance cannot be indexed' is the misconception that pushes people to skip candidate generation and scan every entity instead.",
      },
      {
        prompt: "What is the robust response to a persistently hot spatial cell?",
        options: [
          "Merge all locations into one larger cell",
          "Recursively split or subshard the cell",
          "Stop expiring stale location rows",
          "Order updates by the client clock",
        ],
        answerIndex: 1,
        explanation: "Adaptive splitting isolates the exceptional region and leaves sparse areas at a coarse precision, so only the hot area pays for extra fan-out. Coarsening into one bigger cell is the intuitive but backwards fix; it concentrates even more writes and candidates on the same partition.",
      },
      {
        prompt: "Cells are roughly 1 km square and the query radius is 1 km. How many cells must the candidate scan cover?",
        options: [
          "One cell, the query point's own",
          "Four cells, the nearest quadrant",
          "Nine cells, the cell plus neighbors",
          "Sixty-four cells at that precision",
        ],
        answerIndex: 2,
        explanation: "A 1 km radius reaches into every adjacent cell, so the cover is the query cell plus its eight neighbors, deduplicated and then filtered by exact distance. Scanning only the query's own cell is the boundary-miss bug: an entity 100 meters away but across an edge simply disappears.",
      },
      {
        prompt: "Nearby search keeps returning drivers who went offline minutes ago. Which stage is missing?",
        options: [
          "Hydrating freshness at query time",
          "Neighbor-cell expansion on reads",
          "Recursive splitting of dense cells",
          "Version checks on location writes",
        ],
        answerIndex: 0,
        explanation: "Cell postings are candidates only; eligibility, TTL and authorization have to be re-read from the authoritative record before anything is returned. Version checks are the tempting near-miss, since they do stop late events from regressing state, but they cannot tell you a driver has since gone offline.",
      },
    ],
    recallCards: [
      { id: "geo-index", prompt: "Compare geohash, quadtree, and S2 for proximity search and state the shared problem.", answer: "Geohash interleaves latitude and longitude into a string prefix so nearby points usually share a prefix, making it trivially indexable in any key-value store; quadtrees recursively subdivide space and adapt to density; S2 projects onto a sphere-filling curve and avoids the distortion of a flat grid. All share the boundary problem: two points can be physically adjacent yet fall in different cells, so any correct query must search neighboring cells and then filter by true distance, never trusting cell membership alone." },
      { id: "geo-privacy", prompt: "Explain the privacy obligations specific to location data.", answer: "Location is re-identifying even when the account is pseudonymous, because a home and workplace pair is close to unique, so precise historical traces must be minimized rather than retained by default. Store coarse buckets for analytics, keep precise coordinates only as long as the live feature needs them with enforced short retention, and separate the precise store from the analytical one. Sharing location with other users needs explicit, revocable consent and coarsening, since exact positions expose more than users expect." },
    ],
  },
  {
    id: "classic-crawler-frontier-politeness-dedupe",
    week: 6,
    day: 3,
    tier: 1,
    title: "Crawler Frontier, Politeness, and Deduplication",
    eyebrow: "Restraint is the hard part",
    estimatedMinutes: 85,
    summary:
      "Schedule a broad crawl through a host-aware frontier that obeys robots policy, limits per-origin load, canonicalizes URLs, and detects URL and content duplicates.",
    whyItMatters:
      "Ten thousand fetchers and a one-request-per-second politeness rule against 2,000 eligible hosts means the crawl runs at 2,000 pages per second while 8,000 workers sit idle. That ratio is the entire design: throughput belongs to the host queue, never to the worker pool. The expensive mistake is a generated calendar that mints a million URLs resolving to nearly the same page, spending a budget that genuinely new hosts were queued behind.",
    objectives: [
      "Design a prioritized URL frontier with per-host eligibility time and bounded concurrency.",
      "Apply robots rules, canonicalization, redirect policy, and crawl-trap defenses before fetching.",
      "Distinguish URL deduplication from post-fetch content fingerprinting and recrawl freshness.",
    ],
    concepts: [
      "URL frontier",
      "host-aware scheduling",
      "robots.txt",
      "crawl politeness",
      "canonical URL",
      "Bloom filter",
      "content fingerprint",
      "recrawl priority",
    ],
    deepDive: [
      {
        title: "Two-level frontier scheduling",
        summary: "A global priority selects important hosts while each host queue enforces its own next-allowed fetch time.",
        points: [
          "Partition host state consistently so one scheduler owns per-origin concurrency, delay, backoff, and robots policy.",
          "Store URLs durably with priority, discovery source, attempts, and next-fetch time; lease work so crashed fetchers do not lose it.",
          "Balance discovery coverage and recrawl freshness using explicit priority classes and bounded starvation.",
        ],
      },
      {
        title: "Polite fetching",
        summary: "Fetchers resolve current policy and origin budget before making a conditional request.",
        points: [
          "Cache robots policy with expiry and conservative behavior when it cannot be refreshed; identify the crawler clearly.",
          "Use per-host connection and request limits, exponential backoff, and conditional headers such as validators when available.",
          "Bound response size, redirects, content types, and fetch time to protect capacity and prevent malicious traps.",
        ],
      },
      {
        title: "Duplicate and trap control",
        summary: "Normalize discovered URLs early, then fingerprint fetched content to detect aliases and mirrors.",
        points: [
          "Normalize only the parts of a URL that cannot change what is served: lowercase the scheme and host, drop `:80` on http and `:443` on https, and discard the fragment. Leave query parameters alone unless a per-host rule marks one as tracking noise, because `?page=2` and `?page=3` are different documents.",
          "Treat a Bloom filter as a prefilter: on a definite negative, attempt an atomic insert into the definitive URL store; on a positive, consult that store before discarding the URL. Never exclude a URL from the Bloom result alone.",
          "Detect unbounded calendars, session parameters, repeated path shapes, and near-identical content; cap crawl depth and host expansion.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Coverage versus freshness",
        preferA: "Prioritize newly discovered hosts and pages when corpus breadth is the product goal.",
        preferB: "Prioritize high-change known pages when index freshness dominates.",
        watch: "A single queue score can starve either the long tail or important recrawls.",
      },
      {
        decision: "Probabilistic versus definitive URL dedupe",
        preferA: "Use a Bloom filter to skip many definitive reads on definite negatives; positives still require a lookup before rejection.",
        preferB: "Use a definitive store before permanently excluding a URL from coverage.",
        watch: "Bloom false positives silently reduce coverage; definitive lookups add storage and latency.",
      },
      {
        decision: "Central versus host-partitioned scheduling",
        preferA: "Use a central priority view for simple global policy at smaller scale.",
        preferB: "Partition by host to scale politeness state and prevent concurrent origin overload.",
        watch: "Global reprioritization is harder when work is distributed across host owners.",
      },
      {
        decision: "Conditional refetch versus blind recrawl",
        preferA: "Send `If-Modified-Since` or `If-None-Match` and accept a 304 when the goal is confirming freshness cheaply across a large known corpus.",
        preferB: "Refetch unconditionally when a host's validators are unreliable, or when the parser changed and old bytes have to be reprocessed anyway.",
        watch: "A 304 saves the body but not the request, so conditional recrawl buys bandwidth and buys nothing back from the per-host politeness budget.",
      },
    ],
    failureModes: [
      {
        mode: "Politeness violation",
        symptom: "One origin sees excess concurrency or request rate, and throttling or complaints increase.",
        mitigation: "Give one owner authority over host budgets, cache robots policy, honor backoff, and alarm on per-host violations.",
      },
      {
        mode: "Crawl trap explosion",
        symptom: "Frontier growth, duplicate ratios, and one host's URL cardinality rise without proportional unique content.",
        mitigation: "Normalize parameters, detect repeating path shapes, cap depth and expansion, and quarantine suspect hosts.",
      },
      {
        mode: "Lease loss or duplicate fetch",
        symptom: "Fetcher crashes cause URLs to disappear or be fetched repeatedly before eligibility.",
        mitigation: "Use expiring leases, idempotent fetch-result writes, deterministic URL IDs, and lease-recovery metrics.",
      },
    ],
    interviewQuestions: [
      "How does the scheduler guarantee per-host politeness with thousands of fetchers?",
      "Which URL transformations are safe to canonicalize?",
      "How do you prevent a generated calendar from consuming the entire crawl budget?",
    ],
    decisionChecklist: [
      "State corpus scope, coverage, and recrawl-freshness goals.",
      "Partition politeness ownership by host or origin.",
      "Persist leased frontier work and explicit next-fetch times.",
      "Apply URL and content duplicate defenses separately.",
      "Bound redirects, bytes, time, depth, and per-host expansion.",
    ],
    exercise:
      "Design the frontier for one billion known URLs. Define host ownership, priority, robots caching, lease recovery, recrawl scheduling, canonicalization, duplicate stores, and crawl-trap controls.",
    prerequisites: ["caching-queues", "networking"],
    relatedDesigns: ["classic-crawler-search"],
    quiz: [
      {
        prompt: "Why is frontier work partitioned by host rather than spread freely across fetchers?",
        options: [
          "To make fetched page bodies smaller",
          "To rank every crawled page equally",
          "To own per-host concurrency and delay",
          "To avoid caching robots.txt at all",
        ],
        answerIndex: 2,
        explanation: "One owner per origin serializes the politeness budget even when thousands of fetchers run concurrently, which no global throttle can do. The robots option inverts the design: host partitioning is precisely what lets a cached robots policy be authoritative in a single place.",
      },
      {
        prompt: "Why is a Bloom filter alone unsafe as the permanent URL source of truth?",
        options: [
          "It always yields false negatives",
          "It cannot hash arbitrary strings",
          "It stores whole page bodies too",
          "False positives drop new URLs",
        ],
        answerIndex: 3,
        explanation: "A positive can be wrong, so a URL that was never crawled would be silently excluded from coverage unless the definitive store confirms it. The 'always false negatives' option reverses the guarantee: Bloom filters never produce false negatives, which is exactly why a definite negative is safe to act on.",
      },
      {
        prompt: "Politeness allows one fetch per second per host. With 10,000 fetchers and 2,000 eligible hosts, what caps throughput?",
        options: [
          "2,000 fetches per second, host-bound",
          "10,000 per second, one per fetcher",
          "12,000 per second, the two combined",
          "5 fetches per second for each fetcher",
        ],
        answerIndex: 0,
        explanation: "Per-host delay makes the eligible-host count the ceiling: 2,000 hosts times one fetch per second is 2,000 fetches per second, and the other 8,000 fetchers wait. Reading throughput off the fetcher count is the standard error, and 'fixing' those idle workers is how politeness violations get shipped.",
      },
      {
        prompt: "One host's frontier cardinality explodes while its unique-content ratio collapses. What is happening?",
        options: [
          "Leases are expiring before fetch",
          "A crawl trap generates URL aliases",
          "Robots policy refreshed too often",
          "Content fingerprints are disabled",
        ],
        answerIndex: 1,
        explanation: "Generated calendars, session parameters and repeating path shapes mint endless URLs that resolve to near-identical pages, so the answer is canonicalization, depth and expansion caps, and quarantine. Missing content fingerprints is the tempting half-answer: they catch the duplicate after the fetch, once the crawl budget is already spent.",
      },
    ],
    recallCards: [
      { id: "crawl-politeness", prompt: "Explain what politeness means for a crawler and how it constrains the frontier.", answer: "Politeness means never overloading a single origin: respect robots.txt, honor crawl-delay, and cap concurrent connections and request rate per host. That constrains scheduling because the frontier cannot simply pop the highest-priority URL globally - it must partition the queue by host so a popular domain with millions of pending URLs is rate-limited independently while other hosts proceed at full speed. Politeness is therefore a per-host scheduling problem, not a global throttle." },
      { id: "crawl-dedupe", prompt: "Describe the two distinct deduplication problems a crawler faces.", answer: "URL-level deduplication avoids re-fetching the same resource reached by different links, requiring canonicalization - normalizing case, ordering query parameters, stripping tracking and session parameters - before membership testing, usually with a Bloom filter to keep the seen-set in memory at the cost of a bounded false-positive rate. Content-level deduplication detects that different URLs returned substantially the same page, handled with a content hash for exact duplicates and a similarity fingerprint such as simhash for near-duplicates, which is what prevents mirrors and templated pages from flooding the index." },
    ],
  },
  {
    id: "classic-inverted-index-incremental-serving",
    week: 6,
    day: 4,
    tier: 1,
    title: "Inverted Indexing, Incremental Serving, and Ranking",
    eyebrow: "Precompute the answer, then rank",
    estimatedMinutes: 90,
    summary:
      "Transform parsed documents into immutable inverted-index segments, publish coherent shard manifests, merge incrementally, and rank bounded candidate sets at query time.",
    whyItMatters:
      "Nothing in a search index is ever edited. A deletion is a tombstone, an update is a newer document version, a flush is a new immutable file — so the only thing that actually changes is which set of files a query is allowed to look at. That one indirection is what lets indexing run continuously underneath a serving fleet, and it is also the source of merge debt, tombstone lag, and the awkward question of whether page two is reading the same index page one did.",
    objectives: [
      "Build term dictionaries and compressed postings with document IDs, frequencies, and optional positions.",
      "Index updates into immutable segments and publish versions atomically before background merge.",
      "Route, retrieve, rank, paginate, and degrade queries across replicated index shards.",
    ],
    concepts: [
      "inverted index",
      "term dictionary",
      "postings list",
      "immutable segment",
      "segment merge",
      "tombstone",
      "query broker",
      "ranking function",
    ],
    deepDive: [
      {
        title: "Index construction",
        summary: "Parsers emit versioned documents that segment builders tokenize into term-to-document postings.",
        points: [
          "Give every document a stable ID that survives recrawl, and normalize terms once at index time so the query analyzer can apply the identical transformation later. Store positions only when phrase queries need them, since positional data roughly doubles posting-list size.",
          "Sort and compress postings, write immutable segment files, then atomically register checksums and document-version coverage.",
          "Deduplicate ingestion by document and source version so event replay does not create multiple live versions.",
        ],
      },
      {
        title: "Incremental publish",
        summary: "Small fresh segments become searchable quickly, and a manifest swap is what makes them visible all at once.",
        points: [
          "Each shard publishes an immutable manifest only after its segments are durable. A query pins either a broker-issued vector with one manifest version per shard or a global manifest published after every referenced shard view is durable.",
          "Represent updates and deletes with newer document versions or tombstones, filtering obsolete postings at query time.",
          "Measure freshness document-to-searchable rather than flush-to-disk. A one-minute target with a 10-second flush interval leaves only about 50 seconds for parse, segment build, durability, and the manifest swap combined.",
        ],
      },
      {
        title: "Merge policy and compaction debt",
        summary: "Compaction is a background efficiency job competing with indexing and serving for the same disk and CPU.",
        points: [
          "Merge in size tiers so any one document is rewritten a bounded number of times. Flushing every few seconds and merging naively can rewrite the same posting a dozen times before it reaches a stable segment.",
          "Reserve compaction throughput instead of letting it run on whatever is left over. The moment ingestion outpaces merging, segment count grows, every query checks more files, and query CPU and indexing latency climb together.",
          "Delete a merged-away file only when no live manifest references it and every query pinned to an older manifest has finished, so a 30-second query timeout sets the minimum grace period before removal.",
          "Track merge debt — segments above the policy target — as a first-class metric, since it leads both the query-latency regression and the disk-exhaustion page by hours.",
        ],
      },
      {
        title: "Distributed query and ranking",
        summary: "A broker parses the query, fans out to relevant shards, and merges each shard's top results.",
        points: [
          "Retrieve candidates through postings intersection or union, compute lexical scores, and return top-K with a unique tie-breaker.",
          "Ask every shard for the full top K, not for its single best hit. A global top 10 across 50 document-sharded shards means the broker merges 500 candidates down to 10, and the cheaper one-per-shard version silently loses results whenever relevance clusters in one shard.",
          "Apply metadata, safety, and access filters before final results; cache only where query and policy scope permit.",
          "Bound shard fan-out and per-query work, return explicit partial status on replica loss, and paginate with a query-version cursor.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Index freshness versus segment efficiency",
        preferA: "Publish small segments frequently for low indexing latency.",
        preferB: "Batch larger segments for better compression and query efficiency.",
        watch: "Too many small segments increase query fan-out and merge debt.",
      },
      {
        decision: "Document-sharded versus term-sharded index",
        preferA: "Shard by document for parallel independent indexing and uniform query fan-out.",
        preferB: "Shard by term for targeted lookup when term distribution and coordination are manageable.",
        watch: "Term frequencies are highly skewed; document sharding requires top-K merge from many shards.",
      },
      {
        decision: "Strict versus partial query availability",
        preferA: "Fail when complete results are required for legal or exact lookup semantics.",
        preferB: "Return labeled partial results for discovery search when a shard is temporarily unavailable.",
        watch: "Silent partial results make relevance and availability metrics misleading.",
      },
    ],
    failureModes: [
      {
        mode: "Incoherent manifest publication",
        symptom: "Queries reference missing segment files or mix document versions across replicas.",
        mitigation: "Verify durable replicated segments, atomically swap manifests, pin versions per query, and delay old-segment deletion.",
      },
      {
        mode: "Merge backlog",
        symptom: "Segment count, storage amplification, query CPU, and indexing latency rise together.",
        mitigation: "Reserve compaction capacity, throttle ingestion selectively, prioritize merges by read cost, and alert on merge debt.",
      },
      {
        mode: "Stale delete or update",
        symptom: "Removed or superseded documents remain searchable beyond the freshness objective.",
        mitigation: "Version every document, prioritize tombstones, filter old versions at query time, and reconcile source versions to index manifests.",
      },
    ],
    interviewQuestions: [
      "What data is stored in a posting and why?",
      "How does a new segment become visible without queries seeing partial files?",
      "What does the system return when one search shard misses its deadline?",
    ],
    decisionChecklist: [
      "Define document identity, source version, and deletion SLA.",
      "Store only posting features required by retrieval and ranking.",
      "Publish immutable segments through atomic manifests.",
      "Budget merge debt and old-version cleanup.",
      "Bound query fan-out, work, cursor version, and partial-result semantics.",
    ],
    exercise:
      "Design incremental indexing and serving for a billion-document corpus with a one-minute freshness target. Specify segment creation, manifest publish, delete handling, merge policy, sharding, top-K merge, cursor, and partial failure.",
    prerequisites: ["classic-crawler-frontier-politeness-dedupe", "storage-indexing"],
    relatedDesigns: ["classic-crawler-search"],
    quiz: [
      {
        prompt: "What gives queries a coherent index view while new segments are being published?",
        options: [
          "Overwriting segment files in place",
          "Client wall-clock time at query",
          "A longer crawler URL frontier",
          "An atomically swapped manifest",
        ],
        answerIndex: 3,
        explanation: "A query pins one manifest whose referenced immutable files are already durable, so nobody observes a half-published segment set. Overwriting in place is the mental model carried over from mutable databases, and it is what leaves readers pointing at files being rewritten beneath them.",
      },
      {
        prompt: "What does very frequent publication of tiny segments cost?",
        options: [
          "More segment checks and merge debt",
          "A lower per-query shard fan-out cost",
          "Globally guaranteed ranking order",
          "No need for tombstone filtering",
        ],
        answerIndex: 0,
        explanation: "Small segments buy indexing freshness and pay for it in file count, per-query segment checks and compaction backlog. 'Lower fan-out' is exactly backwards, and that inversion is why teams keep lowering the flush interval until query CPU and merge debt climb together.",
      },
      {
        prompt: "A query needs the global top 10 across 50 document-sharded shards. How many candidates does the broker merge?",
        options: [
          "10, since the shards agree on order",
          "50, one best hit from each shard",
          "500, ten from each of the 50 shards",
          "Every match, then ranked centrally",
        ],
        answerIndex: 2,
        explanation: "Relevance can be concentrated, so each shard must offer its own top 10 and the broker merges 50 x 10 = 500 candidates down to 10 with a unique tie-breaker. Taking one hit per shard is the classic error: it silently loses results whenever the best documents cluster in a single shard.",
      },
      {
        prompt: "A deleted document is still returned minutes after removal. What is the fix?",
        options: [
          "Publish much larger, less frequent segments",
          "Prioritize tombstones, filter by version",
          "Increase the broker's shard fan-out",
          "Return labeled partial results instead",
        ],
        answerIndex: 1,
        explanation: "A delete is a new document version, so the tombstone travels on a priority path and the query filters obsolete postings rather than waiting for physical removal. Waiting on compaction is the tempting answer and makes the deletion SLA worse, because merge is a background efficiency job and never a correctness path.",
      },
    ],
    recallCards: [
      { id: "index-inverted", prompt: "Describe an inverted index's structure and how a query executes against it.", answer: "The index maps each term to a posting list of the documents containing it, usually with positions and precomputed scoring contributions, stored sorted by document ID so lists can be intersected efficiently. A conjunctive query intersects the posting lists of its terms, skipping ahead using sparse skip pointers rather than scanning, then scores surviving candidates and keeps the top k with a heap. Because the lists are the hot data, they are compressed with delta plus variable-length encoding so more of the index fits in memory." },
      { id: "index-incremental", prompt: "Explain how a search index absorbs continuous updates while serving queries.", answer: "Segments are immutable: new documents accumulate in a small in-memory segment that is flushed to a new immutable segment, and deletions are recorded as a tombstone bitmap rather than by editing existing segments. Queries search all segments and filter tombstoned documents, so freshness is bounded by flush interval rather than by rebuild time. A background merge periodically compacts small segments and drops deleted documents, trading I/O for query efficiency - the same read-write trade an LSM makes." },
    ],
  },
  {
    id: "classic-observability-ingestion-cardinality-retention",
    week: 7,
    day: 1,
    tier: 1,
    title: "Observability Ingestion, Cardinality, and Retention",
    eyebrow: "Cardinality is the real budget",
    estimatedMinutes: 90,
    summary:
      "Collect metrics and logs through durable regional buffers, control tenant and label cardinality before indexing, and tier raw and aggregated data by retention value.",
    whyItMatters:
      "The platform is asked its hardest questions at the exact moment it is least able to answer them: log volume rises with the error rate, one new pod label turns 100,000 active series into 200 million, and every engineer opens the same dashboard at once, making it the most expensive query of the day. Cost is not a finance concern here; it decides whether the tool works when someone needs it.",
    objectives: [
      "Design agent, collector, durable-stream, storage, query, dashboard, and alert paths.",
      "Partition metrics and logs while enforcing tenant isolation, quotas, and cardinality limits.",
      "Choose indexing, aggregation, downsampling, retention, and cold-tier policies from query needs.",
    ],
    concepts: [
      "agent and collector",
      "write-ahead buffer",
      "event-stream partition",
      "time-series index",
      "high-cardinality labels",
      "log indexing",
      "downsampling",
      "retention tier",
    ],
    deepDive: [
      {
        title: "Loss-aware ingestion",
        summary: "Agents batch locally; regional collectors authenticate, validate, buffer, and acknowledge only after a durable boundary.",
        points: [
          "Batch and compress records, attach tenant identity at the trusted edge, and reject malformed or over-quota traffic with explicit counters.",
          "Spool briefly to disk during stream outages with strict byte and age limits; define whether overflow drops newest, oldest, or low-priority data.",
          "Partition the durable stream by tenant plus series hash for metrics and by tenant plus time or source for logs, balancing order and spread.",
        ],
      },
      {
        title: "Cardinality and indexing",
        summary: "Series identity is the metric name plus normalized labels; every unbounded label can multiply memory and index cost.",
        points: [
          "Set the quota against the multiplication, not the label count: service (50) x endpoint (200) x status (10) is already 100,000 active series, and adding a pod label with 2,000 values makes it 200 million, because series identity is the whole label tuple.",
          "Enforce active-series and label-value quotas per tenant, and reject or rewrite unsafe dimensions such as request IDs.",
          "Use an inverted label index to find time-series IDs, then scan time-partitioned compressed sample blocks.",
          "Index a curated subset of log attributes; keep raw bodies in cheaper segments so arbitrary fields do not explode the index.",
        ],
      },
      {
        title: "Aggregation and retention",
        summary: "Recent raw data serves debugging; older rollups preserve trends at lower resolution and cost.",
        points: [
          "Build idempotent time buckets with explicit late-data and out-of-order rules before marking a rollup complete.",
          "Retain raw, indexed, and aggregated tiers independently, and enforce tenant deletion across every tier and cache.",
        ],
      },
      {
        title: "Query cost and fair scheduling",
        summary: "A dashboard refresh is an unbounded scan until something bounds it.",
        points: [
          "Charge a query in bytes scanned and series touched rather than in seconds, and reject a plan whose estimate exceeds the tenant's budget before it starts instead of killing it halfway through.",
          "Cache by time bucket so a dashboard refreshing every 30 seconds re-reads one new bucket rather than rescanning the same 24-hour range 2,880 times a day.",
          "Route by time range and resolution, and label a result partial when a shard misses its deadline. A silently truncated graph during an incident is worse than an error, because somebody will draw a conclusion from it.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Index breadth",
        preferA: "Index dimensions used frequently for filtering and alerting.",
        preferB: "Scan or post-filter rare fields from cheaper raw storage.",
        watch: "Indexing every arbitrary field causes unbounded memory, write, and compaction amplification.",
      },
      {
        decision: "Raw retention versus rollups",
        preferA: "Keep raw high-resolution data for recent incident debugging.",
        preferB: "Keep long-lived aggregates for trends, capacity, and cost control.",
        watch: "Rollups cannot answer later questions that require discarded labels or fine temporal detail.",
      },
      {
        decision: "Drop versus block under overload",
        preferA: "Drop sampled or low-priority telemetry with explicit accounting to protect applications and critical signals.",
        preferB: "Block only when telemetry loss is less acceptable than propagating backpressure to the producer.",
        watch: "Unreported dropping creates false confidence; blocking can worsen the production incident being observed.",
      },
      {
        decision: "Scraped pull versus agent push",
        preferA: "Scrape targets on a fixed interval when the platform should own discovery, timing, and the staleness semantics of every series.",
        preferB: "Accept pushes when workloads are short-lived, network paths run one way, or the source sits outside the platform's discovery scope.",
        watch: "Pull hands you a liveness signal for free — a target that stops answering is visibly absent — while push turns the same condition into silence you cannot distinguish from a healthy idle service.",
      },
    ],
    failureModes: [
      {
        mode: "Cardinality explosion",
        symptom: "Active-series count, index memory, compaction backlog, and ingestion latency rise after a new label appears.",
        mitigation: "Enforce per-tenant budgets, reject unbounded labels, aggregate at collection, and expose top cardinality contributors.",
      },
      {
        mode: "Collector overload and silent loss",
        symptom: "Applications remain healthy while telemetry gaps appear and collector buffers fill or restart.",
        mitigation: "Bound local spools, report accepted/rejected/dropped counts externally, shed by priority, and alert on oldest buffered age.",
      },
      {
        mode: "Retention or deletion leak",
        symptom: "Expired or deleted data remains searchable in a rollup, cold tier, index, or query cache.",
        mitigation: "Track deletion across every derived tier, version retention manifests, verify completion, and audit with periodic scans.",
      },
    ],
    interviewQuestions: [
      "At what boundary is an ingestion acknowledgment durable?",
      "How does one request-ID label affect a time-series database?",
      "Which data remains after raw samples expire, and which queries become impossible?",
    ],
    decisionChecklist: [
      "Define accepted, rejected, delayed, and dropped semantics.",
      "Partition by tenant and stable data identity.",
      "Budget active series, label values, query fan-out, and storage.",
      "Make late-data and rollup completion semantics explicit.",
      "Apply retention and deletion to raw, derived, indexed, cached, and cold data.",
    ],
    exercise:
      "Design a multi-tenant pipeline for metrics and structured logs. Specify acknowledgment, partitions, cardinality quotas, indexes, raw and rollup retention, overload shedding, expensive-query controls, and self-monitoring.",
    prerequisites: ["caching-queues", "storage-indexing"],
    relatedDesigns: ["classic-observability-platform"],
    quiz: [
      {
        prompt: "Why is a request ID unsafe as a metric label?",
        options: [
          "Label values must not be strings",
          "It stops logs from being batched",
          "It makes one series per request",
          "It forces UDP transport for agents",
        ],
        answerIndex: 2,
        explanation: "Series identity is the metric name plus its normalized labels, so a near-unique value multiplies active series, index memory and compaction cost regardless of traffic. The batching option confuses transport efficiency with cardinality, and cardinality is the cost that actually breaks a time-series database.",
      },
      {
        prompt: "What should a collector do when its local buffer reaches a hard limit?",
        options: [
          "Consume memory without a bound",
          "Acknowledge data it did not keep",
          "Disable per-tenant quotas first",
          "Drop by priority and count it",
        ],
        answerIndex: 3,
        explanation: "A bounded, accounted overload policy protects the application being observed and turns telemetry loss into a number someone can see. Acknowledging data that was never retained is the tempting way to keep producers quiet, and it manufactures false confidence during the exact incident the platform exists to diagnose.",
      },
      {
        prompt: "A metric carries service (50 values), endpoint (200) and status (10). Adding a pod label with 2,000 values gives how many series?",
        options: [
          "102,000 active series total",
          "200 million active series",
          "2,000 active series total",
          "100,000 series, pod excluded",
        ],
        answerIndex: 1,
        explanation: "Labels multiply, so 50 x 200 x 10 = 100,000 series becomes 200 million once a 2,000-value pod label joins the tuple. The additive options encode the widespread belief that a new label just adds its own values; series identity is the whole tuple, which is how one unbounded dimension ends a cluster.",
      },
      {
        prompt: "A tenant's deleted data still appears on a dashboard fed by rollups. What went wrong?",
        options: [
          "Deletion never reached derived tiers",
          "The label index was far too narrow",
          "The collector spool had overflowed",
          "Query fan-out exceeded its cost cap",
        ],
        answerIndex: 0,
        explanation: "Every derived tier - raw, indexed, aggregated, cached and cold - holds an independent copy, so deletion has to be tracked across all of them and verified by audit scan. Blaming the collector spool is the tempting incident-shaped answer, but overflow loses data rather than preserving data that was supposed to be gone.",
      },
    ],
    recallCards: [
      { id: "obs-cardinality", prompt: "Explain why cardinality is the dominant cost in a metrics system.", answer: "A metric's storage and memory cost scale with the number of distinct label combinations, not with request volume, because each unique combination is a separate time series with its own index entry and retention. Adding one unbounded label such as user ID or request ID multiplies series count without limit and can exhaust a cluster far faster than traffic growth. Control it by treating labels as a schema with enforced allowlists, rejecting or aggregating high-cardinality dimensions at ingestion, and pushing per-request identity into traces and logs, which are designed for it." },
      { id: "obs-signals", prompt: "Contrast metrics, logs, and traces by what each is good at.", answer: "Metrics are pre-aggregated numeric series: cheap to store and query over long windows, ideal for alerting and trends, but they cannot explain an individual request. Logs are per-event records with high detail, good for forensic reconstruction, but expensive at volume and awkward to aggregate. Traces capture causal structure across services for individual requests, which is what localizes latency in a distributed call graph, and are almost always sampled because retaining every trace is prohibitive. A workable system uses metrics to detect, traces to localize, and logs to explain." },
    ],
  },
  {
    id: "classic-slos-backpressure-degradation",
    week: 7,
    day: 2,
    tier: 1,
    title: "SLOs, Backpressure, and Graceful Degradation",
    eyebrow: "Choose what to shed first",
    estimatedMinutes: 80,
    summary:
      "Translate user outcomes into SLIs and error budgets, then bound admission and queues so overload triggers intentional degradation instead of uncontrolled collapse.",
    whyItMatters:
      "99.9 percent over 30 days is about 43 minutes of badness the product has agreed to spend, and writing that number down changes the argument from whether the system is reliable enough into what the 43 minutes are being spent on. Everything else follows from it: a budget implies a burn rate, a burn rate decides which alerts are allowed to wake someone, and an overload policy is just deciding in advance which half of the product gets dropped so the other half stays inside the number.",
    objectives: [
      "Define latency, availability, correctness, and freshness SLIs with valid-event populations.",
      "Use error budgets and burn-rate alerts to distinguish urgent incidents from normal variance.",
      "Place backpressure, admission control, and graceful degradation at each queue and dependency boundary.",
    ],
    concepts: [
      "service-level indicator",
      "service-level objective",
      "error budget",
      "burn-rate alert",
      "bounded queue",
      "admission control",
      "load shedding",
      "graceful degradation",
    ],
    deepDive: [
      {
        title: "User-centered SLI math",
        summary: "An SLI is a ratio or distribution over clearly defined valid user events, not an internal average.",
        points: [
          "Define good and total events, exclusions, aggregation window, and percentile or threshold from the user-visible operation.",
          "Track availability separately from correctness and freshness so a fast stale response cannot pass the wrong objective.",
          "Allocate end-to-end latency and availability budgets to dependencies without assuming independent failure.",
        ],
      },
      {
        title: "Error budget and alerting",
        summary: "The allowed bad-event fraction guides release pace and multi-window burn alerts.",
        points: [
          "Calculate remaining budget over the objective window and show both fast-burn and slow-burn rates.",
          "Do the arithmetic once so the alert thresholds mean something: 0.1 percent of the 43,200 minutes in 30 days is roughly 43 minutes of bad events, and burning at 14 times the sustained rate would exhaust the whole month in about two days, which is what makes that rate worth a page.",
          "Page on actionable rapid exhaustion; create lower-urgency work for sustained slow burn.",
          "Combine symptom alerts with queue age, saturation, and dependency signals for diagnosis, not as substitutes for the SLO.",
        ],
      },
      {
        title: "Overload control and degradation",
        summary: "Every admitted request consumes bounded concurrency, queue, deadline, and downstream work.",
        points: [
          "Reject early using tenant, priority, and cost-aware admission before scarce workers or database connections are consumed.",
          "Propagate deadlines and backpressure; cap queues by age and bytes, and avoid retries at multiple layers.",
          "Write the degraded modes down before the incident and put them in order: serve the cache past its TTL first, then swap personalized ranking for a popularity fallback, then defer analytics writes, and disable optional fan-out last — while every correctness-critical check keeps running untouched.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Fail open versus fail closed",
        preferA: "Fail open for optional personalization or analytics when a safe default exists.",
        preferB: "Fail closed for authorization, privacy, payment invariants, and unsafe unknown state.",
        watch: "One blanket policy either harms availability or violates correctness and security.",
      },
      {
        decision: "Queue versus reject",
        preferA: "Queue short bounded bursts when work remains valuable within its deadline.",
        preferB: "Reject or shed when queue delay will exceed usefulness or amplify an outage.",
        watch: "Deep queues convert overload into delayed timeouts and make recovery slow.",
      },
      {
        decision: "Strict freshness versus stale serving",
        preferA: "Require fresh source-of-truth reads for money, authorization, and explicit freshness contracts.",
        preferB: "Serve labeled stale cached data for feeds, search, or dashboards when availability is more valuable.",
        watch: "Stale fallback needs a maximum age and must not bypass deletion or access revocation.",
      },
    ],
    failureModes: [
      {
        mode: "Retry amplification",
        symptom: "A slow dependency causes more attempts, longer queues, lower success, and fleet-wide saturation.",
        mitigation: "Use one retry owner, budgets, jitter, circuit breaking, early admission control, and deadline propagation.",
      },
      {
        mode: "Healthy averages hide tail failure",
        symptom: "Mean latency and host uptime look normal while a user cohort or high percentile burns the SLO.",
        mitigation: "Measure event-level thresholds and percentiles by important slice, and alert on multi-window error-budget burn.",
      },
      {
        mode: "Unsafe degradation",
        symptom: "Fallback improves availability but returns unauthorized, financially inconsistent, or excessively stale results.",
        mitigation: "Classify invariants before the incident, test degraded modes, cap staleness, and fail closed on safety-critical checks.",
      },
    ],
    interviewQuestions: [
      "What are the exact good and total events for this design's primary SLO?",
      "Where does the system reject work before overload reaches storage?",
      "Which features degrade safely, and which invariants must never degrade?",
    ],
    decisionChecklist: [
      "Define user-visible latency, availability, correctness, and freshness separately.",
      "Specify error-budget window and burn-rate alerts.",
      "Bound every queue by age, bytes, or work cost.",
      "Propagate deadlines and assign one retry owner.",
      "Predefine and test safe degraded modes and recovery.",
    ],
    exercise:
      "For a newsfeed or nearby service, write the primary SLO, dependency budgets, overload admission policy, queue limits, retry owner, three degraded modes, and one invariant that must fail closed.",
    prerequisites: ["classic-observability-ingestion-cardinality-retention", "classic-retries-reconciliation"],
    relatedDesigns: ["classic-newsfeed", "classic-nearby-service", "classic-observability-platform"],
    quiz: [
      {
        prompt: "Which is the best availability SLI for a user-facing read API?",
        options: [
          "Good responses over valid requests",
          "Average CPU across the whole fleet",
          "Count of currently deployed hosts",
          "Total application log volume",
        ],
        answerIndex: 0,
        explanation: "The ratio counts the user outcome, so both an error and a response too slow to be useful register as bad events. Average CPU is the classic internal proxy: a fleet can sit at 40 percent utilization while an entire cohort of users is timing out.",
      },
      {
        prompt: "When should a system reject new work instead of queueing it?",
        options: [
          "Only after memory is exhausted",
          "Whenever cache hit rate is high",
          "Never, because queues ensure recovery",
          "When queue delay beats the deadline",
        ],
        answerIndex: 3,
        explanation: "Work that will time out before it is served consumes scarce capacity and lengthens recovery, so admission control declines it early with a retry hint. Waiting for memory exhaustion is the tempting threshold and arrives far too late, once the queue is full of requests whose callers already gave up.",
      },
      {
        prompt: "A read API has a 99.9 percent availability SLO over 30 days. How large is the error budget?",
        options: [
          "About 7.2 hours in the window",
          "About 4.3 minutes in the window",
          "About 43 minutes in the window",
          "About 8.6 hours in the window",
        ],
        answerIndex: 2,
        explanation: "0.1 percent of 43,200 minutes is roughly 43 minutes of bad events, which is the quantity burn-rate alerts are measured against. The 4.3-minute option slips a decimal by reading the objective as 99.99 percent, and that one digit changes what the team is allowed to spend by a factor of ten.",
      },
      {
        prompt: "Mean latency and host uptime look normal while one customer cohort keeps missing the objective. What exposes it?",
        options: [
          "Alert on average request latency",
          "Percentiles by slice, burn alerts",
          "Add more hosts to the read pool",
          "Raise the queue depth threshold",
        ],
        answerIndex: 1,
        explanation: "An SLI is event-level over valid events, so per-slice thresholds and percentiles plus multi-window burn alerts are what reveal a cohort the aggregate hides. Alerting on average latency is the very instrument that created the blind spot, since a healthy mean is compatible with a badly broken tail.",
      },
    ],
    recallCards: [
      { id: "slo-budget", prompt: "Explain what an error budget is and how it changes engineering decisions.", answer: "An SLO of 99.9 percent permits 0.1 percent failure over the window, and that allowance is the error budget - a quantified amount of unreliability the product has agreed to tolerate. It converts reliability from an argument into an accounting question: while budget remains, teams may ship and take risk; once it is exhausted, the policy is to stop feature work and spend on reliability. It also stops the pursuit of unnecessary nines, because consuming the budget is expected and remaining far under it may mean over-investment." },
      { id: "slo-degrade", prompt: "Describe how a system should degrade under overload, in priority order.", answer: "Decide in advance which functionality is sheddable and shed it early rather than failing uniformly late: serve stale cached data instead of blocking on a slow source, disable optional personalization, ranking, or recommendations and fall back to a cheap default, defer asynchronous work such as analytics, and reject new low-priority work at admission with a retry hint while protecting in-flight requests. The invariant is that correctness-critical paths and paying operations survive while optional enrichment is sacrificed, and that rejection is fast and explicit rather than a timeout." },
    ],
  },
  {
    id: "classic-multi-region-disaster-recovery",
    week: 7,
    day: 3,
    tier: 1,
    title: "Multi-Region Design and Disaster Recovery",
    eyebrow: "Where truth lives during failover",
    estimatedMinutes: 90,
    summary:
      "Place traffic and data across regions according to consistency and latency needs, fence failover to prevent split brain, and prove recovery through explicit RPO, RTO, restore, and failback procedures.",
    whyItMatters:
      "Replication running 45 seconds behind means promoting the standby discards up to 45 seconds of acknowledged writes, whatever the recovery objective says on paper. Failover is the least-rehearsed path in the system, exercised under the worst conditions, with cold caches and a standby whose capacity drifted below production sometime last quarter. The return trip is harder still, because by then both regions hold writes the other has never seen. The question worth answering is not how many regions exist but which one owns each invariant, and what evidence has to exist before that ownership moves.",
    objectives: [
      "Choose active-passive, active-active, or home-region ownership per data invariant.",
      "Explain global routing, replication, fencing, failover, conflict handling, and failback.",
      "Define RPO and RTO, backup independence, restore verification, and disaster exercises.",
    ],
    concepts: [
      "active-passive",
      "active-active",
      "home region",
      "asynchronous replication",
      "fencing token",
      "split brain",
      "recovery point objective",
      "recovery time objective",
    ],
    deepDive: [
      {
        title: "Traffic and data placement",
        summary: "Route reads and writes only after deciding which region owns each consistency domain.",
        points: [
          "Use a home region or single leader for strongly ordered keys; route or proxy writes there while serving safe local replicas when allowed.",
          "Let the round trip set the ceiling: New York to London is about 70 ms, so any write that waits for a remote acknowledgment pays 70 ms before local work begins, and a request making two such hops has spent 140 ms of its budget on geography alone.",
          "Use active-active local writes only with a defined merge rule, commutative operation, or partitioned ownership that preserves invariants.",
          "Keep region routing metadata small, replicated, and versioned; clients and edges must tolerate stale placement information.",
        ],
      },
      {
        title: "Hidden cross-region dependencies",
        summary: "A region is independent only if everything on its critical path also lives inside it.",
        points: [
          "Walk the standby's request path and name every call that leaves the region. An identity service, a feature-flag store, a license check, or a config bucket still hosted in the failed region each turn a survivable outage into a total one.",
          "Audit the control plane too. Deploy pipelines, secret issuance, DNS management, and the dashboard an operator would use to run the failover are the components most often hosted only where the incident is.",
          "Prove independence by blocking the primary region at the network level rather than by reading a diagram, because the dependency nobody documented is precisely the one the diagram omits.",
        ],
      },
      {
        title: "Failover and fencing",
        summary: "Promotion requires evidence that the old writer cannot continue, not only evidence that it looks unreachable.",
        points: [
          "Acquire a higher fencing token (also called a fencing epoch) from an independent quorum before the standby accepts writes.",
          "Measure and disclose replication lag so operators know the potential data loss before promotion.",
          "Storage rejects new mutation commands carrying a stale fencing token. Establish a committed cutover log position, drain or replay pre-cutover events, and never discard a committed event solely because its fencing token predates the current writer.",
        ],
      },
      {
        title: "Backup, restore, and failback",
        summary: "Replication handles availability; independent versioned backups handle corruption, deletion, and correlated software failure.",
        points: [
          "Derive backup frequency and replication mode from RPO, and automate restore measurement against RTO.",
          "Store backups in a separate failure and access domain, verify checksums, and run application-level invariant checks after restore.",
          "Treat failback as a second failover requiring the same evidence: reconcile whatever the recovered region wrote before it was fenced, seed it from the current owner, and let replication run in the new direction long enough to show lag is bounded. Only then issue a fresh fencing token and move ownership, because reusing the old one re-admits the writer you fenced.",
        ],
      },
    ],
    tradeoffs: [
      {
        decision: "Active-passive versus active-active writes",
        preferA: "Use active-passive or home-region writes for simpler ordering and conflict avoidance.",
        preferB: "Use active-active when local write availability justifies explicit conflict or partitioned ownership semantics.",
        watch: "Active-active is not a routing toggle; every non-commutative invariant needs a resolution strategy.",
      },
      {
        decision: "Synchronous versus asynchronous replication",
        preferA: "Replicate synchronously within the latency budget when near-zero data loss is required.",
        preferB: "Replicate asynchronously for lower write latency and regional independence with a nonzero RPO.",
        watch: "Distance makes synchronous cross-region writes slower and more vulnerable to network partition.",
      },
      {
        decision: "Automatic versus operator-confirmed failover",
        preferA: "Automate for stateless or safely fenced data paths with clear health evidence.",
        preferB: "Require confirmation for money or ambiguous split-brain conditions where wrong promotion is worse than downtime.",
        watch: "Slow manual decisions increase RTO; unsafe automation can create unrecoverable divergence.",
      },
      {
        decision: "Where the promotion decision is made",
        preferA: "Let an independent quorum outside both regions arbitrate promotion, so the decision survives losing either side.",
        preferB: "Require a human confirmation when the blast radius of promoting wrongly exceeds the cost of the extra minutes it takes.",
        watch: "A control plane hosted inside the primary region cannot arbitrate that region's failure, and it is the configuration most systems ship with by accident.",
      },
    ],
    failureModes: [
      {
        mode: "Split-brain writers",
        symptom: "Two regions accept conflicting writes for the same ownership domain after a partition.",
        mitigation: "Use quorum-issued fencing tokens, enforce token checks at storage, and choose availability loss over invariant loss where necessary.",
      },
      {
        mode: "Failover with hidden replication lag",
        symptom: "The promoted region serves stale state or lacks recently acknowledged writes.",
        mitigation: "Expose per-stream lag and safe restore points, compare with RPO, pause promotion or disclose degraded consistency.",
      },
      {
        mode: "Untested or correlated backup failure",
        symptom: "Backups exist on paper but restore misses its RTO, violates invariants, or shares the corrupted credentials and region.",
        mitigation: "Use independent immutable copies, recurring full restores, checksum and domain validation, and measured disaster exercises.",
      },
      {
        mode: "Standby capacity drifted below production load",
        symptom: "The promoted region serves traffic for a few minutes, then collapses under cold caches and a connection-pool ceiling nobody resized after two quarters of growth.",
        mitigation: "Keep real traffic flowing through the standby continuously, even a small share, so capacity, cache warmth, and configuration drift surface on an ordinary Tuesday instead of during the failover.",
      },
    ],
    interviewQuestions: [
      "Which writes can be accepted in two regions without conflict?",
      "How is the old primary fenced before promotion?",
      "What are the RPO and RTO, and when was a full restore last proven?",
    ],
    decisionChecklist: [
      "Assign an owner and consistency policy to every mutable dataset.",
      "State read and write behavior during regional partition.",
      "Fence writers with quorum-issued fencing tokens before failover.",
      "Measure replication lag against RPO and restore against RTO.",
      "Design backup independence, invariant validation, reconciliation, and failback.",
    ],
    exercise:
      "Take a payment ledger and a newsfeed through loss of their primary region. Define routing, replication, promotion evidence, fencing, RPO/RTO, degraded behavior, reconciliation, restore, and failback for each.",
    prerequisites: ["replication-partitioning", "classic-slos-backpressure-degradation"],
    relatedDesigns: ["classic-payment-ledger", "classic-newsfeed", "classic-chat"],
    quiz: [
      {
        prompt: "What does a fencing token accomplish during failover?",
        options: [
          "It orders writes inside one region",
          "It proves the old primary is offline",
          "It blocks writes from the old primary",
          "It removes replication lag at cutover",
        ],
        answerIndex: 2,
        explanation: "Storage rejects any mutation carrying a fencing token older than the current one, so a reconnecting old primary is harmless even while it still believes it leads. 'Proves the old primary is offline' is the split-brain misconception: fencing never establishes that the old writer stopped, only that its writes no longer count.",
      },
      {
        prompt: "Why are cross-region replicas not a substitute for backups?",
        options: [
          "Replicas cannot store enough bytes",
          "They copy corruption and deletions",
          "They always run with infinite lag",
          "They are unable to serve any reads",
        ],
        answerIndex: 1,
        explanation: "Replication faithfully reproduces logical damage and usually shares credentials and an access domain, so only independent versioned copies allow point-in-time recovery. The lag option confuses an availability property with the corruption and accidental-deletion problem that backups exist to solve.",
      },
      {
        prompt: "Replication runs 45 seconds behind at peak and the stated RPO is 10 seconds. What does promoting the standby now mean?",
        options: [
          "RPO is met; lag is under a minute",
          "No loss; the log replays them later",
          "RPO applies only to backup copies",
          "Up to 45 seconds of writes are lost",
        ],
        answerIndex: 3,
        explanation: "Measured lag is the real recovery point, so promotion discards as much as 45 seconds of acknowledged writes and misses a 10-second RPO by more than four times. 'The log replays them later' is the seductive belief that asynchronous replication is merely delayed; whatever never left the failed region is gone.",
      },
      {
        prompt: "After a partition heals, two regions have accepted conflicting writes for the same keys. What was missing?",
        options: [
          "Token checks enforced at storage",
          "A larger cross-region link budget",
          "More frequent backup verification",
          "Lower replication lag on the link",
        ],
        answerIndex: 0,
        explanation: "Promotion must obtain a higher quorum-issued fencing token and storage must refuse mutations bearing older ones, or 'unreachable' silently becomes 'stopped.' Blaming replication lag is the tempting neighbor: lag decides how much data is lost, not whether two writers are allowed to commit at once.",
      },
    ],
    recallCards: [
      { id: "dr-rpo-rto", prompt: "Define RPO and RTO and explain how they drive the replication choice.", answer: "RPO is the maximum acceptable data loss measured in time; RTO is the maximum acceptable time to restore service. A near-zero RPO forces synchronous cross-region replication, which puts inter-region round-trip latency into every write and couples availability to the remote region. A tolerable RPO of seconds allows asynchronous replication, which keeps writes fast but means a regional loss discards whatever had not yet shipped. Stating both numbers first is what makes the active-active versus active-passive decision arguable rather than aesthetic." },
      { id: "dr-failover", prompt: "Explain why regional failover is dangerous even when replication is healthy.", answer: "Failover is the least-tested path exercised under the worst conditions: the standby's capacity may have quietly drifted below production load, caches start cold so the database sees a traffic pattern it never normally serves, asynchronous replication means some committed writes are missing and their absence must be reconciled, and split-brain becomes possible if the original region is partitioned rather than dead. Failing back is often harder than failing over. This is why failover must be rehearsed regularly, capacity verified continuously, and promotion arbitrated by a fenced, quorum-based decision rather than by a human guess." },
    ],
  },
  {
    id: "distributed-transactions-2pc-saga",
    week: 3,
    day: 1,
    tier: 1,
    title: "Distributed transactions: 2PC, sagas & TCC",
    eyebrow: "Atomicity across owners",
    estimatedMinutes: 85,
    summary: "Compare atomic commitment with compensation-based workflows, explain why two-phase commit blocks, and choose a protocol from failure behavior rather than from familiarity.",
    whyItMatters: "Two-phase commit does not fail by returning an error; it fails by not returning at all. A participant that has prepared has already surrendered its right to abort, so when the coordinator dies between phases those rows stay locked until it comes back — perfectly safe and completely unavailable. A saga buys that liveness back by giving up isolation instead, which means somebody can read the half-finished order, and TCC buys a workable approximation of isolation back again by converting the irreversible step into a hold that expires.",
    objectives: ["State what 2PC guarantees and the precise condition under which it blocks", "Design saga workflows with compensations and semantic rollback", "Apply TCC reservations where compensation is unacceptable", "Choose isolation and visibility semantics for multi-step business operations"],
    concepts: ["atomic commitment", "two-phase commit", "coordinator log", "in-doubt transaction", "saga", "compensation", "TCC", "semantic lock", "isolation anomaly"],
    deepDive: [
      {
        title: "Two-phase commit and why it blocks",
        summary: "2PC gives atomicity across resource managers but couples liveness to the coordinator.",
        points: [
          "In the prepare phase each participant durably promises it can commit and gives up unilateral abort; in the commit phase the coordinator's durable decision is applied. The participant's prepared state is the whole point—and the whole problem.",
          "If the coordinator fails after participants prepare but before the decision is delivered, those participants are in doubt: they may not commit and may not abort, and they hold locks until the coordinator recovers. 2PC is safe but not live, which is why it is called a blocking protocol.",
          "Three-phase commit reduces blocking under stronger synchrony assumptions that rarely hold in practice. The production answer is usually a replicated coordinator log (Raft/Paxos), which makes the decision itself highly available rather than eliminating prepare.",
        ],
      },
      {
        title: "Sagas trade atomicity for availability",
        summary: "A saga is a sequence of local transactions, each with a compensating action.",
        points: [
          "Each step commits independently and is visible immediately, so there is no global isolation: other transactions can observe intermediate states. A saga provides eventual atomicity of effect, never the illusion that nothing happened.",
          "Compensation is semantic, not a rollback. You cannot un-send an email or un-ship a package; you send a correction, issue a refund, or record a reversing entry. Compensations must be idempotent and must themselves be retried until they succeed.",
          "Orchestration uses a central workflow engine that is easy to reason about and monitor; choreography reacts to events with no central owner and is loosely coupled but far harder to debug. Either way, persist workflow state so recovery resumes rather than restarts.",
        ],
      },
      {
        title: "Reservations when compensation is unacceptable",
        summary: "Try-Confirm/Cancel converts an irreversible effect into a reversible hold.",
        points: [
          "TCC splits an operation into a try that reserves capacity, and a confirm or cancel that finalizes it. Reserved inventory or a payment authorization is invisible to other callers, which restores a usable approximation of isolation.",
          "Reservations require expiry: a hold whose confirm never arrives must time out, or capacity leaks. The expiry must be longer than the maximum workflow duration and shorter than the business tolerance for locked capacity.",
          "Pin the number between the two bounds you can actually measure. If the workflow's p99 is 20 minutes and the business tolerates held inventory for 2 hours, a 45-minute hold cancels nothing still running and leaks nothing longer than an afternoon; 10 minutes would cancel live workflows and no expiry at all leaks a unit of stock every time a confirm is lost.",
          "Where a semantic lock is impossible, apply commutative operations instead—an append-only ledger entry or a counter increment can be applied in any order and reversed by a compensating entry without a lock.",
        ],
      },
    ],
    tradeoffs: [
      { decision: "2PC vs saga", preferA: "Use atomic commitment when participants share a transactional boundary and short lock hold times are acceptable.", preferB: "Use a saga across services, long-running steps, or third parties that cannot prepare.", watch: "A saga exposes intermediate state; if the product cannot tolerate that, you need reservations." },
      { decision: "Orchestration vs choreography", preferA: "Orchestrate for visibility, explicit state, and easier debugging.", preferB: "Choreograph for loose coupling and independent deployment.", watch: "Event-chained workflows are hard to trace and easy to make non-terminating." },
      { decision: "Compensation vs reservation", preferA: "Compensate when the effect is reversible and intermediate visibility is safe.", preferB: "Reserve when overselling or double-spending is unacceptable.", watch: "Reservations leak capacity without enforced expiry." },
    ],
    failureModes: [
      { mode: "Coordinator fails after prepare", symptom: "Participants hold locks in doubt and throughput collapses", mitigation: "Replicate the coordinator decision log and support operator-driven heuristic resolution with reconciliation." },
      { mode: "Compensation itself fails", symptom: "Workflow stalls midway, leaving money or inventory inconsistent", mitigation: "Make compensations idempotent, retry them with backoff, and route exhausted retries to a reconciliation queue." },
      { mode: "Saga assumes isolation it does not have", symptom: "A concurrent reader acts on an intermediate state that is later compensated", mitigation: "Use semantic locks, reservation status, or hide pending state behind an explicit lifecycle field." },
    ],
    interviewQuestions: ["What exactly happens if the coordinator dies after prepare?", "Which steps here are truly reversible?", "What can another user observe halfway through this workflow?", "How long may a reservation be held before it expires?"],
    decisionChecklist: ["State whether participants can prepare at all", "Identify irreversible steps before choosing compensation", "Define expiry for every reservation", "Make compensations idempotent and retried", "Say what intermediate state is visible"],
    exercise: "Design order placement spanning payment authorization, inventory reservation, and shipping, then walk through failure at each step and state precisely what the customer sees.",
    prerequisites: ["consistency-idempotency", "replication-partitioning"],
    relatedDesigns: ["classic-payment-ledger", "classic-notifications", "classic-file-sync"],
    quiz: [
      { prompt: "Why is two-phase commit called a blocking protocol?", options: ["It relies on blocking socket calls", "It needs tightly synchronized clocks", "Its coordinator cannot be replicated", "Prepared participants cannot decide"], answerIndex: 3, explanation: "Once a participant promises in prepare it has surrendered unilateral abort, so a lost decision leaves it in doubt holding locks until the coordinator recovers. 'Blocking I/O' is the name-driven misreading: the blocking here is a liveness property of the protocol, not a threading model." },
      { prompt: "What does a saga fundamentally give up compared with a distributed transaction?", options: ["Durability of committed steps", "The ability to retry a step", "Isolation of intermediate state", "Idempotency of its operations"], answerIndex: 2, explanation: "Every step commits and becomes visible immediately, so other transactions can read partial progress that a later compensation reverses. Choosing durability is the common inversion: saga steps are fully durable, and that is precisely why they cannot be rolled back and must be compensated instead." },
      { prompt: "A workflow can run for 20 minutes and the business tolerates held inventory for 2 hours. Which reservation expiry is valid?", options: ["45 minutes, inside both bounds", "10 minutes, half the workflow", "6 hours, well past tolerance", "No expiry; confirm will arrive"], answerIndex: 0, explanation: "Expiry must exceed the maximum workflow duration and stay under the business tolerance, so any value between 20 minutes and 2 hours works. Ten minutes is the tempting 'tight' choice and cancels holds out from under workflows still running, while no expiry leaks capacity forever every time a confirm is lost." },
      { prompt: "A reader acts on an order state that is compensated moments later. What was missing?", options: ["A replicated coordinator log", "An explicit pending lifecycle", "Idempotent compensation steps", "A shorter reservation expiry"], answerIndex: 1, explanation: "Sagas have no global isolation, so intermediate state must sit behind a semantic lock, a reservation status, or an explicit lifecycle field that readers respect. Reaching for a replicated coordinator log is the 2PC reflex and fixes a coordinator-liveness problem this workflow never had." },
    ],
    recallCards: [
      { id: "dtx-2pc-block", prompt: "Walk through both phases of 2PC and explain exactly when and why a participant becomes stuck in doubt.", answer: "In prepare, the coordinator asks each participant to durably promise it can commit; the participant writes a prepared record and gives up the right to abort on its own. In commit, the coordinator durably records the decision and broadcasts it. If the coordinator crashes after participants prepare but before the decision reaches them, those participants may neither commit nor abort — they are in doubt and hold their locks until the coordinator recovers. 2PC is therefore safe but not live; replicating the coordinator's decision log with consensus is the usual production mitigation." },
      { id: "dtx-compensation", prompt: "Why is saga compensation not a rollback, and what two properties must every compensation have?", answer: "A rollback restores state as though nothing happened; a compensation is a new business action that semantically offsets a committed, already-visible effect — a refund rather than an un-charge, a reversing ledger entry rather than a delete. Because the original effect was observable, the system never regains the illusion of atomicity. Every compensation must be idempotent, since it will be retried after ambiguous failures, and it must be retried until it succeeds or escalates to reconciliation, since a failed compensation leaves the workflow inconsistent." },
    ],
    furtherReading: [{ label: "Garcia-Molina & Salem — Sagas", url: "https://www.cs.cornell.edu/andru/cs711/2002fa/reading/sagas.pdf" }],
  },
  {
    id: "membership-discovery-failure-detection",
    week: 3,
    day: 5,
    tier: 1,
    title: "Membership, failure detection & service discovery",
    eyebrow: "Who is alive, and where",
    estimatedMinutes: 75,
    summary: "Propagate cluster membership with gossip, detect failure without declaring healthy nodes dead, and let callers find live instances through discovery, health checking, and leader election.",
    whyItMatters: "The gap between a process dying and traffic stopping is a sum, and it runs longer than anyone estimates: three missed 2-second heartbeats to suspect, then up to a 10-second registry TTL before the entry disappears, is sixteen seconds of requests dropped into a black hole. Shortening either number trades that outage for false positives, which are the worse failure — a healthy-but-slow node evicted during a latency blip takes its shard's traffic down with it.",
    objectives: ["Explain gossip dissemination and its convergence properties", "Distinguish failure detection from failure, and tune the trade-off", "Design service discovery with health checking and staleness bounds", "Use leader election safely with leases and fencing tokens"],
    concepts: ["gossip protocol", "SWIM", "phi accrual detector", "heartbeat", "service registry", "client-side discovery", "health check", "leader election", "split brain", "quorum"],
    deepDive: [
      {
        title: "Gossip spreads membership without a central registry",
        summary: "Each node periodically exchanges state with a random peer, and information reaches everyone epidemically.",
        points: [
          "A node picks a small random subset of peers each round and exchanges membership state, so information spreads in roughly logarithmic rounds in cluster size while each node's per-round message cost stays constant. That bounded per-node cost is why gossip scales where all-to-all heartbeating does not.",
          "SWIM separates failure detection from dissemination: a node probes one random peer per period, and on timeout asks k other members to probe indirectly before suspecting it. Indirect probing is what prevents a single congested link from being read as a dead peer.",
          "Gossip is eventually consistent by construction, so two nodes can briefly disagree about membership. Every consumer must tolerate a stale view, which is precisely why membership alone must never be the authority for correctness-critical exclusivity.",
        ],
      },
      {
        title: "Detection is a guess with a tunable cost",
        summary: "You cannot distinguish a crashed node from a slow one; you can only choose when to stop waiting.",
        points: [
          "Fixed-timeout heartbeating forces a hard choice: a short timeout declares healthy-but-slow nodes dead and triggers needless failovers, while a long timeout leaves real failures undetected and traffic flowing into a black hole.",
          "A phi accrual detector instead outputs a continuous suspicion level derived from the observed distribution of heartbeat inter-arrival times, letting different subsystems apply different thresholds and adapting automatically to a network that is simply slower than assumed.",
          "Write the removal delay down as a sum rather than quoting the detection half: 3 missed heartbeats at 2 seconds is 6 seconds to suspect, plus a 10-second registry TTL before the entry expires, is up to 16 seconds of traffic still routed to a dead instance. Reporting only the 6 understates the user-visible outage by nearly two thirds.",
          "Detection errors are correlated: a network partition makes each side declare the other dead simultaneously. Any action taken on suspicion must therefore be safe when both sides act at once, which is what forces quorum for anything exclusive.",
        ],
      },
      {
        title: "Discovery, health, and safe leadership",
        summary: "Finding an instance and trusting it are separate steps.",
        points: [
          "Server-side discovery puts a load balancer in the path, keeping clients simple at the cost of an extra hop; client-side discovery lets clients query a registry and choose an instance directly, removing the hop but pushing selection, caching, and staleness handling into every client and language.",
          "A liveness check answers whether the process is running; a readiness check answers whether it should receive traffic right now, which differs during warm-up, cache fill, or dependency loss. Conflating them causes cold instances to receive traffic and, worse, deep dependency checks to fail every instance at once when one shared dependency degrades.",
          "Election makes one node the owner, but the winner's belief is not self-enforcing: a paused leader can resume convinced it still holds office. Grant leadership as an expiring lease carrying a monotonically increasing fencing token (also called a fencing epoch), and have the protected resource reject any write with a stale token, so split brain becomes a rejected write rather than corruption.",
        ],
      },
    ],
    tradeoffs: [
      { decision: "Gossip vs centralized registry", preferA: "Gossip for large, elastic clusters with no central dependency.", preferB: "A consensus-backed registry when membership must be authoritative and immediately consistent.", watch: "Gossip views are eventually consistent and briefly disagree." },
      { decision: "Aggressive vs conservative failure detection", preferA: "Detect fast when failover is cheap and idempotent.", preferB: "Detect slowly when failover is expensive or risks split brain.", watch: "Aggressive timeouts turn a latency blip into a cluster-wide reshuffle." },
      { decision: "Server-side vs client-side discovery", preferA: "Server-side for language-agnostic simplicity and central policy.", preferB: "Client-side to remove a network hop and enable locality-aware choice.", watch: "Client-side caches go stale and every language needs a correct implementation." },
    ],
    failureModes: [
      { mode: "Deep dependency check inside a readiness probe", symptom: "One slow shared dependency marks every instance unready and removes the whole fleet from rotation", mitigation: "Keep readiness local and shallow; report dependency health separately and degrade rather than deregister." },
      { mode: "Partition causes both sides to elect a leader", symptom: "Two owners accept conflicting writes", mitigation: "Require a quorum to hold leadership and fence the resource with a monotonic fencing token." },
      { mode: "Stale registry entry keeps receiving traffic", symptom: "Requests routed to a terminated instance until the TTL expires", mitigation: "Deregister on graceful shutdown, expire entries by TTL, and let clients eject endpoints on connection failure." },
    ],
    interviewQuestions: ["How does a caller learn an instance died?", "What is the delay between failure and traffic stopping?", "What happens to leadership during a partition?", "What does your health check actually verify?"],
    decisionChecklist: ["State how membership propagates and its staleness", "Separate liveness from readiness", "Bound the detection-to-removal delay", "Require quorum for exclusive ownership", "Fence protected resources with a monotonic fencing token"],
    exercise: "Design membership and discovery for a stateful sharded cache: describe how a node death is detected, how ownership moves, what clients do with a stale map, and what prevents two nodes serving the same shard.",
    prerequisites: ["replication-partitioning", "networking"],
    relatedDesigns: ["classic-chat", "classic-observability-platform"],
    quiz: [
      { prompt: "Why does SWIM ask other members to probe a peer before suspecting it?", options: ["To separate a dead peer from a bad path", "To reduce membership metadata memory", "To elect a leader without a quorum", "To compress every gossip message payload"], answerIndex: 0, explanation: "Indirect probes through k other members distinguish a genuinely failed node from congestion on one pair of links, which is where nearly all false positives originate. The memory answer confuses SWIM's dissemination efficiency with its detection accuracy; the extra probes cost messages, not memory." },
      { prompt: "What makes leader election safe when a leader pauses and later resumes?", options: ["A substantially longer heartbeat", "Clocks kept synchronized by NTP", "Immediately retrying the election", "A fencing token the resource checks"], answerIndex: 3, explanation: "A resumed leader still believes it holds office, so safety has to live at the protected resource, which records the highest fencing token it has seen and rejects anything older. Lengthening the heartbeat is the tempting tuning knob and merely widens the window in which the stale leader is still trusted." },
      { prompt: "Heartbeats run every 2 s, a node is declared dead after 3 misses, and the registry TTL is 10 s. How long can traffic keep flowing to a dead instance?", options: ["Up to 6 seconds, detection only", "Up to 16 seconds, detection plus TTL", "Up to 10 seconds, the TTL alone", "None; the first missed beat stops it"], answerIndex: 1, explanation: "The delays compose: three missed beats at two seconds is six seconds to suspect, then up to ten more before the registry entry expires, so roughly sixteen seconds of traffic into a black hole. Quoting detection alone is the usual omission and understates the user-visible outage by the entire TTL." },
      { prompt: "One slow shared database makes every instance report unready and the whole fleet leaves rotation. What is the fix?", options: ["Shorten the failure detection timeout", "Elect a leader to gate the checks", "Keep readiness local and shallow", "Raise the registry TTL for entries"], answerIndex: 2, explanation: "Readiness answers whether this instance can serve traffic right now, so putting a shared dependency inside it converts one degradation into a fleet-wide outage; dependency health belongs on a separate signal that degrades rather than deregisters. Raising the TTL is the tempting patch and only postpones the same mass removal." },
    ],
    recallCards: [
      { id: "mem-gossip", prompt: "Explain how gossip disseminates membership and why its per-node cost scales where heartbeating everyone does not.",
        answer: "Each node periodically picks a small random subset of peers and exchanges membership state, so an update reaches the whole cluster in roughly logarithmic rounds while each node sends a constant number of messages per round regardless of cluster size. All-to-all heartbeating instead makes per-node message cost grow linearly with the cluster, which is what breaks at scale. The price is that gossip converges only eventually, so two nodes can briefly hold different views - which is why membership must never be the sole authority for anything requiring exclusivity." },
      { id: "mem-fencing", prompt: "Explain why winning an election is not enough for safe exclusive ownership, and what makes it safe.",
        answer: "A leader can be paused by a long garbage-collection pause, descheduling, or a partition, lose its lease, and then resume still believing it is the owner - meanwhile a new leader has been elected, so two processes think they own the resource. Safety cannot come from the leader's own belief. Leadership is granted as an expiring lease carrying a monotonically increasing fencing token, and the protected resource records the highest token it has seen and rejects any write with an older one. Split brain then degrades into a rejected write instead of silent corruption." },
    ],
    furtherReading: [{ label: "Das et al. — SWIM", url: "https://www.cs.cornell.edu/projects/Protocols/SWIM/SWIM.pdf" }],
  },
  {
    id: "conflict-resolution-crdts",
    week: 3,
    day: 2,
    tier: 1,
    title: "Causality, version vectors & CRDTs",
    eyebrow: "Merge without losing writes",
    estimatedMinutes: 80,
    summary: "Track causality with logical clocks, detect concurrent updates instead of silently discarding them, and choose between conflict surfacing, operational transform, and convergent replicated data types.",
    whyItMatters: "Last-write-wins is not a merge strategy; it is a decision to delete one of two writes and keep no record that it existed, and on a replica whose clock has drifted 200 milliseconds ahead, the write it deletes is the newer one. Everything in this module exists to make that outcome a choice rather than an accident: detect concurrency first with a version vector, then pick a resolution the product can defend — surface it, union it, or discard it deliberately.",
    objectives: ["Explain why wall-clock timestamps cannot order distributed events", "Use Lamport clocks and version vectors to detect concurrency", "Choose a conflict strategy from product semantics", "Describe CRDT convergence and its real costs"],
    concepts: ["happens-before", "Lamport clock", "vector clock", "version vector", "concurrent update", "last-write-wins", "CRDT", "operational transform", "tombstone"],
    deepDive: [
      {
        title: "Causality needs logical clocks",
        summary: "Physical timestamps order events only as well as clocks agree, which is not well enough.",
        points: [
          "The happens-before relation is a partial order: two events are concurrent when neither causally precedes the other. Wall-clock comparison forces a total order and therefore invents an ordering between genuinely concurrent events.",
          "A Lamport clock is a single counter that guarantees if A happens-before B then L(A) < L(B), but the converse does not hold—so it can order events but cannot detect concurrency. A vector clock keeps one counter per node, and comparing vectors distinguishes “before”, “after”, and “concurrent”.",
          "A version vector is the replica-per-key form used in storage systems. Its cost is real and countable: with 50,000 active clients, keying by client puts 50,000 entries on a single key, while keying by the 12 replicas that actually store it bounds the vector at 12. Production systems therefore key entries by replica rather than by client—bounding the vector by cluster size instead of by an unbounded client population—and truncate least-recently-updated entries when it still grows. Riak's move from client-id to vnode-id vector clocks is the canonical example of getting this the wrong way round first.",
        ],
      },
      {
        title: "Choosing what to do with a detected conflict",
        summary: "Detection is mechanical; resolution is a product decision.",
        points: [
          "Last-write-wins by timestamp is simple and lossy: under clock skew a stale write can silently overwrite a newer one, and there is no record that data was discarded. It is acceptable only when the field is disposable, such as a presence heartbeat.",
          "Surfacing conflicts to the user preserves all information and is right when the data is authored intent—a document, a contact record. Dropbox-style “conflicted copy” files are this strategy made visible.",
          "Application-specific merge is often best: a shopping cart can union added items, a counter can sum increments, a set can take the union. The merge must be commutative, associative, and idempotent so replicas converge regardless of delivery order.",
        ],
      },
      {
        title: "CRDTs and their honest costs",
        summary: "Convergent types make merge automatic by constraining what operations are allowed.",
        points: [
          "State-based (CvRDT) types merge by a join over a lattice; operation-based (CmRDT) types broadcast commutative operations and require exactly-once causal delivery. Both converge without coordination, which is why they suit offline and multi-leader systems.",
          "Deletion is the hard part. A removed element usually leaves a tombstone so a replica that has not yet seen the removal cannot resurrect the element when states merge, and tombstones must be garbage-collected only once every replica has observed the removal. Note the add-wins default: in an observed-remove set a removal cancels only the add-tags it has already observed, so a genuinely concurrent re-add carries a new tag and survives on purpose.",
          "CRDTs guarantee convergence, not correctness of intent: two replicas will agree, but the agreed result may not be what either user meant. Text editing needs sequence CRDTs or operational transform, both of which carry substantial metadata and implementation complexity.",
        ],
      },
    ],
    tradeoffs: [
      { decision: "Last-write-wins vs conflict surfacing", preferA: "Use LWW for disposable, frequently overwritten fields.", preferB: "Surface conflicts when the value represents authored user intent.", watch: "LWW under clock skew loses data with no audit trail." },
      { decision: "Version vectors vs single timestamp", preferA: "Use version vectors when concurrent writes must be detected rather than ordered.", preferB: "Use a single version counter when one leader owns every write.", watch: "Vector size grows with writer count and needs pruning." },
      { decision: "CRDT vs server-authoritative merge", preferA: "Use CRDTs for offline-first and peer-to-peer collaboration.", preferB: "Use a single authoritative merger when you control connectivity and want simple semantics.", watch: "CRDT metadata, tombstone GC, and debugging cost are frequently underestimated." },
    ],
    failureModes: [
      { mode: "Clock skew reorders writes under LWW", symptom: "A newer update silently disappears after a replica's clock drifts", mitigation: "Use logical clocks for ordering; reserve physical time for observability, not correctness." },
      { mode: "Vector clocks grow unbounded", symptom: "Per-key metadata dominates payload size in a large fleet", mitigation: "Key vectors by replica rather than by client so the actor set is bounded by the cluster, cap entries, and truncate the oldest under a durable low-water mark." },
      { mode: "Deleted item is resurrected", symptom: "An element reappears after a partitioned replica reconnects", mitigation: "Retain tombstones until every replica has acknowledged, then collect them under a causal stability threshold." },
    ],
    interviewQuestions: ["How do you tell a concurrent write from a stale one?", "What information does last-write-wins destroy here?", "Who resolves a conflict—the system or the user?", "When can a tombstone safely be deleted?"],
    decisionChecklist: ["Detect concurrency before resolving it", "Justify LWW by showing the field is disposable", "Make any merge commutative, associative, and idempotent", "Bound version-vector growth", "State the tombstone collection rule"],
    exercise: "Design conflict handling for a note-taking app that syncs offline edits from three devices, specifying detection, merge behavior per field, and what the user sees when intent genuinely diverges.",
    prerequisites: ["replication-partitioning", "consistency-idempotency"],
    relatedDesigns: ["classic-file-sync", "classic-chat", "classic-newsfeed"],
    quiz: [
      { prompt: "What can a version vector do that a Lamport clock cannot?", options: ["Order causally related events", "Compress replica metadata", "Detect two concurrent updates", "Guarantee message delivery"], answerIndex: 2, explanation: "A Lamport clock only guarantees that causally ordered events get increasing counters, so it can order events but never tell you two were concurrent; comparing per-replica vectors yields before, after, or concurrent. 'Order causally related events' is the trap, because that is the one capability both mechanisms already share." },
      { prompt: "Why do CRDTs generally need tombstones for deletion?", options: ["So a replica that never saw the delete cannot resurrect the element", "So concurrent deletions can be ordered against each other by clock", "So the merge function can compress all replica state into one value", "So a genuinely concurrent re-add is discarded rather than retained"], answerIndex: 0, explanation: "Without a durable record of the removal, a replica that never observed the delete reintroduces the element the moment states merge. The last option inverts the add-wins default: in an observed-remove set a removal cancels only the add-tags it has already observed, so a genuinely concurrent re-add carries a new tag and survives on purpose." },
      { prompt: "A version vector keyed by client with 50,000 active clients, versus keyed by replica in a 12-node cluster. How large is each?", options: ["Twelve entries under both keyings", "One entry per key in either scheme", "50,000 entries under both schemes", "50,000 entries versus 12 entries"], answerIndex: 3, explanation: "Keying by client grows the vector with an unbounded client population, while keying by replica bounds it at cluster size, so the same key carries 50,000 entries in one scheme and 12 in the other. Assuming both cost the same is the mistake Riak had to undo when it moved from client-id to vnode-id vector clocks." },
      { prompt: "A newer update vanishes after one replica's clock drifts ahead. What is the root cause?", options: ["Tombstones were collected early", "LWW ordered writes by wall clock", "The merge was not idempotent", "Version vectors were truncated"], answerIndex: 1, explanation: "Physical time is not evidence of causal order, so a skewed clock makes a stale write look newest and it overwrites the good one with no audit trail; logical clocks belong in the ordering path and wall time in observability. Truncated version vectors are the tempting neighbor, but truncation causes false conflicts rather than silent loss." },
    ],
    recallCards: [
      { id: "crdt-detect", prompt: "Explain why wall-clock timestamps cannot correctly order distributed writes, and what a version vector adds.", answer: "Happens-before is a partial order, so genuinely concurrent events have no true ordering between them. Comparing wall-clock timestamps forces a total order, inventing an ordering that clock skew can get backwards — a stale write can then overwrite a newer one with no record that anything was lost. A version vector keeps a counter per writer; comparing two vectors yields before, after, or concurrent, so the system can detect a real conflict and apply a deliberate resolution policy instead of silently discarding a write. Its cost is metadata that grows with the number of actors, which is why production systems key entries by replica rather than by client — bounding the vector by cluster size rather than by an unbounded client population — and truncate the oldest entries under a durable low-water mark." },
      { id: "crdt-converge", prompt: "State the properties a merge function must have for replicas to converge, and what CRDT convergence does not promise.", answer: "The merge must be commutative, associative, and idempotent, so replicas reach the same state regardless of the order or number of times updates are delivered — which is what allows convergence without coordination. It does not promise that the converged value reflects either user's intent: two replicas will agree, but the agreed result can be semantically wrong, which is why collaborative text needs sequence CRDTs or operational transform rather than a naive set merge." },
    ],
    furtherReading: [{ label: "Shapiro et al. — A comprehensive study of CRDTs", url: "https://inria.hal.science/inria-00555588/document" }],
  },
];

export const classicPrompts: DesignPrompt[] = [
  {
    id: "classic-unique-id-service",
    title: "Unique ID Service",
    category: "classic",
    difficulty: "medium",
    durationMinutes: 40,
    prompt:
      "Design a highly available service that issues globally unique, roughly time-sortable IDs to services in multiple regions. State the required throughput, lifetime, opacity, ordering scope, and behavior during clock or coordinator failure.",
    requirementsToExplore: [
      "Peak IDs per second, batch allocation, and expected scheme lifetime",
      "Uniqueness scope, time sortability, and whether IDs must be opaque",
      "Multi-region availability and acceptable coordination on the hot path",
      "Clock rollback, sequence exhaustion, process restart, and worker-ID reuse",
      "Migration when the bit layout or capacity assumptions change",
    ],
    expectedTopics: [
      "Database sequences, UUIDs, and Snowflake-style layouts",
      "Epoch, timestamp, region or worker, and per-tick sequence bit budgets",
      "Leased worker identities, encoded incarnation epochs, and coordinator isolation",
      "Monotonic-clock guards and rollback policy",
      "Sortability versus strict global ordering and coordination trade-offs",
    ],
    commonFailureModes: [
      "Claiming rough timestamp order is a gap-free global sequence",
      "Reusing a worker ID before the previous lease is fenced",
      "Allowing sequence wrap or wall-clock rollback to repeat a bit pattern",
      "Putting a strongly consistent coordinator on every ID issuance",
    ],
    followUpQuestions: [
      "How many years does the timestamp field last, and how many IDs can one worker issue per tick?",
      "What does a generator do when its clock moves backward?",
      "How are stale workers prevented from issuing after lease reassignment?",
      "How would you support clients that need IDs in batches while offline?",
    ],
    reference: {
      diagram: {
        caption: "IDs are minted locally from a leased worker identity plus the local clock and a per-tick sequence, so the hot path never touches the coordinator; only lease acquisition and renewal do.",
        nodes: [
          { id: "client", label: "Client service", kind: "client", col: 0, row: 1 },
          { id: "gen", label: "ID generator node", kind: "service", col: 1, row: 1 },
          { id: "mem", label: "In-memory worker lease", kind: "cache", col: 2, row: 0 },
          { id: "lease", label: "Worker lease coordinator", kind: "service", col: 2, row: 2 },
          { id: "store", label: "Lease store (CAS)", kind: "store", col: 3, row: 2 },
          { id: "clock", label: "Node/epoch registry", kind: "store", col: 3, row: 0 },
          { id: "consumer", label: "Consuming writes", kind: "service", col: 1, row: 3 },
        ],
        edges: [
          { from: "client", to: "gen", label: "nextId()" },
          { from: "gen", to: "mem", label: "read lease" },
          { from: "mem", to: "lease", label: "renew / expiring" },
          { from: "lease", to: "store", label: "CAS" },
          { from: "gen", to: "clock", label: "node+epoch" },
          { from: "gen", to: "consumer", label: "monotonic id" },
          { from: "lease", to: "mem", label: "worker id + incarnation", async: true },
        ],
      },
      scope: [
        "Issue unique IDs within a named namespace at the stated peak and availability target.",
        "Provide rough creation-time sortability, not gap-free or globally linearizable order unless explicitly required.",
        "Support regional generators and optional batches; human-readable decoding is an internal diagnostic, not a public security boundary.",
      ],
      apis: [
        "`POST /v1/ids { namespace, count } -> { ids, layoutVersion }`, with bounded count and request idempotency if callers may retry batches.",
        "Internal `AcquireWorker(region, owner, ttl)` returns a non-reused incarnation; `RenewWorker(workerId, incarnation, leaseEpoch, ttl)` renews ownership by compare-and-swap.",
        "Internal `GetLayout(version)` exposes epoch, bit allocation, and compatibility state to SDKs and operators.",
      ],
      dataModel: [
        "ID layout v1: `[timestampSinceCustomEpoch | regionOrWorker | workerIncarnation | sequence]`, with every field's bit width derived from lifetime, reassignment, and peak-per-worker demand.",
        "WorkerLease `{ workerId, region, owner, incarnation, epoch, expiresAt }`, unique on worker ID and renewed by compare-and-swap.",
        "LayoutConfig `{ version, epochMillis, bitAllocation, status }` and an audit record for lease issue, revoke, and conflict.",
      ],
      architecture: [
        "Regional stateful generator processes issue IDs locally from a cached lease, encoded worker incarnation, and retained last-timestamp plus sequence state.",
        "A strongly consistent control plane assigns worker identities and a never-reused incarnation for the scheme lifetime, but is absent from the per-ID hot path.",
        "An SDK can reserve small batches from a local generator; services validate layout version and treat IDs as opaque values.",
        "Generators stop issuance on expired lease, unsafe clock rollback, or exhausted sequence until the next safe tick.",
      ],
      invariants: [
        "No two live or restarted generators may emit the same namespace and bit pattern.",
        "Every ID encodes the lease incarnation; reassignment receives a distinct non-wrapping incarnation so a paused old generator cannot collide with the new owner.",
        "The per-tick sequence never wraps, and clock rollback never moves emitted logical time backward into an already-used range.",
        "Sortability is approximate across hosts; the API does not promise gap-free IDs or strict global order.",
      ],
      deepDives: [
        {
          title: "Bit budget and lifetime",
          summary: "Capacity follows directly from timestamp resolution, scheme lifetime, worker count, and peak IDs per worker per tick.",
          points: [
            "Compute timestamp bits from the custom epoch and desired years, then reserve enough sequence bits for burst capacity with headroom.",
            "Model signed database types and client language precision before selecting a 64-bit layout.",
            "Version the layout and plan dual-read or dual-decode before timestamp or worker space is exhausted.",
          ],
        },
        {
          title: "Clock rollback and sequence exhaustion",
          summary: "The generator protects uniqueness even when wall time is unsafe.",
          points: [
            "Track the last emitted timestamp; for small rollback use logical time only within a documented bound, otherwise stop and alarm.",
            "When a sequence fills, wait for the next safe tick or shed load rather than wrap.",
            "Persist or fence restart state so a process cannot reuse sequence values under the same worker and timestamp.",
          ],
        },
        {
          title: "Lease fencing",
          summary: "Lease expiry alone is not fencing for local generation, so each reassignment must create a disjoint encoded identity or ID range.",
          points: [
            "Issue a monotonically increasing incarnation on reassignment, encode it in every ID, and prevent wrap or reuse during the scheme lifetime.",
            "Use bounded-clock and early-expiry assumptions, persist local timestamp/sequence state, and stop on coordinator uncertainty; availability loss is preferable to duplicate IDs.",
            "Detect concurrent owners through lease-conflict telemetry and duplicate sampling at downstream boundaries.",
          ],
        },
      ],
      scaling: [
        "Keep allocation local and batch-friendly so generation scales with processes rather than coordinator QPS.",
        "Allocate worker or region capacity from measured peak, including one-region and rolling-restart headroom.",
        "Load-test the hottest timestamp tick, lease-renewal outage, sequence saturation, and mass restart.",
        "Add a new layout version before timestamp, incarnation, or worker-bit exhaustion; never reinterpret existing IDs in place.",
      ],
      observability: [
        "Generation QPS and p50/p99 latency by region and layout version",
        "Sequence utilization and saturation waits per worker and tick",
        "Clock rollback count, magnitude, blocked duration, and logical-time use",
        "Lease renew failures, conflicting owners, stale epochs, and remaining scheme lifetime",
        "Duplicate invariant alarms from downstream sampling or uniqueness checks",
      ],
    },
  },
  {
    id: "classic-newsfeed",
    title: "Newsfeed or Timeline",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a personalized home feed where users publish posts, follow authors, and page through fresh ranked or chronological results. Handle celebrity authors, deletes, privacy changes, retries, and a tenfold traffic increase.",
    requirementsToExplore: [
      "Active users, follow-graph skew, publish rate, feed-read rate, and page size",
      "Chronological versus ranked order and publish-to-visible freshness",
      "Post edit, delete, block, privacy, and moderation behavior",
      "Stable pagination during concurrent inserts and ranking changes",
      "Regional reads, write ownership, disaster recovery, and degraded mode",
    ],
    expectedTopics: [
      "Fan-out on write, fan-out on read, and a celebrity-aware hybrid",
      "Post and social-graph sources of truth with an idempotent outbox pipeline",
      "Sharded feed-entry storage, candidate cache, ranking, hydration, and filtering",
      "Opaque keyset or rank-session cursors with unique tie-breakers",
      "Queue backpressure, cache stampede control, tombstones, and reconciliation",
    ],
    commonFailureModes: [
      "Applying fan-out-on-write uniformly to extreme follower counts",
      "Using offset pagination for a rapidly mutating feed",
      "Rendering cached deleted or unauthorized content",
      "Acknowledging a post before its canonical state is durable",
    ],
    followUpQuestions: [
      "What data moves when a celebrity publishes?",
      "How does an unfollow or block affect already materialized entries?",
      "What is encoded in the feed cursor when rank scores change?",
      "Which feature degrades first when the ranking service is slow?",
    ],
    reference: {
      diagram: {
        caption: "Ordinary authors fan out to follower inboxes on write; high-follower authors are pulled at read time and merged, bounding the worst-case write amplification.",
        nodes: [
          { id: "author", label: "Author", kind: "client", col: 0, row: 0 },
          { id: "reader", label: "Reader", kind: "client", col: 0, row: 3 },
          { id: "post", label: "Post service", kind: "service", col: 1, row: 0 },
          { id: "posts", label: "Post store", kind: "store", col: 2, row: 0 },
          { id: "fanout", label: "Fan-out workers", kind: "compute", col: 2, row: 1 },
          { id: "inbox", label: "Inbox store (per user)", kind: "store", col: 3, row: 1 },
          { id: "feed", label: "Feed API", kind: "service", col: 1, row: 3 },
          { id: "merge", label: "Merge & rank", kind: "compute", col: 2, row: 3 },
          { id: "celeb", label: "Celebrity author cache", kind: "cache", col: 3, row: 3 },
        ],
        edges: [
          { from: "author", to: "post", label: "publish" },
          { from: "post", to: "posts", label: "write" },
          { from: "posts", to: "fanout", label: "event", async: true },
          { from: "fanout", to: "inbox", label: "push", async: true },
          { from: "reader", to: "feed", label: "GET /feed" },
          { from: "feed", to: "merge", label: "cursor" },
          { from: "merge", to: "inbox", label: "pull pushed" },
          { from: "merge", to: "celeb", label: "pull celebrity" },
          { from: "merge", to: "posts", label: "hydrate" },
        ],
      },
      scope: [
        "Create and delete posts, follow and unfollow authors, and read a personalized home feed.",
        "Support chronological or lightweight ranked order, stable forward pagination, privacy filtering, and a stated freshness SLI.",
        "Include celebrity skew and regional operation; exclude model-training design unless the interviewer asks for it.",
      ],
      apis: [
        "`POST /posts { clientPostId, bodyRef, visibility } -> { postId, createdAt }` and `DELETE /posts/{postId}`.",
        "`PUT /users/{viewerId}/following/{authorId}` and matching delete, both idempotent.",
        "`GET /feeds/home?cursor&limit -> { items, nextCursor, rankVersion }`.",
        "Events: `PostCreated`, `PostDeleted`, `FollowChanged`, and `VisibilityChanged`, each with stable event ID and source version.",
      ],
      dataModel: [
        "Post `{ postId, authorId, bodyRef, visibility, createdAt, version, deletedAt }` is the content source of truth.",
        "FollowEdge `{ followerId, authorId, createdAt }`, indexed by follower and author for reads and fan-out.",
        "FeedEntry `{ viewerId, postId, authorId, createdAt, rankFeatures, policyVersion }`, unique on `(viewerId, postId)` and sharded by viewer.",
        "Opaque cursor contains ordering tuple plus candidate or ranking version and is authenticated against tampering.",
      ],
      architecture: [
        "Post service commits post and outbox atomically; a durable author-partitioned stream feeds expansion workers.",
        "Fan-out workers read follower batches and idempotently upsert ordinary-author entries into viewer-sharded timeline storage.",
        "Celebrity posts remain in author timelines and are merged with materialized candidates by the feed service.",
        "The read path fetches candidates, applies block and visibility policy, ranks, hydrates post bodies, and fills a versioned cache.",
        "Deletion and access-change tombstones use a priority channel and are rechecked during hydration.",
      ],
      invariants: [
        "Canonical post and current relationship or visibility policy decide whether an item may be returned.",
        "One logical feed entry exists per viewer and post despite outbox or broker replay.",
        "Feed pagination has a deterministic unique order within its documented rank-session semantics.",
        "A successful post response follows durable source-of-truth commit; fan-out may be eventually consistent within the freshness SLO.",
      ],
      deepDives: [
        {
          title: "Push, pull, and hybrid fan-out",
          summary: "Choose the join location per author class from measured amplification and latency.",
          points: [
            "Batch and checkpoint ordinary follower expansion with deterministic entry keys.",
            "Merge high-fan-out author heads at read time and cache them independently.",
            "Version threshold changes and backfill gradually so policy migration does not duplicate or omit candidates.",
          ],
        },
        {
          title: "Ranking, cache, and cursor",
          summary: "Candidate IDs, mutable content, and viewer policy have different cache lifetimes and reuse.",
          points: [
            "Cache immutable post fragments broadly and viewer-ranked pages narrowly by ranking version.",
            "Use request coalescing, jittered expiry, and stale-if-error for safe content to contain stampedes.",
            "Use `(createdAt, postId)` for chronology or a rank-session boundary for mutable scores.",
          ],
        },
      ],
      scaling: [
        "Shard follower expansion by author batches and timeline storage by viewer; reserve independent queue capacity for backfills.",
        "Detect celebrity and hot post keys from observed amplification; replicate their author-head caches.",
        "Hydrate in batches and cap candidate windows so ranking and storage fan-in remain bounded.",
        "Serve local-region replicas when allowed while assigning one home region to ordered writes and policy changes.",
      ],
      observability: [
        "Post commit-to-feed-visible latency and oldest fan-out event age",
        "Entries attempted, deduplicated, failed, and reconciled per post class",
        "Feed read latency by candidate, rank, filter, hydrate, and cache stage",
        "Cache hit rate, stampede suppression, stale fallback, and hot-key distribution",
        "Duplicate, missing, deleted, blocked, or unauthorized exposure invariant counters",
      ],
    },
  },
  {
    id: "classic-chat",
    title: "Chat and Messaging",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a one-to-one and group chat system with persistent connections, offline delivery, per-conversation ordering, delivery and read receipts, presence, and multi-device synchronization.",
    requirementsToExplore: [
      "Concurrent connections, messages per second, group sizes, message retention, and attachment scope",
      "Ordering, durable acknowledgment, delivery, and read semantics",
      "Offline duration, history pagination, and multi-device cursor behavior",
      "Presence freshness, privacy, membership changes, and large groups",
      "Slow clients, reconnect storms, regional partitions, and degradation",
    ],
    expectedTopics: [
      "WebSocket or long-poll gateway capacity and session routing",
      "Conversation-partitioned sequencing and idempotent client message IDs",
      "Durable message log, outbox delivery, per-device cursor, and receipts",
      "Lease-based presence and versioned group membership",
      "Backpressure, reconnect jitter, offline push, and multi-region ownership",
    ],
    commonFailureModes: [
      "Acknowledging a message while it exists only in gateway memory",
      "Promising exactly-once or global message ordering",
      "Using device timestamps for canonical order",
      "Allowing stale membership or presence cache to authorize history",
    ],
    followUpQuestions: [
      "How does a client retry a send after an ambiguous timeout?",
      "What happens when a device receives sequence 43 before 42?",
      "How do you deliver one message to a million-member group?",
      "How does the fleet recover from losing every gateway in one zone?",
    ],
    reference: {
      diagram: {
        caption: "Gateways own live sockets and a session registry routes to them; the conversation partition assigns the monotonic sequence that clients order by.",
        nodes: [
          { id: "sender", label: "Sender", kind: "client", col: 0, row: 0 },
          { id: "recipient", label: "Recipient", kind: "client", col: 0, row: 3 },
          { id: "gw", label: "Connection gateway", kind: "edge", col: 1, row: 1 },
          { id: "router", label: "Message router", kind: "service", col: 2, row: 1 },
          { id: "seq", label: "Conversation partition (seq)", kind: "service", col: 3, row: 1 },
          { id: "msgs", label: "Message store", kind: "store", col: 4, row: 1 },
          { id: "registry", label: "Session registry", kind: "store", col: 2, row: 0 },
          { id: "push", label: "Offline push", kind: "external", col: 3, row: 3 },
          { id: "sync", label: "Sync / backfill API", kind: "service", col: 2, row: 3 },
        ],
        edges: [
          { from: "sender", to: "gw", label: "send" },
          { from: "gw", to: "router", label: "enqueue" },
          { from: "router", to: "seq", label: "assign seq" },
          { from: "seq", to: "msgs", label: "append" },
          { from: "router", to: "registry", label: "locate" },
          { from: "registry", to: "gw", label: "deliver", async: true },
          { from: "gw", to: "recipient", label: "stream" },
          { from: "router", to: "push", label: "offline", async: true },
          { from: "recipient", to: "sync", label: "resume" },
          { from: "sync", to: "msgs", label: "backfill" },
        ],
      },
      scope: [
        "Support one-to-one and group conversations, real-time push, durable history, offline catch-up, receipts, and bounded-stale presence.",
        "Promise order only within a conversation and distinguish accepted, delivered, and read states.",
        "Treat attachments and end-to-end encryption as follow-ups unless required, while preserving access control hooks.",
      ],
      apis: [
        "WebSocket `Connect(deviceId, resumeCursor)` authenticates and registers a leased connection.",
        "`SendMessage(conversationId, clientMessageId, bodyRef) -> { messageId, sequence, acceptedAt }` is idempotent.",
        "`GET /conversations/{id}/messages?afterSequence&limit` and `GET /sync?cursor&limit` support history and account-wide catch-up.",
        "`AckDelivered(conversationId, sequence, deviceId)` and `AckRead(conversationId, throughSequence)` advance monotonic state.",
      ],
      dataModel: [
        "Conversation `{ id, type, homePartition, membershipVersion }` and Member `{ conversationId, userId, role, joinedAt, removedAt }`.",
        "Message keyed by `(conversationId, sequence)` with unique `(conversationId, senderId, clientMessageId)` and immutable body reference.",
        "DeviceCursor `{ userId, deviceId, syncOffset }` and Receipt `{ conversationId, userId, deliveredThrough, readThrough }` use monotonic offsets.",
        "PresenceLease `{ userId, deviceId, connectionId, gatewayId, expiresAt }` is soft state; last-seen policy is separate.",
      ],
      architecture: [
        "Connection gateways terminate sockets, authenticate devices, maintain bounded buffers, and renew session-directory leases.",
        "Chat service routes a conversation to one logical partition, deduplicates the client ID, assigns sequence, and durably stores before acknowledging.",
        "A transactional outbox publishes message events to user or conversation fan-out workers, which push to live gateways and update offline inboxes.",
        "Clients recover through durable cursors; ephemeral presence publishes coalesced state transitions independently of message truth.",
        "Object storage and a CDN serve authorized attachments without routing large bytes through chat gateways.",
      ],
      invariants: [
        "Only current members may send or read; delivery is not proof of continuing authorization.",
        "A durable accepted acknowledgment is sent only after message storage commits.",
        "A repeated client message ID returns the original message ID and sequence without a second effect.",
        "Sequence order is monotonic per conversation, and device or receipt cursors never regress.",
        "Presence is explicitly ephemeral and cannot determine whether durable history exists.",
      ],
      deepDives: [
        {
          title: "Order and retry safety",
          summary: "One conversation owner sequences idempotent commands while clients tolerate replay and gaps.",
          points: [
            "Store client dedupe and sequence assignment atomically; never order by sender clocks.",
            "Buffer a short out-of-order gap and fetch the missing range before advancing the device cursor.",
            "Represent read state as a monotonic high-water mark when product semantics allow it.",
          ],
        },
        {
          title: "Connections and fan-out",
          summary: "Gateways hold sockets, but durable state and recovery live behind them.",
          points: [
            "Size the tier from concurrent sockets rather than message rate: descriptors, per-socket memory, and heartbeat writes all scale with connections that are sitting idle, and the reconnect burst after a zone loss is what sets the headroom.",
            "Use leased routing hints and cursor recovery when gateways fail or routes are stale.",
            "For large groups, persist one message and batch member pointers or notifications with quota isolation.",
          ],
        },
      ],
      scaling: [
        "Shard conversations by ID and isolate exceptional hot groups with dedicated partitions or hierarchical fan-out.",
        "Scale gateways by connection resource budgets and use jittered draining for deploys and zone loss.",
        "Batch offline delivery and receipt updates; compact monotonic watermarks instead of storing unnecessary per-message rows.",
        "Use home-region conversation writers where strong order is needed and asynchronous replicas for allowed local reads.",
      ],
      observability: [
        "Active connections, handshake failures, reconnect rate, heartbeat lag, and buffered bytes",
        "Send acceptance, durable-store, gateway-delivery, and offline-delivery latency",
        "Duplicate suppression, sequence gaps, redelivery, and cursor replay volume",
        "Consumer lag, hot-conversation throughput, and large-group oldest-delivery age",
        "Presence staleness, membership-denied reads, and unauthorized attachment attempts",
      ],
    },
  },
  {
    id: "classic-payment-ledger",
    title: "Payment and Ledger System",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a payment platform that authorizes, captures, settles, and refunds payments through an external processor while maintaining an immutable double-entry ledger, idempotent APIs, auditability, and reconciliation.",
    requirementsToExplore: [
      "Supported payment lifecycle, partial capture or refund, currencies, and excluded chargeback or FX scope",
      "Client retries, provider timeouts, duplicate webhooks, and unknown outcomes",
      "Ledger accounts, balance reads, settlement, and audit requirements",
      "Concurrency, outbox publication, reconciliation cadence, and manual review",
      "Regional write ownership, disaster recovery, security, and retention",
    ],
    expectedTopics: [
      "Explicit payment state machine and optimistic versioning",
      "Scoped idempotency record with request fingerprint and canonical response",
      "Immutable balanced journal transaction and entries",
      "Provider adapter, webhook inbox, transactional outbox, and at-least-once processing",
      "Settlement-file reconciliation, corrections, alerts, and audit trail",
    ],
    commonFailureModes: [
      "Treating a provider timeout as a definite failure",
      "Claiming end-to-end exactly-once message delivery",
      "Mutating posted ledger entries or balances without journal evidence",
      "Updating payment and publishing its event as an unsafe dual write",
    ],
    followUpQuestions: [
      "How is a duplicate capture request recognized after a process restart?",
      "What does the API return while a provider outcome is unknown?",
      "How do you prove every journal transaction balances by currency?",
      "What happens when settlement records disagree with internal state?",
    ],
    reference: {
      diagram: {
        caption: "The idempotency record commits in the same transaction as the ledger entries, so an ambiguous retry returns the original outcome instead of moving money twice.",
        nodes: [
          { id: "client", label: "Merchant client", kind: "client", col: 0, row: 1 },
          { id: "api", label: "Payment API", kind: "service", col: 1, row: 1 },
          { id: "idem", label: "Idempotency record", kind: "store", col: 2, row: 0 },
          { id: "ledger", label: "Double-entry ledger", kind: "store", col: 2, row: 1 },
          { id: "saga", label: "Capture workflow", kind: "compute", col: 3, row: 1 },
          { id: "psp", label: "Payment processor", kind: "external", col: 4, row: 1 },
          { id: "outbox", label: "Outbox", kind: "stream", col: 3, row: 2 },
          { id: "recon", label: "Reconciliation", kind: "compute", col: 4, row: 2 },
          { id: "balance", label: "Balance projection", kind: "store", col: 2, row: 3 },
        ],
        edges: [
          { from: "client", to: "api", label: "POST /payments" },
          { from: "api", to: "idem", label: "claim key" },
          { from: "api", to: "ledger", label: "debit+credit" },
          { from: "api", to: "saga", label: "start" },
          { from: "saga", to: "psp", label: "auth/capture" },
          { from: "saga", to: "outbox", label: "event", async: true },
          { from: "outbox", to: "balance", label: "project", async: true },
          { from: "psp", to: "recon", label: "settlement", async: true },
          { from: "recon", to: "ledger", label: "adjustment", async: true },
        ],
      },
      scope: [
        "Create payments and support authorization, capture, settlement, refund, status, and provider callbacks.",
        "Maintain immutable accounting and reconcile processor records; state whether partial operations and multiple currencies are included.",
        "Exclude card vault details, FX, disputes, and chargebacks unless requested, while preserving security boundaries.",
      ],
      apis: [
        "`POST /payments` with scoped idempotency key and amount/currency returns committed or pending payment state.",
        "`POST /payments/{id}/captures` and `/refunds` each use independent idempotency keys and explicit amounts.",
        "`GET /payments/{id}` exposes state, version, amounts, and unresolved-provider status without leaking sensitive credentials.",
        "`POST /providers/{name}/webhook` verifies signature and deduplicates provider event ID before transition.",
      ],
      dataModel: [
        "Payment `{ id, merchantId, amount, currency, authorized, captured, refunded, state, version }` with legal transition checks.",
        "IdempotencyRecord `{ principal, operation, key, requestHash, status, response }`, unique on its scope.",
        "ProviderAttempt `{ paymentId, operation, providerRequestId, status, responseRef, startedAt }` and deduplicated webhook inbox.",
        "LedgerTransaction `{ id, businessEventId, postedAt, reversalOf }` plus immutable LedgerEntry `{ transactionId, accountId, currency, debit, credit }`.",
        "OutboxEvent and ReconciliationResult records link domain transitions, publication, settlement comparison, and correction.",
      ],
      architecture: [
        "API validates and atomically claims idempotency before an orchestration state machine advances payment intent.",
        "A relational transaction commits payment version, balanced journal entries, idempotency response, and outbox event together.",
        "Provider adapters execute asynchronous commands with stable provider IDs; signed callbacks enter through a deduplicating inbox.",
        "Outbox relay publishes state changes at least once, and all consumers apply inbox deduplication.",
        "A reconciliation service compares closed processor windows with internal attempts, payments, and journal postings and queues exceptions.",
      ],
      invariants: [
        "Only legal versioned payment transitions commit, and cumulative refund never exceeds captured amount.",
        "For every ledger transaction and currency, `SUM(debit) = SUM(credit)`; a signed-entry representation would equivalently require `SUM(amount) = 0`.",
        "Journal entries are immutable; corrections are linked reversing or adjustment transactions.",
        "One financial effect occurs per idempotency scope despite request, webhook, or event redelivery.",
        "An ambiguous provider outcome remains pending or unknown until queried or reconciled; it is never guessed.",
      ],
      deepDives: [
        {
          title: "Ambiguous outcomes and idempotency",
          summary: "The platform binds one intent to one result across client retries and provider uncertainty.",
          points: [
            "Persist request fingerprint and pending claim before work; return the original canonical response on replay.",
            "Use a stable provider request ID and query it after timeout instead of issuing a new authorization blindly.",
            "Deduplicate provider callbacks and outbox events independently because each boundary delivers at least once.",
          ],
        },
        {
          title: "Ledger and reconciliation",
          summary: "The ledger records internal financial truth while reconciliation compares it with external settlement facts.",
          points: [
            "Post all entries for one business event in one balanced transaction and derive or transactionally maintain balances.",
            "Match settlement rows to internal payments on the provider request ID, then report each mismatch under its own class — present here but not there, present twice, right amount in the wrong currency — because a single aggregate error count tells an operator nothing about what to do next.",
            "Correct through new postings and state transitions with audit links, never destructive edits.",
          ],
        },
      ],
      scaling: [
        "Partition payment orchestration by merchant or payment while keeping one journal posting inside an atomic ownership boundary.",
        "Use optimistic versioning to serialize concurrent operations and isolate hot merchants with quotas.",
        "Run provider I/O, webhook processing, publication, and reconciliation asynchronously with separate capacity and backpressure.",
        "Use a fenced home-region writer for money-moving state and independently tested replicas and backups.",
      ],
      observability: [
        "Unbalanced posting count is a zero-tolerance invariant alarm",
        "Payment transition latency, illegal transition attempts, and optimistic conflicts",
        "Provider timeout, throttle, unknown-state count and age, and webhook verification failures",
        "Idempotency replay and conflict rates, inbox dedupe, and outbox oldest age",
        "Unmatched settlement count, value delta, reconciliation age, and manual-review SLA",
      ],
    },
  },
  {
    id: "classic-notifications",
    title: "Notification System",
    category: "classic",
    difficulty: "medium",
    durationMinutes: 40,
    prompt:
      "Design a multi-tenant notification platform for transactional and scheduled email, SMS, push, and in-app messages, including preferences, priorities, deduplication, rate limits, provider failure, retries, and dead-letter handling.",
    requirementsToExplore: [
      "Channels, transactional versus bulk traffic, schedules, latency, and delivery status",
      "Preferences, consent, quiet hours, templates, and localization boundaries",
      "Audience size, tenant fairness, priority, per-recipient and provider quotas",
      "Provider acceptance, callback, retry, failover, cancellation, and duplicate semantics",
      "Retention, privacy, audit, and degraded behavior during provider outage",
    ],
    expectedTopics: [
      "Durable intent and transactional outbox before recipient fan-out",
      "Checkpointed audience expansion and deterministic delivery IDs",
      "Priority and fair channel queues with scheduled ready times",
      "Preference evaluation, rate limiting, provider adapters, and callback inbox",
      "Jittered retry, circuit breaking, dead-letter queue, redrive, and status storage",
    ],
    commonFailureModes: [
      "Acknowledging an intent before it is durable",
      "Letting bulk traffic starve transactional notifications",
      "Retrying an ambiguous provider send through a second provider without duplicate analysis",
      "Applying stale preferences to delayed scheduled work",
    ],
    followUpQuestions: [
      "How do ten million recipients expand without one oversized transaction?",
      "Where and when are user preferences checked?",
      "How does the system respond to provider throttling and callback duplication?",
      "How is dead-letter work safely redriven after a code fix?",
    ],
    reference: {
      diagram: {
        caption: "Preference and dedupe checks run at send time rather than enqueue time, and transactional traffic is isolated from bulk so a campaign cannot delay a security code.",
        nodes: [
          { id: "src", label: "Event sources", kind: "client", col: 0, row: 1 },
          { id: "ingest", label: "Notification API", kind: "service", col: 1, row: 1 },
          { id: "dedupe", label: "Dedupe & rate store", kind: "store", col: 2, row: 0 },
          { id: "txq", label: "Transactional queue", kind: "stream", col: 2, row: 1 },
          { id: "bulkq", label: "Bulk queue", kind: "stream", col: 2, row: 2 },
          { id: "worker", label: "Delivery workers", kind: "compute", col: 3, row: 1 },
          { id: "prefs", label: "Preference store", kind: "store", col: 3, row: 0 },
          { id: "channels", label: "Push / SMS / email", kind: "external", col: 4, row: 1 },
          { id: "receipts", label: "Delivery receipts", kind: "store", col: 4, row: 2 },
        ],
        edges: [
          { from: "src", to: "ingest", label: "event" },
          { from: "ingest", to: "dedupe", label: "logical id" },
          { from: "ingest", to: "txq", label: "urgent" },
          { from: "ingest", to: "bulkq", label: "campaign" },
          { from: "txq", to: "worker", label: "consume" },
          { from: "bulkq", to: "worker", label: "throttled" },
          { from: "worker", to: "prefs", label: "check" },
          { from: "worker", to: "channels", label: "dispatch" },
          { from: "channels", to: "receipts", label: "status", async: true },
        ],
      },
      scope: [
        "Accept transactional and scheduled notification intents across email, SMS, push, and in-app channels.",
        "Apply policy, preference, priority, fairness, rate, retry, and provider status while exposing delivery state.",
        "Treat campaign authoring and content generation as out of scope; accept a versioned template and audience reference.",
      ],
      apis: [
        "`POST /notifications` with idempotency key, template version, audience, channels, priority, and schedule returns intent ID.",
        "`POST /notifications/{id}/cancel` stops not-yet-dispatched deliveries and records the cutoff semantics.",
        "`GET /notifications/{id}` returns aggregate status and paged recipient exceptions.",
        "Preference APIs update versioned channel consent, quiet hours, and mandatory-message policy; provider callbacks are signed and deduplicated.",
      ],
      dataModel: [
        "NotificationIntent `{ id, tenantId, templateVersion, audienceRef, priority, scheduledAt, state }` with scoped idempotency key.",
        "Delivery `{ intentId, recipientId, channel, state, attempt, nextAttemptAt, providerId }`, unique on deterministic delivery key.",
        "Preference `{ userId, channel, category, enabled, quietHours, version }` and immutable ConsentAudit where required.",
        "TemplateVersion, ProviderAttempt, CallbackInbox, OutboxEvent, and DeadLetter records preserve retry and audit context.",
      ],
      architecture: [
        "API persists validated intent and outbox; scheduler releases intents when ready.",
        "A checkpointed planner expands audience pages, evaluates coarse eligibility, and emits deterministic recipient-channel deliveries.",
        "Fair priority queues feed channel workers that recheck current policy, acquire hierarchical rate tokens, render, and call provider adapters.",
        "Callback processors deduplicate provider events and advance monotonic delivery state; retry scheduler and DLQ handle failure classes.",
        "Provider isolation and circuit breakers let healthy channels continue while low-priority work is delayed or shed explicitly.",
      ],
      invariants: [
        "An acknowledged intent is durably recoverable and never silently disappears.",
        "One deterministic recipient-channel delivery exists per intent unless the product explicitly models repeat sends.",
        "Consent and mandatory-message policy are enforced at the documented freshness point.",
        "Delivery state advances monotonically and duplicate callbacks cannot regress it.",
        "Provider, tenant, campaign, recipient, and channel quotas are enforced before external dispatch.",
      ],
      deepDives: [
        {
          title: "Fan-out, priority, and fairness",
          summary: "Audience expansion is incremental and isolated from channel dispatch capacity.",
          points: [
            "Checkpoint expansion pages and use deterministic keys so planner replay is harmless.",
            "Reserve transactional capacity while applying weighted tenant fairness to prevent starvation.",
            "Schedule by ready time and oldest age; depth alone is not a lateness metric.",
          ],
        },
        {
          title: "Provider failures and duplicates",
          summary: "Normalize provider outcomes and treat timeouts as ambiguous until status is resolved.",
          points: [
            "Retry only classified transient errors with capped jitter and provider rate hints.",
            "Persist stable provider request IDs and deduplicate signed callbacks.",
            "Fail over only when duplicate risk and sender identity remain acceptable; otherwise hold for reconciliation.",
          ],
        },
      ],
      scaling: [
        "Shard intent and delivery data by tenant and intent; expand huge audiences in bounded pages.",
        "Use separate channel and priority lanes, fair scheduling, and hierarchical rate limiters.",
        "Batch provider calls where semantics permit and autoscale from ready-item age and downstream headroom.",
        "Isolate exceptional campaigns, tenants, and providers so they cannot monopolize shared partitions.",
      ],
      observability: [
        "Intent acceptance-to-ready, ready-to-dispatch, provider-accept, and confirmed-delivery latency",
        "Oldest ready age and throughput by priority, tenant, channel, and provider",
        "Preference and policy suppression counts with freshness version",
        "Provider success, throttle, timeout, bounce, callback lag, retry, and circuit state",
        "Duplicate-send indicators, deterministic-key conflicts, DLQ age, and redrive outcomes",
      ],
    },
  },
  {
    id: "classic-file-sync",
    title: "File Storage and Synchronization",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a file storage and synchronization service for large resumable uploads, multi-device offline edits, version history, conflict handling, deduplication, sharing, and CDN-backed downloads.",
    requirementsToExplore: [
      "File and account scale, maximum object size, upload reliability, and supported offline period",
      "Folder namespace, moves, deletes, restores, version history, and sharing permissions",
      "Multipart and delta transfer, chunking, dedupe scope, integrity, and scanning",
      "Sync cursor, conflict policy, tombstone retention, and cross-device convergence",
      "Regional metadata ownership, object durability, CDN authorization, and disaster recovery",
    ],
    expectedTopics: [
      "Direct multipart upload, checksummed parts, immutable manifest, and atomic commit",
      "Content-addressed chunks, tenant-scoped dedupe, reference safety, and delayed garbage collection",
      "Strong metadata store with stable node IDs, base-version compare-and-swap, and change journal",
      "Delta sync, cursors, snapshots, tombstones, explicit conflict versions, and restore",
      "Object storage, CDN, signed URLs, access checks, scanning, and retention",
    ],
    commonFailureModes: [
      "Making uploaded bytes visible before every referenced chunk is durable",
      "Trusting a client hash or leaking cross-tenant content existence through dedupe",
      "Using last-writer-wins without acknowledging lost offline edits",
      "Deleting chunks immediately after a stale reference-count decrement",
    ],
    followUpQuestions: [
      "What operation makes a multipart upload visible to other devices?",
      "How do two offline edits to the same binary file converge?",
      "What happens when a sync cursor is older than retained change history?",
      "How does deletion propagate to a device offline for several months?",
    ],
    reference: {
      diagram: {
        caption: "Content-addressed chunks deduplicate storage while version vectors detect genuine concurrency; conflicting edits are preserved as both versions rather than silently merged.",
        nodes: [
          { id: "dev", label: "Device client", kind: "client", col: 0, row: 1 },
          { id: "meta", label: "Metadata service", kind: "service", col: 1, row: 1 },
          { id: "vv", label: "Version vectors", kind: "store", col: 2, row: 0 },
          { id: "chunk", label: "Chunk index (CAS)", kind: "store", col: 2, row: 1 },
          { id: "blob", label: "Object storage", kind: "store", col: 3, row: 1 },
          { id: "notify", label: "Change notifier", kind: "stream", col: 2, row: 2 },
          { id: "other", label: "Other devices", kind: "client", col: 3, row: 2 },
          { id: "conflict", label: "Conflict resolver", kind: "compute", col: 3, row: 0 },
        ],
        edges: [
          { from: "dev", to: "meta", label: "manifest" },
          { from: "meta", to: "vv", label: "compare" },
          { from: "vv", to: "conflict", label: "concurrent" },
          { from: "conflict", to: "meta", label: "conflict copy" },
          { from: "meta", to: "chunk", label: "missing" },
          { from: "dev", to: "blob", label: "upload" },
          { from: "chunk", to: "blob", label: "ref" },
          { from: "meta", to: "notify", label: "changed", async: true },
          { from: "notify", to: "other", label: "sync cursor", async: true },
        ],
      },
      scope: [
        "Support folders and files, resumable upload and download, offline multi-device sync, versioning, conflicts, deletes, restore, and sharing ACLs.",
        "Provide content integrity and optional deduplication without exposing another tenant's content.",
        "Exclude simultaneous collaborative document editing; treat each committed file version as an opaque byte sequence.",
      ],
      apis: [
        "`POST /uploads { nodeId?, size, baseVersion, chunking } -> { sessionId, partUrls, expiresAt }`.",
        "`PUT` signed part URLs are retryable by part number; `POST /uploads/{id}/commit { manifest, fileHash }` atomically creates a version.",
        "`GET /sync?cursor&limit -> { changes, nextCursor, highWatermark }` and a snapshot endpoint recover expired cursors.",
        "Metadata commands `move`, `rename`, `delete`, and `restore` include expected node version; downloads request a short-lived authorized URL.",
      ],
      dataModel: [
        "NamespaceNode `{ id, accountId, parentId, name, currentVersion, aclVersion, tombstonedAt }`, unique on live `(parentId, name)`.",
        "FileVersion `{ nodeId, version, baseVersion, manifestId, size, hash, createdAt, conflictOf }` is immutable.",
        "Chunk `{ tenantScope, hash, size, objectKey, verifiedAt }` and ordered ChunkRef manifest reconstruct bytes.",
        "UploadSession and Part records track authorization, ranges, checksums, expiry, scan state, and commit status.",
        "ChangeLog `{ accountId, cursor, nodeId, operation, version }`, Tombstone, DeviceCursor, and optional reference-mark epochs support sync and lifecycle.",
      ],
      architecture: [
        "A strongly consistent metadata service owns namespace, versions, ACLs, and the append-only account change journal.",
        "Clients transfer parts directly to object storage through scoped URLs; an upload coordinator verifies checksums and durable chunk existence.",
        "Commit transaction compare-and-swaps the base node version, writes immutable version and manifest, updates the node, and appends one change record.",
        "Sync clients page the journal, fetch missing manifests or chunks, and upload local changes with expected versions.",
        "CDN downloads use short-lived capabilities after metadata authorization; background scanners, scrubbers, and delayed mark-and-sweep manage content lifecycle.",
      ],
      invariants: [
        "Every committed version references only verified durable chunks in the correct ordered manifest.",
        "Namespace uniqueness and ACL checks hold in the same transaction as metadata mutation.",
        "A stale base version never silently overwrites a newer version; it produces an explicit deterministic conflict result.",
        "Change-log cursors advance with committed metadata and can be replayed idempotently.",
        "Tombstones outlive the supported offline window, and garbage collection never deletes a chunk reachable from a live or retained version.",
      ],
      deepDives: [
        {
          title: "Chunk, verify, and commit",
          summary: "Byte transfer is asynchronous; one metadata transaction is the visibility boundary.",
          points: [
            "Choose the chunk size by naming what it trades: a smaller chunk resumes nearer to where the transfer broke and reuses more unchanged bytes between versions, and pays for both in object requests and manifest rows.",
            "Verify hashes on trusted infrastructure and scope dedupe to a tenant or encryption domain.",
            "Create the manifest only after all chunks are durable; collect abandoned session bytes after expiry and a safety window.",
          ],
        },
        {
          title: "Delta sync and conflict",
          summary: "A journal distributes committed metadata changes while optimistic versions expose concurrency.",
          points: [
            "Devices advance cursors only after applying contiguous pages and use a snapshot when history is compacted.",
            "Preserve concurrent opaque edits as sibling versions or conflict copies rather than silent last-writer overwrite.",
            "Retain tombstones and version history long enough for offline devices and restore policy.",
          ],
        },
      ],
      scaling: [
        "Shard metadata and change logs by account or namespace while routing all large byte transfer directly to object storage.",
        "Use chunk and manifest batching, CDN caching for hot downloads, and background dedupe or garbage collection with strict rate limits.",
        "Keep one home-region metadata writer per account for simple conflict order; replicate object bytes and metadata independently.",
        "Cap folder listing and sync page sizes, and isolate accounts with extreme file count or churn.",
      ],
      observability: [
        "Upload start-to-commit latency, part retry rate, expired sessions, and orphan bytes by age",
        "Hash mismatch, missing-chunk, corrupt-object scrub, and manifest-integrity alarms",
        "Change-log lag, expired cursors, snapshot frequency, and device sync completion latency",
        "Version conflict, tombstone resurrection attempt, and ACL-denied access rates",
        "Reference-mark violations, garbage-collection candidates and deletes, CDN hit rate, and download p99",
      ],
    },
  },
  {
    id: "classic-nearby-service",
    title: "Nearby-Location Service",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a service that ingests moving-entity locations and returns fresh, eligible entities within a radius, ordered by distance. Address spatial indexing, hot geographic regions, privacy, retention, and regional failure.",
    requirementsToExplore: [
      "Entity and update volume, peak queries, radius distribution, result limit, and location freshness",
      "Eligibility filters, exact distance ordering, pagination, and acceptable approximation",
      "Dense-region hotspots, moving-object churn, stale updates, and deletion",
      "Location authorization, precision, retention, privacy, and access audit",
      "Regional routing, cross-border movement, failover, and degraded results",
    ],
    expectedTopics: [
      "Geohash, quadtree, or spatial index candidate generation with neighboring cells",
      "Versioned latest-location store and exact distance or geometry filter",
      "Update coalescing, adaptive cells, subshards, and candidate caps",
      "Separate stable entity metadata from volatile location state",
      "Freshness SLI, expiry, privacy policy, retention, cache, and multi-region placement",
    ],
    commonFailureModes: [
      "Querying only the center geohash and missing boundary neighbors",
      "Trusting cell membership as exact radius inclusion",
      "Letting late location events move an entity backward in state",
      "Caching precise location beyond eligibility, expiry, or deletion",
    ],
    followUpQuestions: [
      "How does the query cover a radius that crosses cell boundaries?",
      "How do you split a downtown cell without changing every other region?",
      "What happens when an entity moves between regions during a partition?",
      "Which data is retained after an entity stops sharing location?",
    ],
    reference: {
      diagram: {
        caption: "Cell-indexed candidates are always re-filtered by true distance, because adjacent points can fall in different cells; precise location is kept apart from analytics.",
        nodes: [
          { id: "client", label: "Client", kind: "client", col: 0, row: 1 },
          { id: "api", label: "Search API", kind: "service", col: 1, row: 1 },
          { id: "cellidx", label: "Cell index (S2/geohash)", kind: "store", col: 2, row: 1 },
          { id: "cache", label: "Hot-region cache", kind: "cache", col: 2, row: 0 },
          { id: "filter", label: "Distance & filter", kind: "compute", col: 3, row: 1 },
          { id: "places", label: "Place store", kind: "store", col: 4, row: 1 },
          { id: "upd", label: "Location updates", kind: "stream", col: 2, row: 2 },
          { id: "coarse", label: "Coarse analytics store", kind: "store", col: 3, row: 2 },
        ],
        edges: [
          { from: "client", to: "api", label: "lat/lng + r" },
          { from: "api", to: "cache", label: "hot cell" },
          { from: "api", to: "cellidx", label: "cell + nbrs" },
          { from: "cellidx", to: "filter", label: "candidates" },
          { from: "filter", to: "places", label: "hydrate" },
          { from: "client", to: "upd", label: "position", async: true },
          { from: "upd", to: "cellidx", label: "reindex", async: true },
          { from: "upd", to: "coarse", label: "bucketed", async: true },
        ],
      },
      scope: [
        "Authenticated entities publish versioned locations and availability; clients query bounded nearby results with filters.",
        "Results obey freshness, eligibility, authorization, radius, and privacy constraints and may state an approximation contract.",
        "Route optimization, dispatch matching, and map rendering are out of scope unless requested.",
      ],
      apis: [
        "`PUT /entities/{id}/location { lat, lon, eventTime, version, ttl }` is idempotent and rejects version regression.",
        "`DELETE /entities/{id}/location { version }` removes visibility and starts policy-defined deletion propagation.",
        "`GET /nearby?lat&lon&radius&filters&cursor&limit -> { entities, nextCursor, freshness }`.",
        "Eligibility or availability updates are versioned separately from high-churn coordinates.",
      ],
      dataModel: [
        "Entity `{ id, type, eligibility, attributes, policyVersion }` is stable metadata and authorization source.",
        "LatestLocation `{ entityId, lat, lon, cellId, version, eventTime, receivedAt, expiresAt }` rejects late versions.",
        "SpatialPosting `{ cellId, shard, entityId, locationVersion }` supports candidate lookup and idempotent cell movement.",
        "LocationUpdate event log and PrivacyPolicy or RetentionRecord support recovery, deletion, and audit.",
      ],
      architecture: [
        "Regional ingestion authenticates, validates, coalesces, and publishes versioned location updates.",
        "Consumers update the latest-location store and spatial postings, tolerating a brief old/new-cell overlap.",
        "Query planner covers the radius with cells, fans out to their shards, deduplicates candidate IDs, hydrates current state, filters exact distance and eligibility, then ranks.",
        "Stable entity metadata and policy caches have independent invalidation from volatile location indexes.",
        "Regional ownership follows data-residency and latency policy; failover advertises freshness and may reduce radius or result count.",
      ],
      invariants: [
        "No result may be unauthorized, ineligible, expired, or outside the exact requested radius unless approximation is explicitly labeled.",
        "Location version never regresses even when stream events arrive out of order.",
        "Approximate spatial cells only generate candidates; exact geometry decides inclusion and order.",
        "Deletion and privacy changes propagate to indexes, caches, logs, and replicas within a stated SLA.",
        "Query radius, candidate count, and result count are bounded to protect shared capacity.",
      ],
      deepDives: [
        {
          title: "Geohash or quadtree search",
          summary: "Cell precision trades candidate false positives against query fan-out and update churn.",
          points: [
            "Cover all cells intersecting the radius, including neighbors, and deduplicate overlap.",
            "Hydrate the newest coordinates and compute exact spherical distance before ordering.",
            "Use `(distance, entityId)` plus query snapshot or freshness state for stable bounded pagination.",
          ],
        },
        {
          title: "Moving entities and hot regions",
          summary: "Versioned updates and adaptive partitions keep dense, fast-moving areas from dominating the index.",
          points: [
            "Coalesce updates by movement and time while preserving the promised freshness.",
            "Split dense cells recursively or add subshards and query all children behind one logical cell.",
            "Resolve dual postings during movement through latest-version hydration rather than a distributed transaction across cells.",
          ],
        },
      ],
      scaling: [
        "Shard and adapt cells by measured entity density and QPS; isolate dense-event traffic from ordinary regions.",
        "Coalesce noisy updates, batch stream writes, and cap query radius and candidate amplification.",
        "Cache stable metadata, not precise locations beyond their freshness or privacy lifetime.",
        "Geo-route requests to the owning region and define handoff for entities crossing ownership boundaries.",
      ],
      observability: [
        "Location ingest-to-query freshness distribution and stale-result count",
        "Candidates scanned per returned entity, cells per query, and exact-filter rejection rate",
        "Update version regressions, old/new-cell overlap age, and index/store reconciliation mismatch",
        "Hot cell and subshard QPS, storage, throttling, and query p99 by radius",
        "Unauthorized, expired, deleted, or out-of-radius result invariant alarms",
      ],
    },
  },
  {
    id: "classic-crawler-search",
    title: "Web Crawler and Search Index",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a web crawler and keyword search index that discovers and recrawls public pages politely, removes URL and content duplicates, builds an incremental inverted index, and serves fresh ranked results.",
    requirementsToExplore: [
      "Corpus size, seed and discovery sources, coverage, recrawl freshness, and allowed content",
      "Robots policy, host politeness, retries, conditional requests, and crawl-trap limits",
      "URL canonicalization, redirect policy, exact and near content duplicates, and deletions",
      "Index fields, ranking needs, publish freshness, shard count, query latency, and pagination",
      "Regional crawling, storage retention, takedown SLA, and partial search results",
    ],
    expectedTopics: [
      "Host-partitioned URL frontier with priority, leases, robots cache, and next-allowed time",
      "Canonical URL store, probabilistic prefilter, definitive dedupe, and content fingerprints",
      "Fetch, raw object storage, parse, document version, and link extraction pipeline",
      "Immutable inverted-index segments, tombstones, merge, and atomic manifest publication",
      "Query broker, shard replicas, postings retrieval, ranking, cache, and freshness monitoring",
    ],
    commonFailureModes: [
      "Distributing URLs randomly so several schedulers violate one host's limit",
      "Using a Bloom filter as the only permanent seen-URL truth",
      "Overwriting live index files during incremental update",
      "Returning silent partial results when one query shard times out",
    ],
    followUpQuestions: [
      "How do you enforce per-host delay with thousands of fetchers?",
      "How do you stop an infinite calendar or session-parameter crawl trap?",
      "How does a document deletion become visible before a full index rebuild?",
      "What consistency does one search query see while segments are publishing?",
    ],
    reference: {
      diagram: {
        caption: "The frontier is partitioned by host so politeness is enforced per origin, and both URL and content duplication are removed before documents reach the index.",
        nodes: [
          { id: "frontier", label: "Host-partitioned frontier", kind: "store", col: 0, row: 1 },
          { id: "sched", label: "Politeness scheduler", kind: "service", col: 1, row: 1 },
          { id: "fetch", label: "Fetchers", kind: "compute", col: 2, row: 1 },
          { id: "robots", label: "robots.txt cache", kind: "cache", col: 1, row: 0 },
          { id: "urldup", label: "Seen-URL filter (Bloom)", kind: "store", col: 2, row: 0 },
          { id: "cdup", label: "Content fingerprint", kind: "store", col: 3, row: 0 },
          { id: "parse", label: "Parser & extractor", kind: "compute", col: 3, row: 1 },
          { id: "index", label: "Index builder", kind: "compute", col: 4, row: 1 },
          { id: "serve", label: "Search serving", kind: "service", col: 4, row: 2 },
        ],
        edges: [
          { from: "frontier", to: "sched", label: "next per host" },
          { from: "sched", to: "robots", label: "allowed?" },
          { from: "sched", to: "fetch", label: "lease" },
          { from: "fetch", to: "parse", label: "document" },
          { from: "parse", to: "urldup", label: "new links" },
          { from: "urldup", to: "frontier", label: "enqueue", async: true },
          { from: "parse", to: "cdup", label: "simhash" },
          { from: "cdup", to: "index", label: "unique" },
          { from: "index", to: "serve", label: "segments", async: true },
        ],
      },
      scope: [
        "Crawl a defined public corpus from seeds and discovered links while respecting robots policy and origin limits.",
        "Canonicalize and deduplicate pages, parse searchable fields, and incrementally publish a keyword index with ranking and takedown.",
        "Exclude full semantic retrieval and a web-scale ML ranking pipeline unless explicitly requested.",
      ],
      apis: [
        "`POST /crawl/seeds` and `POST /crawl/urls/{id}/recrawl` enqueue durable prioritized work with authorization.",
        "Internal events `URLDiscovered`, `FetchCompleted`, `DocumentParsed`, `DocumentDeleted`, and `SegmentPublished` carry stable versions.",
        "`GET /search?q&filters&cursor&limit -> { results, nextCursor, indexVersion, partial }`.",
        "Takedown API records policy reason and deadline and emits a priority document tombstone.",
      ],
      dataModel: [
        "URLRecord `{ id, canonicalUrl, host, status, priority, nextFetchAt, etag, contentHash, version }` with unique canonical URL.",
        "HostPolicy `{ host, robotsVersion, crawlDelay, maxConcurrency, nextAllowedAt, backoff }` owned by one scheduler partition.",
        "FrontierItem includes priority, eligibility time, lease owner and expiry, attempts, and discovery provenance.",
        "Document `{ docId, sourceUrlId, sourceVersion, fields, contentRef, deletedAt }` and immutable index segments with term dictionary and postings.",
        "ShardManifest `{ version, shard, segmentRefs, publishedAt }`; a GlobalManifest or broker-issued manifest vector pins one durable version for every queried shard.",
      ],
      architecture: [
        "Discovery feeds a canonicalizer and URL dedupe store before durable host-aware frontier insertion.",
        "Schedulers own host budgets and lease eligible work to sandboxed fetchers that enforce size, type, redirect, and time limits.",
        "Raw responses go to object storage; parsers extract canonical documents and links, then content fingerprints suppress duplicates.",
        "Segment builders consume versioned documents into immutable postings; mergers compact segments and publishers atomically swap manifests.",
        "Search brokers parse requests, select replicas, fan out to index shards, merge ranked top-K, hydrate snippets, and label partial responses.",
      ],
      invariants: [
        "Before every fetch, enforce the most recently fetched unexpired robots rules plus per-host limits; when refresh fails after expiry, apply the documented conservative fallback.",
        "A canonical URL and document version are ingested idempotently despite lease recovery and event replay.",
        "A query pins a coherent global manifest or per-shard manifest vector and references only durable immutable segment files.",
        "Newer document versions and tombstones suppress obsolete postings within the freshness or takedown SLA.",
        "Fetch bytes, redirects, duration, depth, host expansion, and query work are bounded.",
      ],
      deepDives: [
        {
          title: "Frontier and duplicate control",
          summary: "Host scheduling protects external origins while layered dedupe protects internal capacity.",
          points: [
            "Partition by host to serialize robots, delay, concurrency, and backoff state.",
            "On a Bloom definite-negative, attempt an atomic insert in canonical storage; on a positive, consult that definitive store before discarding the URL.",
            "Detect traps through path and parameter patterns, depth, host budgets, and unique-content yield.",
          ],
        },
        {
          title: "Incremental inverted index",
          summary: "Immutable small segments provide freshness; atomic manifests and background merges provide coherent efficient reads.",
          points: [
            "Build compressed postings with only the term, field, frequency, and position data ranking requires.",
            "Publish durable shard manifests, then pin a global manifest or per-shard manifest vector for the life of a query.",
            "Filter superseded documents and tombstones until merges remove obsolete postings safely.",
          ],
        },
      ],
      scaling: [
        "Partition crawl scheduling by host, fetch capacity by network domain, and indexing independently by document shard.",
        "Prioritize recrawl from change rate and product importance while guaranteeing bounded new-host coverage.",
        "Compress postings, tier raw content, reserve merge capacity, and cap query shard fan-out and top-K.",
        "Replicate serving shards and return explicit partial status or fail according to search completeness requirements.",
      ],
      observability: [
        "Robots or per-host concurrency violations, fetch status, latency, bytes, and throttle rate",
        "Frontier size and oldest age by priority, host, and new-versus-recrawl class",
        "Canonical and content duplicate ratio, crawl-trap quarantine, parser failure, and unique-content yield",
        "Document-to-searchable freshness, segment count, merge debt, tombstone age, and publish failure",
        "Search p50/p99, shard timeout, partial-result rate, cache hit, zero-result rate, and freshness by corpus slice",
      ],
    },
  },
  {
    id: "classic-observability-platform",
    title: "Metrics, Logging, and Observability Platform",
    category: "classic",
    difficulty: "hard",
    durationMinutes: 45,
    prompt:
      "Design a multi-tenant platform that ingests metrics and structured logs, supports range and aggregate queries, dashboards and alerts, controls cardinality and query cost, and applies tiered retention without failing during incidents.",
    requirementsToExplore: [
      "Tenants, ingestion volume, metric series, log bytes, burst factor, and acknowledgment durability",
      "Out-of-order and duplicate data, indexing, query shapes, latency, and partial-result policy",
      "Cardinality limits, tenant fairness, dashboards, alerts, and notification dependencies",
      "Raw retention, aggregation, downsampling, cold storage, deletion, and cost attribution",
      "Collector outage, stream lag, query overload, regional failure, and platform self-monitoring",
    ],
    expectedTopics: [
      "Agents, regional collectors, local spooling, durable stream, and partition choice",
      "Time-series identity and label index, log attribute index, immutable time segments, and object tier",
      "High-cardinality quotas, aggregation, downsampling, retention, and tenant isolation",
      "Query frontend, shard fan-out, cache, cost limits, dashboards, and alert evaluators",
      "Backpressure, priority shedding, deadman monitoring, SLOs, and disaster recovery",
    ],
    commonFailureModes: [
      "Using unbounded request IDs or user IDs as metric labels",
      "Acknowledging telemetry before any durable buffer owns it",
      "Blocking production applications indefinitely when telemetry is overloaded",
      "Monitoring the observability platform only with itself",
    ],
    followUpQuestions: [
      "What happens when one tenant creates ten million new series in a minute?",
      "Which log attributes are indexed and what does an arbitrary-field query cost?",
      "How does an alert evaluator resume after a crash without double-notifying?",
      "How are retention and deletion enforced across raw, rollup, index, cache, and cold tiers?",
    ],
    reference: {
      diagram: {
        caption: "Cardinality is bounded at ingestion rather than at query time, and the three signals are stored separately because each has a different cost and retention profile.",
        nodes: [
          { id: "agents", label: "Agents / SDKs", kind: "client", col: 0, row: 1 },
          { id: "gw", label: "Ingest gateway", kind: "edge", col: 1, row: 1 },
          { id: "card", label: "Cardinality guard", kind: "service", col: 2, row: 1 },
          { id: "metrics", label: "Metric TSDB", kind: "store", col: 3, row: 0 },
          { id: "logs", label: "Log store", kind: "store", col: 3, row: 1 },
          { id: "traces", label: "Trace store (sampled)", kind: "store", col: 3, row: 2 },
          { id: "roll", label: "Rollup & downsample", kind: "compute", col: 4, row: 0 },
          { id: "query", label: "Query & alerting", kind: "service", col: 4, row: 2 },
        ],
        edges: [
          { from: "agents", to: "gw", label: "OTLP" },
          { from: "gw", to: "card", label: "allowlist" },
          { from: "card", to: "metrics", label: "series" },
          { from: "card", to: "logs", label: "events" },
          { from: "card", to: "traces", label: "spans, sampled" },
          { from: "metrics", to: "roll", label: "tiered", async: true },
          { from: "query", to: "metrics", label: "detect" },
          { from: "query", to: "traces", label: "localize" },
          { from: "query", to: "logs", label: "explain" },
        ],
      },
      scope: [
        "Ingest batched metrics and structured logs from authenticated tenants with explicit accepted, rejected, and dropped semantics.",
        "Serve metric range and aggregate queries, log search, dashboards, and alert rules under cardinality and cost limits.",
        "Apply downsampling, tiered retention, deletion, and cost attribution; traces are optional and may be out of scope.",
      ],
      apis: [
        "Batched OTLP-like `POST /v1/metrics` and `/v1/logs` return accepted, rejected, and retryable counts after the durable boundary.",
        "`GET /v1/query_range?expr&start&end&step` and `POST /v1/logs/search { filters, timeRange, cursor, limit }` expose partial status.",
        "Alert-rule CRUD stores expression, evaluation interval, hold duration, labels, and notification route.",
        "Tenant APIs define retention, label allowlists, active-series budget, ingest quota, and query-cost budget.",
      ],
      dataModel: [
        "Series identity `{ tenantId, metricName, sortedLabels }` maps to series ID; samples are compressed timestamp/value blocks by time partition.",
        "LogEvent `{ tenantId, timestamp, attributes, bodyRef }` is stored in immutable segments with selected attribute indexes.",
        "Durable stream record carries tenant, schema version, producer sequence, and batch checksum for validation and dedupe policy.",
        "RollupBucket `{ seriesId, window, count, min, max, sum, completeness }` and RetentionPolicy govern derived tiers.",
        "AlertRule, EvaluationCheckpoint, AlertInstance state, and NotificationOutbox preserve evaluator retry and delivery state.",
      ],
      architecture: [
        "Agents batch and compress locally; regional collectors authenticate, validate, quota, and spool before durable-stream acknowledgment.",
        "Stream partitions feed metric and log consumers that normalize, aggregate, index, and write immutable time-partitioned storage.",
        "Recent indexed data stays on fast storage; compacted raw segments and rollups move to object-backed tiers by retention policy.",
        "Query frontend authenticates, plans time and label pruning, enforces cost, fans out to shards, merges, caches, and labels partial results.",
        "Checkpointed alert evaluators atomically commit the evaluation watermark, alert-instance transition, and deterministic notification-outbox row; an independent relay publishes the outbox at least once.",
      ],
      invariants: [
        "Tenant data, quotas, encryption, query results, and cost accounting remain isolated.",
        "An acknowledged batch survives collector loss according to the documented durability contract.",
        "Duplicate and out-of-order acceptance windows are explicit and produce deterministic query behavior.",
        "Raw and rollup queries state resolution and completeness; missing shards are never silently treated as zero.",
        "Retention and deletion apply to raw, index, rollup, cache, replica, and cold tiers within their SLA.",
      ],
      deepDives: [
        {
          title: "Cardinality, partitioning, and retention",
          summary: "Series identity drives memory and index cost, so quotas must act before expensive storage work.",
          points: [
            "Partition by tenant plus stable series hash and isolate heavy tenants from shared consumers.",
            "Reject or transform unbounded label values, expose top contributors, and aggregate safe dimensions at collection.",
            "Keep recent raw resolution and long-lived rollups according to known queries and deletion policy.",
          ],
        },
        {
          title: "Query and alert execution",
          summary: "Time and label pruning bound work; checkpoints make repeated evaluation deterministic.",
          points: [
            "Estimate a query's cost before fanning out, and reject the plan when its time range, series count, or projected returned bytes exceed the tenant's budget, rather than cancelling it midway after the shards have already paid for the scan.",
            "Return explicit partial metadata when a shard misses deadline instead of silently undercounting.",
            "Atomically commit the evaluation watermark, alert state, and deterministic notification-outbox row; relay at least once and deduplicate at the notification boundary.",
          ],
        },
      ],
      scaling: [
        "Batch and compress ingestion, shard by tenant and series hash, and scale consumers from oldest-stream age.",
        "Isolate high-volume tenants, enforce active-series budgets, and aggregate before storage where semantics permit.",
        "Downsample and tier cold data, reserve compaction bandwidth, and apply query concurrency and cost limits.",
        "Deploy collectors regionally but keep cross-region query and disaster paths aware of lag and partial data.",
      ],
      observability: [
        "Externally hosted deadman signal and independent health checks for the platform itself",
        "Accepted, rejected, delayed, and dropped samples or log bytes by tenant and reason",
        "Stream and spool oldest age, consumer lag, write latency, and compaction backlog",
        "Active-series cardinality, label-value growth, index memory, storage growth, and cost by tenant",
        "Query p99, scanned-to-returned ratio, partial-result rate, alert-evaluation lateness, and notification delivery",
      ],
    },
  },
];
