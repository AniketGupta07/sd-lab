import type { TopicPrimerEntry } from "../types";

/**
 * From-zero explanations for the LLM infrastructure modules and the week-8
 * mock-interview modules. The LLM primers assume no familiarity with
 * transformers: attention, tokens, and the KV cache are built up from nothing,
 * because the serving trade-offs are incomprehensible without them.
 */
export const llmPrimers: Record<string, TopicPrimerEntry> = {
  "llm-inference-execution": {
    primer: {
      plainSummary:
        "Serving a large language model is unlike serving any other kind of model. One request is not one computation; it is hundreds of sequential computations, one per word produced, each depending on the one before it. Generation therefore splits into two phases with completely different performance characteristics, and a piece of per-request memory called the KV cache decides how many users a single machine can serve.",
      analogy:
        "A typist who keeps an enormous reference manual beside the desk and must walk its entire length, cover to cover, before striking each single key. The walk consumes almost all the time; the keystroke itself takes none. That walk is the machine streaming every model weight out of memory to produce one output token, and it is why generation feels slow. It also predicts something strange. A typist working on ten documents at once can settle the next character of all ten during a single walk, so ten documents cost barely more than one. Batching is that cheap for exactly that reason. The typist's running notes on everything already written are a separate thing: those are the KV cache, kept so the prompt never has to be read again, and they occupy desk space that limits how many documents fit on the desk at the same time.",
      sections: [
        {
          heading: "Tokens, attention, and what a model actually does",
          body: [
            "A language model does not process characters or words but tokens - chunks of text produced by a tokenizer, where a common word is usually one token and a rare word splits into several. English averages roughly four characters per token, and that is the conversion factor to use when estimating. Cost, latency, memory, and context limits are all measured in tokens, so it is worth thinking in them from the start.",
            "The model's job is narrow: given a sequence of tokens, predict a probability distribution over which token comes next. Generating text means sampling one token from that distribution, appending it to the sequence, and running the model again on the now-longer sequence. This loop is why generation is inherently sequential and cannot be parallelized across the tokens of one response: the tenth token literally cannot be computed before the ninth exists.",
            "Inside, the dominant mechanism is attention. For each token being processed, the model computes three vectors: a query representing what this token is looking for, a key representing what it offers to others, and a value representing the information it carries. A token attends to earlier tokens by comparing its query against their keys, and the resulting weights determine how much of each token's value it incorporates. This is how the model relates a pronoun to the noun it refers to, or a closing bracket to its opening one.",
            "The consequence that drives everything else in this module: computing attention for a new token requires the keys and values of every preceding token. Recomputing them for the entire sequence at each step would make generation quadratically expensive and hopelessly slow. So they are computed once and kept. That store is the KV cache, and it is the single most important object in LLM serving.",
          ],
        },
        {
          heading: "Two phases with opposite characteristics",
          body: [
            "Generation splits into prefill and decode, and almost every serving decision follows from their differences.",
            "Prefill processes the entire input prompt at once. Because all input tokens are already known, their keys and values can be computed in parallel as large matrix multiplications, which is exactly what a GPU is built for. Prefill is compute-bound, limited by arithmetic throughput, and it is efficient, achieving high hardware utilization. Its cost grows with prompt length, so prefill for a 10,000-token document is substantial while prefill for a short question is negligible.",
            "Decode generates output tokens one at a time. Each step processes exactly one new token, which means a tiny amount of arithmetic, but to do it the GPU must read the entire model's weights from memory - tens of gigabytes for a large model - plus the KV cache. Decode is memory-bandwidth-bound: the bottleneck is moving weights, not multiplying them, and GPU utilization during decode is often very low. That fact is counter-intuitive and it is the crucial one. Adding more requests to a decode batch costs almost nothing in extra time, since the weights were being read anyway. Batching in LLM serving is extraordinarily valuable for exactly that reason.",
            "The two phases produce two distinct user-facing metrics. Time to first token, or TTFT, is how long before the first word appears, and it is dominated by queue time plus prefill, so it grows with prompt length. Inter-token latency is the gap between successive tokens once streaming starts, and it is set by decode speed and by how many requests share the batch. Users experience these very differently: a slow TTFT feels like the system is broken, while inter-token latency merely needs to comfortably exceed reading speed. Roughly 10 tokens per second is adequate, and much faster is barely noticed. Quoting a single average latency for an LLM service conceals both, so state the two separately.",
            "This also explains streaming. Because tokens are produced one at a time anyway, they can be sent as they are generated, so the user starts reading after the first token instead of waiting for the full response. Streaming does not reduce total generation time at all; it changes perceived latency dramatically, and it is essentially mandatory for interactive use.",
          ],
        },
        {
          heading: "Reading the hardware: bandwidth, arithmetic, and where batching stops paying",
          body: [
            "Two numbers on an accelerator's spec sheet decide most of LLM serving. The first is memory bandwidth: how many bytes per second can be pulled out of the high-bandwidth memory, or HBM, sitting beside the compute die. A current data-center GPU delivers somewhere around 2 to 3 terabytes per second. The second is arithmetic throughput, the floating-point operations per second the chip can perform, which for 16-bit work is in the neighborhood of a thousand trillion.",
            "Divide the second by the first and you get the chip's break-even ratio: it can perform a few hundred arithmetic operations for every byte it reads. Any workload doing less arithmetic per byte than that is starved by memory and leaves the arithmetic units idle. The ratio of operations to bytes is called arithmetic intensity, and it is the single number that tells you which of the two limits you are under.",
            "Now put decode on that scale. Producing one output token for one request touches every weight once and does about two operations per weight. Two operations for every two bytes of 16-bit weight read is roughly one operation per byte, against a chip that wants a few hundred. Decode at batch size one therefore runs at well under one percent of the arithmetic the hardware can do. Batching is the repair and the mechanism is exact: weights are read once and reused for every request in the batch, so a batch of B requests does B operations per byte. Throughput climbs almost linearly with batch size until B approaches the break-even ratio, and only past that crossover does adding a request start to cost real time.",
            "This also gives you a latency floor you can compute before writing any code. A model with 40 gigabytes of weights on a GPU delivering 2 terabytes per second cannot produce a token faster than 40 divided by 2,000 of a second: 20 milliseconds, or 50 tokens per second, however empty the machine is. Storing the weights in 8 bits halves the bytes and halves the floor. No software in the stack beats that number, so a proposed optimization that would emit tokens faster than the weights can be read is simply wrong.",
            "Two consequences are worth carrying into any design conversation. Single-stream latency is set by bandwidth and cannot be bought with more GPUs, so making one user's stream faster requires a smaller model, fewer bits, or speculative decoding. Aggregate throughput is nearly free up to the crossover, so a serving tier running at batch four is wasting most of the hardware it pays for.",
          ],
        },
        {
          heading: "The KV cache is the concurrency budget",
          body: [
            "Each concurrent request holds its own KV cache, growing by one entry per layer for every token in its sequence. The size follows directly: number of layers, times number of key-value heads, times head dimension, times sequence length, times two for keys and values, times bytes per number.",
            "Work through a concrete case, because the number surprises people. A model with 32 layers, 32 key-value heads of dimension 128, in 16-bit precision, holds 32 x 32 x 128 x 2 x 2 bytes per token - about 512 kilobytes, or roughly half a gigabyte for every 1,000 tokens of context. A request carrying 8,000 tokens therefore holds about 4 gigabytes. On an 80 gigabyte GPU with perhaps 30 gigabytes free after the model weights, that allows only about seven such requests - and fewer still at longer contexts, because the cost is linear in sequence length. So the KV cache, not the model weights and not compute, is what caps concurrency.",
            "This is why architectural changes to reduce it matter so much. Multi-query attention has all query heads share a single key-value head, cutting KV cache size by the number of heads - often a factor of 32 - at some quality cost. Grouped-query attention is the middle ground now standard in large models: heads are grouped, each group sharing one key-value head, which gives most of the memory saving with little quality loss. When you see GQA in a model description, this memory arithmetic is what it is for.",
            "The operational consequence is that long contexts are expensive in a way that is easy to underestimate. Doubling context doubles KV memory per request and therefore halves concurrency, so a service allowing 128,000-token contexts must either serve dramatically fewer concurrent users or reserve enormous memory. Charging by token and setting context limits are capacity decisions before they are pricing decisions.",
            "Finally, a generation is a long-lived cancellable state machine and never a single call. It may run for many seconds while holding GPU memory throughout, so it must be cancellable. When a user closes the tab, the generation should stop and free its KV cache immediately; otherwise the server accumulates abandoned generations that consume the memory bounding concurrency. It also needs a maximum output length, since a model can fail to stop and generate until the context limit, occupying a slot for minutes. Both are correctness requirements. Treating them as optimizations is how a serving tier quietly loses a third of its capacity.",
          ],
        },
        {
          heading: "Choosing the next token, and why output length is a distribution",
          body: [
            "What the model actually emits at each step is not a token but a vector of scores, one per vocabulary entry, called logits. Turning that vector into a chosen token is sampling, and the sampling settings live outside the model while forming part of the serving contract.",
            "Greedy decoding always takes the highest-scoring token. It is deterministic and it tends to produce flat, repetitive text, because the most likely continuation of a likely continuation is frequently a loop. Temperature rescales the logits before sampling: below one it sharpens the distribution toward the leading candidates, above one it flattens it and admits unlikely tokens. Top-p sampling, also called nucleus sampling, keeps the smallest set of tokens whose probabilities sum to p and samples inside it, so the candidate pool narrows automatically when the model is confident and widens when it is not.",
            "These settings have direct operational consequences. Sampling makes a response non-deterministic, so an identical request produces different text on each call, and a response cache therefore changes the product's behavior and not only its latency. Sampling also makes output length a random variable. Two identical prompts can generate 40 tokens and 900 tokens, which is precisely why admission control has to reason about a distribution of projected memory instead of a single figure.",
            "Termination is its own mechanism and it fails in its own way. A generation ends when the model emits an end-of-sequence token, when a configured stop sequence appears in the text, or when the output token limit is reached. The first two are model behavior and can simply fail to occur: a model can talk past its natural ending, especially at high temperature or on an unusual prompt. Only the limit is enforced by the server, so it is the one of the three you can rely on to free a slot.",
            "Report the sampling configuration alongside the model version whenever you state a quality result. The same weights at temperature 0 and temperature 1 are, for evaluation purposes, two different systems.",
          ],
        },
      ],
      workedExample: {
        title: "Sizing a chat service on one GPU node",
        setup:
          "A support chat product runs a model with 32 layers, 8 key-value head groups of dimension 128, in 16-bit precision, on an 80 gigabyte GPU. Model weights occupy 40 gigabytes. Typical conversations reach 4,000 tokens of context and generate 300 token responses. The requirement is a TTFT under 2 seconds.",
        steps: [
          "Compute KV cache per token. 32 layers x 8 key-value heads x 128 dimensions x 2 for keys and values x 2 bytes = about 131 kilobytes per token. Note how much grouped-query attention bought: with 32 key-value heads instead of 8 this would be about 524 kilobytes per token, and everything below would be four times worse.",
          "Compute per-request and total capacity. At 4,000 tokens of context that is roughly 524 megabytes per request. With 40 gigabytes of weights and perhaps 5 gigabytes for activations and workspace, about 35 gigabytes remain for KV cache - so about 65 concurrent requests. That single number is the concurrency limit of this node, and memory produced it while compute had nothing to do with it.",
          "Check what decode throughput this implies. Because decode is memory-bandwidth-bound, running 65 requests in one batch costs barely more per step than running one, because the weights are read once for the whole batch. Batching is not an optimization here; it is the entire economics of the deployment, and a design that serves requests one at a time wastes almost all the hardware.",
          "Derive the inter-token latency floor from bandwidth before promising anything. Streaming 40 gigabytes of weights on a GPU delivering roughly 2 terabytes per second takes about 20 milliseconds, so 50 tokens per second is the hard ceiling for a single stream on a completely idle machine. Every latency promise below has to sit above that floor, and the only ways to lower it are a smaller model or fewer bits per weight.",
          "Split the latency budget by phase. Prefill of 4,000 tokens is a large parallel computation taking perhaps 200 milliseconds. That leaves 1.8 seconds of the TTFT budget for queueing, so the admission policy must keep queue time under 1.8 seconds. Given 65 concurrent slots, that bound sets the arrival rate the node can accept before TTFT degrades.",
          "Separate the second latency metric. Inter-token latency at a full batch might be 40 milliseconds, giving 25 tokens per second, comfortably above reading speed, so a 300 token response streams over 12 seconds and feels responsive. Averaging TTFT and inter-token latency into one number would have hidden both constraints and made the design impossible to reason about.",
          "Treat the 300-token response as a distribution instead of a constant. Sampling makes output length random, so some conversations stop at 40 tokens while others run to the cap, and the slot arithmetic above assumed a typical case. Set an explicit output token limit and stop sequences, then size admission against an upper percentile of the observed length distribution so a run of unusually long responses cannot exhaust KV memory mid-generation.",
          "Enforce the lifecycle rules. Cap output length so a non-terminating generation cannot hold a slot indefinitely, and cancel promptly on client disconnect to release KV memory. With only 65 slots, a handful of abandoned generations costs a measurable fraction of total capacity. Lifecycle discipline here is correctness work, and it deserves the same scrutiny as the arithmetic above.",
        ],
        takeaway:
          "Every number came from KV cache arithmetic, and the binding constraint turned out to be memory instead of compute, which is the opposite of most people's intuition about GPUs. Being able to do this calculation, and to explain why decode being memory-bound makes batching so valuable, is the core competence to build here.",
      },
    },
    glossary: [
      { term: "Token", definition: "The chunk of text a model processes, roughly four characters of English on average. Cost, latency, memory, and context limits are all measured in tokens." },
      { term: "Attention", definition: "The mechanism by which each token incorporates information from earlier tokens, by comparing its query against their keys and combining their values." },
      { term: "Query, key, value", definition: "Three vectors per token: what it is looking for, what it offers, and the information it carries. Keys and values of earlier tokens must be available to process a new one." },
      { term: "KV cache", definition: "Stored keys and values for all preceding tokens, so generation does not recompute them each step. Per-request, grows with sequence length, and is what caps concurrency." },
      { term: "Prefill", definition: "Processing the whole input prompt in parallel. Compute-bound, efficient, and its cost grows with prompt length." },
      { term: "Decode", definition: "Generating output one token at a time. Memory-bandwidth-bound because the full model weights are read per step, so extra batched requests are nearly free." },
      { term: "TTFT", expansion: "time to first token", definition: "Latency until the first output token appears, dominated by queue time plus prefill. What makes a service feel broken when it is slow." },
      { term: "Inter-token latency", definition: "The gap between successive output tokens, set by decode speed and batch size. Needs only to exceed reading speed, so it is far less sensitive than TTFT." },
      { term: "Streaming", definition: "Sending tokens as they are produced. Does not reduce total generation time and transforms perceived latency." },
      { term: "MQA / GQA", expansion: "multi-query attention / grouped-query attention", definition: "Sharing key-value heads across query heads to shrink the KV cache, often by a large factor, at small quality cost. Now standard in large models." },
      { term: "Context length", definition: "The maximum tokens a model can attend to. Doubling it doubles KV memory per request and roughly halves concurrency, making it a capacity decision." },
      { term: "Cancellation", definition: "Stopping a generation and freeing its KV cache when the client disconnects. A correctness requirement, since abandoned generations consume the memory that bounds concurrency." },
      { term: "Output token limit", definition: "A cap on generated length, preventing a non-terminating generation from occupying a slot for minutes." },
      { term: "Memory bandwidth", definition: "Bytes per second the accelerator can pull out of its memory. The binding limit during decode, and the source of the per-token latency floor." },
      { term: "HBM", expansion: "high-bandwidth memory", definition: "The fast memory stacked beside the compute die that holds weights and KV cache. Its capacity caps concurrency and its bandwidth caps single-stream speed." },
      { term: "Arithmetic intensity", definition: "Operations performed per byte read from memory. Decode at batch size one sits near one, against hardware that wants a few hundred." },
      { term: "Batch crossover point", definition: "The batch size at which arithmetic intensity reaches the hardware's break-even ratio. Below it, extra requests are nearly free; above it, they cost real time." },
      { term: "Logits", definition: "The raw per-vocabulary scores the model emits each step, before a sampling rule converts them into one chosen token." },
      { term: "Temperature", definition: "A rescaling of the logits before sampling. Below one it sharpens toward leading candidates; above one it flattens and admits unlikely tokens." },
      { term: "Top-p sampling", definition: "Also called nucleus sampling: keeping the smallest set of tokens whose probabilities sum to p, so the candidate pool narrows when the model is confident." },
      { term: "Stop sequence", definition: "Text that ends a generation when it appears. Model-dependent and therefore unreliable, unlike the server-enforced output token limit." },
      { term: "MLA", expansion: "multi-head latent attention", definition: "Caching one low-rank latent vector per token and reconstructing keys and values on the fly, so cached width is the latent dimension rather than key-value heads times head dimension. The standard formula overestimates it, sometimes by an order of magnitude, and the memory saved is paid back as extra projection compute at every decode step." },
      { term: "Sliding-window attention", definition: "Letting each token attend only to the last W tokens, so the cache stops at the window size and stops growing with context. A 100,000-token conversation with a 4,096-token window still holds 4,096 tokens per windowed layer, which makes the linear-in-context model simply wrong for that layer." },
      { term: "Hybrid attention stack", definition: "A model interleaving a few full-attention layers among many windowed ones, so global information survives while most layers stay bounded. An honest capacity estimate sums the two layer classes separately instead of applying one formula to every layer." },
      { term: "KV-cache quantization", definition: "Storing cached keys and values in fewer bits than the compute precision, typically FP8 instead of BF16. It roughly doubles the sequences a fixed pool holds while touching no weight format and no kernel, and its error surfaces on long-context retrieval long before it shows on short chats." },
      { term: "BF16", expansion: "brain floating point, 16 bit", definition: "The default 16-bit format for weights, activations, and cached keys and values. It keeps the exponent range of a 32-bit float and gives up mantissa bits, so gradients and attention scores do not underflow and no loss scaling is required." },
      { term: "FP8", expansion: "8-bit floating point", definition: "An 8-bit numeric format. Used for cached keys and values it halves KV bytes and roughly doubles concurrency; used for weights and activations on hardware with native FP8 matrix units it also speeds the compute-bound phase. Both uses need scales fitted on representative traffic." },
      { term: "SLO", expansion: "service level objective", definition: "An internal target for a metric, such as 99 percent of requests reaching their first token within two seconds. The number engineering designs and alerts against, and in serving it must be stated separately for time to first token and for inter-token latency." },
      { term: "Token budget", definition: "The number of tokens a scheduler allows itself to process in one iteration, split between prompt prefill work and decode steps. It is the dial that keeps a large incoming prompt from consuming an iteration that active decoders needed." },
      { term: "Allocated versus useful KV bytes", definition: "The distinction between memory the pool has handed out and memory actually holding live cached tokens. The gap is block-tail waste plus fragmentation, and a pool reporting 90 percent allocated against 60 percent useful has a scheduling problem rather than a hardware shortage." },
    ],
  },

  "llm-serving-capacity-admission": {
    primer: {
      plainSummary:
        "Because decode is memory-bandwidth-bound, running many requests together costs barely more than running one, so batching is where nearly all LLM serving throughput comes from. Requests arrive at different times and finish at different times, though, and naive batching throws most of that benefit away. The repair has three parts: a scheduler that reforms the batch every token step, a memory allocator that stops KV cache waste, and an admission policy for deciding whom to let in once memory becomes the scarce resource.",
      analogy:
        "A minibus service. Waiting until the bus is full before departing means early passengers wait a long time; that is static batching. Departing on a fixed schedule regardless of occupancy wastes seats. The efficient design lets passengers board and alight at any stop while the bus keeps moving, so a vacated seat is immediately reusable. That is continuous batching. Paged memory is the equivalent of not reserving three seats for every passenger in case they brought luggage.",
      sections: [
        {
          heading: "From static to continuous batching",
          body: [
            "Static batching collects requests until a batch is full or a timeout fires, runs them together, and returns all results when the last one finishes. It is simple and it wastes enormously in this setting, for a reason specific to language models: requests in a batch generate different numbers of tokens. If one request generates 20 tokens and another generates 800, the short one finishes almost immediately and its slot sits idle for the remaining 780 steps. With realistic output length variation, most of the batch is idle most of the time.",
            "Continuous batching, also called iteration-level scheduling, fixes this by making scheduling decisions at every token step instead of once per batch. After each step, completed requests leave and waiting requests join immediately. The batch is continuously reformed, so a finished request's slot is reused on the very next iteration instead of at the end of the batch. Throughput improvements of several times over static batching are typical, and every serious inference server does this.",
            "Prefill and decode compete for the same hardware, which creates a scheduling problem. A long prefill is a large compute job that, if run as a unit, stalls decode for every request in flight. One user submitting a 30,000-token document then causes a visible pause in everyone else's streaming. Chunked prefill splits a long prompt into pieces processed across several iterations, interleaved with decode steps. That raises the long request's TTFT slightly and protects everyone else's inter-token latency. It is the right trade: inter-token latency is visible continuously, while a small TTFT increase on one long request is not.",
            "The metric worth adopting here is goodput rather than throughput: tokens per second that actually met their latency targets. A server can maximize raw throughput by batching enormously and letting latency degrade past usefulness, producing tokens nobody wanted. Goodput makes the trade-off explicit and is the number to optimize.",
          ],
        },
        {
          heading: "Paged attention: allocating KV memory properly",
          body: [
            "The naive KV cache allocator reserves, for each request, enough contiguous memory for its maximum possible length. Since you cannot know how long a response will be, that means reserving for the worst case, so a request that generates 50 tokens might hold memory sized for 4,000. Most of the reserved memory is never used, and because it is reserved it cannot serve anyone else.",
            "Paged attention applies the idea of virtual memory to this problem. KV cache is divided into fixed-size blocks of, say, 16 tokens each, and a request is allocated blocks on demand as it generates, with a block table mapping its logical positions to physical blocks. Blocks need not be contiguous. A request that generates 50 tokens uses four blocks and no more, so waste is bounded to at most one partly-filled block per request instead of the whole unused reservation.",
            "The gain is large and directly translates into concurrency: memory utilization rises from often below 40 percent to above 90 percent, which means roughly double the concurrent requests on the same hardware. Since KV memory is the concurrency limit, this is one of the highest-leverage optimizations in LLM serving.",
            "Paging enables a second capability that is easy to miss. Because blocks are indirected through a table, several requests can share blocks holding identical content. When many requests begin with the same long system prompt, that prefix is stored once and shared instead of duplicated per request. For applications with large shared prompts, the common enterprise pattern, this saves both the memory and the repeated prefill compute for the shared portion. It also makes branching cheap, since alternative continuations share their common prefix.",
            "The cost is a layer of indirection on every attention operation and an allocator to maintain, which is real engineering complexity but has a small runtime cost relative to what it recovers.",
          ],
        },
        {
          heading: "Separating prefill from decode entirely",
          body: [
            "Chunked prefill is one answer to prefill and decode fighting over the same hardware. The other answer is to stop running them on the same machine. The two phases want opposite things: prefill is a short compute-bound burst that saturates the arithmetic units, while decode is a long-lived bandwidth-bound trickle that wants the largest batch it can assemble. Running them together means a long prefill sits at the head of the line and everyone's decode waits behind it, which is head-of-line blocking with a language-model shape.",
            "Disaggregated serving splits them into two pools. A prefill pool computes the prompt's keys and values and transfers that KV cache across the interconnect to a decode pool, which then streams the output tokens. Each pool scales against its own signal: the prefill pool against prompt tokens per second, the decode pool against concurrent sequences and KV memory. Interference disappears by construction, since a 50,000-token prefill cannot stall a decode batch that lives on a different machine.",
            "The cost is the transfer. A long prompt's KV cache is gigabytes, and it has to cross the network before the first token can be produced, so TTFT acquires a network hop that only pays for itself over a fast link. Two pools also mean two capacity risks and a ratio between them that has to stay tuned; get the ratio wrong and one pool idles while the other queues, which is worse than the single pool you started with.",
            "The choice rule is straightforward. With a fairly uniform workload and modest prompts, one pool with chunked prefill is simpler and good enough. Disaggregate when the prompt-to-output ratio is extreme or strongly bimodal, when the two phases would prefer different accelerators, or when TTFT and inter-token latency need to be tuned independently for different customers.",
            "Whichever layout you choose, decompose the latency measurement to match it. Report queue time, prefill time, and per-token decode time separately. A single TTFT number cannot tell you whether the fix is more capacity, a stricter admission policy, or a different prefill strategy, and those are three different projects with three different costs.",
          ],
        },
        {
          heading: "Admission control when memory is the constraint",
          body: [
            "Because a request holds memory for its entire generation, admitting one is a commitment to future memory and not an instantaneous charge. A request admitted now with a 2,000-token prompt may generate 2,000 more tokens over the next minute, doubling its footprint. Admission control must therefore reason about projected memory, not current memory. Accepting requests until memory is full guarantees that some in-flight request will be unable to allocate its next block.",
            "What happens then is worth naming: preemption. The server must either evict a request, discarding its KV cache and restarting it later so that every step of work so far is wasted, or swap its cache to host memory and back, which is slow. Both are expensive, so a good admission policy exists precisely to make preemption rare, and preemption rate is a signal that admission is too permissive.",
            "Admission should be weighted by projected work rather than by request count. A request with a 50-token prompt expecting 50 tokens out is trivially cheap; one with a 30,000-token prompt expecting 4,000 out is hundreds of times more expensive. Counting both as one request produces wildly inaccurate capacity planning. Estimate output length from historical distributions per endpoint or per application, and admit on a token-weighted basis.",
            "Fairness needs deliberate attention, because without it a single tenant submitting many long requests can occupy all memory and starve everyone else. Per-tenant concurrency or token limits, with a shared pool for burst, keep one heavy user from consuming the service. This is the same fairness problem as any shared queue, made sharper because the resource is held for a long time.",
            "Autoscaling needs the right signal, and CPU utilization is useless here. The GPU is the resource, and a decode-bound server can show modest GPU compute utilization while being completely full on memory. Scale on KV cache utilization, queue depth, and TTFT. Then account for the startup delay: loading tens of gigabytes of weights takes minutes, so scaling reacts far too slowly to absorb a spike. Headroom has to be provisioned in advance, and the response to a sudden surge is load shedding.",
          ],
        },
      ],
      workedExample: {
        title: "A serving tier that thrashes under mixed traffic",
        setup:
          "An inference service handles two workloads: short interactive chat requests, and document summarization with prompts of 20,000 to 50,000 tokens. Chat TTFT has degraded from 1 second to 8 seconds, throughput is poor, and logs show frequent request restarts.",
        steps: [
          "Identify the restarts as preemption. Requests are being evicted because memory ran out mid-generation, discarding their KV cache and restarting them later. All the work already done is thrown away, so effective throughput falls while the hardware stays busy: the system is doing more work and producing less. That is the signature of admission control that counts requests instead of projected tokens.",
          "Fix admission to be token-weighted. A 50,000-token summarization request is not equivalent to a chat request. Estimate each request's peak KV footprint from its prompt length plus an expected output length drawn from historical distributions per endpoint, and admit only while projected peak memory stays within a reserve. Keep headroom instead of filling memory exactly, since output length estimates are distributions and some requests will exceed them.",
          "Explain the TTFT degradation and fix it with chunked prefill. A 50,000-token prefill is a long compute job that stalls decode for every in-flight request, so chat responses freeze mid-stream while it runs. Split long prefills into chunks interleaved with decode iterations: the summarization request's TTFT rises modestly, and chat inter-token latency stops stalling. The trade favors the many over the one.",
          "Do not stop at tuning the shared pool; separate the workloads. Interactive chat and batch summarization have genuinely different latency requirements, and mixing them means every tuning decision is a compromise. Route them to separate pools, or enforce per-class memory reservations so summarization cannot consume the memory chat needs. This is the queue-separation lesson from notification systems, applied to GPU memory.",
          "Verify paged attention is in use and check prefix sharing. If the allocator reserves worst-case contiguous memory per request, utilization may be under 40 percent and half the hardware is being wasted. With paging, also enable prefix sharing. If summarization requests share a long instruction prompt, that prefix is stored once and its prefill computed once, a substantial saving on both axes for this workload.",
          "Fix the autoscaling signal. GPU compute utilization looks moderate while memory is full, so scaling on it never triggers. Scale on KV cache utilization, queue depth, and TTFT instead. Because loading weights takes minutes, maintain headroom and shed load on sudden spikes; autoscaling will not answer in time.",
        ],
        takeaway:
          "The visible symptom was latency, and the cause was an admission policy treating a 50,000-token request as equivalent to a 50-token one. LLM serving punishes request-count-based reasoning severely because the cost range between requests spans orders of magnitude and the resource is held for a long time. Think in projected tokens and projected memory, and most of these failures do not occur.",
      },
    },
    glossary: [
      { term: "Static batching", definition: "Collecting requests, running them together, and returning all results at the end. Wastes most slots because generation lengths vary widely." },
      { term: "Continuous batching", definition: "Making scheduling decisions every token step so finished requests leave and new ones join immediately. Typically several times the throughput of static batching." },
      { term: "Chunked prefill", definition: "Splitting a long prompt across several iterations interleaved with decode, so one long request cannot stall everyone else's streaming." },
      { term: "Goodput", definition: "Tokens per second that met their latency targets. The right optimization target, since raw throughput can be maximized by producing tokens too late to be useful." },
      { term: "Paged attention", definition: "Allocating KV cache in fixed-size blocks with a block table, so a request uses only what it needs. Raises memory utilization from under 40 percent to over 90." },
      { term: "KV block", definition: "The fixed-size unit of KV cache allocation, typically 16 tokens, which bounds waste to one partly-filled block per request." },
      { term: "Prefix sharing", definition: "Several requests sharing KV blocks for an identical leading prompt, saving both memory and repeated prefill compute. Enabled by paging's indirection." },
      { term: "Fragmentation", definition: "Unusable memory left by variable-sized allocations. The problem paged allocation eliminates." },
      { term: "Token-weighted admission", definition: "Admitting by projected token work and peak memory, never by request count, since request costs span orders of magnitude." },
      { term: "Preemption (eviction / swapping)", definition: "Discarding or offloading an in-flight request's KV cache when memory runs out. Wastes completed work, and its rate signals over-permissive admission." },
      { term: "Output length estimation", definition: "Predicting how many tokens a request will generate, from historical distributions per endpoint. Required to project memory before admitting." },
      { term: "Per-tenant limits", definition: "Concurrency or token caps per tenant with a shared burst pool, preventing one heavy user from occupying all memory." },
      { term: "Autoscaling signals for inference", definition: "KV cache utilization, queue depth, and TTFT. CPU and GPU compute utilization both stay moderate while memory is full, so neither works as a trigger." },
      { term: "Model load time", definition: "Minutes to load tens of gigabytes of weights, which makes autoscaling too slow for spikes and forces provisioned headroom plus load shedding." },
      { term: "Head-of-line blocking", definition: "One long prefill sitting at the front of the queue while every decode step waits behind it, so a single large prompt degrades everyone's streaming." },
      { term: "Disaggregated serving", definition: "Running prefill and decode on separate pools of machines, so a long prompt cannot stall a decode batch and each pool scales on its own signal." },
      { term: "KV cache transfer", definition: "Shipping a prompt's computed keys and values from a prefill machine to a decode machine. Gigabytes for long prompts, so it adds a network hop to TTFT." },
      { term: "Prefill / decode pool ratio", definition: "The relative sizing of the two pools in a disaggregated deployment. A wrong ratio leaves one pool idle while the other queues." },
      { term: "Latency decomposition", definition: "Reporting queue time, prefill time, and per-token decode time separately, since a single TTFT figure cannot say which of three different fixes is needed." },
      { term: "Load shedding", definition: "Rejecting requests early during a spike. The honest response when model load time makes autoscaling too slow to help." },
      { term: "Cache-aware routing", definition: "Placing a request on the replica that already holds its prompt prefix, scored by the length of the matched prefix rather than by queue depth alone. The gateway keeps a per-replica index of cached prefixes, typically a radix tree over block hashes, because a prefix cache pays nothing when the balancer scatters requests away from it." },
      { term: "Prefix hit rate", definition: "The fraction of prefill tokens served from cached blocks rather than recomputed. Measured in tokens and not requests, since a request whose 4,000-token system prompt hits and whose 40-token question misses is a 99 percent saving that a request-level counter records as a miss." },
      { term: "Affinity load ceiling", definition: "The queue-depth or KV-pressure threshold above which cache affinity is ignored and a request is placed on a cold replica. Without it, one popular system prompt drags every request that shares it onto a single replica and the hit rate looks excellent while tail latency collapses." },
      { term: "Copy-on-write block sharing", definition: "Reference-counting KV blocks so several sequences can point at the same physical blocks for an identical prefix, with a private copy made only when one of them writes. This is the mechanism that turns paging's indirection into prefix sharing." },
      { term: "Weighted fair and deficit scheduling", definition: "Serving tenants and length classes in proportion to declared shares rather than arrival order, so a burst of long contexts cannot monopolize the batch. The alternative extremes both fail: strict first-in-first-out, or FIFO, causes head-of-line blocking, and shortest-job-first starves long contexts." },
      { term: "FLOPS", expansion: "floating-point operations per second", definition: "A measure of raw arithmetic capability. Advertised peak FLOPS is a vendor's theoretical ceiling that a real request mix never reaches, which is why the useful capacity of an inference fleet is set by the scheduler and the token distribution instead." },
      { term: "FP8", expansion: "8-bit floating point", definition: "An 8-bit numeric format. Applied to cached keys and values it halves the bytes both held in the pool and shipped across a disaggregation link, which is why it appears in the transfer arithmetic as well as in the concurrency arithmetic." },
      { term: "Free-block watermark", definition: "A reserved floor of unallocated KV blocks below which admission stops. Shedding at the watermark is what keeps the allocator from running out mid-generation and forcing a preemption that discards completed work." },
      { term: "RPS", expansion: "requests per second", definition: "The arrival-rate metric that works for stateless services and misleads badly here, because it treats a 50-token completion exactly like a 50,000-token one. Capacity for inference is planned in tokens and KV blocks instead." },
      { term: "Warm pool", definition: "Replicas kept loaded with weights and compiled kernels ahead of demand. Required because bringing a new replica up takes minutes, which is far longer than any request deadline, so a spike is answered from warm capacity or not at all." },
    ],
  },

  "llm-inference-optimization-routing": {
    primer: {
      plainSummary:
        "Once batching and memory are handled, four levers remain: split one model across several GPUs when it does not fit, make the model itself cheaper by using fewer bits or by guessing ahead, avoid work entirely by caching, and send each request to the cheapest model that can handle it. Each lever behaves differently on the constraint that matters most: some preserve output quality exactly, and some trade a little of it away.",
      analogy:
        "Reducing the cost of a translation service. You can split a long document among several translators, which only helps if they can coordinate quickly. You can hire a faster translator who is slightly less precise. You can have a junior draft the passage and a senior check it, which is exact if the senior verifies properly and much faster when the junior is usually right. You can keep translations of passages you have seen before. And you can route simple sentences to the junior alone. Each has a different effect on quality, and knowing which are lossless is the whole skill.",
      sections: [
        {
          heading: "Splitting a model across GPUs",
          body: [
            "When a model does not fit on one GPU, or when one GPU is too slow, it must be split. How you split it determines how much the GPUs must talk to each other, and that traffic is what limits the result.",
            "Tensor parallelism splits individual layers across GPUs, so each holds a slice of every weight matrix and every layer requires combining partial results across all of them. This communication happens many times per token, so it demands a very fast interconnect - NVLink within a node. Across nodes over ordinary networking it is disastrous, because the communication cost exceeds the compute saving. The rule to state: tensor parallelism within a node, never across.",
            "Pipeline parallelism assigns different layers to different GPUs, so a token passes through GPU one, then two, and so on. Communication happens only at layer boundaries. That is much less traffic, and therefore tolerable across nodes. The cost is the pipeline bubble: while GPU one works on a token, the others wait, so with a naive schedule utilization is poor. Bubbles are reduced by keeping several requests in flight at different stages, which continuous batching provides naturally.",
            "Data parallelism runs a complete copy of the model on each GPU with different requests on each. It is not a split at all but a replication. It has zero inter-GPU communication and is the simplest and most effective approach whenever the model fits on one device. The general guidance follows: replicate if you can, use tensor parallelism within a node if you must, and add pipeline parallelism across nodes only for models too large for a single node.",
          ],
        },
        {
          heading: "Making the model itself cheaper",
          body: [
            "Quantization stores weights in fewer bits - 8-bit or 4-bit instead of 16. It shrinks memory proportionally, which allows a larger model on the same hardware or more KV cache alongside a smaller one, and it speeds up decode directly because decode is memory-bandwidth-bound and there are simply fewer bytes to read. This is a case where the memory saving and the speed-up come from the same cause.",
            "The cost is some quality loss, and how much depends on method. Naive rounding degrades noticeably. Methods such as GPTQ and AWQ use calibration data to decide where precision matters and protect the weights that carry the most signal, so 4-bit quantization often loses very little on most tasks. The honest caveat is that aggregate benchmarks can hide degradation concentrated in specific capabilities. Reasoning and long-context tasks often suffer first, so evaluate a quantized model on the tasks you actually care about. A general benchmark will not show you the loss.",
            "Speculative decoding is different in kind, and this is the point worth understanding. A small fast draft model proposes several tokens ahead; the large model then verifies all of them in a single forward pass, which is cheap because verification is parallel like prefill rather than sequential like decode. Accepted tokens are kept and the first rejection discards the rest. The remarkable property is that with correct verification the output distribution is mathematically identical to running the large model alone. It is exactly lossless, unlike quantization. The speed-up depends on acceptance rate: a draft model agreeing 70 percent of the time gives a large gain, and one agreeing 30 percent may be slower than not using it at all, because the draft cost is wasted on rejections.",
            "So position them accurately in an interview: speculative decoding trades extra compute for latency with no quality change, while quantization trades quality for memory and speed. Conflating them, or claiming quantization is free, is the mistake to avoid.",
          ],
        },
        {
          heading: "Changing what you deploy: sparse experts and shared-base adapters",
          body: [
            "The levers so far change how a fixed model is run. Two more change what gets deployed at all, and both show up constantly in current systems.",
            "A mixture-of-experts model, or MoE, replaces each dense feed-forward block with many parallel expert blocks plus a small router that sends each token to two or so of them. Total parameters rise while the parameters touched per token stay roughly flat, so the model gains capacity without gaining arithmetic per token.",
            "For serving this is a mixed bargain and it pays to be precise about which way each part cuts. Compute per token falls against a dense model of comparable quality. Memory does not: every expert must be resident somewhere, since any token might route to any of them, so the weight footprint is set by total parameters and never by active ones. Because decode is bandwidth-bound and the active experts change token by token, the bytes actually read per step are unpredictable, and batching helps less than it does for a dense model because two requests in one batch may want different experts. Expert parallelism, placing experts on different GPUs, adds an all-to-all exchange at every layer. Expert load imbalance is the operational hazard: a router favoring a few experts leaves the GPUs holding the unpopular ones idle, so monitor per-expert token counts and treat skew the way you would treat a hot partition anywhere else.",
            "The second lever is serving many fine-tuned variants from one base model. Low-rank adaptation, or LoRA, trains a small pair of low-rank matrices per layer in place of updating the weights, so a customized model is tens of megabytes sitting on top of a shared multi-gigabyte base. A serving stack that supports it keeps one copy of the base weights and applies adapters per request, and good implementations batch requests using different adapters together: the expensive base pass is shared while each adapter's small matrices are applied individually.",
            "The economics change completely once that works. A hundred tenant-specific fine-tunes stop being a hundred deployments with a hundred underused GPUs and become one deployment with a hundred small files, so the routing decision becomes which adapter to apply instead of which cluster to reach. Three limits are worth naming: adapters bind to an exact base model version, quality trails a full fine-tune for large behavior changes, and resident adapter memory is still finite, so a cold adapter costs a load from storage on its first request and that latency lands on a real user.",
          ],
        },
        {
          heading: "Caching and routing",
          body: [
            "The cheapest token is one never computed. Exact prefix caching reuses KV cache for identical leading tokens, which is highly effective for applications with a long shared system prompt, since the prefill for that prefix is computed once for all of them. Its limit is exactness: a single differing character at the start invalidates everything after it. This is why prompt construction order matters operationally. Put the stable shared content first and the variable content last, and a large fraction of prefill disappears.",
            "Full response caching applies where identical requests recur, such as a fixed set of support questions. Because generation is usually sampled, caching a response means returning the same one every time, and that has to be an accepted product behavior. Any cache keyed on user-specific content needs care that one tenant's content is never returned to another; cache keys are a security boundary before they are a performance detail.",
            "Routing sends each request to the cheapest model that can handle it. Most production traffic is easy - simple classification, extraction, formatting - and does not need the largest model. Routing by task type or explicit request parameters is reliable. Routing by a learned difficulty classifier is more powerful and introduces its own failure mode, since the classifier can be wrong and the user experiences that as an inexplicably poor answer. Start with explicit routing and add learned routing with measurement.",
            "Fallback is the operational necessity underneath all of this. Model servers fail, GPUs fail, and a provider may rate-limit you. Define in advance what happens: fall back to a smaller model, to a different provider, or to a non-generative response. Because different models produce different output styles and lengths, a fallback path must be tested and never assumed. A fallback to a model with a different context limit or a different output format will fail exactly when it is needed.",
            "One warning to carry: routing and caching change which model produced a response, so log the model version, the quantization, and the cache status with every response. Without that, a quality complaint is uninvestigable, because you cannot tell which of several possible paths produced the output.",
          ],
        },
      ],
      workedExample: {
        title: "Cutting inference cost by 60 percent without a quality complaint",
        setup:
          "A product spends heavily on a 70-billion-parameter model serving mixed traffic: 60 percent simple classification and extraction, 30 percent moderate summarization, 10 percent complex reasoning. All requests share a 2,000-token system prompt. Latency requirements are modest.",
        steps: [
          "Take the free saving first. Every request pays prefill for the same 2,000-token system prompt. Enable prefix caching and ensure the prompt is constructed with the shared portion strictly first, so the variable content cannot invalidate it. This removes a large share of total prefill compute and changes output quality not at all, so it is the correct first move.",
          "Route by task, starting with the reliable version. The 60 percent of traffic doing classification and extraction does not need a 70-billion-parameter model; an 8-billion model handles it well. Route explicitly by endpoint or request parameter, leaving the learned classifier for later, and measure per-task quality before and after so the claim is evidenced. This is the largest single saving available.",
          "Quantize the large model and evaluate properly. Move the 70B model to 4-bit AWQ, cutting weight memory roughly fourfold - from about 140 gigabytes at 16-bit to roughly 35 to 40 gigabytes once group-wise scales and zero points are counted - and speeding decode because fewer bytes are read per step. Evaluate specifically on the complex reasoning traffic rather than on a general benchmark, since that is the capability most likely to degrade and the one this model is now reserved for.",
          "Add speculative decoding for the remaining large-model traffic. Use the 8B model already deployed for routing as the draft model. Measure the acceptance rate: if it is above roughly 60 percent this is a solid latency win at exactly zero quality cost, and if it is low, disable it, since a poor draft model costs more than it saves.",
          "Choose the parallelism layout deliberately. The quantized 70B model now fits on a single GPU, so use data parallelism: plain replicas, with no inter-GPU communication at all. Note that quantization did not only save memory; it removed the need for tensor parallelism entirely, a second-order benefit worth stating.",
          "Make the change safe to operate. Log model version, quantization, route decision, and cache hit status on every response, so any quality regression can be attributed to a specific path. Define fallbacks, so that an unavailable 8B model routes to the 70B instead of failing, and test them. Then run an evaluation comparing old and new on real traffic before completing the rollout.",
        ],
        takeaway:
          "The largest savings came from not doing work at all. Prefix caching and routing beat any amount of making the model faster, and that ordering is general: eliminate work, then reduce work, then parallelize what remains. Note also which changes were lossless and which were not, because that determines what needs evaluation and what merely needs verification.",
      },
    },
    glossary: [
      { term: "Tensor parallelism", definition: "Splitting individual layers across GPUs, requiring communication many times per token. Viable only within a node over a fast interconnect, and abbreviated TP when a layout is written out as a product of parallel degrees." },
      { term: "TTFT", expansion: "time to first token", definition: "Latency until the first output token appears, dominated by queue time plus prefill. It is where an uncached grammar compilation or a cold adapter load lands, and it is what makes a service feel broken when it is slow." },
      { term: "Pipeline parallelism", definition: "Assigning different layers to different GPUs. Far less communication, so it works across nodes, at the cost of pipeline bubbles." },
      { term: "Pipeline bubble", definition: "Idle time while one stage works and others wait. Reduced by keeping several requests in flight, which continuous batching provides." },
      { term: "Data parallelism (serving)", definition: "Full model replicas each serving different requests. Zero inter-GPU communication, and the right choice whenever the model fits on one device." },
      { term: "Quantization", definition: "Storing weights in fewer bits. Shrinks memory and speeds decode because decode is bandwidth-bound, at some quality cost." },
      { term: "GPTQ / AWQ", definition: "Calibration-based quantization methods that protect the weights carrying most signal, so 4-bit often loses little, though the loss can concentrate in specific capabilities." },
      { term: "Speculative decoding", definition: "A small draft model proposing tokens that the large model verifies in one parallel pass. Exactly lossless when verification is correct, unlike quantization." },
      { term: "Acceptance rate", definition: "How often draft tokens survive verification. Determines whether speculative decoding is a large win or a net loss." },
      { term: "Prefix cache", definition: "Reusing KV cache for identical leading tokens. Highly effective for shared system prompts, and invalidated entirely by one differing character." },
      { term: "Response cache", definition: "Returning a stored response for an identical request. Requires accepting deterministic output, and its keys are a security boundary in multi-tenant systems." },
      { term: "Model routing", definition: "Directing each request to the cheapest capable model. Explicit routing by task is reliable; learned difficulty routing is more powerful and can be wrong." },
      { term: "Fallback path", definition: "The defined behavior when a model or provider is unavailable. Must be tested, since models differ in context limits and output format." },
      { term: "Response provenance logging", definition: "Recording model version, quantization, route, and cache status per response, without which a quality complaint cannot be investigated." },
      { term: "MoE", expansion: "mixture of experts", definition: "A model whose feed-forward blocks are many experts with a router selecting a few per token. Capacity rises while arithmetic per token stays roughly flat." },
      { term: "Expert parallelism", definition: "Placing different experts on different GPUs, which adds an all-to-all exchange at every layer of a mixture-of-experts model." },
      { term: "Expert load imbalance", definition: "A router concentrating tokens on a few experts, leaving the GPUs holding unpopular experts idle. The hot-partition problem in a new place." },
      { term: "LoRA", expansion: "low-rank adaptation", definition: "Training small low-rank matrices per layer in place of updating weights, so a customized model is tens of megabytes on a shared base." },
      { term: "Multi-adapter batching", definition: "Serving requests for different LoRA adapters in one batch, sharing the expensive base pass while applying each adapter's small matrices separately." },
      { term: "Adapter cold start", definition: "The storage load paid on the first request for an adapter that is not currently resident, a latency cost that lands on a real user." },
      { term: "All-to-all", definition: "A collective in which every participant sends a distinct message to every other participant. It is the dispatch-and-combine pattern each sparse layer runs, and because it is bounded by the slowest pairwise path it belongs inside one fast domain." },
      { term: "Capacity factor", definition: "The per-expert cap on how many tokens one expert may accept in a step, with a stated policy for the overflow. Dropping tokens at that cap changes the output, so it is a quality parameter as well as a throughput one and must be pinned identically across replicas." },
      { term: "BF16", expansion: "brain floating point, 16 bit", definition: "The default 16-bit format for weights and activations, keeping a 32-bit float's exponent range and giving up mantissa bits. It is the baseline every quantization claim should be measured against." },
      { term: "FP8", expansion: "8-bit floating point", definition: "An 8-bit format that, on hardware with native FP8 matrix units, roughly doubles arithmetic throughput and so speeds compute-bound prefill as well as decode, while halving activation memory. The price is calibration: activation outliers need per-tensor or per-block scales fitted on representative traffic." },
      { term: "INT4", expansion: "4-bit integer", definition: "A 4-bit representation, roughly a fourfold cut against BF16. In production it almost always means weight-only INT4 with higher-precision activations, because 4-bit activations are generally not viable." },
      { term: "INT8", expansion: "8-bit integer", definition: "An 8-bit integer format. Usable for activations only with per-channel scaling plus outlier smoothing, which is why FP8 is preferred for activations wherever the hardware supports it." },
      { term: "Activation quantization", definition: "Lowering the precision of the intermediate values flowing between layers, as distinct from the stored weights. It shrinks activation memory and can speed the matrix units, and it is the precision decision most sensitive to a calibration set that does not represent real traffic." },
      { term: "KV-cache quantization", definition: "Storing cached keys and values in fewer bits, typically FP8. It buys concurrency rather than speed, roughly doubling the sequences a fixed pool holds, and its error appears first on long-context retrieval rather than on short prompts." },
      { term: "Dequantization overhead", definition: "The unpacking work inserted before every matrix multiply when weights are stored below the compute format. It is why a format benchmarked at batch size one can be slower than the baseline in a server running at batch size sixty-four." },
      { term: "Multi-token prediction", definition: "Training or equipping a model to propose several future tokens from one forward pass, usually through extra prediction heads on its own trunk. It removes the second set of weights a separate draft model requires." },
      { term: "Prompt-lookup decoding", definition: "Drafting by copying the longest matching sequence of words from the prompt itself instead of running any draft model. It costs nothing and accepts remarkably often on summarization, editing, translation, and code-diff traffic where the output largely restates the input." },
      { term: "Self-speculative decoding", definition: "Any speculation scheme whose draft comes from the target model itself - extra heads, feature-level drafting of the next hidden state, or a multi-token training objective - rather than from a second model that must be held, versioned, and kept aligned." },
      { term: "Constrained decoding", definition: "Compiling a schema, regular expression, or grammar into an automaton and masking every logit that would leave the language before sampling, so validity becomes structural rather than probabilistic. It is a scheduler feature, not a prompt technique." },
      { term: "JSON-schema decoding", definition: "The common case of constrained decoding, where the automaton comes from a JSON Schema so a downstream parser can be written against a guarantee. It says nothing about whether the field values are correct, so schema-validity rate and field-level accuracy are two separate numbers." },
      { term: "Logit masking", definition: "Setting the scores of disallowed tokens out of range before sampling. Done per step and per sequence over a vocabulary of a hundred thousand or more, so a naive implementation lands directly on inter-token latency." },
      { term: "Grammar compilation cache", definition: "Storing compiled automatons by schema hash and precompiling registered schemas at deploy. Without it, the first request for a large schema pays a compilation that can exceed the request it is serving." },
      { term: "Paged attention", definition: "Allocating KV cache in fixed-size blocks reached through an indirection table, so a sequence grows block by block and physical blocks need not be adjacent. It eliminates the waste of reserving each request's maximum possible context and raises memory utilization from under 40 percent to over 90." },
      { term: "External fragmentation", definition: "Free memory scattered in pieces too small to satisfy a contiguous request, so admission fails while total free memory looks adequate. It is the second waste, alongside worst-case reservation, that paged allocation removes." },
      { term: "Copy-on-write prefix sharing", definition: "Reference-counting KV blocks so several requests point at the same physical blocks for an identical leading prompt, with a private copy made only when one of them diverges. This is what makes a shared system prompt or a block of few-shot examples cost memory once rather than once per request." },
    ],
  },

  "llm-distributed-training": {
    primer: {
      plainSummary:
        "Training a large model needs far more memory than a single GPU has, and not mainly for the weights: the optimizer state and the intermediate values kept for the backward pass are what dominate. Knowing where the memory actually goes comes first, then the several ways of splitting work across hundreds of GPUs and what each costs in communication, and then why a job running for weeks needs checkpointing and failure recovery designed in from the start.",
      analogy:
        "A large construction project across many crews. You can give every crew identical plans and have them build separate sections, then reconcile at the end of each day. That is simple, but every crew needs a full copy of the plans and all the equipment. Or you can split the equipment among crews so each holds part of it, which saves enormously and means constant coordination to borrow what you need. Because the project runs for months, you also need a way to record progress so that losing a crew does not restart the whole build.",
      sections: [
        {
          heading: "Where the memory goes",
          body: [
            "Model weights are the smallest part of the story, and this surprises people. Take a 7-billion-parameter model trained in 16-bit precision: weights are about 14 gigabytes. Gradients, one per parameter, add another 14. The optimizer is the expensive part - Adam, the standard choice, keeps two running statistics per parameter and a 32-bit master copy of the weights, which is 12 bytes per parameter, so optimizer state is roughly 84 gigabytes. Weights, gradients, and optimizer state together come to about 112 gigabytes for a model whose weights are 14. A rough rule worth carrying: with Adam in mixed precision, training memory for parameters is around 16 bytes per parameter against 2 for inference.",
            "Then there are activations: the intermediate values from the forward pass that must be retained because the backward pass needs them to compute gradients. Activation memory scales with batch size, sequence length, and model depth, and for large models with long sequences it can exceed everything else combined.",
            "Activation checkpointing - also called gradient checkpointing - is the standard control. Instead of keeping every intermediate value, keep only a few checkpoints and recompute the rest during the backward pass. This trades compute for memory, typically costing about 30 percent more time and saving most activation memory. It is one of the few dials that reliably converts a job that does not fit into one that does.",
            "Doing this arithmetic before choosing a parallelism strategy is the correct order, and it is what an interviewer is checking. The strategy is determined by which component does not fit, and someone who reaches for tensor parallelism without knowing that optimizer state is six times the weights is guessing.",
          ],
        },
        {
          heading: "Four ways to split, and what each communicates",
          body: [
            "Distributed data parallelism gives every GPU a full copy of the model and a different slice of the batch. Each computes gradients on its slice, then all GPUs combine gradients with an all-reduce - a collective operation where every participant ends up with the sum of everyone's values. Simple and effective, and it does nothing for memory, since every GPU still holds the full model, gradients, and optimizer state.",
            "Fully sharded data parallelism, and the equivalent ZeRO stages, attack exactly that redundancy. Instead of replicating optimizer state, gradients, and parameters on every GPU, shard them so each holds only a slice. When a layer is needed, its parameters are gathered from whoever holds them with an all-gather; after use they are discarded. Gradients are combined with a reduce-scatter, which sums and distributes slices in one operation. Memory per GPU falls roughly by the number of GPUs, and that is what makes very large models trainable at all, at the cost of substantially more communication. ZeRO stages simply describe how much is sharded: stage 1 the optimizer state, stage 2 gradients too, stage 3 parameters as well.",
            "Tensor parallelism splits individual layers, requiring communication several times per layer. As in serving, this needs a very fast interconnect and belongs within a node.",
            "Pipeline parallelism assigns layer groups to different GPUs, communicating only at boundaries, which suits slower links across nodes. It introduces bubbles, mitigated by splitting each batch into micro-batches so several are in flight at different stages.",
            "Large training runs combine all of these: tensor parallelism within a node, pipeline parallelism across a small group of nodes, and data parallelism with sharding across the rest. The organizing principle is to match each technique's communication frequency to the speed of the link it must cross: the most chatty method gets the fastest link. If you remember one sentence about distributed training, that is the one.",
          ],
        },
        {
          heading: "Precision, stability, and knowing the run is healthy",
          body: [
            "The memory arithmetic above assumed mixed precision, so it is worth saying what that actually is. Weights and activations are stored and multiplied in a 16-bit format for speed and memory, while a 32-bit master copy of the weights and the optimizer statistics are kept at full precision, because accumulating millions of tiny updates into a 16-bit number silently loses them.",
            "Which 16-bit format matters. Float16 has more precision and a narrow exponent range, so gradients underflow to zero and the run needs loss scaling: multiply the loss by a large constant before the backward pass, then divide it out before the update. Bfloat16 trades precision for the same exponent range as 32-bit floats, so it does not underflow and needs no loss scaling, which is why it is the default on hardware that supports it. If someone proposes float16 without mentioning loss scaling, that is a real gap.",
            "Even with the right format, large runs go unstable. The signature is a loss spike: the loss jumps by an order of magnitude in one step and either recovers over a few hundred steps or never does. Gradient clipping, capping the global gradient norm before the update, prevents most of them. When one gets through, the remedy is procedural and not clever: rewind to the last good checkpoint, skip the data shard that triggered it, optionally lower the learning rate, and resume. That procedure only exists if checkpoints are frequent enough and the data loader position was captured, which is why those two details from the previous section are stability requirements and not merely recovery ones.",
            "Watch the gradient norm alongside the loss, since a growing gradient norm usually precedes a loss spike by many steps and gives you the chance to intervene before the damage. Watch per-rank loss as well: if one rank's loss diverges from the others, the problem is data or hardware on that rank rather than the model.",
            "Finally, report throughput as tokens per second per GPU and as model FLOPs utilization, or MFU, the fraction of the hardware's peak arithmetic the run actually achieves. A well-tuned large run reaches somewhere in the 30 to 50 percent range. A run at 10 percent is leaving most of a very expensive cluster idle, and MFU is the number that starts the conversation about whether the cause is communication, memory stalls, or a straggler.",
          ],
        },
        {
          heading: "Failure, checkpointing, and determinism",
          body: [
            "A job on a thousand GPUs for three weeks will experience hardware failures, several per day at realistic per-GPU failure rates. The design question is not whether but how much progress each failure costs, and a job that restarts from the beginning on any failure will never finish.",
            "Checkpointing writes the full training state - weights, optimizer state, data loader position, and step number - so training can resume. The critical property is that a checkpoint must be globally consistent, representing one training step across every rank. A checkpoint assembled from different ranks at different steps is silently corrupt, and it will produce a model that trains to a worse result with no error anywhere, which is among the most painful failure modes available.",
            "For large models this is a serious I/O problem: a checkpoint can be hundreds of gigabytes, and writing it synchronously stalls every GPU. Distributed checkpointing has each rank write its own shard in parallel to shared storage, and asynchronous checkpointing copies state to host memory quickly and writes to storage in the background so GPUs resume almost immediately. Checkpoint frequency is a straightforward trade: more frequent means less lost work and more overhead, and the right interval is roughly where checkpoint cost balances expected lost progress.",
            "Stragglers are the other operational reality. Because collectives require every participant, one slow GPU sets the pace for all of them. A single degraded device can halve the throughput of a thousand-GPU job while every dashboard shows it running normally. Detect stragglers by monitoring per-rank step times, and be willing to remove and replace a persistently slow node.",
            "Determinism matters more than in ordinary services, because debugging a training run that cannot be reproduced is nearly impossible. Record seeds, data ordering, and the exact configuration, and make the data loader's position part of the checkpoint so a resumed run consumes the same data in the same order instead of silently repeating or skipping examples. Bit-exact reproducibility is often unattainable given non-deterministic GPU operations, and that is acceptable. What matters is that a resumed run continues the same trajectory and does not quietly diverge onto a different one.",
          ],
        },
      ],
      workedExample: {
        title: "Planning a 70B training run",
        setup:
          "A team will train a 70-billion-parameter model on 256 GPUs of 80 gigabytes each, connected by fast interconnect within 8-GPU nodes and slower networking between nodes. The run is expected to take three weeks.",
        steps: [
          "Do the memory arithmetic first, because it determines everything else. At roughly 16 bytes per parameter for mixed-precision Adam, weights, gradients, and optimizer state total about 1.1 terabytes, which is 14 times a single GPU's 80 gigabytes. Plain data parallelism is impossible before activations are even considered, and this number forces the strategy; nothing about it is a matter of preference.",
          "Shard state to make it fit. Fully sharded data parallelism across all 256 GPUs reduces per-GPU parameter-related memory to roughly 4 gigabytes, leaving ample room for activations and communication buffers. This is the technique that makes the job feasible.",
          "Add tensor parallelism within nodes where it is affordable. Splitting layers across the 8 GPUs in a node reduces activation memory and speeds each step, and the fast intra-node interconnect can carry its frequent communication. Do not extend it across nodes, where the slower link would make communication dominate compute.",
          "Control activation memory with checkpointing. Even with sharding, long sequences make activations large. Apply activation checkpointing, accepting roughly 30 percent more compute time for a large memory saving that permits a bigger batch. The bigger batch usually improves throughput enough to offset much of the recomputation cost.",
          "Fix the numerical setup before the run starts, since changing it midway invalidates everything before the change. Use bfloat16 for weights and activations so no loss scaling is needed, keep the 32-bit master weights and optimizer statistics the memory arithmetic already assumed, and turn on global gradient-norm clipping. Set the monitoring at the same time: loss, gradient norm, per-rank loss, and model FLOPs utilization, with an alert on gradient norm trending upward so a loss spike is caught before it costs a day.",
          "Plan for failures with distributed asynchronous checkpointing. At 256 GPUs over three weeks, expect several failures. Checkpoint every 30 minutes, with each rank writing its shard in parallel and the copy to host memory made quickly so GPUs resume in seconds. Include the data loader position so a resumed run does not repeat or skip data. Verify the checkpoint is globally consistent at one step across all ranks, since an inconsistent checkpoint fails silently.",
          "Instrument for stragglers and reproducibility. Monitor per-rank step time and alert when one rank consistently lags, since a single degraded GPU throttles all 256 while every high-level metric looks normal. Record seeds, data order, and configuration so the run is reproducible enough to debug, which matters most when something goes wrong at week two.",
        ],
        takeaway:
          "The memory calculation came first and dictated the parallelism strategy; everything else was matching each technique's communication frequency to the speed of the link it crosses. The two operational details that decide whether a three-week run finishes are consistent fast checkpointing and straggler detection, and both are easy to leave until they are urgently needed.",
      },
    },
    glossary: [
      { term: "Optimizer state", definition: "Per-parameter statistics kept by the optimizer. Adam in mixed precision needs roughly 12 bytes per parameter, several times the weights themselves." },
      { term: "Activations", definition: "Intermediate forward-pass values retained for the backward pass. Scale with batch size, sequence length, and depth, and can exceed all other memory." },
      { term: "Activation checkpointing", definition: "Keeping only some intermediates and recomputing the rest, trading roughly 30 percent more compute for a large memory saving." },
      { term: "DDP", expansion: "distributed data parallelism", definition: "Full model replicas processing different batch slices, combining gradients with an all-reduce. Simple, and saves no memory." },
      { term: "FSDP / ZeRO", expansion: "fully sharded data parallelism / zero redundancy optimizer", definition: "Sharding optimizer state, gradients, and parameters across GPUs, cutting per-GPU memory roughly by the device count at the cost of more communication." },
      { term: "All-reduce", definition: "A collective where every participant ends up with the sum of all participants' values. The core operation of data-parallel gradient synchronization." },
      { term: "All-gather", definition: "A collective where every participant receives the full set of shards. Used to reassemble sharded parameters when a layer is needed." },
      { term: "Reduce-scatter", definition: "A collective that sums values and distributes slices of the result, combining reduction and sharding in one step." },
      { term: "Communication-to-link matching", definition: "The organizing principle of parallelism layout: the most communication-intensive method gets the fastest interconnect. Tensor parallelism within a node, pipeline across." },
      { term: "Micro-batch", definition: "A subdivision of a batch kept in flight to fill pipeline stages and reduce bubbles." },
      { term: "Distributed checkpoint", definition: "Each rank writing its shard in parallel, avoiding one process serializing hundreds of gigabytes." },
      { term: "Globally consistent checkpoint", definition: "State captured at one training step across every rank. An inconsistent checkpoint is silently corrupt and produces a worse model with no error." },
      { term: "Asynchronous checkpointing", definition: "Copying state to host memory quickly and writing to storage in the background, so GPUs are back at work within seconds." },
      { term: "Straggler", definition: "One slow rank that sets the pace for all others, since collectives wait for every participant. Detected by per-rank step time monitoring." },
      { term: "Deterministic recovery", definition: "Resuming with the same data order and configuration, which requires the data loader position to be part of the checkpoint." },
      { term: "Mixed precision", definition: "Computing in 16 bits while keeping a 32-bit master copy of weights and optimizer statistics, since tiny updates accumulated in 16 bits are silently lost." },
      { term: "Bfloat16 versus float16", definition: "Bfloat16 keeps the 32-bit exponent range and needs no loss scaling; float16 has finer precision, underflows gradients, and requires it." },
      { term: "Loss scaling", definition: "Multiplying the loss before the backward pass and dividing it out before the update, so float16 gradients do not underflow to zero." },
      { term: "Gradient clipping", definition: "Capping the global gradient norm before each update. The main defense against the loss spikes that destabilize long runs." },
      { term: "Loss spike", definition: "A sudden order-of-magnitude jump in loss that may never recover. Repaired by rewinding to a checkpoint and skipping the offending data shard." },
      { term: "MFU", expansion: "model FLOPs utilization", definition: "The fraction of the hardware's peak arithmetic a training run actually achieves. A well-tuned large run reaches roughly 30 to 50 percent." },
      { term: "FLOPS", expansion: "floating-point operations per second", definition: "A measure of raw arithmetic capability, where one FLOP is a single such operation. Peak FLOPS is a vendor's theoretical ceiling that is almost never reached, which is exactly why utilization is reported as a fraction of it rather than as an absolute." },
      { term: "DP/TP/PP/EP/CP", expansion: "data / tensor / pipeline / expert / context parallelism", definition: "The five axes a large run's process mesh is written along. The product of the degrees equals the world size, and each axis names a different collective, which is what makes the layout a communication decision before it is a memory one." },
      { term: "MLP", expansion: "multi-layer perceptron", definition: "The feed-forward block sitting after attention in each transformer layer. Once a tiled attention kernel stopped materializing the quadratic score matrix, these blocks' activations became the dominant term in the activation budget." },
      { term: "RNG", expansion: "random number generator", definition: "The source of the run's randomness - initialization, dropout masks, data shuffling. Its state belongs in the checkpoint alongside the data-loader cursor, or a resumed run diverges onto a different trajectory than the one it was continuing." },
      { term: "FP32", expansion: "32-bit floating point", definition: "Full single precision. Mixed-precision training computes in 16 bits but keeps the optimizer moments and a master copy of the weights in FP32, because millions of tiny updates accumulated into a 16-bit number are silently lost - which is exactly why the optimizer term dominates the memory budget." },
      { term: "FlashAttention", definition: "An attention kernel that tiles the computation in on-chip memory, never materializes the full score matrix, and recomputes tiles in the backward pass from saved softmax statistics. It trades a modest arithmetic increase for activation memory that grows linearly rather than quadratically with sequence length." },
      { term: "Selective activation checkpointing", definition: "Recomputing only the intermediates that are expensive to store and cheap to redo, instead of checkpointing every layer. It usually beats the all-or-nothing setting that made sense when the quadratic score matrix dominated the activation budget, so the split is worth re-measuring for the current attention kernel." },
      { term: "Sequence parallelism", definition: "Sharding along the sequence dimension the regions tensor parallelism leaves unsharded - layer normalization, dropout, and the residual additions - which would otherwise duplicate a full-sequence activation on every rank of the group. Because an all-reduce is a reduce-scatter followed by an all-gather, it removes that duplication at essentially no extra communication volume." },
      { term: "Context parallelism", definition: "Sharding the sequence for attention itself, so per-device attention memory scales as sequence length divided by the parallel degree. It is what makes training at 128,000 tokens feasible, at the cost of attention communication that now grows with sequence length." },
      { term: "Ring attention", definition: "The context-parallel mechanism in which each device owns a slice of the queries while key and value blocks circulate around a ring until every query has met every key. Causal masking makes the naive slice assignment badly imbalanced, so the sequence is striped or zigzagged across ranks." },
      { term: "MoE", expansion: "mixture of experts", definition: "A model whose feed-forward blocks are many parallel experts with a router selecting a few per token. Capacity rises while arithmetic per token stays roughly flat - but gradients and optimizer moments exist for every parameter, so memory scales with total parameters while compute scales with active ones." },
      { term: "Expert parallelism", definition: "Placing whole experts on different devices and routing tokens to them, which adds an all-to-all dispatch and an all-to-all combine at every sparse layer. Usually overlaid on the data-parallel dimension and confined to one fast domain." },
      { term: "All-to-all", definition: "A collective in which every participant sends a distinct message to every other participant. It is bounded by the slowest pairwise path, which is why expert-parallel groups have to be topology-aware." },
      { term: "Capacity factor", definition: "The per-expert cap on tokens accepted in one step, with a stated overflow policy. Tokens dropped at the cap change the function being learned, so the factor must be pinned identically in training and in serving or the mismatch is a silent skew." },
      { term: "Load-balancing loss", definition: "An auxiliary training term, or a bias-based rule that nudges the router without adding a gradient, pushing expert utilization toward uniform. Its coefficient fails in both directions: too weak and the router collapses onto a few experts, too strong and experts stop specializing." },
      { term: "PEFT", expansion: "parameter-efficient fine-tuning", definition: "Adapting a model by training a small number of added parameters while the base weights stay frozen. It removes the gradient and optimizer terms from the memory budget and leaves the activation term untouched, which is the trap it is most often expected to solve." },
      { term: "LoRA", expansion: "low-rank adaptation", definition: "Freezing the base weights and training two small low-rank matrices per adapted layer, so a customized model is tens of megabytes over a shared multi-gigabyte base. At rank 16 on a 7-billion-parameter model that is on the order of 20 million trainable parameters." },
      { term: "QLoRA", expansion: "quantized low-rank adaptation", definition: "LoRA with the frozen base held in 4 bits, taking a 7-billion-parameter model's 14 GB BF16 weight copy to roughly 4 GB. The base is dequantized block by block during every forward and backward pass, so the saving is paid in step time rather than in memory." },
    ],
  },

  "llm-enterprise-rag-systems": {
    primer: {
      plainSummary:
        "Retrieval-augmented generation answers questions by finding relevant documents and putting them into the model's prompt, so the model can cite real sources instead of relying on what it memorized in training. In an enterprise, the difficulty is never the retrieval algorithm. Documents change constantly, different people are allowed to see different things, and answering from a document the asker may not open is a data breach performed by your own system.",
      analogy:
        "A research assistant with access to a company's filing cabinets. Their competence at finding relevant files is the easy part. The hard parts are that files are updated and removed daily, that the assistant must check what each person is cleared to see before quoting anything to them, and that if a file was deleted last week the assistant must not answer from the copy of it they happen to remember. The last point is the one that turns a retrieval system into a compliance problem.",
      sections: [
        {
          heading: "Ingestion is a versioned pipeline, not a one-off import",
          body: [
            "Documents must be parsed, split into chunks, embedded, and indexed. Each step has decisions that determine answer quality, and all of them must be repeatable, because documents change and the pipeline will be re-run over the same content many times.",
            "Parsing extracts text and structure, and it is more consequential than it sounds: a PDF table flattened into a run of numbers is worse than useless, and headings carry the context that makes a chunk interpretable on its own. Chunking splits documents into retrievable pieces, and the size is a real trade-off: small chunks retrieve precisely and lose surrounding context, while large chunks carry context and dilute the embedding so retrieval becomes vaguer. Splitting on structural boundaries such as sections beats splitting at fixed character counts, and including the document title and section heading in each chunk lets a chunk stand alone.",
            "Every artifact must be versioned and every step idempotent. When a document is updated, its old chunks must be replaced and not merely added to. Otherwise the index holds both versions, the model receives contradictory context, and the answer is confidently wrong and impossible to explain. Chunk identity is derived from document ID plus version plus position, so reprocessing is safe.",
            "Deletion is the case that must work perfectly. When a document is deleted, its chunks must leave the index promptly, and the standard failure is that they linger for hours because deletion was implemented as a background job with no urgency. Since deletion is often a legal requirement, propagate deletions synchronously or with a tight bound, use tombstones so a deletion cannot be lost, and maintain a freshness watermark - the guarantee that the index reflects all changes up to a stated time - so the system can state how stale it might be instead of assuming it is current.",
          ],
        },
        {
          heading: "Retrieval quality: hybrid search and reranking",
          body: [
            "Embedding-based retrieval finds semantically similar text, which handles paraphrase well and reliably fails on exact identifiers. A part number, an error code, or a specific name may be poorly represented in embedding space, so semantically similar but wrong chunks come back instead.",
            "Keyword retrieval, typically BM25, is the opposite: excellent on exact terms and blind to paraphrase. So production systems use hybrid retrieval, running both and combining results, usually with reciprocal rank fusion, which merges ranked lists by position and so avoids the problem that the two systems' scores are not on comparable scales.",
            "A reranker then improves the ordering. Retrieval must be cheap because it scores the whole corpus, so it uses a two-tower structure where document vectors are precomputed. A reranker processes the query and a candidate document together, so it can model their interaction directly. That makes it far more accurate and far too expensive to run over everything, so it runs over the top 50 or 100. This is the same cheap-wide-then-expensive-narrow pattern as feed ranking and search, and recognizing it as the same pattern is worth saying aloud.",
            "Evaluate retrieval separately from answer quality, because they fail differently and need different fixes. Retrieval metrics ask whether the right chunk was in the results at all. If it was not, no amount of prompt engineering can produce a correct answer. Answer metrics ask whether the response is correct, grounded in the retrieved context, and properly cited. A team measuring only answer quality cannot tell whether a bad answer came from retrieval missing the document or from the model misreading it, and those have entirely different remedies.",
            "Groundedness deserves separate measurement: does every claim in the answer actually appear in the retrieved context? An answer that is correct but not supported by the provided sources is still a problem, because it means the model is drawing on training knowledge, and next time that knowledge will be out of date or wrong with the same confidence.",
          ],
        },
        {
          heading: "The vector index: approximate search, filters, and embedding versions",
          body: [
            "Exact nearest-neighbor search over tens of millions of vectors is too slow to serve, so production systems use an approximate nearest-neighbor index, or ANN. Two families dominate. HNSW builds a navigable small-world graph and traverses it greedily, giving very high recall at low latency in exchange for substantial memory and awkward deletion. IVF clusters the vectors and probes only the nearest few clusters, so the number of probes becomes an explicit dial between recall and latency.",
            "Whichever you use, measure recall against exact search on a sample of real queries. An index quietly returning 80 percent of the correct neighbors looks identical from the outside to one returning 99 percent: same latency, same shape of results, answers that are merely a bit worse for reasons nobody can attribute. Retrieval recall is the number that makes that difference visible, and it belongs on the dashboard next to answer quality.",
            "Filters interact badly with approximate search, and this is exactly where the permission model meets the index. Post-filtering runs the search and then drops unauthorized hits, which fails twice over: it is the security ordering error already described, and it is a recall disaster, since a user cleared for 1 percent of the corpus may get nothing at all out of a top-100 search. Pre-filtering restricts candidates before the search, which is correct, and it can cripple graph traversal or collapse into a scan when the predicate is very selective. The workable designs are index implementations that carry the predicate into traversal, or partitioning the index by coarse permission group so most queries search a scoped subindex. Whichever you pick, be ready to state its recall under a selective filter.",
            "The embedding model version is part of the index's identity. Vectors produced by two different embedding models are not comparable, so changing the embedding model means reindexing the whole corpus, which is hours or days of compute at enterprise scale. Treat it as a migration: build the new index alongside the old, embed new documents into both, compare retrieval quality on a fixed query set, then shift reads. A query embedded with a different version from the index it searches returns plausible nonsense and raises no error anywhere.",
            "Deletion has an index-level wrinkle worth knowing. Graph indexes remove a vector by marking it, and traversal quality degrades as marks accumulate, so periodic compaction or rebuild is part of operating the system. That has to be reconciled with the promptness requirement from ingestion: a deleted chunk must be excluded from results the moment it is deleted, even though its vector remains physically present until the next rebuild.",
          ],
        },
        {
          heading: "Access control, and why it must fail closed",
          body: [
            "This is where enterprise RAG differs fundamentally from consumer search, and it is what an interviewer will press on. Different users may see different documents, so retrieval must be filtered by the asking user's permissions, and the filtering must happen before content enters the model's context, not after.",
            "The reason post-filtering fails is worth stating explicitly: once unauthorized text is in the prompt, it has influenced the generated answer even if the citation is stripped afterwards. The model may paraphrase confidential content without quoting it. There is no reliable way to remove an influence, so authorization is a retrieval-time filter, full stop.",
            "Permissions must be evaluated at query time against current state rather than trusted from an index snapshot. Someone who left a project yesterday must not receive its documents today, and an index rebuilt nightly would happily serve them. So either filter against the live permission system, or maintain a permission index updated synchronously with the source, and be able to say which. 'We store ACLs with the chunks' invites the immediate question of how stale they are.",
            "The system must fail closed. If the permission service is unavailable, return no results rather than unfiltered ones. This is the opposite of the usual availability instinct, and it is correct here because the cost of an unavailable answer is inconvenience while the cost of a leaked one is a breach. Saying this explicitly is a strong signal in an interview.",
            "Two further boundaries matter. Caches must be keyed by permission scope, or one user's cached answer can be served to another, turning a cache into a leak. And retrieved content is untrusted input: a document may contain text crafted to manipulate the model, an attack called prompt injection. Because the model cannot reliably distinguish instructions in retrieved content from instructions from you, the mitigation is architectural rather than linguistic. Never let retrieved content authorize a tool call or an action, and treat everything the model produces from it as untrusted output requiring its own authorization check.",
          ],
        },
      ],
      workedExample: {
        title: "An internal knowledge assistant that leaks",
        setup:
          "An internal assistant answers questions over company documents - HR policies, engineering docs, board materials. A user in engineering asks about compensation planning and receives an answer summarizing a confidential board document they cannot open in the document system.",
        steps: [
          "Locate where authorization was supposed to happen and find it was applied to citations instead of to retrieval. The system retrieved semantically relevant chunks regardless of permission, generated an answer, and then filtered the citation list. The confidential content had already shaped the answer, so removing the citation removed the evidence and not the leak. This is the failure mode to name precisely.",
          "Move filtering before retrieval. The permission filter becomes part of the retrieval query, so unauthorized chunks are never candidates. This costs some retrieval quality - fewer candidates survive the filter - and it is still the only correct design: there is no way to un-influence a generated answer.",
          "Measure what pre-filtering did to recall, because correctness here costs retrieval quality and the cost has to be managed rather than discovered. Restricting the candidate set before an approximate search degrades graph traversal, and a user cleared for a small slice of the corpus can end up with almost nothing. Compare filtered recall against exact search on real queries, then either move to an index that carries the predicate into traversal or partition the index by coarse permission group so most queries search a scoped subindex.",
          "Check permission freshness, since a filter is only as good as the data behind it. Permissions were copied into the index at ingestion and refreshed nightly, so up to 24 hours of stale access. Someone removed from a project retains access for a day. Move to evaluating against the live permission service at query time, or maintain a permission index updated synchronously with the source system.",
          "Define fail-closed behavior explicitly. If the permission service is unreachable, return no results and an explanatory message; do not fall back to unfiltered retrieval. Confirm this is implemented and not merely assumed. A permissive fallback deep in a client library is exactly how this class of bug survives review.",
          "Fix the cache, which is a second instance of the same bug. Answers were cached by question text, so an authorized user's cached answer could be returned to an unauthorized one. Include the permission scope in the cache key, accepting the lower hit rate, since a cache that can cross a trust boundary is a leak with better latency.",
          "Add the controls that make recurrence detectable. Log the permission-filtered candidate set for every query so an audit can confirm what was retrievable. Add automated tests that ask a low-privilege user questions answerable only from privileged documents and assert no answer is produced. Treat retrieved content as untrusted for tool use, so a malicious document cannot induce an action.",
        ],
        takeaway:
          "The bug was ordering: authorization applied after generation instead of before retrieval. That single sequencing error is the most common serious flaw in enterprise RAG designs, and 'filter before retrieval, evaluate against live permissions, fail closed' is the sentence worth being able to say without hesitation. Note also that the cache reproduced the same flaw independently, which is typical: a trust boundary has to be enforced at every layer that crosses it.",
      },
    },
    glossary: [
      { term: "RAG", expansion: "retrieval-augmented generation", definition: "Retrieving relevant documents and placing them in the model's prompt so answers are grounded in real sources instead of memorized training data." },
      { term: "Chunking", definition: "Splitting documents into retrievable pieces. Small chunks retrieve precisely and lose context; large chunks carry context and dilute the embedding." },
      { term: "Document version", definition: "The identity of a specific revision, so updates replace old chunks instead of accumulating contradictory versions in the index." },
      { term: "Idempotent ingestion", definition: "Reprocessing a document producing the same index state, achieved by deriving chunk identity from document, version, and position." },
      { term: "Tombstone (in RAG)", definition: "A deletion marker ensuring removals propagate to the index and cannot be silently lost." },
      { term: "Freshness watermark", definition: "The time up to which the index reflects all source changes, letting the system state its staleness rather than assume it is current." },
      { term: "Hybrid retrieval", definition: "Combining embedding search, which handles paraphrase, with keyword search such as BM25, which handles exact identifiers that embeddings miss." },
      { term: "Reciprocal rank fusion", definition: "Merging ranked lists by position rather than by score, avoiding the problem that different retrievers' scores are not comparable." },
      { term: "Reranker", definition: "A model scoring query and document together, far more accurate than two-tower retrieval and far too expensive to run over the whole corpus." },
      { term: "Two-tower retrieval", definition: "Embedding query and document independently so document vectors can be precomputed, which is what makes first-stage retrieval cheap enough to run over the whole corpus." },
      { term: "ANN", expansion: "approximate nearest neighbor", definition: "Index structures that trade exactness for speed, because exact nearest-neighbor search over tens of millions of vectors is far too slow to serve." },
      { term: "HNSW", expansion: "hierarchical navigable small world", definition: "A graph-based approximate index traversed greedily. Very high recall at low latency, in exchange for substantial memory and awkward deletion." },
      { term: "IVF", expansion: "inverted file index", definition: "An approximate index that clusters vectors and probes only the nearest few clusters, so the probe count is an explicit dial between recall and latency." },
      { term: "Retrieval recall", definition: "The fraction of truly nearest neighbors an approximate index returns, measured against exact search on real queries. Without it, a degraded index is invisible from the outside." },
      { term: "Pre-filtering", definition: "Restricting candidates by permission before the vector search runs. Correct, and it can cripple graph traversal when the predicate is very selective." },
      { term: "Embedding model version", definition: "Part of the index's identity, since vectors from two models are not comparable. Changing it means reindexing the corpus and running the change as a migration." },
      { term: "Index compaction", definition: "Rebuilding a graph index to physically remove vectors that deletion only marked, because traversal quality degrades as marks accumulate." },
      { term: "Groundedness", definition: "Whether every claim in an answer is supported by the retrieved context, also called faithfulness to the evidence. An unsupported but correct answer means the model is using training knowledge that will eventually be wrong." },
      { term: "Retrieval-time authorization", definition: "Filtering by the asking user's permissions before content enters the prompt. Post-filtering fails because an influence on a generated answer cannot be removed." },
      { term: "ACL freshness", definition: "How current the permission data used for filtering is. A nightly snapshot means a day of stale access after someone's permissions change." },
      { term: "Fail closed", definition: "Returning no results when the permission service is unavailable, rather than unfiltered ones. The opposite of the usual availability instinct, and correct here." },
      { term: "Permission-scoped cache key", definition: "Including the authorization scope in cache keys, without which a cache serves one user's answer to another." },
      { term: "Prompt injection", definition: "Text in retrieved content crafted to manipulate the model. Mitigated architecturally, by never letting retrieved content authorize an action, since instructing the model not to comply does not work." },
      { term: "ACL", expansion: "access control list", definition: "The record of which identities may see a document or chunk. In retrieval it is a filter evaluated at query time against current state, and an ACL snapshot copied into the index at ingestion is exactly as stale as its refresh interval." },
      { term: "BM25", expansion: "Best Match 25", definition: "The standard keyword ranking function, scoring documents by term frequency and rarity with a penalty for document length. It is the lexical arm of hybrid retrieval and the reason exact identifiers, error codes, and surnames are findable at all." },
      { term: "Parent expansion", definition: "Indexing small chunks so retrieval stays precise and citations stay tight, then handing the generator the enclosing section so the answer has context. It decouples the two different jobs a chunk is otherwise asked to do at one size." },
      { term: "Contextual retrieval", definition: "Prepending a short generated description of the parent document to each chunk before embedding, at the cost of one generation per chunk at ingest. It fixes the classic failure where a chunk reads 'it rose 12 percent' and names neither the company nor the year." },
      { term: "Cross-encoder", definition: "A model that concatenates query and document into one forward pass so they interact directly, which is what lets it see term-level matching, negation, and qualification. It is the mechanism behind a reranker, and the reason its cost is one model call per candidate." },
      { term: "Candidate budget", definition: "The explicit numbers at each retrieval stage: how many candidates come from each retriever, how many survive fusion into reranking, and how many are placed in the context window. Each is tuned against its own metric rather than inherited from a tutorial." },
      { term: "Query rewriting", definition: "Turning the user's question into one or more better search queries before retrieval, by decomposition into sub-queries or by embedding a drafted hypothetical answer, since answers live nearer to answers than questions do in embedding space." },
      { term: "Iterative retrieval", definition: "Running more than one retrieval round, where a later query is constructed from what an earlier one returned. Required for multi-hop questions whose second document is neither lexically nor semantically near the original question, so no amount of reranking one candidate list will surface it." },
      { term: "Agentic retrieval", definition: "The open end of the iterative spectrum: search is a tool, and the model issues a query, reads the evidence, judges sufficiency, reformulates, and stops when the question is answered or the budget is spent. Maximum discretion, and maximum variance in cost and latency." },
      { term: "Graph retrieval", definition: "Following explicit entity, citation, or containment edges built at ingest, so the next hop is a traversal rather than a guess. It sits between deterministic query rewriting and fully agentic search." },
      { term: "Hop budget", definition: "A hard cap on retrieval rounds, paired with a token budget and a defined behavior at exhaustion that answers from the evidence gathered and says so. Hop count is data-dependent, so without the cap a single pathological question can cost a hundred times the median." },
      { term: "Citation", definition: "A reference carrying the immutable document version and the source offsets of the exact text placed in the context window, not the live URL. Version plus offsets is what lets a reader reopen the evidence after the document has been edited, moved, or deleted." },
      { term: "Recall at k", definition: "The share of truly relevant documents that appear in the top k results of a stage. Measured at the first-stage candidate depth it is the ceiling on everything downstream, since a reranker can only reorder what retrieval already returned." },
      { term: "nDCG", expansion: "normalized discounted cumulative gain", definition: "A ranking metric that rewards relevant results in proportion to how high they are placed, normalized so scores are comparable across queries. Used alongside recall to judge ordering rather than mere presence." },
      { term: "Abstention", definition: "Declining to answer and saying so when the retrieved evidence is insufficient. A legitimate and required output here, because a generator that always answers will fill the gap from training knowledge that carries no citation and eventually goes stale." },
    ],
  },

  "llm-post-training": {
    primer: {
      plainSummary:
        "A base language model trained on internet text can continue text but is not helpful, does not follow instructions, and has no notion of what it should decline. Post-training converts it into an assistant: first by showing it examples of good responses, then by teaching it from comparisons of which of two responses people preferred. Both stages matter, the second has several competing implementations with very different operational costs, and none of it is reversible without lineage tracking that records every input to the result.",
      analogy:
        "Training a new employee. First you show them worked examples of the job done well. That is supervised fine-tuning, and it establishes format and basic competence. Then you review their work and say which of two drafts was better and why, repeatedly, which teaches judgment that examples alone cannot convey. The second phase is more powerful and more dangerous: push too hard on the feedback and they start writing what scores well with the reviewer instead of what is actually good. That is exactly the failure the technical machinery is designed to prevent.",
      sections: [
        {
          heading: "Supervised fine-tuning, and treating data as a governed release",
          body: [
            "Supervised fine-tuning, or SFT, trains the model on curated prompt-response pairs demonstrating the desired behavior. This is ordinary supervised learning, and it is remarkably effective: it establishes response format, instruction following, tone, and basic refusal behavior. Most of the visible difference between a base model and an assistant comes from this stage.",
            "Data quality dominates quantity here to an unusual degree. A few thousand carefully written examples routinely outperform hundreds of thousands of scraped ones, because the model learns the style and standard of what it is shown, flaws included. Inconsistent examples teach inconsistency, and a small number of examples that hedge excessively will teach hedging across the board.",
            "Treat the dataset as a governed release rather than a folder of files. Every example needs provenance: who wrote it, when, under which guidelines, reviewed by whom. Guidelines change, and when they do you must be able to find the examples written under the old ones. Without provenance a dataset becomes an undebuggable accumulation, and the first time a behavioral regression appears you will have no way to attribute it.",
            "Contamination control is a specific requirement. If evaluation prompts appear in training data, evaluation results are meaningless and will overstate quality in exactly the way that gets a model shipped. Check for overlap between training data and every evaluation set, including near-duplicates as well as exact matches, and re-check whenever either set changes.",
            "Also decide what proportion of examples cover refusal and edge cases. A model trained only on well-formed requests answered helpfully will attempt everything, including what it should decline. Those behaviors have to be demonstrated, since they cannot be inferred from examples that never contain them.",
          ],
        },
        {
          heading: "Learning from preferences",
          body: [
            "SFT teaches the model to imitate demonstrations. Preference learning teaches it something demonstrations cannot: relative quality. Humans are far better and faster at judging which of two responses is better than at writing an ideal response from scratch, so preference data is both cheaper to collect and richer in the distinctions that matter.",
            "The classic pipeline is reinforcement learning from human feedback. First train a reward model on preference pairs to predict which response a human would prefer. Then optimize the language model against that reward, using an algorithm such as PPO, while constraining it not to drift too far from the starting model. Rollout workers generate responses, the reward model scores them, and the policy is updated.",
            "That constraint is the crucial part and the thing to be able to explain. The reward model is an imperfect approximation of human preference, so if you optimize against it hard enough the model finds inputs where the reward model is wrong and exploits them, producing responses that score highly and are worse: answers that are verbose and confident, because the reward model learned to associate length with quality. This is reward hacking, and it is the central failure mode of the technique. The defense is a penalty on divergence from a frozen reference policy, measured by KL divergence, which keeps the optimized model near the starting distribution. Tuning that penalty is the main dial: too weak and the model hacks the reward, too strong and it barely improves.",
            "Direct preference optimization, or DPO, is the simpler alternative now widely used. It skips the reward model and the reinforcement learning loop entirely, optimizing the language model directly on preference pairs with a loss derived to have the same optimum. Far simpler to implement and operate, far cheaper, and no rollout infrastructure. The trade-off is less control: RLHF's loop can incorporate online feedback, non-differentiable rewards such as whether code compiles, and multiple reward sources. The reasonable default is DPO for its simplicity, moving to a reward-model loop when you need rewards that cannot be expressed as static pairwise preferences.",
            "Either way, preference data has its own biases and they are worth naming: annotators prefer longer answers, more confident answers, and answers that agree with them. Left uncorrected, these become the model's personality. Post-trained models tend toward verbosity and agreeableness for exactly this reason, unless the data is deliberately balanced against it.",
          ],
        },
        {
          heading: "Where preference data comes from, and what it costs",
          body: [
            "Preference pairs do not exist until someone makes them, and the mechanics of making them determine what the model becomes. An annotator pool has a composition - languages, domains, seniority, and the guideline document they were trained against - and every one of those properties ends up in the model. Record the pool and the guideline version alongside the data, because when behavior shifts between two campaigns the annotator cohort is as likely a cause as any hyperparameter, and it is the one nobody thinks to check.",
            "Measure agreement between annotators rather than assuming it. A pair that three people label three different ways is usually not a hard example; it is an underspecified guideline, and training on it teaches noise. Split the disagreement into two buckets. Genuine ambiguity should be removed or escalated to a senior reviewer. Guideline gaps should be fixed at the guideline and the affected examples relabeled. Reporting agreement per slice also tells you exactly where the guideline is thinnest, which is more useful than a single overall agreement number.",
            "Most campaigns now generate part of the data with a model. Rejection sampling is the standard shape: draw several responses from the current policy, score them with a reward model or an explicit rubric, and keep the best as a demonstration or the best and worst together as a pair. This is cheap, and it stays on-policy, which matters because preferences collected against last quarter's checkpoint describe a policy that no longer exists. The hazard is that the generator's blind spots become the dataset's blind spots, so a share of human-collected data has to remain in the mix as an anchor.",
            "Finally, cover the slices deliberately. A dataset assembled from whichever prompts happened to be available will be dominated by the common case, and the model will quietly regress on everything rare: minority languages, very long inputs, tool-using prompts, and the refusal categories. Set target counts per slice before collection rather than discovering the gap during evaluation, and record an under-covered slice as a stated risk on the release rather than treating silence as evidence of safety.",
          ],
        },
        {
          heading: "Lineage, gates, and rollback",
          body: [
            "A post-trained model is a function of many inputs: the base model, the SFT dataset, the preference dataset, the reward model if any, the reference policy, hyperparameters, and code. Any of these changing changes the result, so all of them must be versioned together. 'We fine-tuned Llama on our data' is not a description anyone can reproduce, debug, or roll back.",
            "Promotion should pass gates covering more than a single quality number. Capability evaluations check the model still does the tasks it is meant to. Safety evaluations check refusal behavior on prohibited requests. Regression evaluations check that behaviors which previously worked still do. This is the gate most often skipped and it catches the most damaging surprises, because post-training routinely improves the target behavior while degrading an unrelated one nobody was watching.",
            "Include an over-refusal check explicitly. It is easy to make a model safer by making it decline more, and a model that refuses reasonable requests is a worse product. Measure refusal on benign prompts as a first-class metric alongside refusal on harmful ones, or you will optimize one into the ground.",
            "Evaluation itself is harder than in classic ML because outputs are free text with no single correct answer. The realistic approach is a portfolio: fixed benchmarks for comparability, task-specific evaluations for your actual use cases, pairwise comparisons against the current production model, human review on a sample, and model-based judging for scale. No single one is sufficient, and each has a characteristic weakness.",
            "Finally, rollback must be a state transition and not a retraining job. Keep previous model versions deployable, version the whole serving configuration including system prompts and generation parameters, and be able to revert in minutes. Post-training regressions are often subtle and behavioral: the model becomes slightly more evasive, or slightly worse at one task. They are frequently noticed days after deployment through user reports rather than through metrics, which is precisely when a fast, boring revert is worth the most.",
          ],
        },
        {
          heading: "What ships is a bundle, not a checkpoint",
          body: [
            "The artifact you promote is not a file of weights. It is the checkpoint plus the tokenizer, the chat template that formats roles and turn boundaries, the stop tokens, the system prompt, the sampling configuration, and any tool schemas the model was trained to emit. Change any one of those at serving time and behavior changes, sometimes sharply, while the weights are byte-identical and every dashboard still reports the same version. That gap between what changed and what is labeled is where a surprising share of post-training incidents live.",
            "The chat template is the piece that catches people most often. A model trained with one role delimiter and served with another sees inputs unlike anything in its training distribution, and the symptom is not an error but a diffuse quality loss that reads like a bad fine-tune. Pin the template with the checkpoint, and test the serving path by round-tripping one training example through it and comparing the exact token sequence against what training produced.",
            "If the release is an adapter over a shared base rather than a full set of weights, the base version joins the bundle too, because an adapter trained against one base and applied to another is wrong in the same silent way. That coupling is the price of the cheap multi-tenant deployment, and it has to be recorded and enforced at load time rather than assumed by convention.",
            "Rollback then becomes a routing change instead of a retraining job: keep the previous bundle deployable and shift traffic back to it. Nothing about the historical checkpoint is mutated or relabeled, which matters because the record of what was actually served on a given day is the evidence you will need when a complaint arrives weeks later. Retain enough previous bundles to revert past the last two releases, since behavioral regressions are routinely noticed long after the change that caused them.",
          ],
        },
      ],
      workedExample: {
        title: "A fine-tune that improves its metric and regresses everywhere else",
        setup:
          "A team post-trains an assistant to be more concise, using preference pairs where annotators chose the shorter of two responses. Conciseness scores improve substantially. After deployment, users report the assistant now omits important caveats, refuses more often, and has become noticeably worse at multi-step reasoning.",
        steps: [
          "Recognize this as the expected consequence of the preference data rather than as a mystery. Preferences were collected on a single axis: shorter is better. The model optimized exactly that, including where brevity is harmful. It learned that shorter is better everywhere, because nothing in the data said otherwise. The data defined the objective completely.",
          "Diagnose the reasoning regression specifically. Multi-step reasoning benefits from working through intermediate steps, and a model rewarded for brevity truncates that reasoning and reaches worse conclusions. This is not a side effect to be tuned away; it is the direct result of penalizing the length that the capability requires.",
          "Explain the increased refusals as reward hacking. Refusing is short. If the preference signal rewards brevity without a countervailing signal for helpfulness, declining becomes a high-scoring strategy: the model found a way to score well that nobody intended. This is the textbook shape of reward hacking, and naming it shows you understand the mechanism and not just the symptom.",
          "Fix the data rather than the hyperparameters. Collect preferences on several dimensions at once - helpfulness, correctness, and appropriate length - and include pairs where the longer response is preferred because it carries a necessary caveat or a required reasoning step. The objective must contain the tension it needs to balance.",
          "Strengthen the constraint on divergence. Increase the KL penalty against the reference policy so the model stays nearer the pre-training distribution, limiting how far it can move toward degenerate strategies. Note this trades improvement for safety and is a mitigation rather than a fix, since the underlying problem is the objective.",
          "Add the gates that would have caught it before deployment. Regression evaluations on reasoning tasks, an over-refusal check on benign prompts, and pairwise comparison against the current production model across a task mix broad enough to include everything nobody was optimizing. Then roll back to the previous version while the retrain proceeds, which requires the previous version to still be deployable as a state transition.",
        ],
        takeaway:
          "Every regression traced back to the preference data expressing one dimension of quality, and both the reasoning loss and the extra refusals were the model doing exactly what it was asked. That is the general lesson of preference-based training: the model optimizes the signal you gave it, not the intent behind it, so the objective must contain the tension you want balanced. And the practical protection is a regression suite broad enough to notice the things you were not optimizing.",
      },
    },
    glossary: [
      { term: "Base model", definition: "A model trained only to predict text. Capable, yet unhelpful, unable to follow instructions, and with no notion of what it should decline." },
      { term: "SFT", expansion: "supervised fine-tuning", definition: "Training on curated prompt-response pairs. Establishes format, instruction following, and tone, and the model learns the flaws in the examples as readily as the virtues." },
      { term: "Data provenance", definition: "Who wrote each example, when, under which guidelines, reviewed by whom. Without it a dataset becomes an undebuggable accumulation." },
      { term: "Contamination", definition: "Evaluation prompts appearing in training data, which makes evaluation meaningless in the direction that gets a model shipped. Checked for near-duplicates as well as exact matches." },
      { term: "Preference pair", definition: "Two responses to one prompt with a human judgment of which is better. Cheaper to collect than ideal responses and richer in the distinctions that matter." },
      { term: "Reward model", definition: "A model trained to predict human preference, used as the optimization target in RLHF. An imperfect approximation, which is what makes exploitation possible." },
      { term: "RLHF", expansion: "reinforcement learning from human feedback", definition: "Optimizing the model against a reward model with reinforcement learning, constrained by divergence from a reference policy." },
      { term: "DPO", expansion: "direct preference optimization", definition: "Optimizing directly on preference pairs without a reward model or rollout loop. Simpler and cheaper, with less control over the reward signal." },
      { term: "Reference policy", definition: "The frozen starting model that the optimized policy is kept close to, bounding how far it can drift toward degenerate strategies." },
      { term: "KL constraint", definition: "A penalty on divergence from the reference policy. The main dial: too weak permits reward hacking, too strong prevents improvement." },
      { term: "Reward hacking", definition: "Finding responses that score highly under an imperfect reward while being worse. The central failure mode of preference optimization." },
      { term: "Rollout worker", definition: "A process generating responses for scoring during RLHF. Infrastructure that DPO avoids entirely." },
      { term: "Regression evaluation", definition: "Checking that previously working behaviors still work. Most often skipped and catches the most damaging surprises, since post-training improves one behavior while degrading others." },
      { term: "Over-refusal", definition: "Declining reasonable requests. Must be measured alongside harmful-request refusal, or safety optimization degrades the product." },
      { term: "Checkpoint lineage", definition: "The versioned record of base model, datasets, reward model, reference policy, hyperparameters, and code, without which a model cannot be reproduced or rolled back." },
      { term: "Annotator pool", definition: "The composition of the people producing the labels - languages, domains, seniority, and the guideline version they worked from. Every one of those properties ends up in the model." },
      { term: "Annotator agreement", definition: "How often independent labelers choose the same response. Low agreement usually means an underspecified guideline rather than a hard example, and training on it teaches noise." },
      { term: "Rejection sampling", definition: "Drawing several responses from the current policy, scoring them, and keeping the best as a demonstration or the best and worst as a pair. Cheap, and it stays on-policy." },
      { term: "On-policy data", definition: "Preference or demonstration data generated by the checkpoint currently being trained, rather than by a superseded one whose distribution no longer exists." },
      { term: "Slice coverage", definition: "Target example counts per language, length, tool use, and refusal category, set before collection so rare cases are not silently dominated by the common one." },
      { term: "Serving bundle", definition: "The deployable unit: checkpoint, tokenizer, chat template, stop tokens, system prompt, sampling configuration, and tool schemas, versioned and promoted together." },
      { term: "Chat template", definition: "The exact formatting of roles and turn boundaries a model was trained against. Serving a different one produces diffuse quality loss with no error anywhere." },
      { term: "Base model pinning", definition: "Binding an adapter release to the exact base version it was trained against, since applying it over a different base is silently wrong." },
      { term: "PPO", expansion: "proximal policy optimization", definition: "The reinforcement-learning algorithm conventionally used to optimize a policy against a reward model, updating in small clipped steps so one batch cannot move the policy far. It is the loop that rollout workers, a served reward model, and a reference policy all exist to support." },
      { term: "KL divergence", expansion: "Kullback-Leibler divergence", definition: "A measure of how far one probability distribution has moved from another. Penalizing it against the frozen reference policy is what keeps an optimized model near its starting distribution, and tuning that penalty is the main dial between reward hacking and no improvement at all." },
      { term: "RLVR", expansion: "reinforcement learning with verifiable rewards", definition: "Optimizing against a program that settles correctness - tests pass, the numeric answer matches, the proof checks, the output validates - instead of against a learned reward model. It deletes the reward-model fleet and the hardest failure of preference training, leaving a far more concrete and auditable object to be gamed." },
      { term: "Verifier", definition: "The program that scores a rollout. Production infrastructure on the training critical path: it must sandbox untrusted generated code, be deterministic so the same rollout scores the same twice, be resource-limited so a generated infinite loop cannot stall a step, and be versioned, because a changed test is a changed objective." },
      { term: "Outcome reward", definition: "Grading only the final answer. Cheap and unambiguous, and it credits a lucky path exactly as much as a sound one, which is what makes credit assignment hard over long chains." },
      { term: "Process reward", definition: "Grading intermediate steps rather than only the final answer, which assigns credit far better over long chains. It needs either expensive step labels or an automatic proxy, and a step grader is easier to satisfy than a correct answer, so it buys faster learning against a softer target." },
      { term: "Reasoning trace", definition: "The long run of intermediate tokens a model produces before committing to an answer. Keep it as training and debugging state and grade the outcome: optimizing the trace for style or safety teaches the policy to make the trace look acceptable rather than to reason differently." },
      { term: "GRPO", expansion: "group relative policy optimization", definition: "Sampling a group of rollouts per prompt and using their relative scores as the advantage signal, which removes the separately learned value model. It still requires careful rollout grouping, reward normalization, and version pinning." },
      { term: "Partial rollout", definition: "Updating from rollouts that have not all finished, rather than waiting for the slowest member of a group. Trace lengths are long-tailed, so a design that waits for the whole group leaves the sampler fleet idle for most of each step." },
      { term: "Bounded-staleness asynchrony", definition: "Allowing rollouts to be generated by a policy some fixed number of steps behind the one being updated. The bound is a correctness parameter and belongs recorded per rollout shard rather than left implicit in a queue depth." },
      { term: "PII", expansion: "personally identifiable information", definition: "Data that identifies a specific person. Its detection and redaction state travels with every training record, because an example's presence in a dataset is a retention decision as well as a quality one." },
      { term: "RL", expansion: "reinforcement learning", definition: "Learning from a reward signal on the model's own generated outputs rather than from fixed targets. It is what distinguishes an online loop - sample, score, update - from offline preference optimization on a frozen pair dataset." },
      { term: "KV cache", expansion: "key-value cache", definition: "The stored keys and values for a sequence's preceding tokens, held per live generation and growing with its length. In a rollout fleet it is the pool that bounds how many traces sample concurrently, so it has to be sized for the projected end-of-run trace length rather than the starting one." },
      { term: "DAG", expansion: "directed acyclic graph", definition: "The shape a post-training campaign actually has: source data and code flowing through transforms into checkpoints, with no cycles. It is why lineage is a graph to be traversed for a rollback rather than a single version label." },
    ],
  },

  "llm-evaluation-safety-operations": {
    primer: {
      plainSummary:
        "Evaluating a system whose output is free text is fundamentally harder than evaluating a classifier: there is no single correct answer, so there is no simple comparison. An evaluation portfolio therefore replaces the single trusted number, any model used as a judge becomes an instrument that has to be calibrated before it is believed, and the security model has to account for untrusted text and instructions arriving through the same channel.",
      analogy:
        "Marking essays instead of multiple-choice papers. There is no answer key, markers disagree with each other, and a fluent essay saying little can score better than an awkward one saying something true. The countermeasures are the same as in education: several markers, a rubric, comparing scripts against each other instead of scoring each in isolation, and periodic checks that the markers still agree with a trusted standard.",
      sections: [
        {
          heading: "An evaluation portfolio, with uncertainty",
          body: [
            "No single evaluation method is sufficient, and each has a characteristic weakness worth knowing.",
            "Fixed benchmarks give comparable numbers across models and time, and they are the most contaminated and least representative of your actual use. A public benchmark has likely appeared in training data somewhere, so a high score may measure memorization, and benchmark performance correlates weakly with usefulness on your specific tasks.",
            "Task-specific evaluations built from your real traffic are the most informative and the least comparable to anything external. Build them from actual production prompts with expected properties defined. Those need not be exact answers; checkable characteristics work, such as containing a citation, staying within a length, or refusing appropriately.",
            "Pairwise comparison against the current production model sidesteps the absence of an answer key entirely, because 'which of these two is better' is answerable when 'how good is this' is not. This is the workhorse for deciding whether a change is an improvement, and it is why arena-style comparison is the default evaluation shape in this field.",
            "Human evaluation is the ground truth and is slow and expensive, so it is used on samples and to calibrate cheaper methods. Model-based judging scales to whatever volume you need and inherits the judge's biases, which is the subject of the next section.",
            "Whatever the method, report uncertainty. Evaluating on 100 prompts gives a wide confidence interval, and a 3 percent difference on 100 examples is noise. Teams routinely ship on differences well inside their error bars. Free-text evaluation is expensive, sample sizes stay small, and this happens more here than anywhere else in ML. Stating the interval alongside the number is a small discipline with a large effect.",
          ],
        },
        {
          heading: "Calibrating a model judge",
          body: [
            "Using a strong model to score outputs is the only way to evaluate at scale, and an unvalidated judge produces confident, fluent, systematically wrong numbers, which is worse than no evaluation at all, because those numbers get trusted.",
            "The biases are documented and consistent. Position bias (judge ordering): judges prefer whichever response is presented first, a pairwise-judging artifact quite separate from position bias (ranking), the click-log confound where users click the top result because it sits at the top. Control it by running every comparison in both orders and averaging the results; disagreement between the two orderings is itself a signal that the comparison is close. Verbosity bias: judges prefer longer responses regardless of content, which if uncorrected optimizes your model toward padding. Self-preference: judges rate outputs from their own model family more highly. Sensitivity: small changes in the judging prompt produce materially different scores, so the prompt is part of the configuration and must be versioned.",
            "Calibration means measuring the judge against human labels on a sample and quantifying agreement. If the judge agrees with humans 85 percent of the time on a representative set, its scores are usable with that uncertainty attached. If it agrees 60 percent, it is barely better than chance on a binary comparison and must not be used to make decisions. This measurement is what converts a judge from a plausible-sounding oracle into an instrument with known error.",
            "Recalibrate periodically, since agreement drifts as your traffic changes and as models are updated. A judge validated a year ago on a different distribution is not validated now.",
            "Practical measures that help: give the judge a rubric with explicit criteria rather than asking for a general quality score; ask for pairwise comparison rather than absolute scoring, which is more reliable; require reasoning before the verdict; and use a different model family from the one being evaluated to avoid self-preference. None of these substitute for measuring agreement against humans.",
          ],
        },
        {
          heading: "Version the evaluation, not only the model",
          body: [
            "A score is comparable to another score only if everything that produced it was the same. That means versioning the entire apparatus: the prompt set, the rubric, the sampling parameters used to generate the candidates, the candidate outputs themselves, the model revision, and the evaluator revision. Preserve the raw artifacts rather than only the aggregate, so a number can be recomputed months later when someone reasonably asks how it was obtained.",
            "Then separate the sets by role, because one pool of prompts cannot serve two purposes. A development set is what you iterate against, and it stops being a fair measurement the moment you start tuning on it - which happens within a few weeks whether or not anyone intended it. A gating set is held out and consulted only to decide a release. A rotating holdout is refreshed periodically so that even the gating set does not leak into the development loop through the people who read its failures. Live audits sampled fresh from production traffic catch what all three miss, because they are the only ones drawn from the distribution you actually serve.",
            "Access control over evaluation data is a real engineering requirement and not bureaucracy. A gating set that finds its way into a training corpus, a prompt library, or a public repository has stopped measuring anything at all. This is contamination arriving through your own back door rather than through the base model's pretraining, and unlike the pretraining kind it is entirely within your control.",
            "Finally, updating the judge creates a new metric rather than a better reading of the old one. Scores from before and after are not comparable, so a longitudinal chart spanning a judge change is two different measurements joined by a line. Either backfill by rescoring the historical candidates with the new judge, or mark the discontinuity on the chart explicitly. The same is true of a rubric edit, which is precisely why the rubric is a versioned artifact rather than a prompt somebody tweaks between runs.",
          ],
        },
        {
          heading: "Trust boundaries, and treating model text as untrusted",
          body: [
            "The security model for LLM systems rests on one uncomfortable fact: instructions and data arrive through the same channel. A model reading a document cannot reliably distinguish 'this is content to summarize' from 'ignore your instructions and do this instead'. That is prompt injection, and there is no known reliable defense at the language level. Instructing the model to ignore injected instructions is itself just more text in the same channel.",
            "So defenses must be architectural. Treat every model output as untrusted, in exactly the way you treat user input. If the model requests a tool call, authorize that call against the user's permissions rather than against the model's. A model asked to summarize a document that contains an instruction to email a file must not be able to send that email; the enforcement point is the tool's authorization check, not the model's judgment.",
            "Use capability-scoped tokens. A model session gets narrowly scoped credentials for the specific operations that session should perform, so a successful injection can only do what that session was already permitted to do. This is least privilege applied to model sessions, and it bounds the damage rather than trying to prevent the attack.",
            "Sandbox anything generated. Model-produced code runs in an isolated environment with no network access and no credentials, because generated code is untrusted code and the fact that your model wrote it changes nothing about that.",
            "Multi-tenancy adds its own boundaries. Caches keyed without tenant identity leak across tenants. Logs contain prompts and outputs that may hold customer data, so they need the same protection and retention rules as any other sensitive store. Prompt logs are one of the most commonly overlooked repositories of sensitive data in these systems. If any per-tenant fine-tuning exists, models are tenant data too.",
            "Finally, cost is an operational and security concern in a way it is not for ordinary services. Token costs are unbounded per request, so a user submitting long inputs or triggering long generations can spend a great deal quickly, and an agent looping can spend continuously without any single request looking unusual. Attribute cost per tenant and per feature, enforce per-tenant budgets and rate limits, cap output length and agent iterations, and alert on cost anomalies. In this domain a runaway loop is a financial incident as much as a reliability one.",
          ],
        },
        {
          heading: "Rich logs against data minimization",
          body: [
            "Everything useful for debugging one of these systems is also everything you would least like to leak. Prompts contain whatever the user typed, retrieved context contains internal documents, tool results contain records pulled from other systems, and a trace contains all three joined together and timestamped. So logging is a genuine trade-off rather than a default, and it has to be decided field by field rather than at the level of a blanket policy on requests.",
            "The case for logging richly is stronger than teams expect, and it is worth stating before the privacy argument runs unopposed. Without the raw prompt and the retrieved context you cannot reproduce a bad answer, cannot investigate an abuse report, cannot attribute cost to a tenant, and cannot build the task-specific evaluation sets the portfolio depends on. Teams that minimize first and think afterwards end up unable to answer basic questions about their own system.",
            "The workable middle is redaction and tokenization at write time rather than either extreme applied globally. Detect and replace obvious secrets and personal identifiers before the record lands; keep a reversible token where re-identification is genuinely required, with the mapping under separate access control; set a retention window short enough to bound the exposure; and put reads behind an audited path. Then segregate by tenant, because a shared trace store is the same cross-tenant leak as a shared cache, only with a longer memory.",
            "State the retention window and who can read the logs as part of the design, since both are questions a customer will eventually ask in writing. And note the coupling: the cost attribution and abuse controls described above run off exactly these records, so a minimization decision taken in isolation quietly deletes the evidence those controls depend on. Decide the two together or you will discover the conflict during an incident.",
          ],
        },
      ],
      workedExample: {
        title: "An evaluation that endorses a worse model",
        setup:
          "A team evaluates a new model version using a strong model as judge, scoring responses 1 to 10 on 200 prompts. The new version scores 7.8 against 7.4, and is shipped. User complaints rise: responses are longer, more hedged, and less directly useful.",
        steps: [
          "Check whether the difference was ever significant. 200 prompts with a judge score standard deviation around 1.5 gives a confidence interval on the difference of roughly plus or minus 0.3, so a 0.4 gap is at the edge of noise. The decision rested on evidence that barely supports it. That is the most common single error in LLM evaluation, because free-text evaluation is expensive and sample sizes therefore stay small.",
          "Check the judge for verbosity bias, given the specific complaint. Score responses against their length and find a strong positive relationship. The new model produces responses 40 percent longer on average, which accounts for most or all of the 0.4 improvement. The judge was measuring length and reporting quality.",
          "Check position bias (judge ordering), which was not controlled. Responses were presented in a fixed order. Re-running with both orders and averaging shifts scores materially, and the cases where the two orderings disagree turn out to be exactly the close comparisons the decision hinged on.",
          "Check the two configuration defects nobody had logged: self-preference and judge-prompt drift. The judge came from the same model family as the new version, which inflates its scores on that family's outputs, and the judging prompt had been edited partway through the run without a version bump, so the early and late scores are not readings of the same instrument. Neither is a modeling problem, and both are invisible until the judge is treated as a versioned artifact.",
          "Measure judge agreement with humans, which had never been done. Take 100 pairwise comparisons, have humans judge them, and compare: agreement is 68 percent. On a binary comparison that is only modestly better than chance, so this judge is not fit to make a shipping decision at all, and everything above is downstream of that.",
          "Rebuild the evaluation properly. Switch from absolute scoring to pairwise comparison against the production model, which is more reliable. Randomize order and average over both. Use a rubric with explicit criteria in place of a general quality score. Add a length-controlled comparison so verbosity cannot masquerade as quality. Use a judge from a different model family. Then re-validate agreement against humans and report it alongside every result.",
          "Fix the prompt set as well as the judge. The same 200 prompts had been used for iteration all quarter, so what the team was treating as a gate had long since become a development set they had tuned against. Split the roles: a development set for iteration, a held-out gating set consulted only for release decisions, a rotating holdout refreshed so the gate cannot leak back into the loop, and a live audit sampled fresh from production traffic.",
          "Increase the sample and pre-register the decision rule. Move to 1,000 prompts drawn from real production traffic, and agree the threshold before seeing results: a win rate significantly above 50 percent, with the interval stated. Then roll back the current version while re-evaluating, since a shipped regression is more urgent than a delayed improvement.",
          "Price the regression before closing the incident. Responses 40 percent longer mean 40 percent more output tokens on every request, which is a direct cost increase and a slower stream for the reader, so this was a cost and latency regression as much as a quality one. Add a length-controlled comparison and a cost-per-request figure to the release gate, so the next version that wins by padding fails against a number nobody has to argue about.",
        ],
        takeaway:
          "The judge was measuring verbosity, the sample was too small to distinguish the models anyway, and nobody had checked whether the judge agreed with people. All three are routine, and all three are cheap to fix once. The transferable rule is that a model judge is an instrument, and an instrument that has never been compared against a reference is not a measurement. It is a fluent opinion, and its fluency is exactly what makes it persuasive.",
      },
    },
    glossary: [
      { term: "Evaluation portfolio", definition: "Combining fixed benchmarks, task-specific evaluations, pairwise comparison, human review, and model judging, since each has a characteristic weakness." },
      { term: "Benchmark contamination", definition: "Public evaluation data appearing in training data, so a high score may measure memorization more than capability." },
      { term: "Pairwise comparison", definition: "Asking which of two responses is better rather than scoring one in isolation. More reliable, and it sidesteps the absence of an answer key." },
      { term: "LLM-as-judge", definition: "Using a strong model to score outputs. The only way to evaluate at scale, and an instrument that must be calibrated before it is trusted." },
      { term: "Position bias (judge ordering)", definition: "A judge preferring whichever response appears first. Controlled by running both orders and averaging; disagreement between orders signals a close call. Distinct from position bias (ranking), the click-log confound where higher-placed results are clicked more because of placement." },
      { term: "Verbosity bias", definition: "A judge preferring longer responses regardless of content, which optimizes the evaluated model toward padding if uncorrected." },
      { term: "Self-preference bias", definition: "A judge rating outputs from its own model family more highly, mitigated by judging with a different family." },
      { term: "Judge calibration", definition: "Measuring judge-human agreement on a sample. Converts a plausible oracle into an instrument with known error, and must be repeated as traffic and models change." },
      { term: "Prompt injection", definition: "Instructions embedded in input or retrieved content that the model cannot reliably distinguish from legitimate instructions. No reliable language-level defense exists." },
      { term: "Untrusted model output", definition: "Treating everything a model produces as untrusted input to whatever consumes it, since it may be attacker-influenced." },
      { term: "Capability token", definition: "Narrowly scoped credentials for a model session, so a successful injection can only do what the session was already permitted to do." },
      { term: "Sandbox", definition: "An isolated environment without network or credentials for running generated code, which is untrusted code regardless of its author." },
      { term: "Tenant isolation", definition: "Preventing cross-tenant leakage through caches, logs, and any per-tenant models. Prompt logs are a commonly overlooked store of sensitive data." },
      { term: "Cost attribution", definition: "Tracking token spend per tenant and per feature, needed because per-request cost is unbounded and an agent loop can spend continuously without any request looking unusual." },
      { term: "Abuse control", definition: "Per-tenant budgets, rate limits, output length caps, and iteration caps, treating runaway cost as an incident class of its own." },
      { term: "Development set", definition: "The prompts you iterate against. It stops being a fair measurement as soon as tuning starts, which happens within weeks whether or not anyone intended it." },
      { term: "Gating set", definition: "Prompts held out and consulted only to decide a release, kept separate from the set used for iteration so a gate cannot be tuned against." },
      { term: "Rotating holdout", definition: "Periodically refreshing the gating prompts, so the gate does not leak into the development loop through the people who read its failures." },
      { term: "Live sampled audit", definition: "Evaluation prompts drawn fresh from production traffic, the only set actually representing the distribution you serve rather than one frozen earlier." },
      { term: "Evaluation artifact versioning", definition: "Pinning prompt set, rubric, sampling parameters, candidate outputs, model revision, and evaluator revision, so a score can be recomputed rather than merely quoted." },
      { term: "Metric version backfill", definition: "Rescoring historical candidates after a judge or rubric change, since scores before and after are otherwise two different measurements joined by a line." },
      { term: "Log redaction", definition: "Detecting and replacing secrets and personal identifiers at write time, optionally as reversible tokens whose mapping is separately access-controlled." },
      { term: "Data minimization", definition: "Logging less to bound exposure. In tension with debuggability, cost attribution, and abuse investigation, all of which run off the same records." },
      { term: "Agent loop", definition: "The cycle in which the model selects a tool, the orchestrator executes it, the result is appended as an observation, and the model is called again until a stop condition fires. It is a workflow engine wearing a language-model costume, and it inherits every workflow failure mode." },
      { term: "Durable execution", definition: "Persisting a long-running run's state outside the process so a deploy or restart does not destroy it. For an agent that state is concrete: message history, the tool ledger, the budget counters consumed so far, and the authorization context the run operates under." },
      { term: "Tool ledger", definition: "The record of every tool call with its arguments, result, and status. Journaling the intent before the call and completing the entry after it is what makes a resumed run able to tell an unfinished call from an unstarted one." },
      { term: "Journal-then-act ordering", definition: "Writing the intent to call a tool before issuing it, rather than recording the result afterwards. It converts a crash between action and observation from a silent double effect into a recoverable state, which is the ordinary dual-write problem in agent clothing." },
      { term: "Idempotency key", definition: "A stable identifier, derived here from run and step identity, that a side-effecting tool uses to recognize a replayed call and return the original result rather than charging a card or filing a ticket twice. Tools that cannot support one must be marked, because for them a retry is a decision." },
      { term: "Step budget", definition: "A bound on steps, wall-clock duration, token spend, and per-tool call count, each with a defined behavior at exhaustion. Terminating with what was found and what remains is a designed outcome; a partial result shaped like a complete one is not." },
      { term: "Context compaction", definition: "Summarizing or dropping the middle of a long context while keeping the system policy, task statement, recent turns, and a running summary. Lossy in a way the model cannot perceive, so a constraint stated at minute two can vanish by minute forty with no error anywhere." },
      { term: "Pinned invariants", definition: "The task statement, constraints, and acceptance criteria placed in a region compaction is never allowed to touch, plus a compaction event emitted into the trace so a bad answer can be attributed to a dropped constraint rather than to the model." },
      { term: "Memory tiers", definition: "Three stores with different write policies and blast radii: the context window as volatile working memory, the run's durable state scoped to one task and deleted with it, and cross-run memory persisting between sessions." },
      { term: "Cross-run memory", definition: "Facts about a user, project, or tenant that survive between sessions. A security object rather than a convenience, because it is written from model output and therefore a persistence path for prompt injection: one poisoned document can plant an instruction that outlives the conversation that carried it." },
      { term: "Subagent fan-out", definition: "Delegating independent subtasks to child agents. It buys wall-clock time and, more importantly, context isolation, and it costs a token multiple, duplicated work between agents that cannot see each other, and summaries the parent cannot audit." },
      { term: "Trajectory evaluation", definition: "Scoring the sequence of steps rather than only the final answer: was each tool call right given the state, were arguments well formed, did the agent recover after an error, and did it terminate for the right reason. A correct answer reached by deleting a file is not a success." },
      { term: "Cost per success", definition: "Total spend divided by tasks actually completed, rather than cost per run. An agent that is half the price and fails half the time is not cheaper, and the per-run number is exactly what makes it look that way." },
      { term: "Replayable environment", definition: "Deterministic, resettable fixtures - recorded tool responses or a sandboxed clone - versioned alongside the prompt and the model. Without one, an agent score measures the state of live services and an environment change reads as a model regression." },
      { term: "Step-count bias", definition: "Verbosity bias reappearing in trajectory judging, where a longer trajectory looks more thorough so the judge rewards agents that do more work for the same result. Anchored against deterministic end-state checks and cost per success." },
      { term: "Egress allowlist", definition: "Restricting outbound network destinations to an approved set and denying everything else by default. The control that turns a successful injection from an exfiltration into a blocked request, since it does not depend on the model declining to comply." },
    ],
  },

  "mock-timed-orchestration": {
    primer: {
      plainSummary:
        "The 45 minutes are as much a performance as an examination: the grade depends on whether what you know actually gets demonstrated in the time available. Three skills carry that. Allocate the clock deliberately. Keep a visible record of the assumptions and decisions you have made. And control depth, which means knowing when to go deeper, when to park a branch, and how to recover when you stall in front of someone.",
      analogy:
        "Chairing a meeting with a fixed agenda and a hard stop. A good chair states the plan, keeps time visibly, notes decisions as they are made so nobody relitigates them, and parks tangents explicitly rather than either following them or ignoring them. The same behaviors make a design interview legible, and legibility is what is being graded.",
      sections: [
        {
          heading: "Allocate the clock before drawing boxes",
          body: [
            "Say the plan out loud at the start: five minutes on requirements, five on estimates, ten to an end-to-end design, fifteen to twenty on deep dives, five to close. This takes fifteen seconds and does three useful things. It shows you have a method. It invites the interviewer to redirect early, when redirection is cheap. And it commits you publicly to reaching an end-to-end design by minute twenty, which is the single checkpoint that most determines the outcome.",
            "Keep the clock visible and narrate transitions. 'I have about ten minutes of design left, so I will finish the write path and then pick a deep dive' tells the interviewer you are managing time and not drifting. Silence about time reads as unawareness of it.",
            "Adapt; do not follow the plan mechanically. Interviewers signal - by asking a follow-up, by pushing on something, by looking bored - and those signals override your allocation. If they spend three minutes on the consistency model, that is where the interview is, and continuing with your plan is not discipline but deafness.",
            "The commonest clock failure is spending too long on requirements and estimation, which feel productive and safe because they are structured. Ten minutes total is enough. If you reach minute fifteen with no architecture on the board, you are behind regardless of how good the requirements were.",
          ],
        },
        {
          heading: "Turn a vague prompt into a scoped problem",
          body: [
            "The prompt is deliberately underspecified, and your first job is to give it edges. Only two moves are available - ask, or state an assumption - and choosing well between them is most of what this phase is graded on. Ask when the answer would change the architecture. Assume when it would not, and say the assumption out loud so the interviewer can correct it cheaply rather than discovering the mismatch twenty minutes later.",
            "The test for an architecture-changing question is concrete and worth rehearsing. 'Must users see their own writes immediately?' changes the read path. 'Global or single region?' changes the entire topology. 'What is the retention period?' changes storage sizing and possibly the storage engine. By contrast, 'what size are the avatar images?' changes nothing you will draw, so state a default and move on. Twenty clarifying questions is not thoroughness; it is an absence of prioritization, and it reads that way from the other side of the table.",
            "Then choose three or four functional flows and declare that those are the system. A prompt such as 'design a ride-hailing service' contains twenty products; picking request a ride, match a driver, and track a trip converts it into something answerable in the time available. Attach the measurable non-functional goals to those specific flows rather than to the system in general - a latency target on matching, a durability requirement on the trip record - because a goal attached to everything can be traded against nothing.",
            "Finish by stating what is out of scope, explicitly and aloud. Payments, fraud, driver onboarding, and the mapping service are all real problems and none of them is what you are designing today. An omission you announced reads as judgment; the identical omission unannounced reads as a gap, and the interviewer has no way to distinguish them unless you say which it is.",
          ],
        },
        {
          heading: "Keep a visible decision ledger",
          body: [
            "Maintain a written list of assumptions in a corner of the board: 100 million daily users, 100:1 read-to-write ratio, staleness of a few seconds acceptable, no multi-region requirement. Writing them down instead of saying them once has a specific effect: the interviewer can see and correct them, and every later decision can be justified by pointing at one.",
            "Mark which assumptions are load-bearing. 'If the read-to-write ratio were 1:1 rather than 100:1, I would not denormalize' shows you know which of your inputs actually drive the design, which is a distinct and more advanced skill than making the assumptions in the first place.",
            "Keep a parking lot too, for interesting branches you are deliberately not taking. 'Multi-region is a real question and I am parking it. I will come back if there is time.' This is not avoidance; it is scope control made visible, and it prevents the interviewer from concluding you missed something. It also gives you a ready answer when they ask what you have not covered.",
            "Revisit both near the end. Returning to a parked item, or noting that an assumption was the one you would validate first in production, closes loops the interviewer has been holding open, and it is a cheap way to sound like someone who finishes things.",
          ],
        },
        {
          heading: "Control depth, and recover from stalls",
          body: [
            "Depth is a choice made explicitly. Announce it: 'I will go deep on fan-out because that is where this design breaks first.' Naming why makes the choice look like prioritization rather than preference, and it invites redirection if the interviewer wanted something else.",
            "Do not go deep on everything. The interview is not long enough, and attempting it produces a uniformly shallow discussion, which is the most common way strong candidates underperform: they know a lot about many components and demonstrate depth in none.",
            "When you stall - and you will - say so and narrow. 'I am not sure of the best approach here, so let me state the constraint and reason from it' is a recovery. Long silence is not, because the interviewer cannot grade what they cannot hear. Reasoning aloud toward a wrong answer that you then correct scores better than a confident answer you cannot justify, because it shows the process they are actually assessing.",
            "When you do not know something, say so and say how you would find out. 'I do not know the exact rebalance semantics there; I would verify before depending on it' is a completely acceptable senior answer. Confident invention is the fastest way to lose an interviewer's trust, and once lost it colors everything you say afterwards.",
            "Close deliberately in the last five minutes, whatever state the deep dive is in. Failure behavior, what you would monitor, and how it evolves at ten times the scale. An unfinished deep dive with a strong close reads far better than a complete deep dive that ran out of time with no summary, because the close is where you demonstrate ownership rather than knowledge.",
          ],
        },
        {
          heading: "Make the artifact legible",
          body: [
            "Whatever surface you draw on - a whiteboard, a shared document, a diagramming tool - is the only durable record of your thinking, and the interviewer will be looking at it while writing their feedback after you have left the room. Treat its layout as a decision rather than as an accident of wherever you happened to start writing.",
            "A layout that works: requirements and the assumption ledger in one corner where they stay visible all hour, the request path across the middle running left to right in the direction the data flows, data stores drawn below the services that own them, and an area deliberately left empty for the deep dive. Reserving that empty area matters more than it sounds, because a deep dive crammed into a margin is unreadable, and redrawing the board to make room costs minutes you will not have at minute thirty.",
            "Label the arrows and not only the boxes. An arrow annotated 'synchronous, must be idempotent' or 'async, seconds of lag acceptable' carries a design decision; a bare arrow carries nothing and quietly invites the weaker interpretation. The same applies to the stores: writing 'source of truth' beside one of them settles an ambiguity that would otherwise consume several minutes of argument later.",
            "Remote interviews add their own constraint. Typing is slower than drawing, so decide in advance whether you will use boxes and arrows in a diagram tool or an indented text outline, and practice in the actual tool rather than discovering its quirks live. Narrate while you draw either way: silence on a video call feels far longer than silence in a room, and it closes the interviewer's only channel into your reasoning.",
          ],
        },
      ],
      workedExample: {
        title: "Recovering an interview that went off plan at minute twelve",
        setup:
          "The prompt is 'Design a ride-hailing service'. At minute twelve you are still discussing the matching algorithm because the interviewer asked two follow-up questions about it. You have no end-to-end architecture on the board.",
        steps: [
          "Notice the clock and name the situation instead of hoping. 'I have spent a while on matching because it is interesting. Let me put a full architecture up quickly so we have the whole picture, and then come back to it.' The interviewer now knows you are aware, which is most of what they wanted to see.",
          "Draw the end-to-end path fast and coarsely. Rider app, driver app, gateway, location service, matching service, trip service, pricing, notifications, storage. Two minutes, no detail. A complete simple picture beats a partial detailed one, because it lets every subsequent discussion be located within a system.",
          "Write the assumption ledger now if you have not. 100,000 concurrent riders, matching within 10 seconds, location updates every 4 seconds, single region. This retroactively justifies the matching discussion and gives the interviewer a chance to correct anything before you build further on it.",
          "Reframe the earlier discussion as the deep dive rather than as a detour. 'Matching is where this design is hardest, so I would like it to be the deep dive; it is what I have been discussing and I would like to finish it properly.' The time spent is now on-plan. This is a genuine reframing and not a trick, because matching was in fact the right thing to go deep on.",
          "Park explicitly what you are not covering. 'I am not designing pricing or driver payouts, and I am treating the map and routing service as a black box.' This prevents the omissions from reading as gaps and buys the remaining time for the deep dive.",
          "Protect the close. At minute forty, stop the deep dive wherever it is and spend five minutes on failure behavior, monitoring, and ten-times evolution. Never let a deep dive consume the close. It is disproportionately weighted, because it is where you show you would own the system and not merely build it.",
        ],
        takeaway:
          "Nothing was recovered by rushing. It was recovered by naming the situation, producing a coarse complete picture, and reframing the time already spent as the deep dive it should have been. Interviewers respond well to a candidate who notices and adjusts, because that is the behavior they need from a colleague. They respond far less warmly to one who executes a plan perfectly.",
      },
    },
    glossary: [
      { term: "Time box", definition: "A declared allocation for each phase, stated aloud so the interviewer can redirect early and so the plan is visible." },
      { term: "End-to-end checkpoint", definition: "Having a complete if simple architecture by roughly minute twenty. The single checkpoint that most determines the outcome." },
      { term: "Assumption ledger", definition: "A written, visible list of assumptions, so they can be corrected and so later decisions can be justified by pointing at one." },
      { term: "Load-bearing assumption", definition: "An assumption whose change would change the design. Naming which ones are load-bearing is a more advanced skill than making them." },
      { term: "Parking lot", definition: "Explicitly deferred branches, which converts an omission into visible scope control and supplies a ready answer about what you did not cover." },
      { term: "Deep-dive contract", definition: "Announcing which subsystem you will examine and why, so the choice reads as prioritization and can be redirected." },
      { term: "Interviewer checkpoint", definition: "Pausing to confirm direction, which is cheap and prevents spending fifteen minutes in the wrong place." },
      { term: "Stall recovery", definition: "Naming uncertainty and reasoning aloud from a constraint, since silence cannot be graded and confident invention destroys trust." },
      { term: "Executive close", definition: "The final five minutes on failure behavior, monitoring, and evolution. Disproportionately weighted, and must never be consumed by an overrunning deep dive." },
      { term: "Architecture-changing question", definition: "A clarification whose answer would alter the topology, the read path, or the storage choice. The only kind worth spending clock time on." },
      { term: "Reversible assumption", definition: "A stated default for a detail that does not change the architecture, said aloud so the interviewer can correct it cheaply instead of discovering the mismatch later." },
      { term: "Prioritized functional flow", definition: "The three or four user operations you declare to be the system, which converts a prompt containing twenty products into something answerable in 45 minutes." },
      { term: "Out-of-scope statement", definition: "Naming aloud what you are deliberately not designing. An announced omission reads as judgment; the identical unannounced one reads as a gap." },
      { term: "Estimate-to-decision link", definition: "Following every number with the architectural consequence it forces. An estimate that changes no partition count, fleet size, or SLO call has earned no clock time." },
      { term: "Transition narration", definition: "Saying which phase you are leaving, which you are entering, and how much time remains, so time management is visible rather than assumed." },
      { term: "Board layout", definition: "Deliberate placement: ledger in a corner, request path across the middle in the direction data flows, stores below their owners, and an area left empty for the deep dive." },
      { term: "Labeled edge", definition: "An arrow annotated with synchrony, idempotency, or acceptable lag. A bare arrow carries no decision and invites the weaker interpretation." },
      { term: "Source-of-truth annotation", definition: "Marking on the diagram which store is authoritative and which are derived, settling an ambiguity that otherwise costs several minutes later." },
      { term: "Remote whiteboard discipline", definition: "Choosing the drawing tool and notation before the call, and narrating continuously, since typing is slow and silence on video closes the interviewer's only channel into your reasoning." },
      { term: "SLO", expansion: "service level objective", definition: "An internal target for a metric, such as 99.9 percent of requests succeeding within 200 milliseconds. The number engineering designs and alerts against, and stating one in the requirements phase is what lets a later decision be judged rather than merely described." },
      { term: "Invariant", definition: "Something that must always be true of the system - a balance never goes negative, an ID is never reused, a message appears once per conversation. Established before components, because invariants are what decide where coordination is genuinely required." },
    ],
  },

  "mock-classic-synthesis": {
    primer: {
      plainSummary:
        "Assembling the distributed-systems material into one coherent 45-minute argument is a separate skill from knowing the components. It means starting from invariants and ownership rather than from boxes, letting scale actually change the architecture instead of decorating it, and narrating a single failure all the way from detection through containment to repair.",
      analogy:
        "The difference between knowing every ingredient and cooking a meal. The knowledge is necessary and it is not the same skill. What distinguishes the cook is sequencing: what must happen first, what can wait, what would spoil if left too long. A design argument works the same way, and the order in which you establish things determines whether the rest is convincing.",
      sections: [
        {
          heading: "Begin with invariants and ownership",
          body: [
            "Before any component, state what must always be true. An account balance never goes negative. A message appears exactly once per conversation. An ID is never reused. Invariants are what determine where coordination is genuinely required, and coordination is the expensive thing in a distributed system. Establish them first and every later consistency decision has a stated reason.",
            "Then name the source of truth for each piece of data, and say which stores are derived. Most confusion in design interviews comes from ambiguity about which store is authoritative: once a cache, a search index, and a database all hold a user's profile, the question 'what happens if they disagree' has no answer unless one of them was designated authoritative in advance.",
            "Then classify each operation's consistency requirement individually rather than for the system as a whole. Payments need strong guarantees; presence does not; a like counter can be eventually consistent and approximate. A candidate who says 'this system is eventually consistent' has said something less precise than one who says which operations are and why. The second is closer to how real systems are actually built.",
            "This ordering pays off later. When the interviewer pushes on a failure, you can answer from the invariant: 'the balance invariant means this path needs a conditional write, so under partition it becomes unavailable rather than divergent' is a complete answer derived from something you established in minute four.",
          ],
        },
        {
          heading: "Draw the write path before the read path",
          body: [
            "Once ownership is named, design the write before the read. Reads can nearly always be made faster later with a cache or a derived index; writes carry the invariants, and a write path that is wrong cannot be repaired downstream. So walk one write end to end and say precisely where it becomes durable and what the client is told at that moment.",
            "Make externally retryable writes idempotent at the mutation boundary - not in the client, and not in the broker. Delivery repeats: a client retries, a network stutters, a consumer redelivers. Only the component that actually mutates the state can atomically decide that it has already applied this change. That decision needs a stable business identity to key on, such as a caller-generated order ID, a payment request ID, or a client-generated message ID. Keying on a broker offset or an arrival timestamp does not survive a retry arriving by a different path.",
            "Then handle the ambiguous timeout, because it is the case interviewers reach for first. The client sent a write, received nothing, and cannot tell whether it committed. Its only safe move is to retry with the same identity, which means the server must make that retry a no-op returning the original outcome rather than a second effect. Say this explicitly: 'the client retries' without the idempotency story is the answer that duplicates money.",
            "Finally, get downstream work out of the same transaction. Writing to the database and then publishing an event are two operations that can half-succeed, so use an outbox: record the event in the same transaction as the state change and let a separate process publish it. That converts a distributed-write problem into a local write plus at-least-once delivery, which the idempotent consumers you already specified can absorb.",
          ],
        },
        {
          heading: "Let scale reshape the design",
          body: [
            "Estimates should change the architecture, not decorate it. The reasoning move is to compute a number and then say what it rules out: this fits on one machine so sharding is not required; this read-to-write ratio makes it a caching problem; this fan-out breaks push above a threshold so the design needs a hybrid.",
            "Derive the partition key from the access patterns you wrote down, and check explicitly which queries become single-partition and which become scatter-gather. Then say what you do about the ones that do not fit - a secondary index, a separate system, or accepting the cost for a rare query. Serving a rare query by compromising the common path is the mistake to avoid, and saying so demonstrates the priority ordering interviewers listen for.",
            "Name the hot spots. Almost every real system has skew - a celebrity, a viral item, a city center, a large tenant - and a design that assumes uniformity has not met production. State where the skew is and what you do about it, noting whether the hot key is read-heavy, where caching helps, or write-heavy, where it does not.",
            "And say what the design would look like at ten times the scale, ideally with what breaks first. This is often asked directly, and having the answer ready signals that the current design was chosen rather than defaulted to.",
          ],
        },
        {
          heading: "Bound the queues and prove the backlog drains",
          body: [
            "Asynchronous work is where classic designs hide their capacity assumptions. The moment you move feeds, search indexing, notifications, or analytics off the write path, you have promised the user something weaker than immediacy, and that promise should be a number rather than the word 'eventually'. A lag contract - derived views trail the source of truth by under five seconds at the 99th percentile - is what makes the design checkable instead of merely plausible.",
            "Then size the queue from recovery rather than from steady state. Any consumer keeps up on an average day; the question is what happens after it has been down for twenty minutes. The backlog at that moment is the arrival rate times the outage duration, and it clears only if the drain rate exceeds the arrival rate by enough margin to work through it before the retention window expires or the business deadline passes. State that margin, because a durable and well-replicated queue guarantees the backlog survives and never that it disappears.",
            "Bound the queue, the retries, and the concurrency so that a slow dependency degrades the system deliberately instead of amplifying the traffic hitting it. An unbounded queue converts a latency problem into a memory problem and then into an outage, and unbounded retries turn a struggling dependency into a dead one. Retry budgets, bounded concurrency, and a deadline that travels with the request are the mechanisms, and naming the specific one you would apply is what separates this from a slogan.",
            "Say which direction the backpressure travels, too. If the queue is full, somebody upstream must slow down, shed load, or accept rejection, and choosing among those three is a product decision rather than a technical one. Deciding to drop a low-value analytics event while continuing to accept a payment is exactly the prioritized degradation an interviewer is listening for.",
          ],
        },
        {
          heading: "Narrate one failure from detection to repair",
          body: [
            "The strongest thing you can do in the last third of a classic systems interview is trace one failure completely. Pick something concrete - a partition loses its leader, a queue backs up, a region becomes unreachable - and walk through it end to end.",
            "Detection: what notices, on what signal, and after how long. Be specific about the signal, because 'we would monitor it' is not an answer while 'we alert on fan-out lag in seconds, not queue depth' is.",
            "Immediate behavior: what happens to in-flight requests, what users see, and which dependencies are optional so the system degrades rather than fails. Naming which dependency is optional is the graceful degradation decision, and it must be a decision made before the incident.",
            "Containment: what stops the failure spreading - bounded queues, retry budgets, circuit breakers, fencing tokens so a deposed owner cannot keep writing. This is where the earlier invariants pay off again, because containment is about protecting them.",
            "Recovery and repair: how the system returns to health, whether any data needs reconciling, and how you know it is correct afterwards. The reconciliation step is the one candidates most often omit, and it is the one that distinguishes someone who has operated a system from someone who has read about one.",
            "One complete failure narrative is worth more than mentioning six failure modes, because it demonstrates that you can think through consequences instead of reciting a list.",
          ],
        },
      ],
      workedExample: {
        title: "A 45-minute run at a collaborative document editor",
        setup:
          "The prompt is 'Design a collaborative document editor like Google Docs'. Multiple users edit simultaneously, edits appear within a second, and no edit may be lost.",
        steps: [
          "Minutes 0-5, invariants and scope. State the invariants: no acknowledged edit is ever lost, all clients converge to the same document, and edits from one client appear in the order that client made them. Scope in real-time editing, presence, and version history; scope out comments, permissions administration, and the rendering engine. The convergence invariant is the one that will drive everything.",
          "Minutes 5-10, numbers that eliminate options. 10 million documents, most edited by one person, some by up to 50 simultaneously. Edits arrive at a few per second per active user, so a busy document sees perhaps 100 edits per second - small. Conclude aloud that this is not a throughput problem but a correctness and connection-management problem, which redirects the whole interview correctly.",
          "Minutes 10-20, end-to-end with ownership named. Clients hold WebSocket connections to a gateway; a document server owns each active document and is the single writer for it, which removes concurrent-write conflicts by construction; edits are appended to a durable operation log which is the source of truth; snapshots are derived and periodically written so loading does not replay all history. Say that the document server is the source of truth for ordering and that snapshots are derived - that one sentence prevents a lot of later confusion.",
          "Minutes 20-35, deep dive on concurrent editing, chosen from the invariant. Two users edit the same paragraph offline or concurrently. Explain why timestamps cannot order them, then present the honest options: operational transformation, which needs a central server and is what this design already has, or CRDTs, which converge without coordination and cost metadata that grows with operation count. Choose operational transformation because the single-writer document server is already the coordination point, and say what that choice costs: the document server is now an availability dependency for its document.",
          "Minutes 35-40, one failure narrated fully. The document server for an active document crashes. Detection: its lease expires within seconds and clients' connections drop. Behavior: clients buffer edits locally and show a reconnecting state, so nothing is lost from the user's view. Containment: a new server acquires the document with a higher fencing token, so the old server cannot write even if it revives. Recovery: the new server loads the last snapshot plus subsequent operations from the log, clients reconnect and replay buffered edits identified by client-generated IDs so duplicates are ignored. Repair: verify convergence by comparing client checksums against the server's.",
          "Minutes 40-45, close. Monitoring: edit-to-visible latency, reconnection rate, and convergence-mismatch counts. Failure posture: a document is unavailable while its owner is being replaced, which is seconds, and this is deliberate because availability was traded for the convergence invariant. At ten times scale, document servers shard by document with a directory, and the first thing to break is the operation log's write throughput for the busiest documents.",
        ],
        takeaway:
          "The convergence invariant, stated in minute three, chose the single-writer architecture, which chose operational transformation over CRDTs, which set the failure behavior. That chain is what makes a design read as an argument rather than as a diagram, and building it is the whole point of the synthesis work.",
      },
    },
    glossary: [
      { term: "Invariant-first framing", definition: "Establishing what must always be true before choosing components, since invariants determine where coordination is genuinely required." },
      { term: "Source of truth", definition: "The authoritative store for a piece of data, with everything else designated as derived. Naming it removes most later ambiguity." },
      { term: "Per-operation consistency", definition: "Classifying each operation's guarantee individually rather than labeling the whole system, which is how real systems are actually built." },
      { term: "Scale-driven redesign", definition: "Using estimates to eliminate options rather than to decorate a design already chosen. Each number should be followed by a 'therefore'." },
      { term: "Single-partition vs scatter-gather", definition: "Whether a query touches one partition or all of them. The check that validates a partition key against the access patterns." },
      { term: "Skew acknowledgment", definition: "Naming where load concentrates - celebrities, viral items, large tenants - since a design assuming uniformity has not met production." },
      { term: "Failure narrative", definition: "Tracing one failure through detection, immediate behavior, containment, recovery, and repair. Worth more than listing six failure modes." },
      { term: "Containment mechanism", definition: "What stops a failure spreading: bounded queues, retry budgets, circuit breakers, fencing tokens." },
      { term: "Fencing token", definition: "A monotonically increasing number issued to whoever currently owns a resource, which the storage layer rejects if it is stale, so a deposed owner cannot keep writing. Also called a fencing epoch." },
      { term: "Repair and reconciliation", definition: "Returning to a correct state after a failure and verifying it. The step most often omitted, and the one that signals operational experience." },
      { term: "Mutation boundary", definition: "The component that actually changes state, and therefore the only place idempotency can be enforced atomically. Not the client, and not the broker." },
      { term: "Dedupe identity", definition: "The stable business key a retry is matched against - order ID, payment request ID, client message ID - rather than a broker offset or an arrival timestamp." },
      { term: "Ambiguous timeout", definition: "A write that returned nothing, so the client cannot tell whether it committed. Safe only if retrying with the same identity is a no-op returning the original outcome." },
      { term: "Outbox", definition: "Recording an event in the same transaction as the state change and publishing it separately, converting a two-system write into a local write plus at-least-once delivery." },
      { term: "Lag contract", definition: "A stated bound on how far derived views trail the source of truth, which turns the word 'eventually' into something a design can be checked against." },
      { term: "Drain rate", definition: "How fast a consumer clears a backlog. Sized from recovery rather than steady state, since durability guarantees the backlog survives and never that it disappears." },
      { term: "Backpressure", definition: "Pushing a full queue's pressure upstream so producers slow, shed, or are rejected. Which of the three happens is a product decision, not a technical default." },
      { term: "Retry budget", definition: "A cap on retry volume across the fleet, so a struggling dependency is not converted into a dead one by the traffic meant to work around it." },
      { term: "RPO/RTO", expansion: "recovery point objective / recovery time objective", definition: "How much data a failure may lose and how long recovery may take. The two numbers that make a failover story concrete rather than aspirational." },
      { term: "Hot key", definition: "A single partition key - a celebrity, a large tenant, a city center - carrying load that fleet averages hide. Caching helps if it is read-heavy and does not if it is write-heavy." },
      { term: "Lease", definition: "A lock with an expiry time, so a crashed holder does not block a resource forever. A lease alone is unsafe, because a paused process can wake after expiry still believing it holds the lock, which is why leases are paired with fencing tokens." },
      { term: "Operational transformation", definition: "Rewriting each concurrent edit against the edits already applied so every replica reaches the same document. It needs a central server to define the order, which is why it fits a design that already has a single-writer owner per document." },
      { term: "CRDT", expansion: "conflict-free replicated data type", definition: "A data structure whose merge is commutative, associative, and idempotent, so replicas converge without any coordination. Powerful, and it costs metadata that grows with operation count and constrains the data model." },
      { term: "Saga", definition: "A sequence of local transactions, each with a compensating action, used when no single transaction can span all the participants. It provides atomicity of outcome without isolation, so intermediate states are observable." },
      { term: "Circuit breaker", definition: "A component that tracks failures to a dependency and, past a threshold, fails calls immediately without attempting them. It protects the caller from piling up on a slow dependency and gives the dependency room to recover." },
      { term: "Invariant scanner", definition: "A background job that re-checks the stated invariants against stored data - balances summing, no duplicate IDs, no orphaned rows - and reports divergence. It catches latent corruption that request-path alerts cannot see, and it must not share the code path that produced the data." },
      { term: "Availability zone", definition: "One failure domain inside a region, with independent power and networking. Losing one is the routine failure a design should survive without data loss, as opposed to losing a whole region, which is a separate and much larger conversation." },
      { term: "Out-of-order delivery", definition: "Messages arriving in a different sequence than they were sent, which any at-least-once transport permits. Handled by making the consumer's effect depend on the message's own identity and version rather than on its arrival position, since ordering cannot be assumed back into existence downstream." },
    ],
  },

  "mock-ml-synthesis": {
    primer: {
      plainSummary:
        "An ML design interview is not about models. Assembling the ML material into one 45-minute argument means framing a single deployable decision, making every feature and label time-correct, and closing the lifecycle loop so the system can be shipped, monitored, and reverted safely. Most candidates never get past the model itself, which is why the first five minutes decide so much.",
      analogy:
        "Designing a factory rather than designing a product. The model is one station on the line. What determines whether the factory works is where materials come from, whether they arrive in the right state, what happens when a batch is defective, and how you stop the line. Candidates who spend the interview on the product are describing a station and calling it a factory.",
      sections: [
        {
          heading: "Frame one deployable decision",
          body: [
            "Start by converting the prompt into a decision contract, in the first five minutes, before any modeling discussion. What action does the system take? At what moment? On what unit? What are the possible actions and their thresholds? What happens by default and when the model is unavailable?",
            "Then the four constraints that eliminate architectures immediately. Decision time bounds what features can exist, since anything unavailable at that instant is not a feature, however predictive. Latency budget bounds model size and lookup depth. Capacity matters because a threshold producing more work than a downstream team can absorb is not a threshold. And error costs, stated as a ratio, are what set the threshold.",
            "Name the eligible population and the outcome horizon. Who gets scored, and how long until you learn whether you were right? The horizon determines label maturity, retraining cadence, and how quickly you can detect that something has broken. A 90-day horizon means a problem introduced today is fully visible in three months, and that changes the monitoring design entirely.",
            "State the non-ML baseline and what it would take for the model to be worth its complexity. This is a fast credibility signal, and occasionally it is the right answer.",
            "If you do only one thing differently from an average candidate, do this: spend the first five minutes here rather than on model architecture. Interviewers are listening for whether you can turn a vague prompt into a specified decision, and most candidates never do it.",
          ],
        },
        {
          heading: "Choose the operating point, then defend it by slice",
          body: [
            "A model produces a score; a product needs an action. The rule converting one into the other is the operating point, and choosing it is a business decision wearing statistical clothing. Start from the cost ratio you already stated - what a false positive costs against a false negative - and read the threshold off the precision-recall curve where the expected costs balance. Then check it against capacity, because a threshold producing more positives than the downstream team can absorb is not a threshold, it is a wish.",
            "If the score is used as a probability anywhere - to compute an expected value, to combine with a price, to set per-category thresholds - it has to be calibrated. A model with excellent ranking can be badly calibrated, meaning its 0.8 does not occur eighty percent of the time, and every downstream arithmetic step then compounds that error silently. Say which of the two properties you actually need: ranking quality if you are ordering candidates, calibration if you are multiplying the score by anything.",
            "Consider a third action rather than two. Allow and block is the default framing and it is usually wrong for high-stakes decisions; allow, review, and block adds an abstention band where the model declines to decide and a human decides instead. The width of that band is set by review capacity, which is exactly why capacity belongs in the framing rather than arriving later as an operational surprise.",
            "Then defend the operating point per slice. Predeclare the slices that matter - region, language, device, customer tier, and whatever protected attribute the domain requires - and report performance at the threshold for each, with the support count and an uncertainty interval beside it. A slice with 200 examples cannot distinguish a five-point difference from noise, and quoting the point estimate without its support is how a team persuades itself that a rare cohort is fine. Say which of these checks are blocking guardrail metrics and which are merely monitored, because that distinction decides what happens automatically when one of them regresses.",
          ],
        },
        {
          heading: "Make every feature and label time-correct",
          body: [
            "The middle of the interview should establish that the data pipeline cannot lie. Two mechanisms carry this, and naming them precisely is what demonstrates experience.",
            "Point-in-time joins: every feature for a training example is computed from facts whose availability time precedes that example's decision time. Say the words 'availability time' as distinct from 'event time', because that distinction is the mechanism and not merely the warning; it separates someone who has built this from someone who has only read about leakage.",
            "Shared feature definitions: training and serving derive from one definition rather than two implementations, so training-serving skew is prevented structurally rather than by care. Add the replay test as the continuous check: recompute features for logged production requests and compare against what was actually served. It is the only test that puts the two paths side by side.",
            "Then the label definition, stated as an operational rule: what event, what window, who decided, and when it became available. Address censoring explicitly - immature labels either excluded or modeled - and address selective labels if the system's decisions determine which outcomes are observable, because that is the failure that makes every metric look healthy while the system narrows.",
            "Finish this section with the feature compute paths chosen per feature by required freshness - batch, streaming, or request-time - and the missing-value policy applied identically in both paths. These are small details and they are exactly the ones that distinguish a designed pipeline from an assumed one.",
          ],
        },
        {
          heading: "Close the lifecycle loop",
          body: [
            "The final third should establish that the system can be operated. Offline gates first: a temporal backtest rather than a random split, comparison against the current production model on identical data, pre-registered slices, calibration if probabilities feed decisions, and operational limits on latency and cost. These are guardrail metrics in the strict sense, meaning a regression past the stated bound blocks the release automatically, as opposed to a monitored metric that is watched and informs a human.",
            "Then online validation, and be precise about what each stage proves. Shadow mode validates inputs, latency, and output distribution and cannot measure impact. A canary bounds harm and gives real impact on a small fraction. An experiment measures value, with the randomization unit chosen from the causal mechanism and the duration fixed in advance.",
            "Then monitoring in layers by how fast each signal arrives: inputs and prediction distributions immediately, leading indicators in hours, true outcomes on the horizon you named at the start. Rule out data quality before diagnosing drift, since it is more common and needs a completely different response.",
            "Then feedback loops, which is the ML-specific closing move. If the system's decisions shape the data it will next train on, and in recommendation, fraud, and lending they always do, say so, say which kind of loop it is, and name the countermeasure: an exploration slice, decision-distribution monitoring, and logging the intervention alongside the prediction at decision time.",
            "Finally rollback: previous version deployable as a state transition, the whole release versioned together including thresholds and calibration, and a kill switch to the baseline. Then close with what breaks at ten times the scale.",
          ],
        },
        {
          heading: "Serve it: bundle, defaults, and fallback",
          body: [
            "What you deploy is not a model. It is a bundle: the model artifact, the feature definitions it expects, the calibration mapping, the threshold, and the policy converting a decision into an action. Those are versioned, promoted, and reverted together, because a rollback that restores the model while leaving yesterday's threshold in place produces a combination nobody has ever tested.",
            "Specify the missing-value policy per feature and apply it identically in training and serving. A feature absent for two percent of training rows and forty percent of production requests is a different feature in the two places, and imputing a mean on one path while a null travels down the other is the most ordinary source of skew there is. Write the default beside the feature definition rather than leaving it to whichever code path happens to run first.",
            "Then design for the feature store or the model being unavailable, because both will be. The system needs a defined behavior - serve a cached score, fall back to the rules baseline, or take the default action - decided in advance rather than left to whatever the exception handler does. Name the latency budget too, split across feature fetch, inference, and policy evaluation, since a budget stated only end to end cannot tell you which of the three to cut when it is exceeded.",
            "The hazard specific to fallbacks is correlation. When the feature store degrades it degrades for everyone at once, so every request takes the fallback simultaneously and the fallback becomes the system for the duration. Size it for full traffic rather than for the trickle it handles normally, and monitor the rate at which it is taken as a first-class signal: a quiet rise in fallback rate is a quality regression that no model metric will ever surface.",
          ],
        },
      ],
      workedExample: {
        title: "A 45-minute run at content moderation",
        setup:
          "The prompt is 'Design a system to detect policy-violating content'. The platform has 500 million posts per day. Violations are rare. Wrongly removing legitimate content is a serious harm; missing genuinely harmful content is also serious.",
        steps: [
          "Minutes 0-5, decision contract. Three actions, not two: allow, route to human review, remove automatically. Decision time is at upload with a latency budget of a few hundred milliseconds for the synchronous path, plus an asynchronous deeper pass. Unit is one post. Reviewer capacity is perhaps 50,000 posts per day, which immediately bounds the review threshold regardless of what the model can do. State that as a constraint discovered in minute four.",
          "Minutes 5-10, costs and population. Wrongly removing legitimate content is a censorship harm with appeal costs; missing harmful content is a safety harm. The asymmetry differs by category: for imminent-harm categories, err toward removal; for ambiguous speech, err toward review. Thresholds are therefore per category rather than global, and that is a policy decision the design must support explicitly.",
          "Minutes 10-20, architecture with the two paths distinguished. Synchronous: cheap model on the upload path making allow-or-hold decisions in milliseconds. Asynchronous: expensive multi-modal models on a queue for a deeper pass within minutes. Review queue prioritized by predicted severity and by reach, since a violating post with a million views matters more than one with three. Human decisions feed back as labels, and this feedback path is a source of selective labels to be flagged now rather than later.",
          "Minutes 20-32, deep dive on labels and the feedback loop, chosen because it is the hardest part rather than the most familiar. Labels come from human reviewers who only see what the model routed to them, so the training data covers the model's own selection and never the content it confidently allowed. Countermeasures: a random-sample review queue independent of model scores, which is the only unbiased label source; monitoring the distribution of automated removals by category and by user segment; and logging the intervention with every decision. Note that appeals are a second biased label source, since only removed content generates them.",
          "Minutes 32-40, lifecycle. Offline gates with temporal backtest, per-category slices, calibration since thresholds derive from cost ratios, and an over-removal guardrail metric that blocks the release if it regresses. Shadow first to compare removal-rate distributions against production. Canary at 1 percent with automated rollback on removal rate exceeding a bound. Then layered monitoring: input and prediction distributions immediately, appeal rate within hours, and reviewer agreement on the random sample as the slow ground truth. Those three are monitored metrics; they inform a human, and none of them blocks a release on its own.",
          "Minutes 40-45, close. Failure posture: if the model is unavailable, fall back to the rules-based classifier and route more to review, accepting a backlog over allowing unmoderated content. Kill switch to rules-only. At ten times the scale, reviewer capacity becomes the binding constraint far more sharply, so the design's leverage moves from model accuracy to precision at a fixed review volume, a different optimization target and worth saying explicitly.",
        ],
        takeaway:
          "The interview was decided in the first ten minutes by establishing a three-action decision, a capacity constraint, and per-category cost asymmetry, and in the deep dive by choosing selective labels over model architecture. Note that the phrase 'which model' never came up, and the design is nonetheless complete and specific. That is what an ML design interview is actually assessing.",
      },
    },
    glossary: [
      { term: "Decision contract", definition: "The action, moment, unit, options, thresholds, and default behavior a model informs. Established before any modeling discussion." },
      { term: "Capacity constraint", definition: "The downstream limit on how many positive decisions can be handled, which frequently binds harder than model quality." },
      { term: "Outcome horizon", definition: "How long until the true label arrives. Determines label maturity, retraining cadence, and how fast a problem becomes visible." },
      { term: "Point-in-time correctness", definition: "Building each training example from facts available before its decision time, distinguishing availability time from event time." },
      { term: "Shared feature definition", definition: "One definition serving both training and inference, which prevents skew structurally rather than by care." },
      { term: "Replay test", definition: "Recomputing features for logged production requests and comparing against served values - the only continuous check that compares the two paths." },
      { term: "Selective labels", definition: "Outcomes observable only where the system acted, so the model's blind spot never appears in any metric." },
      { term: "Staged validation", definition: "Shadow for mechanics, canary for bounded impact, experiment for value. Each proves something the others cannot." },
      { term: "Layered monitoring", definition: "Signals organized by arrival speed: inputs now, leading indicators in hours, true outcomes on the stated horizon." },
      { term: "Guardrail metric", definition: "A metric wired into the release gate, so that a regression past its stated bound blocks the rollout automatically without anyone deciding." },
      { term: "Monitored metric", definition: "A metric tracked on dashboards and alerts that informs a human decision, and which never blocks a release by itself. The counterpart to a guardrail metric." },
      { term: "Feedback loop countermeasure", definition: "An exploration slice, decision-distribution monitoring, and logging interventions at decision time, since the confound cannot be reconstructed later." },
      { term: "Release rollback", definition: "Reverting model, features, calibration, thresholds, and policy together as one versioned artifact, so no untested combination is produced." },
      { term: "Operating point", definition: "The threshold converting a score into an action, read from the cost ratio between a false positive and a false negative, then checked against downstream capacity." },
      { term: "Calibration", definition: "Whether a predicted probability matches observed frequency. Required whenever the score is multiplied by anything; ranking quality alone does not supply it." },
      { term: "Abstention band", definition: "The score range where the model declines to decide and a human decides instead. Its width is set by review capacity, not by the model." },
      { term: "Slice support", definition: "The example count behind a slice metric. Without it a point estimate on 200 examples reads as a finding when it cannot be distinguished from noise." },
      { term: "Feature/model/policy bundle", definition: "The deployable unit: model artifact, feature definitions, calibration mapping, threshold, and action policy, versioned and reverted as one thing." },
      { term: "Missing-value policy", definition: "The per-feature default, applied identically in training and serving. Imputing on one path while a null travels the other is the most ordinary source of skew." },
      { term: "Latency budget split", definition: "Allocating the deadline across feature fetch, inference, and policy evaluation, since an end-to-end figure cannot say which stage to cut when it is exceeded." },
      { term: "Correlated fallback", definition: "Every request taking the fallback at once because the shared dependency failed for everyone. It must be sized for full traffic and its rate monitored as a quality signal." },
      { term: "AUC", expansion: "area under the curve", definition: "A single-number summary of how well a model ranks positives above negatives across all thresholds, usually the area under the ROC curve. It says nothing about whether the probabilities are honest, and it can improve while nothing changes at the feasible operating point." },
      { term: "Feature freshness", definition: "How recently a feature's value was computed relative to the decision that uses it. It selects the compute path per feature - batch, streaming, or request-time - and a value that is fresher in serving than in training is a skew even when both pipelines are correct." },
      { term: "Propensity logging", definition: "Recording the probability with which the policy took each action at the moment it was taken. A confound created by the system's own decision cannot be reconstructed from the outcome later, so this has to be written at decision time or not at all." },
      { term: "Bake time", definition: "A minimum period a canary or ramp stage must run before promotion, chosen so that slower signals - delayed outcomes, appeal rates, weekly cycles - have a chance to appear. Without it a ramp measures only the fastest metric." },
    ],
  },

  "mock-llm-infrastructure-synthesis": {
    primer: {
      plainSummary:
        "Assembling the LLM infrastructure material into one 45-minute argument starts from a token workload envelope rather than from request counts. From there you pick a single subsystem to open properly, and you close on overload behavior, safety boundaries, and cost. Those last three are one connected concern in LLM systems; treating them as three separate lists is what makes a close fall flat.",
      analogy:
        "Planning electricity supply for a factory rather than counting machines. What matters is total load, peak load, and what happens when demand exceeds supply. Machines differ enormously in what they draw. Counting requests in an LLM system is like counting machines: a 50-token request and a 50,000-token request are both one request and differ by three orders of magnitude in what they consume.",
      sections: [
        {
          heading: "Start from the workload envelope",
          body: [
            "The first estimate in an LLM design is not requests per second. It is tokens. Prompt tokens per request, output tokens per request, and the distribution of both, because the tail matters far more than the mean when one request can be a thousand times another.",
            "From tokens, derive the things that actually constrain the design. Prefill compute scales with prompt tokens. Decode time scales with output tokens and is memory-bandwidth-bound, which is what makes batching so valuable. KV cache memory scales with total sequence length times concurrency, and that product is the concurrency limit of the deployment.",
            "Do the KV arithmetic explicitly, because it is what separates someone who has operated these systems from someone who has read about them: layers times key-value heads times head dimension times two times bytes gives per-token cost, times sequence length gives per-request, and available memory divided by that gives concurrency. State the number. It sets everything downstream.",
            "State the two latency objectives separately - time to first token and inter-token latency - and note that they have very different sensitivities: TTFT is what makes a service feel broken, while inter-token latency needs only to comfortably exceed reading speed. Then state failure headroom: what happens when a GPU node is lost, since capacity that assumes every node is healthy has planned for the only case that does not matter.",
            "Ten minutes spent here makes the rest of the interview concrete, because every subsequent decision can be justified against a number.",
          ],
        },
        {
          heading: "Choose the fleet shape from the envelope",
          body: [
            "Once the token distribution exists, the next decision is how many pools you run and what separates them. The default is one shared fleet, and pooling is genuinely valuable here: because batching is nearly free until the crossover, one pool at high occupancy serves more requests per GPU than two half-empty ones. So the burden of proof sits on separation, and you should be able to say why you are separating rather than why you are not.",
            "Four things justify a separate workload class. Long-context requests, because their KV footprint is orders of magnitude larger and mixing them collapses everyone else's concurrency. Batch or offline work, because it has no latency target and should be free to absorb whatever the interactive class leaves. Regulated or safety-critical traffic, where residency and isolation are contractual rather than preferences. And a distinct model or adapter set whose weights simply cannot coexist in memory with the others. Nearly everything else is better handled by per-tenant token budgets inside a single pool.",
            "Then say what warm capacity you hold and why. Loading tens of gigabytes of weights takes minutes, so a pool that scales from zero can answer neither a spike nor a node loss. Reserve headroom sized so that losing one failure domain still leaves benchmarked goodput at the target token mix above the SLO, and state that number rather than asserting that the system is highly available.",
            "Finally, write the degradation order before you need it. When the envelope is exceeded, what gives way first: batch work, then long-context requests, then a shorter retrieved context, then a smaller validated model, and only then outright rejection. An ordered list is a design artifact. 'We would degrade gracefully' is not one, and an interviewer hears the difference immediately.",
          ],
        },
        {
          heading: "Open one differentiating subsystem",
          body: [
            "There is far too much in this domain to cover in one interview - serving, retrieval, training, post-training, evaluation - so choose one and go properly deep, selected from the hardest requirement in the prompt rather than from what you know best.",
            "If the prompt emphasizes latency at scale, open the serving path: continuous batching, paged attention, token-weighted admission, preemption behavior, and what happens under overload. If it emphasizes answering from private documents, open retrieval: ingestion versioning, hybrid search with reranking, and above all retrieval-time authorization that fails closed. If it emphasizes model quality, open evaluation: the portfolio, judge calibration, and the fact that a difference inside the confidence interval is not a difference.",
            "Whichever you choose, go to mechanism. Not 'we use continuous batching' but what happens when memory fills mid-generation, what preemption costs, how output length is estimated for admission, and what signal autoscaling uses given that GPU compute utilization stays moderate while memory is full.",
            "Say what you are not covering and why. 'I am treating the model itself as fixed and not discussing training' is scope control, and it prevents the interviewer from reading the omission as a gap.",
          ],
        },
        {
          heading: "Version every artifact the answer depends on",
          body: [
            "An LLM system's behavior is set by far more than its code. The weights, the adapter, the quantization, the system prompt, the retrieval index and the embedding model that built it, the safety policy, the evaluator, and the routing rules all change what a user sees, and every one of them can change without a deployment. That is the practical reason these systems are hard to operate: the usual mental model, in which behavior changes when code ships, is simply false here.",
            "So treat each of those as a versioned artifact with an owner and a change record, and stamp the versions onto every response. When the complaint arrives - the assistant gave a worse answer than it did last week - the only way to investigate is to reconstruct which model, prompt, index snapshot, route, and cache state produced that specific response. Without the stamps the investigation ends in a shrug, and the team's usual next move is to change something and hope.",
            "Routing telemetry deserves separate mention, because routing makes the population non-uniform. If ten percent of traffic quietly reached a smaller fallback model during an incident, the aggregate quality metric dips slightly and explains nothing at all. Log the route taken, the reason it was taken, and whether the response came from a cache, then slice every quality and cost metric by those fields rather than reporting one blended number.",
            "Say this aloud as a lineage requirement rather than as a logging detail, because it is what makes the evaluation story and the cost story credible in the same breath. An evaluation result belongs to an exact bundle of artifacts and a cost per thousand tokens belongs to an exact route mix; quoting either without naming the bundle is quoting a number that cannot be reproduced.",
          ],
        },
        {
          heading: "Close on overload, safety, and cost together",
          body: [
            "The close in an LLM design interview covers three things that are more connected here than elsewhere, and treating them as one closing argument is what makes it strong.",
            "Overload: because a request holds memory for its whole generation, overload does not resolve itself the way it does in a stateless service. State the admission policy, what is shed and by what priority, whether preemption is possible and what it costs, and why autoscaling cannot be the answer to a spike given that loading model weights takes minutes. Provisioned headroom plus shedding is the honest answer.",
            "Safety: the trust boundary. Model output is untrusted, retrieved content may contain injected instructions, tool calls are authorized against the user's permissions rather than the model's, generated code runs sandboxed, and caches and logs are per-tenant. State that there is no reliable language-level defense against prompt injection and that the mitigation is therefore architectural. That single sentence demonstrates you understand the actual threat model rather than reciting mitigations.",
            "Cost: unbounded per request, which makes it an operational concern before it is a finance one. Attribute per tenant and per feature, enforce budgets and rate limits, cap output length and agent iterations, and alert on anomalies, because a looping agent is a financial incident that no single request makes visible.",
            "These three connect: admission control bounds cost, output caps bound both cost and memory, and tenant isolation bounds both blast radius and spend. Presenting them as one connected closing argument rather than three lists is what makes it land.",
          ],
        },
      ],
      workedExample: {
        title: "A 45-minute run at an enterprise assistant",
        setup:
          "The prompt is 'Design an AI assistant for a large enterprise that answers questions over internal documents'. 50,000 employees, documents with varying access permissions, answers must cite sources.",
        steps: [
          "Minutes 0-8, token envelope. Assume 50,000 employees, 20 percent daily active, 5 questions each: 50,000 questions per day, which concentrated into an eight-hour working day is under 2 per second on average and perhaps 10 at peak. Each question retrieves roughly 8 chunks of 500 tokens plus a 1,000-token system prompt, so about 5,000 prompt tokens, with 400 output tokens. That is 250 million prompt tokens and 20 million output tokens daily - roughly 9,000 prompt tokens per second of prefill across the working day. Modest - so throughput is not the hard problem, which redirects the interview toward correctness and permissions where it belongs.",
          "Minutes 8-12, KV arithmetic and concurrency. At roughly 131 kilobytes per token for a GQA model, a 5,400-token request holds about 700 megabytes. With 35 gigabytes free after weights, that is about 50 concurrent requests per GPU, far above the 10 per second peak given a few seconds per request. Conclude aloud that one or two GPUs suffice and that the design's difficulty lies elsewhere. Note also that the shared 1,000-token system prompt is a prefix-cache opportunity worth taking.",
          "Minutes 12-20, end-to-end. Ingestion pipeline with versioned chunks and tombstoned deletions; hybrid retrieval combining embeddings and BM25, fused by reciprocal rank; a reranker over the top 100; permission filtering applied as part of the retrieval query; generation with citations; and a freshness watermark so the system can state its staleness. Name the source of truth as the document system, with the index derived.",
          "Minutes 20-22, fleet shape, decided from the envelope rather than by habit. One shared pool is right here, because the token volume is modest and pooling keeps batches full; the only separation worth making is a batch class for overnight reindexing so it cannot compete with interactive questions. Hold warm headroom for one node loss rather than scaling from zero, and state the degradation order: batch work first, then shorter retrieved context, then a smaller validated model, then rejection.",
          "Minutes 22-36, deep dive on authorization, chosen because varying permissions is the hardest requirement in the prompt. Filtering happens before retrieval, never after generation, because an influence on a generated answer cannot be removed. Permissions are evaluated against live state rather than an index snapshot, since a nightly snapshot means a day of stale access. The system fails closed when the permission service is unavailable. Caches are keyed by permission scope. Retrieved content is untrusted, so no retrieved document can authorize a tool call. Add the audit log of what was retrievable per query and automated tests asserting a low-privilege user cannot obtain privileged answers.",
          "Minutes 36-41, evaluation, because 'must cite sources' is a testable claim. Separate retrieval metrics from answer metrics, since a missing document and a misread document need different fixes. Measure groundedness - whether every claim appears in the retrieved context - and citation correctness. Use pairwise comparison against the current version rather than absolute scoring, and calibrate any model judge against human labels before trusting it.",
          "Minutes 41-45, close. Overload: admission by projected tokens, shed by priority, provisioned headroom in place of autoscaling given multi-minute model load. Safety: no reliable defense against injection at the language level, so tool calls are authorized against the user and generated code is sandboxed. Cost: per-department attribution, output caps, and anomaly alerts. At ten times the scale, the binding constraint becomes the permission check on the retrieval path, so that is where caching with a short TTL and a tight staleness bound would go first.",
        ],
        takeaway:
          "The token envelope showed within eight minutes that serving throughput was not the problem, which freed the interview to go deep on the authorization model where the real difficulty was. That redirection is the payoff of estimating first: it prevents spending the interview optimizing something that was never constrained.",
      },
    },
    glossary: [
      { term: "RAG", expansion: "Retrieval-augmented generation", definition: "Retrieving relevant documents at request time and placing them in the prompt, so answers cite a corpus the model was never trained on." },
      { term: "Token workload envelope", definition: "Prompt and output token distributions rather than request counts, since one request can consume a thousand times another." },
      { term: "KV memory arithmetic", definition: "Layers times key-value heads times head dimension times two times bytes, times sequence length, giving the per-request memory that caps concurrency." },
      { term: "Two latency objectives", definition: "Time to first token and inter-token latency, which have different causes and very different user sensitivity." },
      { term: "Failure headroom", definition: "Capacity reserved so losing a node does not saturate the rest. Especially important where model load time makes replacement slow." },
      { term: "Differentiating subsystem", definition: "The one area opened to mechanism depth, chosen from the prompt's hardest requirement rather than from familiarity." },
      { term: "Token-weighted admission", definition: "Admitting by projected token work and peak memory, since a request holds memory for its whole generation." },
      { term: "Retrieval-time authorization", definition: "Filtering by permissions before content enters the prompt, failing closed, because a generated answer's influences cannot be removed." },
      { term: "Trust boundary", definition: "The rule that model output and retrieved content are untrusted, so tool calls are authorized against the user and generated code is sandboxed." },
      { term: "Cost envelope", definition: "Per-tenant attribution, budgets, output caps, and iteration limits, since per-request cost is unbounded and a looping agent spends invisibly." },
      { term: "Workload class", definition: "A separately scheduled pool. Justified by long context, batch work, regulated traffic, or incompatible weights - and by little else, since pooling keeps batches full." },
      { term: "Long-context class", definition: "Requests whose KV footprint is orders of magnitude larger than typical. Mixed into a shared pool, they collapse the concurrency available to everything else." },
      { term: "Warm capacity", definition: "Weights already resident on standby hardware. Required because loading tens of gigabytes takes minutes, so scaling from zero answers neither a spike nor a node loss." },
      { term: "Degradation order", definition: "The written sequence of what gives way under overload - batch, then long context, then shorter context, then a smaller model, then rejection. An ordered list, not an intention." },
      { term: "Benchmarked goodput", definition: "Measured SLO-compliant token throughput at the real token mix, which is the capacity figure that must still hold after one failure domain is lost." },
      { term: "Per-tenant token budget", definition: "A token and concurrency cap per tenant inside a shared pool. Usually the right alternative to carving out another workload class." },
      { term: "Fallback contract", definition: "The schema, context limit, tool behavior, residency, and safety semantics a cheaper model must preserve. Untested, it breaks precisely when overload invokes it." },
      { term: "Artifact lineage stamp", definition: "Recording model, adapter, quantization, prompt, index snapshot, policy, and evaluator versions on each response, since any of them can change without a deployment." },
      { term: "Routing telemetry", definition: "Logging which model served a request, why, and whether a cache answered it, so quality and cost can be sliced by route instead of blended into one number." },
      { term: "Embedding index version", definition: "The embedding model plus index snapshot behind a retrieval answer. Changing it changes results with no code deployment and no error." },
      { term: "Cost per SLO-compliant task", definition: "Unit economics measured against work that actually met its latency targets, rather than GPU utilization, which can look healthy while producing tokens too late to use." },
      { term: "FLOPS", expansion: "floating-point operations per second", definition: "A measure of raw arithmetic capability. Peak FLOPS is a vendor's theoretical ceiling that serving almost never reaches, so sizing a fleet from it predicts neither achievable concurrency nor latency - which is precisely why the envelope is stated in tokens and KV bytes instead." },
      { term: "ACL", expansion: "access control list", definition: "The record of which identities may see a document. Retrieval filters against it before content enters the prompt, evaluated at query time against current state rather than against a snapshot copied into the index." },
      { term: "KV reservation", definition: "Memory set aside at admission for a request's projected peak footprint - its prompt plus the output it is allowed to generate - rather than for the prompt already received. Admitting against current memory guarantees that some in-flight request cannot allocate its next block." },
      { term: "Token scheduler", definition: "The component that decides, every iteration, how many prompt-prefill tokens and how many decode steps to run. It is the unit of allocation in an inference fleet, which is why capacity is planned in tokens and KV blocks rather than in requests." },
      { term: "Judge calibration", definition: "Measuring a model judge's agreement with human labels on a representative sample before believing its scores. It converts a fluent oracle into an instrument with known error, and it must be repeated as traffic and models change." },
      { term: "Agent loop", definition: "The cycle in which the model selects a tool, the orchestrator executes it, the observation is appended, and the model is called again until a stop condition fires. Choose it as the deep dive when the prompt's risk is a long-running loop with side effects rather than GPU capacity." },
      { term: "Durable run state", definition: "What an agent run must persist outside the process to survive a restart: message history, the tool ledger, budget counters, and the authorization context. Held in process memory instead, a routine deploy destroys a forty-step task and everything the user paid for it." },
      { term: "Tool ledger", definition: "The record of every tool call with arguments, result, and status, written before the call and completed after it, so a resumed run can tell an unfinished call from an unstarted one and an idempotency key can suppress a replay." },
      { term: "Trajectory evaluation", definition: "Scoring the sequence of an agent's steps rather than only its final answer, since a run can reach the right answer having paid twice or read a document it should not have seen, and a wrong answer alone cannot say which step failed." },
      { term: "Cost per success", definition: "Spend divided by tasks actually completed rather than by runs attempted. An agent that is half the price and fails half the time is not cheaper, and the per-run figure is what conceals that." },
      { term: "RPS", expansion: "requests per second", definition: "The arrival-rate figure that sizes an ordinary service and misleads here, because one request can consume a thousand times another. Long prompts and long outputs exhaust KV memory at perfectly ordinary RPS, which is why the envelope is stated in tokens." },
      { term: "OOM", expansion: "out of memory", definition: "The allocator failing because KV memory ran out, which in a fleet sized from request counts arrives long before compute saturates. It surfaces as aborted or preempted generations rather than as a clean rejection, so it is a capacity-planning defect and not a runtime one." },
      { term: "Parallel layout", definition: "How one model execution is spread over devices: replicas when a copy fits, tensor parallelism within a node where the interconnect is fast enough for per-layer collectives, and pipeline parallelism across nodes when it does not fit. The envelope chooses it, since replica count follows from token demand and the layout follows from whether the weights fit at all." },
    ],
  },

  "mock-critique-remediation": {
    primer: {
      plainSummary:
        "Doing mock interviews without a structured critique produces slow, uneven improvement, because you remember how the mock felt rather than what specifically went wrong. This module is about extracting a small number of causal, observable mistakes from each mock and converting each one into a drill with a success criterion and a review date. That conversion is what turns practice into improvement.",
      analogy:
        "Reviewing recorded footage after a match. Watching it and feeling bad about the loss changes nothing. Identifying three specific decisions, understanding what led to each, and drilling those particular situations does. The value is entirely in the specificity, and 'play better' is not a finding.",
      sections: [
        {
          heading: "Critique observable behavior, not identity",
          body: [
            "The distinction that makes critique useful is between what you did and what you are. 'I am bad at estimation' is an identity statement: it is not actionable, it is demoralizing, and it is almost certainly too broad to be true. 'At minute 7 I computed storage without stating the replication factor, so my number was off by three times and I did not notice' is an observable behavior that can be drilled.",
            "So score against separate dimensions rather than forming a global impression, because a global impression averages away exactly the information you need. Useful dimensions: requirements and scoping, estimation, architecture, data modeling, reliability and failure handling, trade-off articulation, communication, and time management. Scoring these separately reveals patterns. A candidate strong on architecture and weak on time management has a completely different improvement plan from the reverse, and a global score of 'medium' hides both.",
            "Attach evidence with a timestamp to every score. 'Communication: weak, minute 22, spent 90 seconds silent while thinking about the partition key.' The timestamp forces specificity and makes the finding verifiable later.",
            "Do this immediately after the mock while the detail is available. Within a few hours you will retain the feeling and lose the specifics, and the specifics are the entire value.",
          ],
        },
        {
          heading: "Separate the four root causes",
          body: [
            "Two candidates can make an identical visible mistake for completely different reasons, and the repairs have nothing in common. So once the behavior is named, classify its cause into one of four buckets: recall, reasoning, prioritization, or delivery. Skipping this step is the main reason practice produces no improvement, because the drill then treats a delivery problem as a knowledge problem and rehearses something that was never broken.",
            "A recall failure is not knowing the mechanism at all - you could not describe how a fencing token works because you have not learned it. The repair is study followed by retrieval practice, and this is the only one of the four that reading actually fixes. It is also the least common cause among candidates who feel underprepared, which is worth establishing before you spend a week reading.",
            "A reasoning failure is holding the pieces and connecting them wrongly: you knew what idempotency was and did not notice that the retry path needed it. The repair is worked examples - take designs you have already seen and practice deriving the mechanism from the requirement rather than recognizing it once somebody names it. A prioritization failure is different again: eighteen minutes on seat locking before any architecture existed is a clock problem, and it is repaired by a timed drill with a hard checkpoint rather than by knowing anything more.",
            "A delivery failure is the one candidates most often misdiagnose as ignorance. The reasoning happened, correctly, and stayed inside your head, so the interviewer graded silence. The repair is narration practice: run the same design twice and force yourself to speak every intermediate step, including the ones that feel too obvious to say aloud. Obvious-to-you is precisely the category the interviewer needed to hear, since they are grading a process and can only grade the part of it that reaches them.",
          ],
        },
        {
          heading: "Prioritize three causal misses",
          body: [
            "A mock will produce a long list of imperfections. Fixing all of them is impossible and attempting it produces no improvement anywhere. Pick three, chosen by causality rather than by severity.",
            "A causal miss is one that produced other misses. If you spent too long on requirements, then rushed the architecture, then had no time for a deep dive, the deep dive is a symptom and the time allocation is the cause. Fixing the cause fixes several symptoms at once. The surface symptoms are usually what feels worst, so unstructured self-review tends to repair the wrong things.",
            "For each of the three, write the counterfactual: what specifically you would do differently at that moment. Not 'manage time better' but 'at minute 8, whatever state requirements are in, draw the end-to-end path'. The counterfactual must be concrete enough to execute under pressure, because that is the condition in which you will need it.",
            "Three is a deliberate limit. More than three and none of them get the attention that changes behavior. The remaining items go on a list and may become the three after the next mock. That list also records whether the same item keeps recurring, and a recurring item signals that your drill for it was wrong rather than that you lack discipline.",
          ],
        },
        {
          heading: "Repair with progressively broader retrieval",
          body: [
            "Each of the three misses becomes a drill with a success criterion. A drill is a small repeatable exercise targeting exactly that behavior: 'given five prompts, produce a complete end-to-end architecture in eight minutes each' targets time allocation directly, and it is far more efficient than another full mock, because a full mock exercises everything and drills the weakness for two minutes.",
            "Define what success looks like before drilling. 'Four of five prompts reach a complete architecture within eight minutes.' Without a criterion you cannot tell whether the drill worked, and you will either abandon it too early or continue it long past the point of value.",
            "Then schedule review at spaced intervals and not immediately. Retrieving something after a delay strengthens it far more than reviewing it while it is still fresh. The fresh review feels easier and produces less. Schedule the first review a few days out, the next a week or two later, and let successful retrievals extend the interval.",
            "Finally, test for transfer. Drilling one prompt repeatedly can produce fluency on that prompt rather than the underlying skill, so the real test is whether the behavior appears on a prompt you have not practiced. That is the transfer test, and it is the only evidence that the skill generalized rather than the answer being memorized. If it does not transfer, the drill was too narrow. That is a finding about the drill, not about you.",
          ],
        },
        {
          heading: "Run the mistake queue as a queue",
          body: [
            "Across a dozen mocks, the individual critiques matter less than the artifact they accumulate into. Keep one list rather than one per session. Each entry carries the mock it came from, the observable behavior, the root-cause category, the drill, the success criterion, and the review date, and the working set stays bounded at three while everything else waits behind them.",
            "The point of a single list is recurrence. An item appearing once is noise; an item appearing in three consecutive mocks is a stable habit and should be promoted to the working set regardless of how minor each individual instance felt. Recurrence is also how you learn that a drill failed: if you drilled time allocation and the same miss returns, the finding is that the drill was too narrow or its criterion too easy, not that you lack discipline. Rewrite the drill rather than repeating it harder.",
            "Close items on evidence rather than on effort. The rule worth adopting is two successful retrievals on different designs, with at least one separated from the drill by several days and at least one on a prompt you had never practiced. Anything weaker closes on the feeling of having addressed it, and that feeling is exactly what unstructured review produces in abundance while changing nothing.",
            "Keep a strengths inventory beside the queue, and treat it as reusable material rather than as reassurance. The sequence you use to open a design, the failure narrative that reliably lands, the estimation move that always produces a decision - writing these down converts something you do intermittently when relaxed into something you do deliberately under pressure, which is where the difference actually shows.",
          ],
        },
      ],
      workedExample: {
        title: "Turning one mediocre mock into a two-week plan",
        setup:
          "A mock on 'design a ticketing system' went poorly. The candidate felt they knew the material but performed badly. The recording is available and the critique is being done immediately afterwards.",
        steps: [
          "Score dimensions separately with timestamped evidence. Requirements: strong, scoped well by minute 5. Estimation: weak, computed QPS but never used it for anything, minute 9. Architecture: adequate but reached only at minute 28. Data modeling: strong. Reliability: not covered, ran out of time. Trade-offs: weak, named choices without stating costs, throughout. Communication: adequate. Time management: weak, minutes 10 to 28 spent on the seat-locking mechanism before any full architecture existed.",
          "Identify the causal chain rather than the worst symptom. Spending 18 minutes on seat locking before an end-to-end design caused the late architecture, which caused reliability to be skipped and the close to be rushed. So the cause is going deep before breadth, not the missing reliability section, even though the missing section is the thing that felt worst.",
          "Pick three causal misses. One: went deep before an end-to-end design existed. Two: estimates were computed but never connected to a decision, so they were arithmetic theater. Three: trade-offs were stated as choices without costs, so 'I will use optimistic locking' where the answer should have been 'optimistic locking, which avoids holding locks during payment and means a user can lose a seat at checkout, which I accept because contention is brief'.",
          "Write concrete counterfactuals. One: at minute 10, whatever else is happening, draw the complete path. Two: after every estimate, say a sentence beginning 'therefore'. Three: after every technology choice, say 'which costs' and complete the sentence.",
          "Design drills with success criteria. For one: five prompts, complete architecture in ten minutes each, success at four of five. For two: ten estimation exercises, each ending in an architectural consequence, success when every one has a 'therefore'. For three: list twenty decisions from past designs and state each as choice-plus-cost, success at eighteen of twenty without prompting.",
          "Schedule spaced review and a transfer test. Drill each within two days, review at day 5 and day 14. Then run a full mock on an unpracticed prompt and check specifically whether an end-to-end architecture appears by minute 20 and whether trade-offs carry costs unprompted. Behavior appearing on a new prompt is the evidence; behavior appearing on a drilled prompt is not.",
        ],
        takeaway:
          "The finding that mattered, going deep before breadth, was not the thing that felt worst; that was the missing reliability section. Structured critique finds causes while unstructured reflection finds symptoms, and the difference compounds over a dozen mocks. The transfer test is what stops you from mistaking familiarity with a practiced prompt for an improved skill.",
      },
    },
    glossary: [
      { term: "Observable behavior", definition: "A specific timestamped action, as opposed to an identity claim. Only the former can be drilled." },
      { term: "Dimension scoring", definition: "Rating requirements, estimation, architecture, data, reliability, trade-offs, communication, and time separately, since a global impression averages away the useful signal." },
      { term: "Evidence timestamp", definition: "The moment in the mock supporting a finding, which forces specificity and makes the finding verifiable later." },
      { term: "Causal miss", definition: "A mistake that produced other mistakes. Fixing it resolves several symptoms, and it is usually not the thing that felt worst." },
      { term: "Counterfactual", definition: "The specific alternative action at a specific moment, concrete enough to execute under pressure." },
      { term: "Micro-drill", definition: "A short repeatable exercise targeting one behavior. Far more efficient than another full mock, which drills a weakness for two minutes." },
      { term: "Success criterion", definition: "The observable standard that ends a drill, without which you cannot tell whether it worked." },
      { term: "Spaced review", definition: "Revisiting at increasing intervals, which strengthens retention far more than immediate review that feels easier." },
      { term: "Transfer test", definition: "Checking the behavior on an unpracticed prompt. The only evidence the skill generalized rather than the answer being memorized." },
      { term: "Rubric", definition: "The fixed set of scored dimensions applied to every mock, so findings are comparable across sessions instead of reflecting whatever stood out that day." },
      { term: "Root-cause category", definition: "Which of recall, reasoning, prioritization, or delivery produced a miss. The same visible mistake has four possible causes with four different repairs." },
      { term: "Recall failure", definition: "Not knowing the mechanism at all. The only category that reading fixes, and the least common one among candidates who feel underprepared." },
      { term: "Reasoning failure", definition: "Holding the pieces and connecting them wrongly. Repaired by deriving mechanisms from requirements on familiar designs, not by learning more mechanisms." },
      { term: "Prioritization failure", definition: "Spending the clock on the wrong thing. Repaired by a timed drill with a hard checkpoint, since no amount of extra knowledge addresses it." },
      { term: "Delivery failure", definition: "Correct reasoning that stayed inside your head, so silence was what got graded. Most often misdiagnosed as ignorance and repaired by narration practice." },
      { term: "Mistake queue", definition: "One cumulative list across all mocks, each entry carrying behavior, root cause, drill, criterion, and review date, with the working set bounded at three." },
      { term: "Recurrence signal", definition: "The same miss appearing across consecutive mocks. Evidence of a stable habit, and evidence that a drill already attempted against it was too narrow." },
      { term: "Closing rule", definition: "Two successful retrievals on different designs, one delayed by several days and one on an unpracticed prompt. Anything weaker closes on a feeling." },
      { term: "Retrieval practice", definition: "Producing the correct approach from memory rather than rereading it. Feels harder than review and is what builds the representation available under pressure." },
      { term: "Strengths inventory", definition: "The written list of moves that reliably work, kept as reusable material so they are deployed deliberately rather than only when you happen to feel relaxed." },
      { term: "KV cache", expansion: "key-value cache", definition: "The stored keys and values for all preceding tokens in a generation, held per request and growing with sequence length, which is usually what caps concurrency on an accelerator. Sizing one from layers, key-value heads, head dimension, and precision is a standard ten-minute drill." },
      { term: "ACL", expansion: "access control list", definition: "The record of which identities may see a document. Tracing one end to end - who is asking, where the filter runs, and what happens when the permission service is down - is another self-contained drill with a checkable answer." },
      { term: "Idempotency", definition: "The property that applying the same operation twice has the same effect as applying it once, enforced at the component that mutates state and keyed on a stable business identity. It is one of the mechanisms whose absence recurs across mocks, which makes it a queue entry rather than a one-off correction." },
      { term: "Backpressure", definition: "Pushing a full queue's pressure back upstream so producers slow down, shed work, or are rejected, rather than letting an unbounded queue convert a latency problem into an outage. Like idempotency, a commonly missing mechanism worth grouping under one root cause rather than logging once per mock." },
    ],
  },

  "mock-evolution-executive-close": {
    primer: {
      plainSummary:
        "Two things reliably appear at the end of a senior interview: 'what happens at ten times the scale?' and the moment where you summarize. Both are graded heavily and both are usually improvised. The scale question is answered by stressing one dimension at a time until the first bottleneck appears; the migration that follows has to stay reversible at every stage; and the close has to sound like an owner rather than a candidate auditioning for the job.",
      analogy:
        "A structural engineer asked what happens if a building takes ten times the load. They do not say 'we would strengthen everything'. They identify which element fails first, at what load, and what the failure looks like, because that is the only answer that tells you what to do. And any strengthening work has to be done while the building stays occupied, which constrains the plan as much as the engineering does.",
      sections: [
        {
          heading: "Stress one dimension at a time",
          body: [
            "The weak answer to 'what about ten times the scale' multiplies every component by ten. It demonstrates nothing, because it does not identify what actually breaks, and real growth is never uniform anyway.",
            "The strong answer picks one dimension, changes it, and traces the consequence to the first thing that fails. Ten times the users is a different problem from ten times the data, which is different again from ten times the write rate or ten times the concurrent connections. Ask which dimension, or name one and say why you chose it.",
            "Then find the first bottleneck rather than listing all of them. The first one is the only one that matters until it is fixed, because everything downstream of it never sees the increased load. 'At ten times the write rate the fan-out queue saturates first, at around 400,000 writes per second, because it is single-partitioned by author' is a real answer. It names the component, the threshold, and the reason, which means it also implies the fix.",
            "Sensitivity analysis is the related move worth doing unprompted: which assumption, if wrong, would change the architecture most? 'If the read-to-write ratio were 10:1 rather than 100:1, I would not denormalize, and that is the assumption I would validate first in production.' This shows you know which of your inputs are load-bearing, and it is a distinctly senior thing to say.",
            "Also say which parts do not need to change, because a candidate who says everything needs rearchitecting at ten times the scale has not understood their own design's headroom.",
          ],
        },
        {
          heading: "Evolution beyond traffic: regions, policy, model swaps",
          body: [
            "Growth is the follow-up everyone rehearses, and it is one of three that actually arrive. The other two reshape the design just as much and get rehearsed far less: a new region with a stricter rule attached to it, and a swap of the model or the data underneath. Notice which one you are being asked about, because they stress entirely different parts of the architecture and a capacity answer to a residency question lands badly.",
            "A second region is rarely a capacity question. It asks where the source of truth lives and what happens when the link between regions is slow or gone. Say which data is region-local, which is globally replicated, and which invariants need coordination that a partition would make unavailable. Then add the residency rule if one exists: when a category of record may not leave a jurisdiction, the partition key, the backup destinations, and the log pipeline all inherit that constraint, and a design that routes user traffic regionally while shipping every log line to one central store has complied with nothing.",
            "A stricter policy - a retention limit, a deletion guarantee, an audit requirement - propagates the same way and to equally surprising places. Deletion is the sharpest case: honoring it means knowing every derived copy, which is caches, search indexes, analytics warehouses, backups, and any downstream consumer that retained what it was sent. Naming the derived copies is the answer. Promising to delete the row is not.",
            "A model or data migration is the third, and correctness is hardest to define there, because the new system's output is supposed to differ from the old one's. Equality comparison is unavailable, so decide in advance what agreement you expect and on which slices, and treat a divergence outside that band as a defect to investigate rather than as an improvement you had not anticipated.",
          ],
        },
        {
          heading: "Evolve through a reversible migration",
          body: [
            "Identifying what breaks is half the answer. The other half is how you would change it on a running system, because in practice you never get to rebuild.",
            "The standard shape is dual write with backfill. Write to both the old and new systems; backfill historical data in the background with a watermark tracking progress; read from the old system while comparing against the new to verify correctness; shift reads gradually once they agree; then stop writing to the old system. Every stage is reversible until the last, and that reversibility is the whole point.",
            "Two details make this work in practice. The comparison stage is where you find the bugs. Reading from both and reporting mismatches, at no user risk, is far more informative than any test suite, because it runs on real data. And the backfill watermark must be tracked so the migration can pause and resume, since a backfill of significant size will be interrupted.",
            "Maintain compatibility in both directions during the transition. New code must handle old data and old code must tolerate new data, because a deployment is not instantaneous and a rollback must be possible while both exist. This is what makes the migration reversible rather than a cutover with a rollback plan on paper.",
            "State the failure plan for the migration itself as well as for the system. What happens if the new system is wrong halfway through? If reads have already shifted? A migration is a project with its own failure modes, and treating it as one is what distinguishes a plan from an intention.",
          ],
        },
        {
          heading: "Close like an accountable owner",
          body: [
            "Reserve the last five minutes regardless of the state of the deep dive, and use them for a structured close. The same five elements every time, so it becomes automatic and does not consume thinking under pressure.",
            "One: restate what you built, in two sentences, tied to the requirements from minute five. 'A read-optimized feed with hybrid fan-out, serving a 100:1 read-to-write ratio with sub-second freshness.'",
            "Two: name the two or three decisions that mattered and what each cost. Not the full list, only the ones that shaped everything else, each with its trade-off stated, because that is the sentence interviewers are listening for.",
            "Three: risks. What worries you about this design, honestly. A candidate who names a real weakness is more credible than one who presents a design with no downsides, and interviewers know every design has them, so claiming otherwise reads as either inexperience or salesmanship.",
            "Four: validation. What you would measure to know it is working, and what you would alert on. Be specific about the signal - fan-out lag in seconds rather than CPU - since the choice of signal is itself a design decision.",
            "Five: evolution. What breaks first at ten times the scale and what you would do about it, in one sentence.",
            "Deliver this even if the deep dive is unfinished. An unfinished deep dive with a strong close reads far better than a complete deep dive that ran out of time in silence, because the close is where you demonstrate that you would own the system rather than merely build it. Ownership is what the seniority signal actually is.",
          ],
        },
        {
          heading: "Recommend one design and name what would overturn it",
          body: [
            "The close should end in a recommendation rather than a survey. Having laid the trade-offs out honestly, say which design you would build and why, against the requirements you established at minute five. A candidate who presents three options with balanced advantages and disadvantages and stops there has demonstrated analysis and withheld judgment, and judgment is by far the scarcer of the two.",
            "The honest way to be decisive without being reckless is to attach the recommendation to its evidence. 'I would build the hybrid fan-out, because the 100:1 read ratio makes read cost dominate, and if that ratio turned out closer to 10:1 I would not' commits to an answer while stating the condition under which it changes. It is the same sensitivity move as before, now pointed at the recommendation itself instead of at an input assumption.",
            "Then name the measurement that would overturn it: the first production benchmark, the first week of real traffic, the sample of real documents. Saying 'the number I want in week one is the actual follower distribution, because the pull threshold is derived from it and I estimated it' tells the interviewer you know which part of your design rests on the weakest evidence and that you intend to go and check. Very few candidates volunteer this, and it lands every time.",
            "Two things to avoid in the last two minutes. Do not introduce a new subsystem: a component appearing for the first time in the close cannot be evaluated and reads as an afterthought that undermines everything before it. And do not enumerate every technology named during the hour, because a component inventory leaves the listener unable to state what the central decision was, which is the single thing the close exists to deliver.",
          ],
        },
      ],
      workedExample: {
        title: "Answering the ten-times question and closing",
        setup:
          "Forty minutes into a feed design interview, with a hybrid fan-out architecture on the board. The interviewer asks: 'What happens if this grows ten times?'",
        steps: [
          "Ask which dimension, or choose one explicitly. 'Ten times the users behaves differently from ten times the posting rate; the second is harder here because fan-out cost scales with posts times followers. Let me take ten times the posting rate.' This takes ten seconds and immediately shows the question was understood and not answered by reflex.",
          "Trace to the first bottleneck with a number. 'Fan-out writes go from 460,000 to 4.6 million per second. The inbox store can be partitioned further, so it is not first. The fan-out queue is the constraint, because celebrity fan-out already shares capacity with ordinary posts, so at ten times it saturates and ordinary users' feeds go stale by minutes.' Component, threshold, and reason.",
          "Give the fix and what it costs. 'Separate the queues fully, and lower the pull threshold so more accounts are pulled at read time instead of pushed. That shifts cost from write to read, which is acceptable because reads are cached, and it increases feed assembly latency, so I would measure that before choosing the new threshold.'",
          "Say what does not change. 'The inbox store, the hydration path, and the caching layer all scale horizontally without redesign. The only structural change is the fan-out threshold and queue isolation.' This bounds the work and shows the original design had headroom.",
          "Describe the migration as reversible. 'Changing the pull threshold is a gradual rollout - move it for a small percentage of accounts, compare feed latency and freshness against the control, then ramp. It is reversible at every stage because both paths remain live, so nothing needs a cutover.'",
          "Deliver the five-part close. Built: a read-optimized feed with hybrid fan-out for a 100:1 read ratio and sub-second freshness. Decisions: push by default because reads dominate, costing write amplification; pull above a follower threshold, costing read-time merge complexity; visibility enforced at hydration, costing a lookup per read and buying instant deletion. Risks: the threshold is tuned from measured fan-out cost and would drift as follower distributions change. Validation: alert on fan-out lag in seconds, cache hit rate, and feed assembly p99. Evolution: the fan-out queue saturates first, and queue isolation plus a lower threshold addresses it.",
        ],
        takeaway:
          "The scale answer was specific about which component fails, at what load, and why, and it named what does not change, which shows the original design had headroom rather than luck. The close then took ninety seconds and covered what was built, what it cost, what worries you, what you would measure, and what breaks next. That structure is worth rehearsing until it is automatic, because it is the last thing the interviewer hears and it is where seniority is most legible.",
      },
    },
    glossary: [
      { term: "Dimensional stress", definition: "Changing one axis - users, data, write rate, connections - instead of multiplying everything, since real growth is never uniform." },
      { term: "First bottleneck", definition: "The component that fails soonest under the stressed dimension. The only one that matters until it is fixed, since nothing downstream sees the load." },
      { term: "Sensitivity analysis", definition: "Identifying which assumption, if wrong, would change the architecture most - and therefore which to validate first in production." },
      { term: "Dual write", definition: "Writing to old and new systems simultaneously during a migration, so both remain live and the change stays reversible." },
      { term: "Backfill watermark", definition: "A record of how far historical migration has progressed, so a long backfill can pause and resume." },
      { term: "Shadow comparison", definition: "Reading from both systems and reporting mismatches at no user risk. Where migration bugs are actually found, because it runs on real data." },
      { term: "Bidirectional compatibility", definition: "New code handling old data and old code tolerating new data, which is what makes a rollback possible mid-migration." },
      { term: "Risk register", definition: "The honest list of what worries you about a design. Naming a real weakness is more credible than presenting one with no downsides." },
      { term: "Executive close", definition: "A five-part summary - what was built, decisions and their costs, risks, validation signals, and evolution - delivered regardless of the deep dive's state." },
      { term: "Trigger metric", definition: "The measurement that shows the first bottleneck approaching its limit, which is what converts a scaling answer from a prediction into something operable." },
      { term: "Region-local ownership", definition: "Keeping the source of truth for a record inside one region and accepting bounded lag elsewhere, so a cross-region partition degrades freshness rather than availability." },
      { term: "Data residency", definition: "A rule that a category of record may not leave a jurisdiction. It binds the partition key, the backup destinations, and the log pipeline, not only user routing." },
      { term: "Derived copy", definition: "Any downstream store holding a duplicate: cache, search index, analytics warehouse, backup, or consumer. Deletion guarantees are only as complete as this list." },
      { term: "Model migration", definition: "Replacing the model or the data beneath a system. Correctness cannot be checked by equality, because the new output is supposed to differ from the old." },
      { term: "Agreement band", definition: "The expected range of divergence between old and new during a migration, declared in advance per slice, so an unexpected difference is investigated rather than celebrated." },
      { term: "Point of irreversibility", definition: "The stage after which rollback is no longer possible, which must be named explicitly so that everything before it is genuinely reversible." },
      { term: "Conditional recommendation", definition: "Committing to one design while stating the input whose change would reverse the choice. Decisiveness and honesty in the same sentence." },
      { term: "Overturning evidence", definition: "The first production measurement that could invalidate the recommendation, named at the close because it shows which part of the design rests on the weakest evidence." },
      { term: "Component-inventory close", definition: "The anti-pattern of listing every technology named during the hour, which leaves the listener unable to state the central decision." },
      { term: "CDC", expansion: "change data capture", definition: "Deriving a stream of changes by reading the source database's replication log rather than having the application write to both systems. It gives one ordered feed from one source of truth, and it couples the event schema to the table schema." },
      { term: "Fencing token", definition: "A monotonically increasing number issued with ownership of a resource, also called a fencing epoch, which the target rejects if it is stale. During a migration it is what stops a superseded writer from overwriting new state, and it prevents rather than merely detects the problem." },
      { term: "Canary", definition: "Serving a small fraction of real traffic with the new path so impact is observable and harm is bounded to that fraction. It is the stage between shadow comparison, which changes nothing for users, and a full cutover." },
      { term: "Reconciliation", definition: "An independent periodic comparison of two systems that should agree, repairing the divergence it finds. Required after a migration because dual writes and backfills leave gaps that no retry policy can detect, and it must not reuse the code path that produced the data." },
      { term: "Control plane", definition: "The machinery that configures, schedules, and coordinates the serving path rather than carrying requests itself. At ten times the scale it frequently saturates before aggregate compute does, which is why multiplying every server by ten misses the real bottleneck." },
      { term: "KV cache", expansion: "key-value cache", definition: "In model serving, the stored keys and values for a request's preceding tokens, held for the whole generation and growing with its length. It is one of the candidate first bottlenecks under a ten-times question, alongside partition throughput, cache working set, queue drain, and collective time." },
      { term: "Hot key", definition: "A single partition key - a celebrity, a large tenant, a city center - carrying load that fleet averages hide. Growth concentrates on it faster than on the average, so new hot keys are among the first things a ten-times question should surface." },
      { term: "SLO", expansion: "service level objective", definition: "An internal target for a metric, such as 99.9 percent of requests succeeding within 200 milliseconds. It is the line a scaling answer measures against, since 'it gets slower' means nothing without the number it must stay under." },
    ],
  },
};
